# Auditoria R-006 — sobre S-006

| Campo | Valor |
|---|---|
| Auditoria | R-006 |
| Informe auditado | `_audit/S-006.md` |
| Commit auditado | 081385a |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-006.md
d420647
$ git show d420647:_audit/S-006.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `081385a` |
$ git show --stat --name-only --format= d420647 | grep -c .
3
$ git show --stat --name-only --format= 081385a | grep -c .
9
```

Hash literal distinto del derivado: se audita `081385a`; `d420647` es el commit de anclaje (3
archivos: `_audit/S-006.md`, `_persistence/decisions.md`, `_persistence/tasks.md`). Los 9 archivos de
`081385a` coinciden con la lista de la seccion 1 del informe.

### 1.2 Informe contra el diff

Contrastado `git show 081385a` archivo por archivo contra la seccion 1 del informe:

- `.claude/skills/protocol-close/SKILL.md`: 4 hunks — plantilla de la seccion 1 (T-009), NOTA DE
  CIERRE con cuarta cosa y sexto rotulo (T-011), parrafo del 7c (T-009), parrafo `⚠️` «compara texto:
  no reejecuta» en el 7c-bis (T-008), 7c-ter de cinco a seis rotulos, 7c-quater con segunda orden
  `SIN ANCLAR`. Respaldado.
- `_audit/000_preproject/005_phase_exit_record_001.md`: tabla 5.2 con la firma. Respaldado.
- `_audit/findings.md`: F-006 y F-007 pasan a `Aceptado — pendiente` en fila y ficha, con
  `Registrado en`. Respaldado; ninguno escrito como `Implementado`.
- `_persistence/decisions.md`: nacen D-020, D-021, D-022; D-015 pasa a `Revocada por D-020` en indice y
  ficha. Respaldado.
- `_persistence/lessons.md`: nace L-008. `_persistence/tasks.md`: T-008 y T-009 a `Implementada`,
  nacen T-010 y T-011. `_persistence/progress.md`: nace S-006. Respaldado.
- Nada relevante en el diff que el informe no mencione.

### 1.3 Reejecucion de los criterios de cierre publicados (sobre `081385a`)

```
$ git show 081385a:_audit/000_preproject/005_phase_exit_record_001.md | grep -cF '<pendiente>'
0
$ git show 081385a:_audit/000_preproject/005_phase_exit_record_001.md | grep -nE '^\| (Quien|Fecha|Decision) \| ' | tail -3
430:| Quien | JD Rodriguez - Main Sponsor |
431:| Fecha | 2026-09-16 |
432:| Decision | ETAPA CERRADA |
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -cF 'Y compara texto: no reejecuta ninguna orden'
1
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -cF 'las ordenes `git diff --cached` de la prosa se traducen aqui'
1
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -cF 'SIN ANCLAR en la seccion 1'
1
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -cF '**ORDEN DEL PASO 2d ANCLADA — salida:**'
3
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -cE 'cinco (rotulos|salidas|bloques)'
0
$ git show 081385a:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
0
```

Todas coinciden con lo publicado en D-020, D-022 y T-010 (esto cubre lo que DT-002 deja a la
auditoria: las salidas no solo no se tocaron, son las que las ordenes devuelven).

### 1.4 Evidencia de contexto de D-020, D-021 y pruebas de D-022

```
$ git show 673a97a:_audit/000_preproject/005_phase_exit_record_001.md | grep -nE "Mientras falte|^\| (Quien|Fecha|Decision) \| .<pendiente>"
21:> ⛔ **Mientras falte la segunda firma, la etapa sigue abierta**, por bien que salgan las casillas.
430:| Quien | `<pendiente>` |
431:| Fecha | `<pendiente>` |
432:| Decision | `<pendiente>` |
$ git show 673a97a:_persistence/decisions.md | grep -n "el acta no se edita"
770:  entrada es la segunda firma que pide la seccion 5.2 del acta; el acta no se edita, porque la
$ git log --oneline 5884e98..673a97a -- _audit/000_preproject/
(sin salida)
$ git show 673a97a:_audit/S-005.md | grep -nE "NOTA DE CIERRE trae la version anclada|git diff -U0 5884e98\^ 5884e98 -- _persistence _audit"
178:en su forma de staging, y la NOTA DE CIERRE trae la version anclada):
$ git show 673a97a:.claude/skills/protocol-close/SKILL.md | grep -nF '<la orden se escribe en su forma anclada al commit y con el propio informe excluido'
1249:<la orden se escribe en su forma anclada al commit y con el propio informe excluido
$ git show 673a97a:.claude/skills/protocol-close/SKILL.md | sed -n '1258,1290p' | grep -c "Paso 2d"
0
$ git diff -U0 5884e98^ 5884e98 -- _persistence _audit ":(exclude)_audit/S-005.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
14
$ for c in 74749f7:_audit/S-004.md 2846f62:_audit/S-005.md; do echo "== $c"; git show $c | sed -n '/^## 1\./,/^## 2\./p' | grep -F 'diff --cached' | grep -vF -- '--stat --name-only' | wc -l; done
== 74749f7:_audit/S-004.md
2
== 2846f62:_audit/S-005.md
3
```

Todas reproducen lo publicado. (La ultima cifra, sin embargo, cuenta prosa: ver F-008.)

### 1.5 Orden del Paso 2d anclada sobre este commit

```
$ git diff -U0 081385a^ 081385a -- _persistence _audit ":(exclude)_audit/S-006.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
14
```

Coincide con el `14` de la seccion 7 y de la NOTA DE CIERRE.

### 1.6 Controles 7c-ter y 7c-quater de la skill nueva, sobre el informe anclado (`d420647`)

```
$ for m in "**BARRIDO DE ANCLAJE — salida:**" "**CONTROL DE PROSA BORRADA — salida:**" "**CONTROL DE SALIDA REPRODUCIDA — salida:**" "**SEGUNDA PASADA anclada del Paso 2e — salida:**" "**CONTROL DE CIFRA ADYACENTE — salida:**" "**ORDEN DEL PASO 2d ANCLADA — salida:**"; do git show d420647:_audit/S-006.md | grep -qF "$m" || echo "FALTA: $m"; done
(sin salida)
$ git show d420647:_audit/S-006.md > S-006.md   # copia en el scratchpad para correr el 7c-quater literal
$ grep -qE '^(> )?\$ git show --stat --name-only --format= 081385a$' S-006.md || echo "FALTA en la seccion 1"
(sin salida)
$ sed -n '/^## 1\./,/^## 2\./p' S-006.md | grep -F 'diff --cached' | grep -vF -- '--stat --name-only' && echo "SIN ANCLAR en la seccion 1: ordenes git diff --cached que el Paso 7c tenia que traducir"
  por el Paso 1c); el Paso 7c traduce **en su sitio** las ordenes `git diff --cached` de la prosa de
SIN ANCLAR en la seccion 1: ordenes git diff --cached que el Paso 7c tenia que traducir
```

7c-ter limpio. La segunda orden del 7c-quater **no** sale limpia: ver F-008.

### 1.7 Controles mecanicos del Paso 4 (sobre `081385a`)

a) Indice contra detalle en `_persistence/`:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 081385a:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 081385a:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas del arbol contra las declaradas:

```
$ diff <(git ls-tree -d --name-only 081385a | sed 's|$|/|' | sort) <(git show 081385a:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos diferencias tienen razon escrita en `project.md` («Carpetas propias»): `temporal/` excluida en
`.gitignore`; `005_discovery/` declarada por adelantado, con D-002. No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" 081385a -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
```

d) Codigos instanciados en `_phases` y `_workflow`:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 081385a -- _phases _workflow | grep -vE ':PI-[0-9]+$' | wc -l
0
```

e) Coherencia indice/detalle de `findings.md` en el commit auditado:

```
$ diff <(git show 081385a:_audit/findings.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show 081385a:_audit/findings.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u)
(sin salida)
```

f) Fecha de la sesion contra la del commit:

```
$ git log -1 --format=%ad --date=short 081385a
2026-09-16
```

Coincide con la fecha del informe.

## 2. Hallazgos

### F-008 — El control SIN ANCLAR del 7c-quater salta con prosa y el anclaje de S-006 se commiteo igual

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `.claude/skills/protocol-close/SKILL.md` (Paso 7c-quater, segunda orden, introducida por T-009 / D-022); `_audit/S-006.md` en `d420647` |

- **Que se observo:** la segunda orden del 7c-quater busca la cadena `diff --cached` en cualquier
  linea de la seccion 1, no en lineas de orden. Corrida literal sobre el propio informe anclado de
  esta sesion, imprime una linea de **prosa** (la que describe T-009) y el aviso `SIN ANCLAR` (salida
  completa en 1.6):

  ```
  $ sed -n '/^## 1\./,/^## 2\./p' S-006.md | grep -F 'diff --cached' | grep -vF -- '--stat --name-only' && echo "SIN ANCLAR en la seccion 1: ordenes git diff --cached que el Paso 7c tenia que traducir"
    por el Paso 1c); el Paso 7c traduce **en su sitio** las ordenes `git diff --cached` de la prosa de
  SIN ANCLAR en la seccion 1: ordenes git diff --cached que el Paso 7c tenia que traducir
  ```

  La tabla del 7c-quater manda, para esa salida, «detente ... No commitees el anclaje hasta que salga
  vacio». El anclaje `d420647` se commiteo, y el informe no menciona ni la salida ni la parada:

  ```
  $ grep -nE "SIN ANCLAR|Sin resolver|7c-quater" S-006.md
  53:  de staging), y el 7c-quater gana una segunda orden (`SIN ANCLAR en la seccion 1`) que lo comprueba;
  163:+  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'SIN ANCLAR en la seccion 1'
  190:b/_persistence/decisions.md :: +  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'SIN ANCLAR en la seccion 1'
  ```

  La prueba de D-022 («el control de T-009 habria detenido los anclajes de S-004 y S-005», cifras 2 y
  3) ya contaba prosa: de las 3 lineas de S-005, 2 son prosa.

  ```
  $ git show 2846f62:_audit/S-005.md | sed -n '/^## 1\./,/^## 2\./p' | grep -F 'diff --cached' | grep -vF -- '--stat --name-only'
  Por archivo, lo que nace y lo que se edita (derivado con `git diff --cached -U0`, no de memoria):
    `git diff --cached` de la seccion 1 del informe, tercera recomendacion de `R-004`). Ninguna decision
    `git diff --cached` de la seccion 1, `Origen: report_auditor`, cita `D-019`). Ninguna tarea existente
  ```

- **Por que importa:** es exactamente el punto 3 que la seccion 6 del informe pedia revisar: la prueba
  se hizo sobre commits viejos y no sobre `S-006`. En su primera ejecucion real el control da un falso
  positivo y el cierre siguio adelante sin publicarlo: o no se corrio, o se ignoro su parada. Cualquier
  informe que describa el control (o hable de `git diff --cached` en prosa) lo volvera a disparar, y un
  control que salta siempre acaba ignorandose — que es como ocurrio aqui. T-009 figura `Implementada`
  con un control que no distingue orden de prosa. No es `Alta`: la seccion 1 de S-006 no conserva
  ninguna orden de staging real, asi que el registro no afirma nada falso sobre cifras.
- **Que lo corregiria:** acotar la segunda orden del 7c-quater a lineas de orden (por ejemplo, las que
  empiezan por `$ ` o las que llevan `git diff --cached` entre comillas invertidas seguido de `| grep`),
  volver a probarla sobre `S-006` en `d420647` ademas de sobre S-004/S-005, y registrar en el informe
  la salida del 7c-quater cuando no sale vacia. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-006 | Implementado (081385a) | La correccion esta en el diff: tabla 5.2 del acta firmada, 0 `<pendiente>` (1.3). D-020 registrada, T-010 citada |
| F-007 | Implementado (081385a) | La plantilla de `protocol-close` exige la forma anclada bajo el sexto rotulo y el 7c-ter lo busca (1.3); la primera sesion con la regla la publica y reproduce `14` (1.5, 1.6). Se acepto no reescribir S-005 (D-021) |

## 4. Lo que NO se pudo comprobar

- Que la firma escrita en la tabla 5.2 sea la que el patrocinador dio «literal en la conversacion»
  (D-020) y que D-022 la pidiera el usuario: ocurrio fuera de `git`. Se comprobo el resultado, no la
  aprobacion.
- La salida del Paso 2f (diferencia con el esqueleto `SDAI_TripleS`) describe arboles de trabajo, no
  un commit; no se reejecuto porque su valor de entonces no es reproducible y el repositorio del
  esqueleto no forma parte de este commit.

## 5. Recomendaciones sin hallazgo

- **Revocacion parcial de D-015 (punto 1 de la seccion 6).** El vocabulario de `decisions.md` solo
  tiene `Vigente` / `Revocada por D-XXX`, asi que el indice lee ahora «Etapa 000_preproject cerrada |
  Revocada por D-020» mientras `progress.md` y D-020 mantienen la etapa cerrada. Lo explica D-020, pero
  quien lea solo el indice (el arranque, por ejemplo) puede entender que el cierre se revoco. Conviene
  decidir una forma para la revocacion parcial (una nota en la ficha de D-015 o un estado propio).

  ```
  $ git show 081385a:_persistence/decisions.md | grep -oE "^\| Estado \| .*" | sort | uniq -c
        1 | Estado | Revocada por D-020 |
       22 | Estado | Vigente |
        1 | Estado | `Vigente` / `Revocada por D-XXX` |
  ```

- **Punto 2 de la seccion 6 (limite del control sin citar DT-002).** El puntero en direccion contraria
  es legible: DT-002 nombra el control por su rotulo, y la skill lo describe con el mismo nombre.

  ```
  $ git show 081385a:_persistence/techdebt.md | grep -n "DT-002" | head -3
  25:| [DT-002](#dt-002---el-control-de-salida-reproducida-no-reejecuta-las-ordenes) | El CONTROL DE SALIDA REPRODUCIDA no reejecuta las ordenes | No implementada | Confirmada | Baja | No bloqueante |
  151:### DT-002 - El CONTROL DE SALIDA REPRODUCIDA no reejecuta las ordenes
  ```

- El «Que» de T-008 sigue diciendo «remitir a `DT-002`», que D-022 decidio no hacer. La desviacion esta
  registrada en D-022, pero la ficha de la tarea no la refleja; una nota de una linea evitaria leerla
  como incumplida.
