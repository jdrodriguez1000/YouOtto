# Informe de auditoria — S-004

| Campo | Valor |
|---|---|
| Sesion | S-004 |
| Fecha | 2026-09-16 |
| Etapa | 000_preproject |
| Rama | main |
| Commit auditado | `c07680f` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`613ef8a`), sobre el estado que `101db28` dejo (auditado por `R-003.md`).

```
$ git rev-parse --short HEAD
613ef8a
$ git log -1 --format='%h %s' HEAD
613ef8a auditoria R-003 sobre S-003 (101db28)
```

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-005 — El anclaje de T-006 sustituyo una salida que no reproducia y la nota afirma que reproduce | Aceptado — corregido en este commit | `T-007`. La correccion esta en el diff de esta sesion: nota fechada bajo el criterio de cierre de `T-006` en `_persistence/tasks.md` (las dos salidas, publicada y anclada, sin reescribir el bloque commiteado) y CONTROL DE SALIDA REPRODUCIDA nuevo en el Paso 7c-bis de `.claude/skills/protocol-close/SKILL.md`, con su rotulo anadido a la lista del Paso 7c-ter y a la plantilla de la NOTA DE CIERRE. `D-013` documenta la decision y las alternativas descartadas |

## 1. Que se hizo

```
$ git show --stat --name-only --format= c07680f
.claude/skills/protocol-close/SKILL.md
_audit/S-004.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

Segun el diff:

- **`.claude/skills/protocol-close/SKILL.md`** — 7 hunks (`git diff --cached -U0 -- .claude/skills/protocol-close/SKILL.md | grep -c '^@@'`). Nace el **CONTROL DE SALIDA REPRODUCIDA** dentro del Paso 7c-bis (compara, entrada por entrada, la salida de los bloques de «Criterio de cierre» entre el commit de la sesion y el arbol ya anclado); se renombra el Paso 7c-ter de «las cuatro salidas» a «las cinco salidas» y su lista de rotulos pasa de cuatro a cinco; se actualiza la referencia cruzada en la NOTA DE CIERRE de la plantilla del informe (Paso 6b) para anadir el quinto rotulo.
- **`_audit/findings.md`** — 3 hunks. Ninguna entrada nace: se edita la fila de `F-005` en el indice (`Abierto` → `Aceptado — pendiente`) y su ficha (`Estado`, `Registrado en: T-007`, y el campo `Que se hizo`).
- **`_persistence/decisions.md`** — 2 hunks. Nacen `D-013` (el anclaje de criterios de cierre compara la salida anclada con la publicada) y `D-014` (cosecha de `L-007`). No se edita ninguna entrada existente.
- **`_persistence/lessons.md`** — 2 hunks. Nace `L-007` (un criterio que comparten dos entradas se copia del archivo, no se reescribe). No se edita ninguna entrada existente.
- **`_persistence/tasks.md`** — 2 hunks. Nace `T-007`. Se edita la entrada existente `T-006`: se le anade una nota fechada (`🕐 2026-09-16`, citando `F-005` y `T-007`) bajo su bloque de criterio de cierre, con las dos salidas (la publicada en `101db28` y la que devuelve hoy la orden anclada), sin tocar el bloque original.
- **`_audit/index.md`** — nace la fila de `S-004` en el tablero, con `Pendiente` en las tres ultimas columnas (las escribe la auditoria).
- **`_persistence/progress.md`** — nace la entrada `S-004` en la Bitacora, con su fila en el indice de sesiones; se sobrescriben las secciones 1 (Estado general), 2 (Ultimo realizado) y 3 (Siguiente paso), segun manda el Paso 3 del cierre.
- **`_audit/S-004.md`** — este mismo informe, nuevo.

Que entradas nacen (derivado, no de memoria):

```
$ git diff --cached -U0 -- _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md | grep -E '^\+### '
+### D-013 - El anclaje de criterios de cierre compara la salida anclada con la publicada
+### D-014 - Cosecha de L-007
+### L-007 - Un criterio que comparten dos entradas se copia del archivo, no se reescribe
+### T-007 - Anclar criterios de cierre comparando la salida anclada con la publicada
```

Todo lo demas editado en esos tres archivos (y en `findings.md`) es edicion sobre entradas ya
existentes: la nota de `T-006` en `tasks.md`, y la fila+ficha de `F-005` en `findings.md`.

## 2. Que NO se hizo, y por que

Ninguna tarea del indice de `tasks.md` esta `No implementada`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
(sin salida)
```

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep -cE '^\| \[T-'
7
```

Las siete tareas del proyecto (`T-001` a `T-007`) estan `Implementada`. No queda trabajo abierto de
sesiones anteriores en `tasks.md`.

Lo que si queda pendiente, y no es una tarea a medias sino la mecanica normal del cierre:

- **Lanzar `report_auditor`** sobre el commit de esta sesion — es el paso siguiente, fuera de este
  informe.
- **`DT-001`** (`.claude/` se aleja del esqueleto de arranque) sigue `No implementada`, `Propuesta
  (pendiente del usuario)`. Esta sesion la ensancha: `protocol-close/SKILL.md` volvio a cambiar sin
  promoverse (ver seccion 9).
- **`A-001`** (si el historial de juegos registrados es dato de persona) sigue `Abierto`, sin cambios;
  su disparador es la clasificacion de actores de `005_discovery`.

## 3. Decisiones tomadas

### D-013 — El anclaje de criterios de cierre compara la salida anclada con la publicada

- **Que se decidio:** aceptar `F-005` entero y hacer dos cosas: (1) una nota fechada bajo el bloque
  de `T-006` que publica de nuevo las dos salidas (la que se publico en `101db28` y la que devuelve
  hoy la orden anclada) y dice que la linea `📌` no era exacta, sin reescribir lo ya commiteado; (2)
  un CONTROL DE SALIDA REPRODUCIDA nuevo en el Paso 7c-bis de `protocol-close`, que compara, entrada
  por entrada, las lineas de salida de los bloques «Criterio de cierre» entre el commit de la sesion
  y el arbol ya anclado — si no coinciden, el paso se detiene y pega las dos en vez de sustituir.
- **Por que:** la regla de detenerse ante una salida que no coincide ya estaba escrita (justo encima,
  en el propio Paso 7c-bis) y no basto: «coincide» se decidia a ojo. Es el mismo patron que abrio el
  CONTROL DE PROSA BORRADA — una regla de redaccion sola falla, y lo que la sostiene es una
  comparacion mecanica.
- **Alternativas descartadas** (con su razon, citadas en `decisions.md`):
  - Solo la nota, sin el control: corrige el registro pero deja intacto el mecanismo que permitio el
    defecto.
  - La nota ahora y el control como `DT-XXX` para mas adelante: descartada porque el control es
    corto y se pudo probar en la misma sesion sobre los dos anclajes reales que existen (`S-002` y
    `S-003`).
  - Anadir el filtro que falta a la orden de `T-006` para que cuadre: descartada porque cambia lo que
    se publico como evidencia, que es justo lo que el 7c-bis prohibe.
- **Verificacion previa**, contra `HEAD` (`613ef8a`): ver el bloque completo en
  `_persistence/decisions.md`, entrada `D-013` (cuatro ordenes con su salida: la nota vigente de
  `T-006`, el diff que introdujo el defecto en el anclaje de `S-003`, el origen del filtro perdido en
  `D-010`, y la regla incumplida en el propio `protocol-close`).
- **Criterio de cierre** (con `<hash>` literal, pendiente de anclar por el Paso 7c-bis de esta misma
  sesion): la funcion `salidas()` que la propia skill define, aplicada a los dos anclajes ya
  commiteados (`101db28→2a72df8` y `e222812→4b27ae4`), y el conteo del rotulo del nuevo control en la
  plantilla. Ver `decisions.md`, entrada `D-013`.

### D-014 — Cosecha de L-007

- **Que se decidio:** `L-007` (nacida al atender `F-005`) queda **ya cubierta por `LG-98`** del
  archivo de lecciones globales (version 4, commit `9e883a9`), sin subir nada nuevo al archivo
  global. Se contrasto tambien con `LG-08` y `LG-103` por el sintoma «dos copias del mismo documento
  discrepan», y con los cuatro filtros de promocion de `global_lessons.md`.
- **Por que:** `LG-98` ya cubre la forma general —una copia de un bloque diverge del original porque
  nadie comprueba que siga coincidiendo—; el «como se comprueba» de `L-007` es un caso particular de
  esa misma leccion. El filtro de repeticion/coste de la seccion de promocion no se cumple: es un
  solo caso, con coste reversible (una nota fechada).
- **Alternativas descartadas:** enmendar `LG-98` para nombrar copias dentro de un mismo registro (no
  es una cara nueva del problema); marcarla `Solo proyecto` (descartado: la forma sobrevive al cambio
  de lenguaje y dominio, filtro 1 — lo que la descarta es el filtro 4, no el 1).
- **Verificacion**, sobre el repositorio de lecciones: ver `decisions.md`, entrada `D-014` (dos
  ordenes con su salida: version del archivo global y su hash en `origin/main`).
- **Criterio de cierre:** ninguna leccion de `000_preproject` queda `Sin evaluar`, sobre el commit de
  esta sesion.

## 4. Supuestos vigentes y riesgos

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -oE 'A-[0-9]+' | sort -u
A-001
A-002
A-003
```

De ellos, solo `A-001` sigue `Abierto`: si el historial de juegos registrados que describe el brief
puede quedar asociado a personas identificables. Sobre el se apoya la nota de seguridad de `D-007` y
la posible aplicacion de `C-001` (permiso previo para datos de personas) al diseño del registro de
juegos. Si resulta cierto y no se detecta a tiempo, el proyecto disenaria sin el permiso previo que
`C-001` exige. Su disparador es la clasificacion de actores de `005_discovery`
(`_templates/005_discovery/010_actors.md`); no toca a esta sesion.

`A-002` esta `Refutado` y `A-003` `Confirmado`; ninguno de los dos esta vigente como riesgo.

## 5. Siguiente tarea propuesta

Ninguna tarea nueva queda abierta en `tasks.md` (las siete existentes estan `Implementada`). La
siguiente accion concreta no es una `T-XXX`: es repasar, casilla por casilla y con orden y salida
cruda, la condicion de salida de `_phases/000_preproject.md` — con `F-005` atendido y ninguna leccion
`Sin evaluar`, ya no queda ningun bloqueo conocido para esa revision. Importancia `Alta`, urgencia
`Bloqueante` para poder lanzar `phase_exit_auditor`.

## 6. Que pedimos auditar

- **El CONTROL DE SALIDA REPRODUCIDA en si mismo:** es la segunda vez que se anade un control al
  Paso 7c-bis para sostener una regla de redaccion que ya fallo una vez (la primera fue el CONTROL DE
  PROSA BORRADA). Conviene que la auditoria compruebe si la funcion `salidas()` tal como esta escrita
  cubre de verdad los casos que puede tomar un bloque de «Criterio de cierre» — por ejemplo, si
  alguna vez una entrada lleva **mas de un** bloque de codigo tras su criterio, o si el corte por
  `/^---$/` puede fallar con una entrada mal cerrada.
- **La cosecha de `L-007` como «ya cubierta»:** es un juicio de similitud entre dos enunciados
  (`L-007` y `LG-98`) hecho por `manager` en la misma sesion en que se escribio la leccion. No hay
  ninguna verificacion mecanica de que la forma sea realmente la misma; es la lectura mas debil de
  esta sesion.
- **Los seis archivos de `DT-001`:** esta sesion vuelve a tocar `protocol-close/SKILL.md` sin
  promoverlo al esqueleto, ensanchando una deuda ya propuesta y todavia sin confirmar por el usuario.

## 7. Evidencia del Paso 2d

Orden (sobre el area de staging, antes del commit):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Recuento (la unica cifra de este paso):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
9
```

Lista completa, numerada (`cat -n`), sin deduplicar:

```
     1	+  $ git log --format="%h %s" -S"| cut -d: -f2-" -- _persistence/decisions.md
     2	+  $ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
     3	+  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
     4	+  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show 9e883a9:global_lessons.md | grep -nE '^\| \*\*LG-98\*\*|Versión: 4' | cut -c1-90
     5	+  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" ls-remote origin refs/heads/main | cut -c1-7
     6	+  $ git show <hash>:_persistence/lessons.md | grep -E '^\| \[L-' | grep '000_preproject' | grep -c 'Sin evaluar'
     7	+  $ git grep -cF '/^[[:space:]]*```/' 101db28 -- .claude
     8	+  $ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
     9	+  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
```

Cuales se repiten (`sort | uniq -d`, salida entera):

```
+  $ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
+  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
```

(la 2 repite la 8, y la 3 repite la 9: son las mismas dos ordenes citadas dos veces, una en `D-013`
y otra en `T-007`).

Procedencia por archivo (derivada, no a mano):

```
$ git diff --cached -U0 -- _persistence _audit | awk '/^\+\+\+ /{f=$2} /^\+[[:space:]]*\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
b/_persistence/decisions.md :: +  $ git log --format="%h %s" -S"| cut -d: -f2-" -- _persistence/decisions.md
b/_persistence/decisions.md :: +  $ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
b/_persistence/decisions.md :: +  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
b/_persistence/decisions.md :: +  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show 9e883a9:global_lessons.md | grep -nE '^\| \*\*LG-98\*\*|Versión: 4' | cut -c1-90
b/_persistence/decisions.md :: +  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" ls-remote origin refs/heads/main | cut -c1-7
b/_persistence/decisions.md :: +  $ git show <hash>:_persistence/lessons.md | grep -E '^\| \[L-' | grep '000_preproject' | grep -c 'Sin evaluar'
b/_persistence/tasks.md :: +  $ git grep -cF '/^[[:space:]]*```/' 101db28 -- .claude
b/_persistence/tasks.md :: +  $ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
b/_persistence/tasks.md :: +  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
```

Reejecucion, una por una:

| # | Orden (fragmento discriminante) | Reproduce | Nota |
|---|---|---|---|
| 1 | `git log --format="%h %s" -S"| cut -d: -f2-"` | Si | devuelve `101db28 S-003: firmas de los Gates…`, igual que lo publicado en `D-013` |
| 2 | `eval "$(git show <hash>:...salidas() {...)"` (comparacion de anclajes) | No anclable todavia | lleva `<hash>` literal a proposito: es el criterio de cierre de `D-013`, nacida esta sesion. El Paso 7c-bis lo ancla despues del commit sustantivo, con el hash real, y compara contra la salida ya publicada |
| 3 | `git show <hash>:...SKILL.md \| grep -cF '**CONTROL DE SALIDA REPRODUCIDA...'` | No anclable todavia | misma razon que la 2: criterio de cierre de `D-013` |
| 4 | `git -C ".../TripleS_Lessons" show 9e883a9:...` | Si | el hash esta presente (commit del repo de lecciones), solo que en una posicion (`-C <ruta> show <hash>:`) que el patron del Paso 2d no reconoce; reejecutada devuelve exactamente lo publicado |
| 5 | `git -C ".../TripleS_Lessons" ls-remote origin ...` | No anclable por naturaleza | pregunta por el `HEAD` remoto actual de otro repositorio, no por un commit de este; es la misma clase que `git status` |
| 6 | `git show <hash>:_persistence/lessons.md \| grep ... 'Sin evaluar'` | No anclable todavia | criterio de cierre de `D-014`, nacida esta sesion |
| 7 | `git grep -cF '.../` — cerca` 101db28 -- .claude` | Si | falso positivo conocido, documentado en el propio Paso 2d: el hash va detras del patron y la segunda orden del control no lo reconoce, pero **si** esta anclado; reejecutada devuelve lo publicado |
| 8 | igual a la 2 | No anclable todavia | citada en `T-007`, mismo caso que la 2 |
| 9 | igual a la 3 | No anclable todavia | citada en `T-007`, mismo caso que la 3 |

Las cuatro marcadas «no anclable todavia» (2, 3, 8, 9) son los criterios de cierre de `D-013` y
`T-007`, nacidas en esta sesion: llevan `<hash>` literal porque el commit que describen no existia
al escribirlas. El Paso 7c-bis, mas abajo en este mismo protocolo, existe exactamente para este caso
y las ancla despues del commit sustantivo, comparando la salida anclada con la publicada (con el
CONTROL DE SALIDA REPRODUCIDA que esta misma sesion anade).

**NOTA DE CIERRE (Paso 7c-bis, sobre el commit de anclaje `c07680f`):**

El commit sustantivo de esta sesion es `c07680f` (Paso 7c). Las cuatro ordenes marcadas «no anclable
todavia» arriba (2, 3, 8, 9) son, en realidad, dos ordenes distintas citadas dos veces cada una — una
en `D-013` y otra copia identica en `T-007` — y las cuatro quedaron ancladas a `c07680f` por este
mismo paso, con su salida cruda pegada debajo de cada bloque en `_persistence/decisions.md` y
`_persistence/tasks.md`, mas la linea `📌` correspondiente. Ninguna orden de la lista del Paso 2d
queda ya con `<hash>` sin una forma anclada debajo.

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal, anclado a `c07680f`, incluye lo heredado de sesiones anteriores que la regla de no
reescribir congela):

```
$ for f in $(git ls-tree -r --name-only c07680f _persistence _audit | grep -v '_audit/S-004.md'); do n=$(git show c07680f:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/S-003.md: 5
_persistence/decisions.md: 5
_persistence/tasks.md: 3
```

CONTROL (lo que este commit anadio, forma literal `git show <hash>:` al principio de la orden):

```
$ for f in $(git diff --name-only c07680f^ c07680f -- _persistence _audit ":(exclude)_audit/S-004.md"); do n=$(git diff -U0 c07680f^ c07680f -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 2
_persistence/tasks.md: 1
```

Los dos archivos del CONTROL (`decisions.md`, `tasks.md`) son los dos que el Paso 7c-bis tiene
autorizado a escribir; ningun otro archivo aparece. Condicion de parada cumplida.

Desglose por entrada de lo ancladO esta sesion (derivado, no tecleado):

```
$ awk '/^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} c&&/^[[:space:]]*\$ .*c07680f/{print d}' _persistence/decisions.md _persistence/tasks.md | sort | uniq -c
      2 D-013
      1 D-014
      2 T-007
```

Total ancladas: **5**. El CONTROL solo cuenta 3 (`decisions.md: 2` + `tasks.md: 1`) porque su patron
exige la forma literal `git show <hash>:` al principio; las 2 restantes son las ordenes `eval
"$(git show <hash>:... | grep -F 'salidas() {')"...` de `D-013` y de su copia en `T-007`, que llevan
`<hash>` dentro de una subsustitucion y no en esa posicion exacta. Se enumeran aparte, sin doblarlas
dentro del total del CONTROL:

- `_persistence/decisions.md`, entrada `D-013`: `$ eval "$(git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in …`
- `_persistence/tasks.md`, entrada `T-007`: la misma orden, copiada.

3 (CONTROL) + 2 (enumeradas aparte) = 5, que coincide con el desglose por entrada de arriba. No
queda ninguna orden de la lista del Paso 2d sin una forma anclada debajo.

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | outside > /tmp/prosa_antes; outside < "$f" > /tmp/prosa_despues; echo "== $f =="; diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea `< …`: ninguna linea de prosa desaparecio.

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /^[[:space:]]*```/{f=!f; next} c&&f&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | salidas > /tmp/salida_publicada; salidas < "$f" > /tmp/salida_anclada; echo "== $f =="; diff /tmp/salida_publicada /tmp/salida_anclada | grep -E '^[<>]'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Solo las dos lineas `== … ==`: todas las salidas ancladas son las publicadas. Las cinco entradas
reproducen — la linea `📌` de cada bloque dice que reproducen, y es cierto.

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d c07680f^ c07680f); do n=$(git show c07680f:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(sin salida)
$ git diff --name-only --diff-filter=d c07680f^ c07680f | wc -l
8
$ for f in $(git diff --name-only --diff-filter=d c07680f^ c07680f); do git show c07680f:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El commit lleva 8 archivos frente a los 5 de la tabla de la seccion 8 (que midio el area de staging
antes de que el cierre escribiera `progress.md`, este informe y la fila de `_audit/index.md`). Los 3
archivos que quedaron fuera de esa tabla son exactamente esos tres, y el barrido anclado confirma
que ninguno de los ocho tiene caracteres de control.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-004.md
_audit/S-004.md:62: existentes: la nota de `T-006` en `tasks.md`, y la fila+ficha de `F-005` en `findings.md`.
_audit/S-004.md:78: Las siete tareas del proyecto (`T-001` a `T-007`) estan `Implementada`. No queda trabajo abierto de
_audit/S-004.md:149: De ellos, solo `A-001` sigue `Abierto`: si el historial de juegos registrados que describe el brief
_audit/S-004.md:150: puede quedar asociado a personas identificables. Sobre el se apoya la nota de seguridad de `D-007` y
_audit/S-004.md:217: (la 2 repite la 8, y la 3 repite la 9: son las mismas dos ordenes citadas dos veces, una en `D-013`
_audit/S-004.md:218: y otra en `T-007`).
_audit/S-004.md:264: Contraste 1 — filas que tiene que tener la tabla:
_audit/S-004.md:271: Contraste 2 — total sin pasar por la tabla:
_audit/S-004.md:278: Los dos cuadran (5 archivos, 0 caracteres de control en total). Detalle heredado vs. nuevo, por
_audit/S-004.md:314: Seis archivos con promociones pendientes, las mismas seis que ya cubre `DT-001` — mas ancha ahora,
```

Lectura linea por linea: 62 y 217-218 citan codigos (`T-006`, `F-005`, `D-013`, `T-007`) y numeros de
posicion (2, 8, 3, 9) que ya estan en la tabla y la lista numerada de mas arriba en esta misma
seccion — no son cifras nuevas. 78 esta respaldado por el bloque `grep -c` que lo precede (7). 149-150
citan codigos (`A-001`, `D-007`), no cifras. 264 y 271 son rotulos de tabla («Contraste 1», «Contraste
2»), no afirmaciones de conteo. 278 esta respaldado por los dos bloques anteriores (5 y 0). 314 esta
respaldado por el bloque `wc -l` que lo precede (6). Ninguna linea obliga a corregir nada.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(sin salida — cero archivos con caracteres de control)
```

Contraste 1 — filas que tiene que tener la tabla:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
5
```

Contraste 2 — total sin pasar por la tabla:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

Los dos cuadran (5 archivos, 0 caracteres de control en total). Detalle heredado vs. nuevo, por
archivo:

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do echo "$f head=$(git show HEAD:"$f" 2>/dev/null | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]') staged=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')"; done
.claude/skills/protocol-close/SKILL.md head=0 staged=0
_audit/findings.md head=0 staged=0
_persistence/decisions.md head=0 staged=0
_persistence/lessons.md head=0 staged=0
_persistence/tasks.md head=0 staged=0
```

Esta seccion mide el area de staging previa al commit: `progress.md`, este informe y la fila de
`_audit/index.md` quedan fuera por construccion. La SEGUNDA PASADA anclada, en la NOTA DE CIERRE de
la seccion 7, cubre esa brecha sobre el commit ya hecho.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/agents/gate1_auditor.md and .claude/agents/gate1_auditor.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/agents/gate2_auditor.md and .claude/agents/gate2_auditor.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/agents/phase_exit_auditor.md and .claude/agents/phase_exit_auditor.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-audit/SKILL.md and .claude/skills/protocol-audit/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-start/SKILL.md and .claude/skills/protocol-start/SKILL.md differ
```

Recuento (derivado de la propia salida):

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; { for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; } | wc -l
6
```

Seis archivos con promociones pendientes, las mismas seis que ya cubre `DT-001` — mas ancha ahora,
porque `protocol-close/SKILL.md` volvio a cambiar esta sesion. **Esto no es un fallo del cierre**:
son promociones pendientes, y el paso informa, no frena. Todas las diferencias son `Files ... differ`
(promocion pendiente en el sentido normal); ninguna es `Only in` el arbol del esqueleto.
