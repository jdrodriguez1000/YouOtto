# Auditoria R-010 — sobre S-010

| Campo | Valor |
|---|---|
| Auditoria | R-010 |
| Informe auditado | `_audit/S-010.md` |
| Commit auditado | a61e453 |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Fila pendiente y commit auditado

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep Pendiente
| `S-010.md` | S-010 | 2026-09-16 | Pendiente | Pendiente | Pendiente | - |
$ git log -1 --format=%h -- _audit/S-010.md
25b8e41
$ git show 25b8e41:_audit/S-010.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | a61e453 |
$ git show --stat --name-only --format= a61e453 | grep -c .
8
$ git show --stat --name-only --format= 25b8e41
_audit/S-010.md
_persistence/decisions.md
```

El hash literal (`a61e453`) difiere del derivado (`25b8e41`, commit de anclaje): se audita `a61e453`.
Sus 8 archivos coinciden con la lista de la seccion 1 del informe. El anclaje toca, ademas del
informe, `decisions.md`: solo anade las cinco lineas `📌` del Paso 7c-bis (ver 1.6).

### 1.2 Informe contra el diff

```
$ for f in .claude/skills/protocol-close/SKILL.md _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md _persistence/techdebt.md _persistence/progress.md _audit/index.md; do echo "== $f $(git diff a61e453^ a61e453 -U0 -- $f | grep -c '^@@')"; done
== .claude/skills/protocol-close/SKILL.md 1
== _persistence/decisions.md 2
== _persistence/lessons.md 2
== _persistence/tasks.md 2
== _persistence/techdebt.md 4
== _persistence/progress.md 6
== _audit/index.md 1
```

Los recuentos de hunks coinciden con la seccion 1. Leido `git show a61e453` entero: nacen
`D-028`..`D-032`, `T-017`..`T-019`, `L-012`, `DT-004`; `DT-003` pasa a `Implementada`/`Alta` con dos
notas fechadas; la nota de `D-029` cuelga de `D-027`; la frase de `D-028` esta en el Paso 4 de
`protocol-close`. Nada relevante del diff queda sin mencionar. Excepcion: el alcance de `T-019`
(ver `F-011`).

### 1.3 Criterios de cierre de D-028 a D-032, anclados, reejecutados

```
$ H=a61e453
$ git show $H:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
1
$ git grep -nE "YouOtto|Company_TripleS|github.com" $H -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
$ git show $H:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
0
$ git show $H:_persistence/decisions.md | sed -n '/^### D-027 /,/^### D-028 /p' | grep -c '🕐 \*\*Nota 2026-09-16 (`R-009`, `D-029`)'
1
$ git show $H:_persistence/techdebt.md | grep -E '^\| \[DT-003\]' | grep -c '| Alta | No bloqueante |'
1
$ git show $H:_persistence/techdebt.md | sed -n '/^### DT-003 /,$p' | grep -c '^| Importancia | Alta |$'
1
$ git show $H:_persistence/tasks.md | grep -E '^\| \[T-019\]' | grep -c '| Alta | No bloqueante |'
1
$ git show $H:_audit/S-009.md | sed -n '47p'
  contra los precedentes de `S-004`/`S-006`/`S-007` (`87d10b8`, `081385a`, `c07680f`), la decision,
$ git show $H:_persistence/techdebt.md | grep -E '^\| \[DT-003\]' | grep -c '| Implementada |'
1
$ git show $H:_persistence/techdebt.md | sed -n '/^### DT-003 /,$p' | grep -c '^| Estado | Implementada |$'
1
```

Todas reproducen lo publicado. La orden de la nota de `D-029`:

```
$ for p in "c07680f 74749f7 D-013 D-014" "081385a d420647 D-022 D-023" "87d10b8 ee59542 D-023 D-024"; do set -- $p; for c in $1 $2; do echo "== $c $3 $(git show $c:_persistence/decisions.md | sed -n "/^### $3 /,/^### $4 /p" | grep -cE '^[[:space:]]*\$ .*<hash>')"; done; done
== c07680f D-013 2
== 74749f7 D-013 0
== 081385a D-022 7
== d420647 D-022 0
== 87d10b8 D-023 4
== ee59542 D-023 0
```

Reproduce.

### 1.4 Promocion de D-032 contra el esqueleto (lectura, sin escribir)

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" show --stat --format='%h %s' 447c2a0 | tail -5
 .claude/skills/protocol-close/SKILL.md     | 92 +++++++++++++++++++++++++-----
 .claude/skills/protocol-start/SKILL.md     |  4 ++
 _persistence/decisions.md                  |  7 ++-
 _templates/000_preproject/020_decisions.md |  7 ++-
 4 files changed, 93 insertions(+), 17 deletions(-)
$ git -C "$ESQ" ls-remote origin refs/heads/main | cut -c1-7
447c2a0
$ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md; do [ "$(git rev-parse 95c5cfd:$f)" = "$(git -C "$ESQ" rev-parse 447c2a0:$f)" ] && echo "igual $f" || echo "DISTINTO $f"; done
igual .claude/skills/protocol-close/SKILL.md
igual .claude/skills/protocol-start/SKILL.md
igual _templates/000_preproject/020_decisions.md
$ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md; do [ "$(git rev-parse a61e453:$f)" = "$(git -C "$ESQ" rev-parse 447c2a0:$f)" ] && echo "igual-a61 $f" || echo "DISTINTO-a61 $f"; done
DISTINTO-a61 .claude/skills/protocol-close/SKILL.md
igual-a61 .claude/skills/protocol-start/SKILL.md
igual-a61 _templates/000_preproject/020_decisions.md
```

La promocion existe, esta en el remoto y es identica al origen declarado. El unico desfase con
`a61e453` es `protocol-close/SKILL.md`, el que declaran `D-032` y `DT-004`.

### 1.5 Orden del Paso 2d anclada

```
$ git diff -U0 a61e453^ a61e453 -- _persistence _audit ":(exclude)_audit/S-010.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
26
```

Coincide con la NOTA DE CIERRE.

### 1.6 Commit de anclaje: prosa y salidas intactas

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /^[[:space:]]*```/{f=!f; next} c&&f&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f prosa"; diff <(git show a61e453:"$f" | outside) <(git show 25b8e41:"$f" | outside) | grep '^<'; echo "== $f salida"; diff <(git show a61e453:"$f" | salidas) <(git show 25b8e41:"$f" | salidas) | grep -E '^[<>]'; done
== _persistence/decisions.md prosa
== _persistence/decisions.md salida
== _persistence/tasks.md prosa
== _persistence/tasks.md salida
```

El anclaje no borro prosa ni altero salidas; solo anade las cinco lineas `📌`.

### 1.7 D-031 contra su precedente

`git show a61e453:_persistence/decisions.md | sed -n '/^### D-018 /,/^### D-019 /p'`: `D-018` rechaza
corregir una inexactitud sin efecto en un informe ya auditado, con el mismo argumento. `D-031` aplica
ese criterio y lo cita. La cuarta recomendacion existe en `R-009` seccion 5 (cuarto bullet).

### 1.8 Controles mecanicos (Paso 4)

a) Indice ↔ detalle en `_persistence/`:

```
$ H=a61e453; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas:

```
$ diff <(git ls-tree -d --name-only $H | sed 's|$|/|' | sort) <(git show $H:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con razon escrita: `temporal/` excluida en `.gitignore` (`project.md`, fila de la carpeta) y
`005_discovery/` declarada por adelantado con su decision (`decisions.md`, lineas 189-200 en
`a61e453`). No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" $H -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas.

d) Codigos instanciados:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $H -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

## 2. Hallazgos

### F-011 — T-019 queda Implementada con un alcance que su propio commit declara no cumplido
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md`, `T-019`; `_persistence/decisions.md`, `D-032`; `_persistence/techdebt.md`, `DT-004` |

- **Que se observo:** el «Que» de `T-019` incluye expresamente la frase de `D-028` en la promocion, y
  la tarea esta `Implementada`; `D-032`, en el mismo commit, dice que esa frase se dejo fuera, y el
  esqueleto no la tiene.

  ```
  $ git show a61e453:_persistence/tasks.md | sed -n '/^### T-019 /,$p' | grep -nE 'Estado|incluida la frase|Criterio'
  4:| Estado | Implementada |
  12:  esqueleto de arranque los archivos del andamiaje que difieren, incluida la frase de `D-028`. Paga
  16:- **Criterio de cierre:** el de `D-032`, con sus ordenes ancladas y sus salidas. El commit del
  $ git show a61e453:_persistence/decisions.md | grep -nF 'Se deja fuera la'
  1643:  se dejo ninguno. Hash de origen `95c5cfd`; commit del esqueleto `447c2a0`, subido. **Se deja fuera la
  $ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" show 447c2a0:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
  0
  $ git show a61e453:_persistence/techdebt.md | grep -E '^\| \[DT-004\]'
  | [DT-004](#dt-004---la-frase-de-d-028-quedo-fuera-de-la-promocion-de-d-032) | La frase de `D-028` quedo fuera de la promocion de `D-032` | No implementada | Propuesta (pendiente del usuario) | Baja | No bloqueante |
  ```

  `T-019` no lleva nota que ajuste su alcance. Ademas, el criterio que cita (el de `D-032`) no mira el
  esqueleto: su tercera orden comprueba que la frase esta en la skill **de este repositorio**, no en
  el esqueleto. Por eso reproduce aunque esa parte del «Que» no se hiciera.
- **Por que importa:** el Paso 4 de `protocol-close` dice que lo que queda a medias sigue en
  `No implementada` y su entrada dice en que punto quedo. Aqui la tarea dice «hecha» sobre un alcance
  que el mismo commit declara incumplido. El resto solo se sigue en `DT-004`, que esta
  `Propuesta (pendiente del usuario)`: si el usuario no la confirma, esa parte desaparece del registro
  mientras `T-019` sigue diciendo que se hizo. `Media` y no `Alta`: la omision esta declarada en
  `D-032`, en la nota de `DT-003` y en `DT-004`, asi que alguien que lea todo no se equivoca; lo que
  falla es la tarea leida sola.
- **Que lo corregiria:** una de dos, a decidir por `manager`. (a) Una nota fechada en `T-019` que
  quite la frase de `D-028` del alcance y remita a `D-032`/`DT-004`, sin reescribir el «Que». (b)
  Si se considera que la frase formaba parte de la tarea, devolver `T-019` a `No implementada`. En los
  dos casos conviene que `DT-004` pase de propuesta a confirmada o rechazada.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

```
$ sed -n '/^## Indice/,/^---/p' _audit/findings.md | grep -v 'Implementado'
## Indice

| Codigo | Hallazgo | Auditoria | Gravedad | Estado |
|---|---|---|---|---|

---
```

Ninguna fila de hallazgo queda fuera de `Implementado`.
`F-001` a `F-010` ya estan `Implementado`. No hay ningun hallazgo abierto: nada que cambiar.

## 4. Lo que NO se pudo comprobar

- **Estado del arbol de trabajo durante la sesion** (`D-032`: `git status --short` vacio antes de
  promover, «fin P1» despues de promover y antes de reponer la frase, retirada con `git checkout` y
  reposicion). El estado era temporal y ya no existe. Se comprobo el resultado: la frase esta en
  `a61e453` (1.3), y la promocion es identica a `95c5cfd` (1.4).
- **`D-030`, orden `git archive 95c5cfd | tar -x -C "$T"; ...`**: depende de una carpeta del
  scratchpad que ya no existe. Su conclusion (tres archivos distintos) es compatible con el
  `--stat` de `447c2a0` (1.4), pero no se reejecuto la orden.
- **Comparacion byte a byte de la copia de la raiz `_persistence/decisions.md` del esqueleto** contra
  su plantilla (orden 35 del informe): no se reejecuto. Solo se verificaron los tres archivos del
  andamiaje.

## 5. Recomendaciones sin hallazgo

- Seccion 7 del informe: «Las 12 ordenes con `<hash>` (15, 16, 17, 18, 21, 22, 23, 24, 36, 37, 38 mas
  su duplicada)» enumera 11 items, y la duplicada de la 15 ya es la 38. La cifra correcta es 11, la
  misma que da la NOTA DE CIERRE. Es una errata que no lleva a error.
- `progress.md` (seccion 3 y bitacora de `S-010`) describe el desfase residual («no es una `DT-003`
  nueva») sin nombrar `DT-004`, que el mismo commit propone. El informe si la nombra. Citarla evitaria
  que quien arranque solo con `progress.md` no sepa donde se sigue.
- Sobre la pregunta de la seccion 6 del informe (`DT-004` frente a una nota en `DT-003`): una deuda
  aparte parece correcta. `DT-003` esta `Implementada` y una nota no se sigue en el indice. Lo que no
  esta escrito es cuando un desfase residual merece su propia `DT-XXX`. Es un criterio para
  `manager`, no un defecto.
- Un criterio de cierre que respalda una tarea deberia comprobar lo que dice el «Que» de esa tarea
  (ver `F-011`: el de `T-019` no mira el esqueleto). Para una promocion, basta con una orden
  `git -C "$ESQ" rev-parse` que compare los blobs.
