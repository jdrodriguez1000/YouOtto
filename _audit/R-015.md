# Auditoria R-015 — sobre S-013

| Campo | Valor |
|---|---|
| Auditoria | R-015 |
| Informe auditado | `_audit/S-013.md` |
| Commit auditado | d54e314 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (2) |

**Sobre el id.** El ultimo id del arbol es `R-013`, pero `R-014` y `F-015` ya se consumieron: los creo
`798500e` y los revirtio `c180fd5`, cuyo mensaje los declara retirados. Ningun codigo se reutiliza, asi
que esta auditoria es `R-015` y sus hallazgos empiezan en `F-016`. El contenido de `R-014` **no se uso
como evidencia**: todo lo de abajo sale de ordenes corridas en esta pasada.

```
$ git log --all --format=%h -- _audit/R-014.md _audit/R-015.md
c180fd5
798500e
$ git show 798500e:_audit/findings.md | grep -oE "^### F-01[5-9] - .*"
### F-015 - El CONTROL DE CIFRA ADYACENTE publicado en la NOTA DE CIERRE de S-013 no reproduce contra el commit de anclaje, por su propia insercion
$ git show d54e314:_persistence/decisions.md c180fd5:_persistence/decisions.md | grep -cE 'R-014|F-015'
0
```

## 1. Que se comprobo

### 1.1 Fila del tablero y commit auditado

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep S-013
| `S-013.md` | S-013 | 2026-09-17 | Pendiente | Pendiente | Pendiente | - |
$ git log -1 --format=%h -- _audit/S-013.md
17d769f
$ git show 17d769f:_audit/S-013.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | d54e314 |
$ git show --stat --name-only --format= d54e314 | grep -c .
13
$ git show --stat --name-only --format= 17d769f
_audit/S-013.md
_persistence/decisions.md
```

La cabecera esta anclada con hash literal, distinto del derivado: se audita `d54e314`; `17d769f` es el
commit de anclaje (informe + las lineas `<hash>` y `📌` de `decisions.md`, Paso 7c-bis). Los 13 archivos
de `d54e314` coinciden con la lista anclada de la seccion 1 del informe.

### 1.2 Controles mecanicos (Paso 4) sobre `d54e314`

**a) Indice ↔ detalle en `_persistence/`:**

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show d54e314:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show d54e314:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

**b) Carpetas del arbol contra las declaradas:**

```
$ diff <(git ls-tree -d --name-only d54e314 | sed 's|$|/|' | sort) <(git show d54e314:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos diferencias tienen razon escrita: `temporal/` esta excluida en `.gitignore` (fila de
`project.md`), y `005_discovery/` es declaracion anticipada con su decision:

```
$ grep -n "005_discovery/" _persistence/decisions.md | head -2
201:  arranca, no todas de golpe. Hoy se declara solo `005_discovery/`, la siguiente etapa, por
205:  fila pendiente se lee, seis se acaban ignorando. `005_discovery/` se declara ya porque su etapa
```

**c) Fuga de datos propios en las seis carpetas agnosticas:**

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" d54e314 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas.

**d) Codigos instanciados en `_phases` y `_workflow`:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' d54e314 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.3 Correccion de `F-014` en el diff

Las seis notas fechadas estan en `d54e314`, sin reescribir las fechas publicadas:

```
$ git show d54e314:_persistence/decisions.md | grep -n '🕐 \*\*Nota 2026-09-17' | cut -c1-60
2018:- 🕐 **Nota 2026-09-17 (`F-014`, `D-041`):** esta decisi
2075:- 🕐 **Nota 2026-09-17 (`F-014`, `D-041`):** esta decisi
2126:- 🕐 **Nota 2026-09-17 (`F-014`, `D-041`):** esta decisi
2174:- 🕐 **Nota 2026-09-17 (`F-014`, `D-041`):** esta decisi
$ git show d54e314:_persistence/decisions.md | grep -nE '^### D-0(3[6-9]|40) '
1942:### D-036 - F-013 se acepta con nota fechada y puerta mecanica en el anclaje
2020:### D-037 - El desfase 4 del arranque ignora los commits de solo anclaje
2077:### D-038 - R-011 y F-012 quedan retirados por la auditoria revertida
2128:### D-039 - El trailer del cierre nombra el modelo que lo ejecuta
2176:### D-040 - Promocion al esqueleto de protocol-close y protocol-start de S-012
$ git show d54e314:_persistence/lessons.md | grep -c '🕐 \*\*Nota 2026-09-17 (`F-014`, `D-041`)'
1
$ git show d54e314:_audit/S-011.md | grep -c '🕐 \*\*Nota 2026-09-17 (`F-014`, `D-041`)'
1
```

Una nota al final de cada una de `D-036`..`D-039` (cada linea cae antes del `###` siguiente), una en
`L-013` y una en `S-011.md`.

El control nuevo del Paso 7d, copiado literal del bloque de `d54e314`, detecta el caso de `F-014` y sale
limpio sobre el propio commit de la sesion:

```
$ H=cf2992f; d=$(git log -1 --format=%ad --date=short $H); D=$(git diff -U0 $H^ $H -- _persistence _audit); nuevos=$(printf '%s\n' "$D" | grep -oE '^\+#{3} [A-Z]+-[0-9]+ ' | grep -oE '[A-Z]+-[0-9]+' | paste -sd'|'); printf '%s\n' "$D" | grep -E "^\+(\| Fecha \| [0-9]{4}-[0-9]{2}-[0-9]{2} \|$|\| \[(${nuevos:-NINGUNO})\].*\| [0-9]{4}-[0-9]{2}-[0-9]{2} \||[[:space:]]*(> )?(- )?🕐 \*\*Nota [0-9]{4}-[0-9]{2}-[0-9]{2})" | grep -vF "$d" | cut -c1-120; echo fin
+> 🕐 **Nota 2026-09-16 (`F-013`, `T-021`), escrita por `manager` en la sesion siguiente.** La linea
+| [D-036](#d-036---f-013-se-acepta-con-nota-fechada-y-puerta-mecanica-en-el-anclaje) | F-013 se acepta con nota fechada
+| [D-037](#d-037---el-desfase-4-del-arranque-ignora-los-commits-de-solo-anclaje) | El desfase 4 del arranque ignora los
+| [D-038](#d-038---r-011-y-f-012-quedan-retirados-por-la-auditoria-revertida) | R-011 y F-012 quedan retirados por la a
+| [D-039](#d-039---el-trailer-del-cierre-nombra-el-modelo-que-lo-ejecuta) | El trailer del cierre nombra el modelo que 
+| Fecha | 2026-09-16 |
+| Fecha | 2026-09-16 |
+| Fecha | 2026-09-16 |
+| Fecha | 2026-09-16 |
+| [L-013](#l-013---antes-de-lanzar-la-auditoria-se-mira-en-el-historial-que-el-cierre-no-audito-su-sesion) | Antes de l
+| Fecha | 2026-09-16 |
fin
$ H=d54e314; (misma orden, con echo "nuevos=$nuevos d=$d" antes del filtro y echo "fin 7d" al final)
nuevos=A-004|A-005|D-040|D-041|D-042|D-043|D-044|S-013|T-025|T-026|T-027|T-028|T-029 d=2026-09-17
fin 7d
```

### 1.4 Criterios de cierre anclados de `D-040`..`D-044` (y por tanto de `T-025`..`T-028`)

Corridos otra vez, literal, sobre `d54e314`:

```
$ git show d54e314:_persistence/decisions.md | grep -cE '^(\| \[D-040\]|### D-040 )'
2
$ git show d54e314:_persistence/lessons.md | grep -c '🕐 \*\*Nota 2026-09-17 (`F-014`, `D-041`)'
1
$ git show d54e314:_audit/S-011.md | grep -c '🕐 \*\*Nota 2026-09-17 (`F-014`, `D-041`)'
1
$ for c in cf2992f 079b0a4; do echo "== 7d fechas @ $c: $(git show d54e314:.claude/skills/protocol-close/SKILL.md | awk '/^d=\$\(git log -1 --format=%ad --date=short <hash[>]\)$/{f=1} f&&/^```$/{exit} f' | sed "s/<hash[>]/$c/g" | bash | wc -l)"; done
== 7d fechas @ cf2992f: 11
== 7d fechas @ 079b0a4: 0
$ git show d54e314:_audit/findings.md | grep -cE '^\| \[F-014\].*\| Aceptado — pendiente \|$|^\| Registrado en \| T-025, T-026, D-041 \|$'
2
$ git show d54e314:.claude/skills/protocol-close/SKILL.md | grep -cF -- '-m "Co-Authored-By: Claude <modelo> <noreply@anthropic.com>" && git push'
1
$ SP=$(mktemp -d); mkdir "$SP/_audit"; git show d54e314:.claude/skills/protocol-close/SKILL.md | awk '/^git add _audit\/S-XXX.md _persistence\/decisions.md$/{f=1} f&&/^```$/{exit} f' | sed -e 's/<hash[>]/079b0a4/g; s/S-XXX/S-011/g' -e 's/^git add .*/: add/' -e 's/^  git commit -m .* && git push$/  echo COMMIT/' -e 's/^git status -sb$/:/' > "$SP/puerta.sh"; for c in fdfca7b 1358c3c; do echo "== puerta @ $c"; git show $c:_audit/S-011.md > "$SP/_audit/S-011.md"; (cd "$SP" && bash puerta.sh) | tail -1; done
== puerta @ fdfca7b
PUERTA CERRADA: no se commitea el anclaje
== puerta @ 1358c3c
COMMIT
$ git show d54e314:_templates/000_preproject/050_audit_findings.md | grep -cE '^\| Codigo \| Hallazgo \| Auditoria \| Gravedad \| Urgencia \| Estado \|$|^\| Urgencia \| `Bloqueante` / `No bloqueante` \|$|^\| Urgencia \| \|$'
3
$ git show d54e314:_audit/findings.md | grep -cE '^\| \[F-[0-9]+\].*\| (Alta|Media|Baja) \| — \| [^|]+ \|$'
13
$ git show d54e314:.claude/skills/protocol-audit/SKILL.md | grep -cF '| Urgencia | Bloqueante / No bloqueante |'
1
$ git show d54e314:.claude/skills/protocol-start/SKILL.md | grep -cF '(gravedad, urgencia)'
2
$ git show d54e314:_persistence/decisions.md | grep -cE '^(\| \[D-044\]|### D-044 )'
2
$ git show d54e314:_persistence/assumptions.md | grep -cE '^(\| \[A-00[45]\]|### A-00[45] )'
4
```

Las 13 reproducen lo publicado en `17d769f`. El diff de `17d769f` sobre `decisions.md` no borra ninguna
linea que no sea una orden con `<hash>`:

```
$ git show 17d769f -- _persistence/decisions.md | grep -E '^-' | grep -v '^---' | grep -vc '<hash>'
0
```

### 1.5 Control 2b de `findings.md` sobre la version del commit

```
$ git show d54e314:_audit/findings.md > /tmp/fd.md; diff <(awk '/^[[:space:]]*```/{c=!c; next} !c' /tmp/fd.md | grep -E '^\| \[F-[0-9]+\]' | awk -F'|' '{match($2,/F-[0-9]+/); e=$(NF - 1); gsub(/^ +| +$/,"",e); print substr($2,RSTART,RLENGTH)" "e}' | sort) <(awk '/^[[:space:]]*```/{c=!c; next} c{next} /^### F-[0-9]+ /{match($0,/F-[0-9]+/); cur=substr($0,RSTART,RLENGTH); got=0} /^\| Estado \|/ && cur!="" && !got{s=$0; sub(/^\| Estado \| */,"",s); sub(/ *\|$/,"",s); print cur" "s; got=1}' /tmp/fd.md | sort); echo "fin 2b"
fin 2b
```

Fila e indice de cada hallazgo coinciden en estado, con la columna `Urgencia` nueva.

### 1.6 Promocion de `D-040` contra el esqueleto

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" log --oneline -2
a75cfb6 Promocion del andamiaje desde YouOtto (origen 4409bb9)
7f4381e Promocion del andamiaje desde YouOtto (origen 1add289)
$ for f in protocol-close protocol-start; do F=.claude/skills/$f/SKILL.md; [ "$(git rev-parse 4409bb9:$F)" = "$(git -C "$ESQ" rev-parse a75cfb6:$F)" ] && echo "igual $F" || echo "DISTINTO $F"; done
igual .claude/skills/protocol-close/SKILL.md
igual .claude/skills/protocol-start/SKILL.md
$ git -C "$ESQ" ls-remote origin refs/heads/main | cut -c1-7
a75cfb6
$ for f in protocol-audit protocol-close protocol-start; do F=.claude/skills/$f/SKILL.md; [ "$(git rev-parse d54e314:$F)" = "$(git -C "$ESQ" rev-parse HEAD:$F)" ] && echo "igual $F" || echo "DISTINTO $F"; done
DISTINTO .claude/skills/protocol-audit/SKILL.md
DISTINTO .claude/skills/protocol-close/SKILL.md
DISTINTO .claude/skills/protocol-start/SKILL.md
```

Lo que `D-040` afirma se sostiene, y el desfase posterior que declara la seccion 9 del informe tambien.

### 1.7 Informe contra el diff

- Seccion 0: `F-014` `Aceptado — corregido en este commit` — respaldado (1.3). La columna `Veredicto` no
  usa `Implementado`.
- Seccion 1: cada archivo y cada entrada enumerada esta en `git show d54e314` (`A-004`/`A-005`,
  `D-040`..`D-044`, `T-025`..`T-029`, notas, columna `Urgencia` en registro, plantilla y dos skills,
  control de fechas y trailer en `protocol-close`). No hay en el diff nada relevante sin mencionar.
- Seccion 2: `T-029` `No implementada` — coincide con `tasks.md`.
- Estados: `T-025`..`T-028` `Implementada` en el commit de la sesion, dentro del criterio de `D-027`
  (trabajo en el diff y criterio que reproduce; 1.4).
- `git log -1 --format=%ad --date=short d54e314` = `2026-09-17`, igual a la cabecera del informe y al
  7d (1.3).

### 1.8 Seccion 6 del informe («Que pedimos auditar»)

1. **`T-025`..`T-028` a `Implementada` antes del anclaje.** Dentro de `D-027`: la decision dice que la
   tarea cuyo trabajo esta en el commit y cuyo criterio reproduce pasa a `Implementada` en ese commit y
   el anclaje solo publica la evidencia. Los 13 criterios reproducen (1.4). Sin hallazgo.
2. **Filas 1, 5 y 12 de la seccion 7 que no reproducen.** Son lecturas de un arbol de trabajo en un
   instante, no de un commit, y `D-040` las presenta como el estado antes y despues de promover; no hay
   forma de anclarlas y su no reproduccion no contradice nada publicado. Sin hallazgo. Precision: la
   fila 1 (`git status --short -- .claude ...`) mira el arbol de **este** repositorio, no el del
   esqueleto como dice el texto de la seccion 6; la tabla de la seccion 7 si lo describe bien.
3. **Promover una vez o dos por sesion.** Es una cuestion de practica, no un defecto del commit: va a
   recomendaciones.

### 1.9 Barridos que dieron hallazgo

Ver seccion 2 (`F-016`, `F-017`).

## 2. Hallazgos

### F-016 — `D-041` afirma el resultado de una prueba sobre commits historicos sin publicar la orden

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/decisions.md`, `D-041`, «Alternativas descartadas», tercera vineta |

- **Que se observo:** la alternativa descartada afirma un resultado medido, sin orden ni salida:

  ```
  $ git show d54e314:_persistence/decisions.md | sed -n '2320,2322p'
    - **Mirar todas las filas de indice del diff:** cambiar el estado de una entrada antigua vuelve a
      meter su fila, con su fecha de entonces. Probado sobre los commits de sesion, salta en `079b0a4`,
      `a61e453`, `8509c18`, `27c03bb`, `5884e98` y `101db28`.
  ```

  El bloque que sigue publica solo la version **con** filtro (la adoptada); la version sin filtro, que es
  la que sostiene la frase, no aparece en ningun sitio del commit. El informe la repite en su seccion 3
  («probado sobre seis commits de sesion»). La reconstruyo quitando el filtro `nuevos` y **el resultado
  se sostiene**:

  ```
  $ todas() { d=$(git log -1 --format=%ad --date=short $1); D=$(git diff -U0 $1^ $1 -- _persistence _audit); printf '%s\n' "$D" | grep -E "^\+(\| Fecha \| [0-9]{4}-[0-9]{2}-[0-9]{2} \|$|\| \[[A-Z]+-[0-9]+\].*\| [0-9]{4}-[0-9]{2}-[0-9]{2} \||[[:space:]]*(> )?(- )?🕐 \*\*Nota [0-9]{4}-[0-9]{2}-[0-9]{2})" | grep -vF "$d" | wc -l; }; for c in $(git log d54e314^ --format='%h %s' | grep -E ' S-0[0-9]+: ' | grep -v 'ancla el informe' | cut -d' ' -f1); do echo "$c $(todas $c)"; done
  cf2992f 14
  1358c3c 0
  079b0a4 3
  a61e453 1
  8509c18 3
  27c03bb 1
  87d10b8 0
  081385a 0
  5884e98 1
  c07680f 0
  101db28 7
  e222812 0
  5cae773 0
  ```

- **Por que importa:** `CLAUDE.md` exige que todo resultado que el registro afirme vaya con su patron y
  su ambito, «aunque nadie lo haya pedido». Aqui el resultado es cierto, pero para saberlo hubo que
  reconstruir la orden: la justificacion del filtro del Paso 7d —la pieza que evita que el control se
  apague por ruido— depende de una medicion que no es reproducible desde el registro. `Baja`: el
  resultado se confirma y ningun control depende de el. `No bloqueante`: nada de lo que se haga despues
  hereda el defecto.
- **Que lo corregiria:** una nota fechada bajo `D-041` que publique la orden sin filtro y su salida, sin
  reescribir la vineta. ⚠️ Es una recomendacion, no una orden.

### F-017 — La NOTA DE CIERRE de `S-013` dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_audit/S-013.md`, seccion 7, NOTA DE CIERRE, prosa tras «CONTROL DE CIFRA ADYACENTE — salida:» |

- **Que se observo:** el bloque publicado tiene 17 lineas y la frase inmediatamente posterior dice 16:

  ```
  $ git show 17d769f:_audit/S-013.md | sed -n '/^\*\*CONTROL DE CIFRA ADYACENTE — salida:\*\*$/,/^Ninguna de las/p' | grep -c '^_audit/S-013.md:'
  17
  $ git show 17d769f:_audit/S-013.md | grep -n 'Ninguna de las dieciseis lineas'
  497:Ninguna de las dieciseis lineas afirma una cifra que no salga del bloque inmediatamente anterior o de
  ```

  Las 17 lineas publicadas son las de los numeros 105, 116, 182, 183, 323, 373, 385, 386, 424, 425, 451,
  465, 466, 498, 499, 515 y 516.
- **Por que importa:** es exactamente el defecto para el que existe ese control —una cifra tecleada en
  la prosa que sigue a un bloque, distinta de la que el bloque da—, y aparece en la lectura del propio
  control, que afirma haber revisado cada linea contra su bloque. El registro afirma un recuento que su
  salida contradice. `Baja`: la salida esta publicada entera y el lector puede contar; ninguna decision
  ni control depende de la cifra. `No bloqueante`: no contamina trabajo posterior.
- **Que lo corregiria:** una nota fechada en `S-013.md` que corrija el recuento, sin reescribir la
  linea. Y valorar si la lectura del control debe citar el recuento con una orden (`| grep -c .`) en vez
  de teclearlo. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-014 | Aceptado — pendiente | **Implementado** (d54e314) | Las dos partes estan en el diff de `d54e314`: seis notas fechadas `2026-09-17` (`D-036`..`D-039`, `L-013`, `S-011.md`) sin reescribir lo publicado, y el control de fechas del Paso 7d en `protocol-close`, que reproduce las 11 lineas de `F-014` sobre `cf2992f` y sale limpio sobre `079b0a4` y sobre `d54e314` (1.3, 1.4) |

Ningun otro hallazgo estaba abierto:

```
$ git show d54e314:_audit/findings.md | grep -E '^\| \[F-' | grep -vc '| Implementado |$'
1
```

(la unica fila es `F-014`.)

## 4. Lo que NO se pudo comprobar

- **Las filas 1, 5 y 12 de la seccion 7 del informe** (arboles de trabajo en un instante ya pasado): no
  reproducibles por naturaleza; ver 1.8.
- **Que el usuario eligiera lo que `D-040`, `D-041`, `D-043` y `D-044` le atribuyen**: nace en la
  conversacion y no deja rastro en el repositorio.

Todos los demas controles corrieron.

## 5. Recomendaciones sin hallazgo

- **Registrar la retirada de `R-014` y `F-015`.** `c180fd5` (posterior al commit auditado) revierte una
  auditoria que el propio `session-closer` ejecuto sobre su cierre y declara los ids retirados, pero
  ninguna `D-XXX` lo recoge todavia (1 del encabezado: `grep -cE 'R-014|F-015'` = `0` en `decisions.md`
  a `c180fd5`). Es la segunda vez tras `R-011`/`F-012` (`D-038`, `L-013`): conviene registrarlo como
  entonces y valorar si `L-013` basta o hace falta algo mecanico.
- **Segunda pasada de promocion al final de la sesion.** `D-040` promovio al principio y `D-041`..`D-043`
  volvieron a separar cuatro archivos del esqueleto en la misma sesion (1.6). Promover al principio no
  es un defecto; si el patron se repite, cada sesion arrancara con promocion pendiente.
- **Paso 9 de `protocol-audit`.** `D-043` anade la urgencia al hallazgo, a `findings.md` y al reporte de
  arranque, pero la plantilla del reporte en pantalla del auditor sigue diciendo `F-NNN (<gravedad>)`.
  No lo pedia `D-043`; seria coherente con ella.
- **Redaccion de la seccion 6 del informe.** Atribuye las filas 1, 5 y 12 al arbol del esqueleto; la
  fila 1 es del arbol de este repositorio (1.8).
