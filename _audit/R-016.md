# Auditoria R-016 — sobre S-014

| Campo | Valor |
|---|---|
| Auditoria | R-016 |
| Informe auditado | `_audit/S-014.md` |
| Commit auditado | 91562b7 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (1) |

**Sobre el id.** El ultimo `R-XXX` del arbol es `R-015`; `R-014` esta retirado (`D-047`). Esta es `R-016`
y su hallazgo es `F-018` (`F-015` retirado, `F-017` el ultimo en uso).

## 1. Que se comprobo

### Paso 1 — commit auditado

```
$ git log -1 --format=%h -- _audit/S-014.md
39b82d1
$ git show 39b82d1:_audit/S-014.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `91562b7` |
$ git show --stat --format= 39b82d1
 _audit/S-014.md           | 155 +++++++++++++++++++++++++++++++++++++++++++---
 _persistence/decisions.md |  14 +++--
 2 files changed, 156 insertions(+), 13 deletions(-)
$ git show --stat --name-only --format= 91562b7 | grep -c .
11
```

Hash literal distinto del derivado: `39b82d1` es el commit de anclaje. Se audita `91562b7`, cuyos 11
archivos coinciden con la lista anclada de la seccion 1 del informe (9 de staging + `_audit/S-014.md` +
`_audit/index.md`).

### Paso 3 — informe contra diff (lo reproducido)

```
$ for f in _audit/findings.md _persistence/progress.md _persistence/decisions.md; do echo "$f U3=$(git diff 91562b7^ 91562b7 -- $f | grep -c '^@@') U0=$(git diff -U0 91562b7^ 91562b7 -- $f | grep -c '^@@')"; done
_audit/findings.md U3=5 U0=5
_persistence/progress.md U3=3 U0=6
_persistence/decisions.md U3=2 U0=2
```

Las cifras de hunks del informe (5, 6, 2) cuadran con `-U0`.

Criterios de cierre anclados de `D-046`, `D-047`, `D-048`:

```
$ git show 91562b7:_audit/findings.md | grep -cE '^\| \[F-01[67]\].*\| Aceptado — pendiente \|$|^\| Registrado en \| T-03[01], D-046 \|$'
4
$ git show 91562b7:_persistence/decisions.md | grep -cE '^(\| \[D-047\]|### D-047 )'
2
$ git show 91562b7:.claude/agents/session-closer.md | grep -cF 'command: node .claude/hooks/allow-only-skill.js protocol-close'
1
$ git ls-tree --name-only 91562b7 .claude/hooks/ | wc -l
2
```

Orden anclada del Paso 2d y segunda pasada del 2e:

```
$ git diff -U0 91562b7^ 91562b7 -- _persistence _audit ":(exclude)_audit/S-014.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
17
$ git diff --name-only --diff-filter=d 91562b7^ 91562b7 | wc -l
11
```

Afirmaciones de `D-046`, `D-047` y `D-048` sobre commits anteriores:

```
$ git show 2063f09:_persistence/decisions.md | grep -nF 'salta en `079b0a4`'
2323:    meter su fila, con su fecha de entonces. Probado sobre los commits de sesion, salta en `079b0a4`,
$ git show 2063f09:_audit/S-013.md | grep -nF 'dieciseis lineas'
497:Ninguna de las dieciseis lineas afirma una cifra que no salga del bloque inmediatamente anterior o de
$ git log --all --format=%h -- _audit/R-014.md
c180fd5
798500e
$ git show 798500e:_audit/findings.md | grep -oE "^### F-015 - .*"
### F-015 - El CONTROL DE CIFRA ADYACENTE publicado en la NOTA DE CIERRE de S-013 no reproduce contra el commit de anclaje, por su propia insercion
$ git show 2063f09:_audit/findings.md | grep -cE "^(\| \[F-015\]|### F-015 )"
0
$ git ls-tree --name-only 2063f09 _audit/ | grep -c R-014
0
$ git show -s --format="%h %ad %s" --date=iso 709050c 798500e
709050c 2026-09-16 21:03:06 -0500 auditoria R-011 sobre S-011 (079b0a4)
798500e 2026-09-17 08:06:30 -0500 auditoria R-014 sobre S-013 (d54e314)
$ git show 2063f09:_audit/R-015.md | grep -n "Sobre el id"
11:**Sobre el id.** El ultimo id del arbol es `R-013`, pero `R-014` y `F-015` ya se consumieron: los creo
$ git show 2063f09:.claude/agents/session-closer.md | grep -nE "^tools:"
4:tools: Read, Write, Edit, Glob, Grep, Bash, Skill
$ git show 2063f09:.claude/skills/protocol-close/SKILL.md | grep -nF "Tu no lanzas la auditoria"
2262:- 🚨 **Tu no lanzas la auditoria, pero la reclamas.** El agente `report_auditor` corre despues de ti,
```

Seccion 0 del informe («los dos hallazgos que dejo abiertos, y ningun otro»):

```
$ git show 2063f09:_audit/findings.md | sed -n '/^## Indice/,/^---/p' | grep -E '^\| \[F-' | awk -F'|' '{print $2"|"$(NF-1)}' | grep -v Implementado
 [F-016](#f-016---d-041-afirma-el-resultado-de-una-prueba-sobre-commits-historicos-sin-publicar-la-orden) | Abierto
 [F-017](#f-017---la-nota-de-cierre-de-s-013-dice-dieciseis-lineas-debajo-de-un-control-de-cifra-adyacente-que-publica-diecisiete) | Abierto
```

Test del hook sobre los blobs del commit (no sobre el arbol de trabajo), y prueba de que puede fallar:

```
$ D=$(mktemp -d); mkdir -p $D/.claude/hooks; for f in allow-only-skill.js allow-only-skill.test.js; do git show 91562b7:.claude/hooks/$f > $D/.claude/hooks/$f; done; node --test $D/.claude/hooks/allow-only-skill.test.js 2>&1 | grep -E '^(✔|✖|ℹ (tests|pass|fail))'
✔ bloquea una skill distinta de la permitida (128.2661ms)
✔ deja pasar la skill permitida (114.4382ms)
✔ no toca otras herramientas (100.8107ms)
✔ bloquea si la entrada no es JSON (126.4775ms)
✔ bloquea una skill sin nombre (100.0765ms)
ℹ tests 5
ℹ pass 5
ℹ fail 0
$ sed 's/if (allowed \&\& skill === allowed) process.exit(0);/process.exit(0);/' $D/.claude/hooks/allow-only-skill.js > $D/m.js && cp $D/m.js $D/.claude/hooks/allow-only-skill.js && node --test $D/.claude/hooks/allow-only-skill.test.js 2>&1 | grep -E '^ℹ (pass|fail)'
ℹ pass 3
ℹ fail 2
```

Con el script mutado para dejar pasar todo, dos tests fallan: el test no esta escrito para pasar.

Fechas que anade el commit (Paso 7d):

```
$ git diff -U0 91562b7^ 91562b7 | grep -oE '^\+.*20[0-9]{2}-[0-9]{2}-[0-9]{2}' | grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' | sort | uniq -c
      1 2026-09-15
      1 2026-09-16
     22 2026-09-17
$ git diff -U0 91562b7^ 91562b7 | grep -nE '^\+.*2026-09-1[56]' | cut -c1-160
215:+| [A-001](#a-001---los-juegos-registrados-pueden-ser-datos-de-personas) | Los juegos registrados pueden ser datos de personas | 2026-09-15 | Abierto |
636:+  709050c 2026-09-16 21:03:06 -0500 auditoria R-011 sobre S-011 (079b0a4)
```

Las dos fechas distintas de `2026-09-17` son salidas citadas dentro de bloques (fila de `A-001` en el
informe, fecha de `709050c` en `D-047`), no fechas de entradas nuevas.

Historia del agente temporal `hook-probe`:

```
$ git log --all --oneline -- .claude/agents/hook-probe.md
$ ls .claude/agents/
gate1_auditor.md
gate2_auditor.md
phase_exit_auditor.md
report_auditor.md
session-closer.md
session-starter.md
```

Nunca se commiteo; `A-006` y `D-048` solo lo citan. Ver seccion 4.

### Paso 4 — controles mecanicos sobre `91562b7`

**a) Indice ↔ detalle en `_persistence/`:** coherentes.

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 91562b7:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 91562b7:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

**b) Carpetas contra las declaradas:** dos diferencias, las dos con razon escrita (`temporal/` excluida en
`.gitignore` segun `project.md`; `005_discovery/` declaracion anticipada, igual que la trato `R-015`).

```
$ diff <(git ls-tree -d --name-only 91562b7 | sed 's|$|/|' | sort) <(git show 91562b7:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort); echo "exit=$?"
1a2
> 005_discovery/
8a10
> temporal/
exit=1
$ git show 91562b7:.gitignore | grep -n temporal
5:temporal/
```

**c) Fuga de datos propios:** cero lineas.

```
$ git grep -nE "YouOtto|Proyectos_TripleS|github\.com" 91562b7 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

**d) Codigos instanciados en `_phases` y `_workflow`:** cero lineas.

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 91562b7 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

**Coherencia de `_audit/findings.md` en el commit auditado**, indice y detalle, y fila contra ficha:

```
$ diff <(git show 91562b7:_audit/findings.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) <(git show 91562b7:_audit/findings.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u); echo "exit=$?"
exit=0
$ git show 91562b7:_audit/findings.md | awk '/^### F-/{id=$2} /^\| Estado \|/{print id" ficha:"$0}' | tail -2
F-016 ficha:| Estado | Aceptado — pendiente |
F-017 ficha:| Estado | Aceptado — pendiente |
```

## 2. Hallazgos

### F-018 — D-045 publica en resumen, sin orden ni salida, los resultados del Paso 1b de la promocion
| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/decisions.md`, `D-045` (commit `91562b7`) |

- **Que se observo:** `D-045` afirma tres resultados sin la orden literal ni su salida cruda:

  ```
  $ git show 91562b7:_persistence/decisions.md | sed -n '/^### D-045 /,/^### D-046 /p' | grep -nE 'Paso 1b|salida vacia|diez parejas|nueve copias'
  26:  (`ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"`.) Paso 1b: nueve copias de la raiz
  80:  Barrido del Paso 1 despues de promover, antes de tocar `.claude/` por `D-048`: salida vacia. Paso 1b
  81:  sobre `1bec59a` (plantilla y copia leidas con `git -C "$ESQ" show 1bec59a:`): las diez parejas dan `0`.
  ```

  La orden del Paso 1b de `protocol-promote` imprime una linea por pareja (diez); `D-045` publica solo
  el resumen («nueve dan `0` y `_audit/findings.md` da `21`», «las diez parejas dan `0`»). La segunda
  medicion, ademas, se hizo con una forma distinta de la literal del protocolo (lee con
  `git -C "$ESQ" show 1bec59a:`, mientras la orden del protocolo lee `$ESQ/$c` del arbol), y esa forma no
  esta escrita. El «Barrido del Paso 1 despues de promover: salida vacia» tampoco lleva su orden.
- **Por que importa:** `CLAUDE.md` exige orden y ambito para todo resultado que el registro afirme; es la
  misma clase de defecto que `F-016`, que `manager` acepto con `D-046`. La medicion posterior al commit
  del esqueleto es justo la que confirma que la promocion no dejo copias viejas, y no se puede reproducir
  desde el registro sin reinventar la orden. `Baja`: el resto de la promocion si lleva orden y salida
  (`cmp` de contenido contra `1bec59a`, `--stat` del commit), y ningun control depende de estas cifras.
  `No bloqueante`: nada posterior hereda el defecto.
- **Que lo corregiria:** una nota fechada en `D-045`, sin reescribir lo publicado, con la orden del Paso
  1b en la forma anclada a `1bec59a` que se uso y su salida de diez lineas (y la del barrido del Paso 1
  posterior). ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-016 | Aceptado — pendiente | Aceptado — pendiente | `T-030` sigue `No implementada`; el diff de `91562b7` no toca `D-041` (hunks de `decisions.md`: `@@ -67,0 +68,4 @@` indice y `@@ -2538,0 +2543,267 @@` al final del archivo, entradas nuevas) |
| F-017 | Aceptado — pendiente | Aceptado — pendiente | `T-031` sigue `No implementada`; `_audit/S-013.md` no esta en el commit (`git show --stat --format= 91562b7 -- _audit/S-013.md \| wc -l` = `0`) |

Los dos estan registrados con `T-XXX` y `D-046`, que es lo que exige `Aceptado — pendiente`. Ningun otro
`F-NNN` sigue sin cerrar.

## 4. Lo que NO se pudo comprobar

- **La salida del agente temporal `hook-probe` que confirma `A-006`.** El agente nunca se commiteo
  (`git log --all -- .claude/agents/hook-probe.md` sin salida), asi que la unica evidencia es la linea
  pegada en `A-006`. Es verosimil y consistente con el script (el texto «Bloqueado: este agente solo puede
  invocar la skill…» es literal del script), pero no es reproducible desde el repositorio.
- **Si el `session-closer` de esta misma sesion corrio ya bajo el hook.** No hay rastro en el repositorio
  que lo diga en un sentido u otro. Lo unico observable: no hay commit de auditoria del cierre posterior a
  `39b82d1` (`git log --oneline -12` muestra `39b82d1` como `HEAD` al lanzar esta auditoria).
- **La promocion al esqueleto (`1bec59a`).** Vive fuera de este repositorio; no se contrasto.
- **La afirmacion de `D-048` sobre la documentacion de Claude Code** (hooks en cabecera de agente, solo
  activos con ese agente). No lleva la consulta publicada. Indicio indirecto a favor: esta auditoria cargo
  `protocol-audit` con la herramienta `Skill` sin bloqueo, luego el hook no afecta a `report_auditor`.

## 5. Recomendaciones sin hallazgo

- **`T-032` y `A-006` hablan de dos cosas distintas con el mismo nombre.** `A-006` esta `Confirmado` (sin
  reinicio), pero el criterio de `T-032` exige «`A-006` confirmado tras reiniciar Claude Code». Lo que de
  verdad queda pendiente —que el `session-closer` real este protegido— no tiene supuesto propio (el
  informe lo dice en su seccion 4). Conviene que el criterio de `T-032` nombre esa comprobacion concreta
  en vez de remitir a un supuesto ya cerrado.
- **Limite 1 de `D-048` (seccion 6 del informe).** El hook solo cubre la herramienta `Skill`: el agente
  puede leer `.claude/skills/protocol-audit/SKILL.md` con `Read` y seguirlo, o escribir `_audit/R-XXX.md`
  con `Write`/`Bash`. Esta declarado; si se quiere cerrar, la via mecanica analoga seria un hook sobre
  `Write`/`Edit` que rechace rutas `_audit/R-*.md` en ese agente.
- **Fallo abierto del hook.** Si `node` no esta en el `PATH` o el comando sale con un codigo distinto de
  `2`, la llamada pasa. `A-006` lo dice; valdria anadirlo a los limites de `D-048` para que quien lo lea
  no tenga que llegar al supuesto.
- **Aplazamientos `No bloqueante` sin horizonte (seccion 6 del informe).** `D-046` es un uso conforme a
  `D-043` y a las convenciones de `findings.md` («Aplazar no es olvidar»: siguen apareciendo en cada
  arranque). No es un defecto hoy; si se quiere un limite, seria una decision nueva de `manager`.
- **Consulta de documentacion en `D-048`.** Publicar la orden `ctx7` y el extracto relevante haria
  contrastable el fundamento de la decision, aunque `A-006` ya lo respalde empiricamente.
