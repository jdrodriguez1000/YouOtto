# Informe de auditoria — S-010

| Campo | Valor |
|---|---|
| Sesion | S-010 |
| Fecha | 2026-09-16 |
| Etapa | 005_discovery |
| Rama | main |
| Commit auditado | a61e453 |

## 1. Que se hizo

```
$ git show --stat --name-only --format= a61e453
.claude/skills/protocol-close/SKILL.md
_audit/S-010.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
```

📌 **Ancladas por el Paso 7c al commit `a61e453`.** La lista reproduce la publicada arriba (era la
misma, obtenida del area de staging antes del commit).

`report_auditor` audito `S-009` en `R-009` y no entrego hallazgos: solo tres recomendaciones sin
hallazgo y el estado, ya cerrado como `Implementado`, de los diez hallazgos anteriores. Esta sesion
evalua esas tres recomendaciones mas una cuarta sobre una errata, y ejecuta la promocion al
esqueleto de arranque que una de ellas pedia.

- `.claude/skills/protocol-close/SKILL.md` — 1 hunk (`git diff a61e453^ a61e453 -U0 -- <archivo> | grep -c
  '^@@'`). Anade, en el bullet «El estado lo decide la evidencia del diff» del Paso 4, la frase que
  declara el caso contrario al de `D-027`: si el trabajo esta en el diff pero el criterio de cierre
  no reproduce, la tarea sigue en `No implementada`. Es la unica edicion del andamiaje de esta
  sesion.
- `_persistence/decisions.md` — 2 hunks (`git diff a61e453^ a61e453 -U0 -- <archivo> | grep -c '^@@'`): una
  fila de indice y un bloque de 5 entradas nuevas, `D-028 (nace)`, `D-029 (nace)`, `D-030 (nace)`,
  `D-031 (nace)`, `D-032 (nace)`; `D-029` es en si misma una nota fechada que se ancla debajo de
  `D-027` (entrada existente que el commit edita).
- `_persistence/lessons.md` — 2 hunks: `L-012 (nace)`.
- `_persistence/tasks.md` — 2 hunks: `T-017 (nace)`, `T-018 (nace)`, `T-019 (nace)`, las tres ya
  escritas en `Implementada` porque su trabajo esta en este mismo commit y su criterio de cierre
  reproduce (Paso 4 de `protocol-close`, regla de `D-027`/`D-028`).
- `_persistence/techdebt.md` — 4 hunks: `DT-004 (nace)`; `DT-003` (entrada existente que el commit
  edita: `Estado` pasa a `Implementada`, `Importancia` a `Alta`, y dos notas fechadas).
- `_persistence/progress.md` — 6 hunks: sobrescritas las secciones 1, 2 y 3, y nace la entrada
  `S-010` en la Bitacora (seccion 5), con su fila en el indice de sesiones.
- `_audit/S-010.md` (este informe) y `_audit/index.md` (su fila) — los anade el propio cierre.

## 2. Que NO se hizo, y por que

El indice de `_persistence/tasks.md` no tiene ninguna fila `No implementada`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
(sin salida)
```

No queda ninguna tarea abierta al cierre de esta sesion.

## 3. Decisiones tomadas

- **`D-028` — El Paso 4 declara tambien el caso del criterio que no reproduce.** Se acepta la
  primera recomendacion sin hallazgo de `R-009`: el bullet que `D-027` anadio al Paso 4 solo
  enunciaba el caso positivo (trabajo en el diff + criterio que reproduce → `Implementada`); el caso
  contrario se deducia de los bullets vecinos, pero no estaba escrito. Se anade una frase al final
  del mismo bullet. Alternativa descartada: no tocarla, porque los bullets vecinos ya lo resuelven
  en la practica — se descarta porque la propia `R-009` declaro que no es obvio para quien lee.
- **`D-029` — `D-027` se completa con los criterios sin anclar de sus precedentes.** Se acepta la
  segunda recomendacion: la orden de precedentes de `D-027` probaba el estado de las tareas, pero no
  que su criterio de cierre siguiera con `<hash>` en el commit de sesion; esa mitad la aportaba
  `R-008`. Se anade una nota fechada debajo de `D-027`, con orden anclada a los commits de sesion y
  de anclaje de `S-004`, `S-006` y `S-007`, y su salida cruda. Alternativas descartadas: reescribir
  el bloque de precedentes (una decision no se reescribe) y descartarla (la prueba ya existe, pero
  repartida entre dos archivos).
- **`D-030` — La promocion de `DT-003` pasa a `Alta` y va antes que los actores.** Se acepta la
  tercera recomendacion: `DT-003` lleva tres sesiones seguidas creciendo (`S-007`, `S-008`, `S-009`)
  y `D-028` la hace crecer otra vez. Sube a Importancia `Alta` y ordena promoverla ya, antes de la
  clasificacion de actores. Alternativas descartadas: subir a `Alta` sin fecha de promocion (los
  actores irian antes y el desfase seguiria creciendo) y dejarla en `Media` (es lo que ha dejado
  crecer la deuda tres sesiones).
- **`D-031` — No se corrige el orden de los hashes de la seccion 3 de `S-009`.** Se rechaza la
  cuarta recomendacion: la seccion 3 de `S-009.md` escribe los hashes de los precedentes en orden
  inverso a las sesiones (`S-007`/`S-006`/`S-004` en vez de `S-004`/`S-006`/`S-007`), pero cada hash
  va etiquetado con su sesion y el propio auditor senalo que no induce a error. `S-009.md` ya esta
  auditado; corregirlo lo haria describir un estado que `R-009` no juzgo. Mismo criterio que `D-018`.
  Alternativa descartada: nota fechada en `S-009.md` — se descarta por ser un cambio a un informe
  auditado por una errata que no engana.
- **`D-032` — Promocion al esqueleto de los tres archivos de `DT-003`.** El usuario ejecuta la
  promocion que pedia `D-030`. El primer intento choco con el Paso 0 de `protocol-promote`: la frase
  de `D-028` estaba sin commitear en `protocol-close/SKILL.md`, y esa skill es una de las seis areas
  que la promocion exige limpias. El usuario aprobo retirar la frase del arbol de trabajo (guardada
  aparte), promover los tres archivos del andamiaje y la copia de la raiz `_persistence/decisions.md`
  desde el ultimo commit auditado (`95c5cfd`) al esqueleto de arranque (commit `447c2a0` en
  `SDAI_TripleS`, subido), y reponer la frase despues. `DT-003` queda `Implementada`, con la frase de
  `D-028` fuera a proposito de esa promocion. Alternativas descartadas: cerrar la sesion, auditar y
  promover en la siguiente (es el circulo que motivo la decision: cada sesion que corrige el
  andamiaje antes de promover vuelve a bloquear la promocion) y promover tambien la frase sin
  commitear (el esqueleto tendria una version sin procedencia citable).

## 4. Supuestos vigentes y riesgos

```
$ grep -oE '^\| \[A-[0-9]+\][^|]*\|[^|]*\|[^|]*\| Abierto' _persistence/assumptions.md
| [A-001](#a-001---los-juegos-registrados-pueden-ser-datos-de-personas) | Los juegos registrados pueden ser datos de personas | 2026-09-15 | Abierto
```

`A-001` sigue `Abierto`: si el historial de juegos registrados es dato de persona. Su disparador —la
clasificacion de actores de `005_discovery`— ya esta activo desde que la etapa se declaro (`S-005`) y
sigue siendo el siguiente trabajo de producto pendiente. Si resulta falso (no es dato de persona), no
hay impacto; si resulta cierto, la clasificacion de actores tiene que tratarlo como tal desde el
primer borrador.

Riesgo propio de esta sesion, sin codigo todavia: la promocion de `D-032` dejo fuera, a proposito, la
frase de `D-028` en `protocol-close/SKILL.md`. Se propone como `DT-004` en `_persistence/techdebt.md`
(`Propuesta (pendiente del usuario)`), verificado con el Paso 2f de este mismo cierre.

## 5. Siguiente tarea propuesta

Correr `protocol-promote` de nuevo para llevar al esqueleto de arranque la frase de `D-028`, ya
commiteada en `protocol-close/SKILL.md`, y cerrar `DT-004`. Importancia `Baja`, `No bloqueante`: el
desfase es de una sola linea y ya esta declarado.

## 6. Que pedimos auditar

- Que las tres tareas nacidas esta sesion (`T-017`, `T-018`, `T-019`) esten bien escritas
  `Implementada` desde su nacimiento: las tres citan un criterio de cierre (`D-028`, `D-029`,
  `D-032`) cuyo trabajo esta en este mismo commit y cuyas ordenes sin `<hash>` ya reproducen contra
  el arbol antes de commitear (ver seccion 7). Es la primera vez que una tarea nace directamente en
  `Implementada` en la misma sesion en que se escribe su decision — todas las anteriores nacieron y
  se movieron en sesiones distintas.
- El punto en el que menos seguros estamos: si `DT-004` es la forma correcta de registrar el
  desfase residual que deja `D-032`, o si bastaba con la nota fechada que ya tiene `DT-003` (que dice
  explicitamente «no es esta deuda»). Se abrio como propuesta nueva por el mismo patron que `DT-001`
  y `DT-003`, pero es la primera vez que una promocion deja un desfase de una sola frase en vez de un
  archivo entero, y el criterio de cuando vale la pena una `DT-XXX` separada frente a una nota no
  esta escrito en ningun lado.
- Que `D-031` clasifique correctamente: no se corrige un informe ya auditado por una errata que no
  induce a error. No hay tabla de acciones reversibles/irreversibles que cubra este caso especifico;
  se aplico el mismo criterio que `D-018`, sin volver a declararlo como criterio en la propia `D-031`.

## 7. Evidencia del Paso 2d

Orden (forma de staging; el Paso 7c la traducira a la forma anclada al commit, excluyendo este
propio informe):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

⚠️ **Esta orden es parcialmente autorreferencial**, porque este mismo informe vive en `_audit/` y
sus propios bloques de evidencia (incluido este) contienen lineas `$ ...`. Los items 4, 5 y 6 de la
lista de abajo son las tres cabeceras de esta misma seccion (Orden / Recuento / `uniq -d`); el item 7
es la linea de salida de `uniq -d`, que reproduce texto de otra orden y por eso tambien empieza por
`$ `. Los items 1, 2 y 3 son ordenes nuevas de las secciones 1, 2 y 4 de este informe, escritas hoy
y sujetas a la misma regla que cualquier otra: tienen que reproducir.

Recuento de LINEAS devueltas (el unico que publica este paso; incluye las autorreferenciales):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
39
```

Lista completa, numerada:

```
 1  $ git diff --cached --stat --name-only
 2  $ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
 3  $ grep -oE '^\| \[A-[0-9]+\][^|]*\|[^|]*\|[^|]*\| Abierto' _persistence/assumptions.md
 4  $ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
 5  $ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
 6  $ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
 7  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
 8  $ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
 9  $ git diff --cached --name-only --diff-filter=d | wc -l
10  $ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
11  $ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
12  $ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
13  $ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
14  $ for p in "c07680f 74749f7 D-013 D-014" "081385a d420647 D-022 D-023" "87d10b8 ee59542 D-023 D-024"; do set -- $p; for c in $1 $2; do echo "== $c $3 $(git show $c:_persistence/decisions.md | sed -n "/^### $3 /,/^### $4 /p" | grep -cE '^[[:space:]]*\$ .*<hash>')"; done; done
15  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
16  $ git grep -nE "YouOtto|Company_TripleS|github.com" <hash> -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
17  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
18  $ git show <hash>:_persistence/decisions.md | sed -n '/^### D-027 /,/^### D-028 /p' | grep -c '🕐 **Nota 2026-09-16 (`R-009`, `D-029`)'
19  $ git log --format='%h %s' -- .claude/skills/protocol-close/SKILL.md | head -3
20  $ git archive 95c5cfd | tar -x -C "$T"; (cd "$T" && for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md)
21  $ git show <hash>:_persistence/techdebt.md | grep -E '^\| \[DT-003\]' | grep -c '| Alta | No bloqueante |'
22  $ git show <hash>:_persistence/techdebt.md | sed -n '/^### DT-003 /,$p' | grep -c '^| Importancia | Alta |$'
23  $ git show <hash>:_persistence/tasks.md | grep -E '^\| \[T-019\]' | grep -c '| Alta | No bloqueante |'
24  $ git show <hash>:_audit/S-009.md | sed -n '47p'
25  $ git status --short -- .claude CLAUDE.md _phases _methodology _templates _workflow
26  $ git status -sb | head -1
27  $ git rev-parse --short HEAD; git rev-parse --short origin/main
28  $ git grep -nE "YouOtto|Company_TripleS|github.com" 95c5cfd -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
29  $ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 95c5cfd -- _phases _workflow | grep -vE ':PI-[0-9]+$' | wc -l
30  $ git grep -nE "USUARIO|jdrodriguez|gmail|SDAI_TripleS|TripleS|Triple S|[A-Z]:[/\\\\]|/Users/|AppData" 95c5cfd -- .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md | wc -l
31  $ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" show --stat --format='%h %s' 447c2a0 | tail -5
32  $ git -C "$ESQ" ls-remote origin refs/heads/main | cut -c1-7
33  $ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md; do [ "$(git rev-parse 95c5cfd:$f)" = "$(git -C "$ESQ" rev-parse 447c2a0:$f)" ] && echo "igual $f" || echo "DISTINTO $f"; done
34  $ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; echo "fin P1"
35  $ for p in 005_project.md:project.md 010_progress.md:_persistence/progress.md 015_tasks.md:_persistence/tasks.md 020_decisions.md:_persistence/decisions.md 025_constraints.md:_persistence/constraints.md 030_assumptions.md:_persistence/assumptions.md 035_lessons.md:_persistence/lessons.md 040_techdebt.md:_persistence/techdebt.md 045_audit_index.md:_audit/index.md 050_audit_findings.md:_audit/findings.md; do t=${p%%:*}; c=${p#*:}; printf '%3d  %s\n' "$(diff <(git -C "$ESQ" show 447c2a0:_templates/000_preproject/$t) <(git -C "$ESQ" show 447c2a0:$c) | grep -c '^[<>]')" "$c"; done
36  $ git show <hash>:_persistence/techdebt.md | grep -E '^\| \[DT-003\]' | grep -c '| Implementada |'
37  $ git show <hash>:_persistence/techdebt.md | sed -n '/^### DT-003 /,$p' | grep -c '^| Estado | Implementada |$'
38  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
39  $ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; diff -rq --strip-trailing-cr "$ESQ/.claude" .claude
```

Salida de `uniq -d` (pares repetidos), entera:

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'no reproduce, la tarea'
```

Las lineas 15 y 38 son la misma orden (criterio de cierre de `D-028` citado en `T-017`, vuelto a
citar en `D-032`), y estan en la salida de `uniq -d`.

Reejecucion de cada linea, contra el arbol antes de commitear:

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git diff --cached --stat --name-only` | Si | los ocho archivos de la seccion 1 |
| 2 | `sed -n .../tasks.md \| grep "No implementada"` | Si | sin salida |
| 3 | `grep -oE ... assumptions.md` | Si | la fila de `A-001` |
| 4 | `git diff --cached -U0 ... \| grep ... \| grep -v ...` (sin `wc -l` ni `uniq`) | Autorreferencial | es la propia «Orden» de esta seccion; no hay nada que reejecutar aparte, es la lista misma |
| 5 | igual que 4, con `\| wc -l` | Autorreferencial | es el propio «Recuento» de esta seccion |
| 6 | igual que 4, con `\| sort \| uniq -d` | Autorreferencial | es el propio `uniq -d` de esta seccion |
| 7 | `git show <hash>:...SKILL.md \| grep -cF 'no reproduce, la tarea'` | No aplica | es la linea de **salida** del item 6 (la orden duplicada que `uniq -d` encontro), no una orden nueva a reejecutar; coincide con los items 15 y 38 |
| 8 | barrido de caracteres de control | Si | sin salida (ver seccion 8) |
| 9 | `git diff --cached --name-only --diff-filter=d \| wc -l` | Si | `6` |
| 10 | total de caracteres de control | Si | `0` |
| 11 | `ESQ="..."` | Si | asignacion, sin salida propia |
| 12 | `for d in ...; do diff -rq ...; done` | Si | 1 linea, `protocol-close/SKILL.md differ` (ver seccion 9) |
| 13 | `diff -q "$ESQ/CLAUDE.md" CLAUDE.md` | Si | sin salida |
| 14 | `for p in "c07680f 74749f7 D-013 D-014" ...` | Si | `c07680f D-013 2`, `74749f7 D-013 0`, `081385a D-022 7`, `d420647 D-022 0`, `87d10b8 D-023 4`, `ee59542 D-023 0` |
| 15 | `git show <hash>:...SKILL.md \| grep -cF 'no reproduce, la tarea'` | No anclable todavia | `<hash>` sin resolver: es el criterio de cierre de `D-028`/`T-017`, lo ancla el Paso 7c-bis |
| 16 | `git grep -nE "YouOtto\|Company_TripleS\|github.com" <hash> -- ...` | No anclable todavia | idem, criterio de `D-028` |
| 17 | `git show <hash>:...SKILL.md \| grep -noE ...` | No anclable todavia | idem, criterio de `D-028` |
| 18 | `git show <hash>:_persistence/decisions.md \| sed -n ...D-029...` | No anclable todavia | criterio de `D-029` |
| 19 | `git log --format='%h %s' -- ...SKILL.md \| head -3` | Si | `8509c18 ...`, `27c03bb ...`, `87d10b8 ...` (contexto de `D-030`) |
| 20 | `git archive 95c5cfd \| tar -x -C "$T"; ...` | No anclable | depende de `$T` (carpeta del scratchpad de `S-010`, ya no existe) y `$ESQ`; es evidencia de contexto de `D-030`, no un criterio de cierre |
| 21 | `git show <hash>:_persistence/techdebt.md \| grep ... DT-003 ... Alta` | No anclable todavia | criterio de `D-030` |
| 22 | `git show <hash>:_persistence/techdebt.md \| sed -n .../DT-003/... Importancia Alta` | No anclable todavia | criterio de `D-030` |
| 23 | `git show <hash>:_persistence/tasks.md \| grep ... T-019 ... Alta` | No anclable todavia | criterio de `D-030`/`T-019` |
| 24 | `git show <hash>:_audit/S-009.md \| sed -n '47p'` | No anclable todavia | contexto/criterio de `D-031` |
| 25 | `git status --short -- .claude CLAUDE.md _phases _methodology _templates _workflow` | No anclable | pregunta por el arbol de trabajo, no por un commit (evidencia de `D-032`, ya ejecutada) |
| 26 | `git status -sb \| head -1` | No anclable | idem |
| 27 | `git rev-parse --short HEAD; git rev-parse --short origin/main` | Si | `95c5cfd`, `95c5cfd` |
| 28 | `git grep -nE "YouOtto\|..." 95c5cfd -- ...` | Si | `0` |
| 29 | `git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 95c5cfd -- _phases _workflow ...` | Si | `0` |
| 30 | `git grep -nE "USUARIO\|..." 95c5cfd -- ...` | Si | `0` |
| 31 | `ESQ=...; git -C "$ESQ" show --stat --format=... 447c2a0 \| tail -5` | Si | mismas 5 lineas |
| 32 | `git -C "$ESQ" ls-remote origin refs/heads/main \| cut -c1-7` | Si | `447c2a0` |
| 33 | `for f in ...; do [ "$(git rev-parse 95c5cfd:$f)" = "$(git -C "$ESQ" rev-parse 447c2a0:$f)" ] ...` | Si | `igual` en los tres |
| 34 | `for d in ...; do diff -rq ...; done; diff -q ... CLAUDE.md; echo "fin P1"` | No — difiere del publicado, y es lo esperado | publicado `fin P1` (sin diferencias); hoy devuelve 1 linea (`protocol-close/SKILL.md` differ) porque la frase de `D-028` ya esta repuesta en el arbol; el propio `D-032` lo anuncia en su nota siguiente |
| 35 | `for p in 005_project.md:... ; do ... done` | Si | los diez pares en `0` |
| 36 | `git show <hash>:_persistence/techdebt.md \| grep ... DT-003 ... Implementada` | No anclable todavia | criterio de `D-032`/`T-019` |
| 37 | `git show <hash>:_persistence/techdebt.md \| sed -n .../DT-003/... Estado Implementada` | No anclable todavia | criterio de `D-032`/`T-019` |
| 38 | `git show <hash>:...SKILL.md \| grep -cF 'no reproduce, la tarea'` | No anclable todavia | criterio de `D-032` (repetida de la 15, es el mismo verificador citado en dos decisiones) |
| 39 | `ESQ=...; diff -rq --strip-trailing-cr "$ESQ/.claude" .claude` | Si | 1 linea, `protocol-close/SKILL.md differ` — evidencia de `DT-004` |

Las 12 ordenes con `<hash>` (15, 16, 17, 18, 21, 22, 23, 24, 36, 37, 38 mas su duplicada) son
bloques «Criterio de cierre» de `D-028`, `D-029`, `D-030`, `D-032` (y de las tareas
`T-017`/`T-018`/`T-019` que los citan) escritos durante la sesion, cuando el commit todavia no
existe: las ancla el Paso 7c-bis, en `decisions.md` y `tasks.md`, que son archivos de este cierre.
Las lineas 25, 26 y 20 preguntan por el arbol de trabajo o por una carpeta de scratchpad efimera, no
por un commit, y no son anclables por naturaleza (igual que un `git status`). La linea 34 tiene
salida distinta a la publicada, pero la discrepancia esta explicada en la nota que sigue en `D-032`
— no es un defecto sin declarar. Los items 4, 5, 6 y 7 son la propia evidencia de este paso
(autorreferencial: el informe cita su propio barrido dentro de si mismo).

**NOTA DE CIERRE**, escrita por el Paso 7c-bis tras el commit sustantivo `a61e453`.

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal y anclado, no tiene que salir vacio):

```
$ for f in $(git ls-tree -r --name-only a61e453 _persistence _audit | grep -v '_audit/S-010.md'); do
  n=$(git show a61e453:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_persistence/decisions.md: 14
_persistence/tasks.md: 1
```

CONTROL (acotado a lo que este commit anadio y a la forma literal `git show <hash>:`):

```
$ for f in $(git diff --name-only a61e453^ a61e453 -- _persistence _audit ":(exclude)_audit/S-010.md"); do
  n=$(git diff -U0 a61e453^ a61e453 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
_persistence/decisions.md: 10
```

Las 11 lineas que el commit `a61e453` anadio con `<hash>` en `_persistence/decisions.md` (10 en
forma `git show <hash>:`, mas una en forma `git grep ... <hash> --`, del criterio de `D-028`) son
las que este mismo Paso 7c-bis acaba de anclar arriba, en `D-028`, `D-029`, `D-030`, `D-031` y
`D-032`. No queda ninguna orden de esta sesion sin forma anclada. Las tres lineas restantes del
CENSO en `decisions.md` (las de `D-013`/`T-003`) y las de `_audit/R-*.md`/`_audit/S-0*.md` son
anteriores a esta sesion: son citas de patron o bloques ya auditados, y no se tocan.

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | outside > /tmp/prosa_antes; outside < "$f" > /tmp/prosa_despues; echo "== $f =="; diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea de prosa desaparecio.

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /^[[:space:]]*```/{f=!f; next} c&&f&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | salidas > /tmp/salida_publicada; salidas < "$f" > /tmp/salida_anclada; echo "== $f =="; diff /tmp/salida_publicada /tmp/salida_anclada | grep -E '^[<>]'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Todas las salidas ancladas son las publicadas; las lineas `📌` de `D-028`, `D-029`, `D-030`, `D-031`
y `D-032` dicen que reproducen, y este control lo confirma.

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d a61e453^ a61e453); do n=$(git show a61e453:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(sin salida)
$ git diff --name-only --diff-filter=d a61e453^ a61e453 | wc -l
8
$ for f in $(git diff --name-only --diff-filter=d a61e453^ a61e453); do git show a61e453:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El recuento anclado (8 archivos) coincide con la tabla de la seccion 8, ya corregida a 8 antes del
commit: ningun archivo quedo fuera de esa tabla.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print NR": "$0; if (since>3) armed=0 }
' _audit/S-010.md
28: `report_auditor` audito `S-009` en `R-009` y no entrego hallazgos: solo tres recomendaciones sin
110: `A-001` sigue `Abierto`: si el historial de juegos registrados es dato de persona. Su disparador —la
111: clasificacion de actores de `005_discovery`— ya esta activo desde que la etapa se declaro (`S-005`) y
154: sus propios bloques de evidencia (incluido este) contienen lineas `$ ...`. Los items 4, 5 y 6 de la
218: Las lineas 15 y 38 son la misma orden (criterio de cierre de `D-028` citado en `T-017`, vuelto a
219: citar en `D-032`), y estan en la salida de `uniq -d`.
294: ⚠️ El primer barrido de esta seccion (item 8 de la seccion 7) se corrio antes de que `_audit/S-010.md`
308: 1 linea. No es un fallo del cierre: es la promocion pendiente que deja `D-032` a proposito (la frase
309: de `D-028`, repuesta en el arbol despues de promover sin ella), registrada como `DT-004`
```

Las ocho lineas leidas contra su bloque precedente: 28/110/111/154/218-219/294/308-309 citan codigos
(`S-009`, `A-001`, items 4/5/6, lineas 15/38, item 8, `DT-004`) y no cifras derivadas de un bloque de
salida; las dos que si son cifras («8 archivos, 0 ocurrencias» en 308-309 de la version final, y el
recuento de la seccion 8) coinciden con el bloque de arriba. Ninguna correccion necesaria.

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 a61e453^ a61e453 -- _persistence _audit ":(exclude)_audit/S-010.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
26
```

Excluyendo el propio informe, el recuento anclado (26) coincide con el que se habria publicado sin
las lineas autorreferenciales del propio Paso 2d (items 1-13 de la lista de la seccion 7, que solo
existen porque el informe se cita a si mismo): `39 - 13 = 26`.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(sin salida)
```

Ningun archivo del commit lleva caracteres de control. Los dos contrastes:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
8
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

⚠️ El primer barrido de esta seccion (item 8 de la seccion 7) se corrio antes de que `_audit/S-010.md`
y `_audit/index.md` entraran al area de staging; los dos contrastes de aqui, corridos despues con los
ocho archivos ya presentes, son los que valen. La tabla vacia (8 archivos, 0 ocurrencias) cuadra con
los dos contrastes.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
```

1 linea. No es un fallo del cierre: es la promocion pendiente que deja `D-032` a proposito (la frase
de `D-028`, repuesta en el arbol despues de promover sin ella), registrada como `DT-004`
(propuesta, pendiente del usuario) en `_persistence/techdebt.md`.
