# Auditoria R-004 — sobre S-004

| Campo | Valor |
|---|---|
| Auditoria | R-004 |
| Informe auditado | `_audit/S-004.md` |
| Commit auditado | c07680f |
| Fecha | 2026-09-16 |
| Veredicto | Sin hallazgos |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep S-004
| `S-004.md` | S-004 | 2026-09-16 | Pendiente | Pendiente | Pendiente | - |
$ git log -1 --format=%h -- _audit/S-004.md
74749f7
$ git show 74749f7:_audit/S-004.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `c07680f` |
$ git show --stat --format='%h %ad %s' c07680f
c07680f Wed Sep 16 10:26:50 2026 -0500 S-004: hallazgo F-005 de R-003 atendido (D-013, T-007), CONTROL DE SALIDA REPRODUCIDA en el 7c-bis, y cosecha de L-007 (D-014)
 .claude/skills/protocol-close/SKILL.md |  72 ++++++--
 _audit/S-004.md                        | 317 +++++++++++++++++++++++++++++++++
 _audit/findings.md                     |  11 +-
 _audit/index.md                        |   1 +
 _persistence/decisions.md              | 114 ++++++++++++
 _persistence/lessons.md                |  18 ++
 _persistence/progress.md               |  66 ++++---
 _persistence/tasks.md                  |  49 +++++
 8 files changed, 610 insertions(+), 38 deletions(-)
```

El hash literal de la cabecera (`c07680f`) es distinto del derivado (`74749f7`, commit de anclaje):
se audita `c07680f`. Sus 8 archivos coinciden con la lista anclada de la seccion 1 del informe.
El informe se leyo en la version de `74749f7` (anclada) y en la de `c07680f`.

### 1.2 Informe contra el diff

- Seccion 0 (`F-005`, `Aceptado — corregido en este commit`): la nota fechada bajo `T-006` y el
  CONTROL DE SALIDA REPRODUCIDA estan en el diff de `c07680f` (`git show c07680f -- _persistence/tasks.md .claude`).
  La columna `Veredicto` no usa `Implementado`.
- Recuentos de hunks de la seccion 1, reejecutados sobre el commit:

```
$ git diff -U0 c07680f^ c07680f -- .claude/skills/protocol-close/SKILL.md | grep -c '^@@'
7
$ for f in _audit/findings.md _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md; do echo "$f $(git diff -U0 c07680f^ c07680f -- $f | grep -c '^@@')"; done
_audit/findings.md 3
_persistence/decisions.md 2
_persistence/lessons.md 2
_persistence/tasks.md 2
```

- La nota de `T-006` no altera el bloque original (anclado en `2a72df8`):

```
$ diff <(git show 2a72df8:_persistence/tasks.md | awk '/^### T-006/,/^📌/') <(git show c07680f:_persistence/tasks.md | awk '/^### T-006/,/^📌/'); echo rc=$?
rc=0
```

- Las dos salidas que la nota publica reproducen:

```
$ git show 101db28:_persistence/tasks.md | awk '/^### T-006/,0' | grep -F '.claude/skills/'
  .claude/skills/protocol-audit/SKILL.md:4
  .claude/skills/protocol-close/SKILL.md:6
  .claude/skills/protocol-start/SKILL.md:2
$ git grep -cF '/^[[:space:]]*```/' 101db28 -- .claude
101db28:.claude/skills/protocol-audit/SKILL.md:4
101db28:.claude/skills/protocol-close/SKILL.md:6
101db28:.claude/skills/protocol-start/SKILL.md:2
```

- Criterios de cierre de `D-013`/`T-007` y `D-014`, anclados, reejecutados:

```
$ eval "$(git show c07680f:.claude/skills/protocol-close/SKILL.md | grep -F 'salidas() {')"; for p in "101db28 2a72df8" "e222812 4b27ae4"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $1..$2 $f"; diff <(git show $1:"$f" | salidas) <(git show $2:"$f" | salidas) | grep -cE '^[<>]'; done; done
== 101db28..2a72df8 _persistence/decisions.md
0
== 101db28..2a72df8 _persistence/tasks.md
6
== e222812..4b27ae4 _persistence/decisions.md
0
== e222812..4b27ae4 _persistence/tasks.md
0
$ git show c07680f:.claude/skills/protocol-close/SKILL.md | grep -cF '**CONTROL DE SALIDA REPRODUCIDA — salida:**'
2
$ git show c07680f:_persistence/lessons.md | grep -E '^\| \[L-' | grep '000_preproject' | grep -c 'Sin evaluar'
0
$ git show c07680f:.claude/skills/protocol-close/SKILL.md | grep -cF 'salidas() {'
1
```

- Commit de anclaje `74749f7`: solo toca bloques de criterio y anade lineas `📌` (`git show 74749f7 -- _persistence/`).
  Controles de prosa borrada y de salida reproducida, reejecutados entre `c07680f` y `74749f7`:

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; diff <(git show c07680f:"$f" | outside) <(git show 74749f7:"$f" | outside) | grep '^<'; done
== _persistence/decisions.md
== _persistence/tasks.md
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; diff <(git show c07680f:"$f" | salidas) <(git show 74749f7:"$f" | salidas) | grep -E '^[<>]'; done
== _persistence/decisions.md
== _persistence/tasks.md
```

- Fecha: `date +%F` → `2026-09-16`; `git log -1 --format=%ad --date=short c07680f` → `2026-09-16`.

### 1.3 Seccion 6 del informe: robustez de `salidas()`

Mas de un bloque tras el criterio, y `---` dentro de un bloque:

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show 74749f7:$f | salidas | cut -d' ' -f1 | sort | uniq -c | tr '\n' ' '; echo; done
      6 D-008       7 D-009       4 D-010       1 D-011       1 D-012       9 D-013       1 D-014 
      2 T-001       1 T-003       1 T-004       1 T-005      10 T-006       9 T-007 
$ for f in _persistence/decisions.md _persistence/tasks.md; do git show 74749f7:$f | awk '/^[[:space:]]*```/{f=!f; next} f&&/^---$/{print NR}' | wc -l; done
0
0
```

`T-006` captura 10 lineas: las 3 de su bloque original mas las 6 de la nota y 1 mas de su bloque —
la captura sigue hasta el siguiente `### `, asi que varios bloques tras el criterio se cubren. Ningun
bloque contiene `---` en el estado actual. Variantes del rotulo que `salidas()` no reconoceria:

```
$ for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; git show c07680f:$f | grep -niE 'criterios? de (cierre|terminado)' | grep -v 'Criterio de cierre'; done
```

Salida: solo titulos de indice, el propio encabezado de `D-013`/`T-007` y prosa descriptiva (lineas
36, 64, 74, 263, 636, 652 de `decisions.md`; 26, 301, 311 de `tasks.md`). Ninguna entrada usa un
rotulo de criterio distinto de `Criterio de cierre`.

### 1.4 Seccion 6 del informe: cosecha de `L-007` como «ya cubierta por `LG-98`»

```
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show 9e883a9:global_lessons.md | grep -nE '^\| \*\*LG-98\*\*'
329:| **LG-98** | **Un contrato que solo conoce una parte no es un contrato: es una suposición** | Cada lado guarda su copia de las reglas comunes porque necesita tenerlas a mano. La duplicación es inevitable; que **nadie compruebe que las copias siguen coincidiendo**, no. [...] | ¿Existe una comprobación de que las dos copias dicen lo mismo, y está escrito **quién la corre y cuándo**? [...]
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show 9e883a9:global_lessons.md | grep -n 'dos copias del mismo documento discrepan'
92:| dos copias del mismo documento discrepan | `LG-08`, `LG-98` |
```

(fila 329 recortada con `[...]` solo en esta cita). La forma de `L-007` —copia de un bloque que
diverge y la comprobacion con `diff` contra el original— es la de `LG-98`. El juicio se sostiene.

### 1.5 Controles mecanicos (Paso 4)

a) Indice ↔ detalle en `_persistence/`:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show c07680f:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show c07680f:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

b) Carpetas:

```
$ diff <(git ls-tree -d --name-only c07680f | sed 's|$|/|' | sort) <(git show c07680f:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con motivo escrito: `temporal/` excluida en `.gitignore` con su comentario; `005_discovery/`
declarada por adelantado con su decision (`git grep -nE '005_discovery/' c07680f -- _persistence/decisions.md`
→ lineas 166, 170, 171). No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|C:\\\\Users\\\\USUARIO|C:/Users/USUARIO|github.com" c07680f -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

d) Codigos instanciados:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' c07680f -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas en (c) y (d).

## 2. Hallazgos

Ninguno.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-005 | Aceptado — pendiente (`T-007`) | Implementado | La correccion esta en `c07680f`: nota fechada bajo `T-006` con las dos salidas, que reproducen, sin tocar el bloque original (`rc=0`); CONTROL DE SALIDA REPRODUCIDA en el 7c-bis con su rotulo en el 7c-ter y en la plantilla (`grep -cF` → `2`), y sobre el anclaje de `S-003` senala las 6 lineas del defecto (seccion 1.2) |

F-001 a F-004 ya estaban `Implementado`.

## 4. Lo que NO se pudo comprobar

Nada quedo sin comprobar.

## 5. Recomendaciones sin hallazgo

- El CONTROL DE SALIDA REPRODUCIDA compara el **texto** del bloque antes y despues del anclaje, no
  reejecuta la orden: detecta la sustitucion de una salida (el caso que abrio `F-005`), pero si
  quien ancla no reemplaza la salida por la de una ejecucion real, sale limpio igual. Valorar si
  conviene que el control reejecute las ordenes ancladas.
- La NOTA DE CIERRE de `S-004` dice «Las cuatro ordenes marcadas "no anclable todavia" (2, 3, 8, 9)»,
  pero la tabla del Paso 2d marca tambien la 6 (`D-014`); el desglose posterior si cuenta 5. Es
  inexactitud de redaccion, sin efecto en el registro.
- La seccion 1 del informe anclado conserva `git diff --cached -U0 ...` como orden de los 7 hunks,
  que solo tiene sentido antes del commit; reproduce con `git diff -U0 c07680f^ c07680f`.
