# Auditoria R-027 — sobre S-025

| Campo | Valor |
|---|---|
| Auditoria | R-027 |
| Informe auditado | `_audit/S-025.md` |
| Commit auditado | 0c07cc8 |
| Fecha | 2026-09-19 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Fila pendiente y commit auditado

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep Pendiente
| `S-025.md` | S-025 | 2026-09-19 | Pendiente | Pendiente | Pendiente | - |
$ git log -1 --format=%h -- _audit/S-025.md
7cfbe0a
$ git show 7cfbe0a:_audit/S-025.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `0c07cc8` |
$ git show --stat --name-only --format= 0c07cc8 | grep -c .
7
$ git show --stat 7cfbe0a | tail -2
 _audit/S-025.md | 181 ++++++++++++++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 175 insertions(+), 6 deletions(-)
```

Una sola fila pendiente. Hash literal `0c07cc8`, distinto del derivado `7cfbe0a` (commit de anclaje,
un solo archivo). Se audita `0c07cc8`; sus 7 archivos coinciden con la lista anclada de la seccion 1
del informe. Las 6 lineas que el anclaje borra son solo huecos de plantilla:

```
$ git diff 0c07cc8 7cfbe0a | grep '^-'
--- a/_audit/S-025.md
-| Commit auditado | <se rellena en el Paso 7c> |
-Nota: esta lista sale del area de staging porque el commit todavia no existe; el Paso 7c pega debajo
-su version anclada con `git show --stat --name-only --format= <hash>`.
-**NOTA DE CIERRE** (la completa el Paso 7c-bis, despues del commit sustantivo, con el hash y las
-seis salidas obligatorias):
-📌 Se completa mas abajo, tras el commit del Paso 7.
```

### 1.2 Indice ↔ detalle en `_persistence/` (sobre `0c07cc8`)

```
$ H=0c07cc8; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(...indice...) <(...detalle...); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes (orden literal del Paso 4a de `protocol-audit`, con `<hash>` = `0c07cc8`).

### 1.3 Carpetas del arbol contra las declaradas

```
$ diff <(git ls-tree -d --name-only 0c07cc8 | sed 's|$|/|' | sort) <(git show 0c07cc8:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
10a11
> temporal/
```

Unica diferencia `temporal/`, con motivo escrito en `project.md` («Fuera del repositorio, excluida en
`.gitignore`»). Sin hallazgo.

### 1.4 Fuga de datos propios (seis ambitos)

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" 0c07cc8 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.5 Codigos instanciados en `_phases` y `_workflow`

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 0c07cc8 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.6 Controles del cierre, reejecutados sobre el commit

```
$ H=0c07cc8; git diff -U0 $H^ $H -- _persistence _audit ":(exclude)_audit/S-025.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
5
$ for f in $(git diff --name-only $H^ $H -- _persistence _audit ":(exclude)_audit/S-025.md"); do n=$(git diff -U0 $H^ $H -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done; echo fin
fin
$ for f in $(git diff --name-only --diff-filter=d $H^ $H); do git show $H:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
$ git log -1 --format=%ad --date=short 0c07cc8
2026-09-19
$ git show 0c07cc8:_audit/S-025.md | sed -n '/^| Fecha/p'
| Fecha | 2026-09-19 |
```

Reproducen lo publicado (Paso 2d anclado = 5; CONTROL vacio; 0 caracteres de control; fecha del
informe = fecha del commit).

### 1.7 El registro de la sesion `003` (punto 2 de la seccion 6 del informe)

```
$ git show 0c07cc8:010_prototype/015_session_003.md | grep -n "<"
195:- [ ] **No queda ni un solo `<` en el archivo.**
199:grep -n "<" 010_prototype/015_session_003.md                 # debe no devolver nada
$ git show 0c07cc8:010_prototype/015_session_003.md | grep -n "Guia de llenado"
196:- [ ] La seccion «Guia de llenado» **esta borrada**.
200:grep -n "Guia de llenado" 010_prototype/015_session_003.md   # debe no devolver nada
$ git log --diff-filter=A --format=%ad -- 010_prototype/015_session_003.md
Sat Sep 19 09:28:22 2026 -0500
$ git log 0c07cc8 --format=%h -- 010_prototype/app/
e45185d
```

Coincide con la evidencia de `D-102` (lineas 195/199 y 196/200). Cabecera `2026-09-19` y commit del
mismo dia (08:51 → 09:28). El prototipo no se toco despues de `e45185d`. Contra `_phases/010_prototype.md`
Paso 7 (`sed -n '238,262p'`): perfil y encaje (§1), uno de los cuatro estados (§3), recorrido con
punto del flujo (§4), comentarios (§6, «Ninguno» entrecomillado de la facilitadora), cinco
respuestas posteriores (§7). Todos presentes. Casillas de §9 sin marcar y salida cruda en `D-102`
en vez de en el propio registro: misma forma que `015_session_002.md` (mismas lineas en
`git show 0c07cc8:010_prototype/015_session_002.md | sed -n '185,215p'`). Sin hallazgo.

### 1.8 `A-009` (punto 3 de la seccion 6)

```
$ git show 0c07cc8:_persistence/decisions.md | sed -n '/^### D-098/,/^---$/p' | grep -n -i "limite\|anota"
23:- **Limite:** confirma la primera sesion, no las cuatro siguientes. Si en alguna el participante
24:  vuelve sobre la discrepancia, se anota en su registro y va al Gate 1 como limite de la ronda. El
```

El limite se dispara solo si el participante vuelve sobre la discrepancia; §6 registra cero
comentarios y ninguna respuesta de §7 la menciona. «Nada que anotar» esta bien aplicado. Sin
hallazgo.

### 1.9 Informe contra el diff

Las afirmaciones de la seccion 1 (`D-102` y `T-058` nacen; fila `003` en `010_participants.md`;
`progress.md` secciones 1-3, bitacora e indice) estan en `git show 0c07cc8`. El diff no borra
ninguna linea de `decisions.md` ni de `tasks.md`:

```
$ git show 0c07cc8 -- _persistence/decisions.md | grep -c "^-[^-]"
0
$ git show 0c07cc8 -- _persistence/tasks.md | grep -c "^-[^-]"
0
```

La seccion 0 contiene una afirmacion que el registro contradice: ver `F-036`.

## 2. Hallazgos

### F-036 — La seccion 0 de `S-025` enumera como `Aceptado — pendiente` cuatro hallazgos donde el registro tiene seis

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_audit/S-025.md`, seccion 0 |

- **Que se observo:** la seccion 0 presenta como la lista de hallazgos que siguen pendientes solo
  cuatro:

  ```
  $ git show 0c07cc8:_audit/S-025.md | grep -n "F-028"
  15:`_audit/findings.md` (`F-028`, `F-029`, `F-030`, `F-034`) no fueron tocados en esta sesion: sus
  ```

  El registro del mismo commit tiene seis en ese estado:

  ```
  $ git show 0c07cc8:_audit/findings.md | grep -n "^| \[F-0" | grep "Aceptado — pendiente" | cut -c1-12
  38:| [F-016]
  39:| [F-017]
  50:| [F-028]
  51:| [F-029]
  52:| [F-030]
  56:| [F-034]
  ```

  Faltan `F-016` y `F-017` (`T-030`, `T-031`), que el propio informe lista como `No implementada`
  en su seccion 2.
- **Por que importa:** la seccion 0 es la que se lee para saber que hallazgos siguen vivos; quien
  se fie de ella da por cerrados dos que no lo estan. `Baja` porque `findings.md` y la seccion 2 del
  mismo informe tienen el dato correcto; `No bloqueante` porque el registro que se lee al arrancar
  (`findings.md`) no hereda el error.
- **Que lo corregiria:** una nota fechada al final de `_audit/S-025.md` que complete la lista con
  `F-016` y `F-017`, sin reescribir la seccion 0; y que el cierre derive esa lista de
  `findings.md` con una orden publicada en vez de escribirla a mano.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | Aceptado — pendiente (sin cambios) | `T-030` sigue `No implementada`; el diff de `decisions.md` no borra ni cambia ninguna linea (0 lineas `-`), luego no toca `D-041` |
| F-017 | Aceptado — pendiente (sin cambios) | `T-031` sigue `No implementada`; `_audit/S-013.md` no esta en el `--stat` de `0c07cc8` |
| F-028 | Aceptado — pendiente (sin cambios) | `T-047` sigue `No implementada`; `_audit/S-019.md` no esta en el `--stat` |
| F-029 | Aceptado — pendiente (sin cambios) | `T-048` sigue `No implementada`; el acta de `005_discovery` no esta en el `--stat` |
| F-030 | Aceptado — pendiente (sin cambios) | `T-049` sigue `No implementada` pendiente de la decision del usuario (`D-095`). Esta vez la NOTA DE CIERRE de `S-025` no repite el hueco: `git show 7cfbe0a:_audit/S-025.md \| grep -n "<se \|commit de anclaje"` → sin salida, `rc=1`. Pero eso no corrige la regla que el hallazgo pide |
| F-034 | **Aceptado — pendiente (sigue abierto)** | Lo que `R-026` dejo abierto era la cifra de la seccion 3 de `progress.md`. El cierre de `S-025` la reescribio y **la vuelve a dejar corta en uno**. Ver abajo |

**Por que `F-034` sigue abierto:**

```
$ git show 0c07cc8:_persistence/progress.md | grep -n "faltan \`004\`\|restante"
96:| Bloqueos activos | Paso 5 de `010_prototype` en marcha: sesiones `001`, `002` y `003` de cinco corridas, faltan `004`-`005`; el prototipo no se toca entre sesiones. ...
124:la sesion restante el participante vuelve sobre la discrepancia, se anota en su registro como limite
129:alguna de las dos sesiones restantes el participante vuelve sobre la discrepancia, se anota en su
$ git show 0c07cc8 -- _persistence/progress.md | grep -n "restante"
83:-alguna de las dos sesiones restantes el participante vuelve sobre la discrepancia, se anota en su
97:+la sesion restante el participante vuelve sobre la discrepancia, se anota en su registro como limite
```

(la linea 129 del primer bloque es la bitacora de `S-024`, historica.) Faltan dos sesiones (`004` y
`005`, linea 96 del mismo archivo), y la seccion 3 dice «la sesion restante». Es el mismo defecto que
`R-026` describio —el cierre decrementa la cifra de la frase en vez de derivarla—, repetido por
segundo cierre consecutivo. Se trata como el mismo hallazgo, no como uno nuevo (mismo criterio que
`R-026` aplico a `F-030`).

## 4. Lo que NO se pudo comprobar

Ninguno. Los cinco controles corrieron. La orden del Paso 4a se abrevia en la seccion 1.2 con
`(...indice...)`/`(...detalle...)`; es literal la del protocolo, con `<hash>` = `0c07cc8`.

## 5. Recomendaciones sin hallazgo

- **Sobre `T-058` (punto 1 de la seccion 6 del informe):** la forma de tarea es correcta —el cierre
  no puede editar un bloque «Evidencia» de `decisions.md`—, pero `D-102` ya declara en prosa que sus
  cuatro ordenes se corrieron «antes del commit», que es la mitad «nota fechada» de su propio
  criterio de cierre. Conviene que `manager` evalue si `T-058` ya esta satisfecha por lo escrito o
  si pide ademas el ancla (`git log d802324 --oneline --name-only -- 010_prototype/` reproduciria lo
  publicado).
- **La NOTA DE CIERRE llama «no anclable» a la orden de `T-058`**, pero solo lo es respecto a
  `0c07cc8`: anclada al padre (`0c07cc8^` = `d802324`) contesta la misma pregunta. La frase de cierre dice ademas que las cinco
  lineas no estan en bloques «Criterio de cierre», y la linea 5 si esta dentro del de `T-058`. No
  cambia ningun estado; es precision de redaccion.
- **La tabla de la seccion 7 del informe cita solo la linea 199 y la 200** para las dos primeras
  ordenes, que devuelven dos lineas cada una (195/199 y 196/200). La salida cruda completa esta en
  `D-102`.
- **`F-034` se ha repetido dos cierres seguidos por el mismo mecanismo** (la cifra en prosa se
  decrementa a mano). Vale la pena que la correccion no dependa de reescribir la frase en cada cierre:
  por ejemplo, que la seccion 3 remita a la linea «Bloqueos activos» en vez de repetir el numero.
