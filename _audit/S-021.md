# Informe de auditoria — S-021

| Campo | Valor |
|---|---|
| Sesion | S-021 |
| Fecha | 2026-09-18 |
| Etapa | 010_prototype |
| Rama | main |
| Commit auditado | `22747a9` |

## 0. Respuesta a la auditoria anterior

**Verificado contra `HEAD` (`ca6f999`, la auditoria `R-022` sobre el commit `7b0c48f`), sobre el
estado que `7b0c48f` dejo.** La tabla lista **todos** los hallazgos de `_audit/findings.md` que no
estan `Implementado`, no solo los de `R-022`:

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-016 — D-041 afirma el resultado de una prueba sobre commits historicos sin publicar la orden | Aceptado — pendiente | `T-030`, `No implementada` (sin cambios esta sesion) |
| F-017 — La NOTA DE CIERRE de S-013 dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete | Aceptado — pendiente | `T-031`, `No implementada` (sin cambios esta sesion) |
| F-028 — La nota de anclaje de S-019 dice que la escribio 70fe40c, y esta en e610906 | Aceptado — pendiente | `T-047`, `No implementada` (sin cambios esta sesion) |
| F-029 — El acta de cierre de 005_discovery conserva el D-XXX de la plantilla donde debe citar D-074 | Aceptado — pendiente | `T-048`, `No implementada` (sin cambios esta sesion) |
| F-030 — La NOTA DE CIERRE de S-020 deja el commit de anclaje como hueco de plantilla, sin instanciar y sin derivar | Aceptado — pendiente | `T-049`, `D-091` — **evaluado en esta sesion**: la primera mitad (la nota fechada que declara `30276a9` como commit de anclaje) ya esta escrita en `_audit/S-020.md`; la segunda (ampliar el control de huecos a los marcadores inline) queda en `T-049` |

Ningun hallazgo de esta lista se marca `Aceptado — corregido en este commit`: la primera mitad de
`F-030` se corrigio en el commit anterior (`ca6f999` lo hereda de `7b0c48f`), no en el que esta
sesion va a producir, y las demas siguen sin tocar.

## 1. Que se hizo

```
$ git show --stat --name-only --format= 22747a9
010_prototype/005_happy_path.md
010_prototype/010_participants.md
010_prototype/012_facilitator_guide.md
_audit/S-020.md
_audit/S-021.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/constraints.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
project.md
```

Lista anclada al commit `22747a9`. Frente a la que se publico mientras el commit no existia (area
de staging), se anaden los dos archivos que el propio cierre agrega al final: `_audit/S-021.md`
(este informe) y `_audit/index.md` (su fila en el tablero) — 14 archivos en total, dos mas que los
12 que el area de staging mostraba.

**Por archivo, que nacio y que se edito:**

- **`010_prototype/005_happy_path.md` (nace):** camino feliz y tarea sellados del Paso 1/2 de la
  etapa, aprobados por el patrocinador. `D-086`.
- **`010_prototype/010_participants.md` (nace):** participantes y sesiones del Paso 3, con su
  desviacion de espaciado declarada en §5. `D-087`, `D-088`.
- **`010_prototype/012_facilitator_guide.md` (nace):** guion de la facilitadora externa para las
  sesiones. `D-089`.
- **`project.md`:** anade la fila de la carpeta `010_prototype/` a «Carpetas propias», declarada por
  adelantado, citando `D-084`.
- **`_persistence/decisions.md`:** nacen `D-083` a `D-091` (nueve decisiones). No edita ninguna
  decision anterior; los dos hunks del diff son el indice y el bloque de detalle que las nueve
  anaden al final del archivo.
- **`_persistence/constraints.md`:** nace `C-006` (limite de lectura de baloto.com segun su
  `robots.txt`), citando `D-083`.
- **`_persistence/assumptions.md`:** **edita** `A-008` (no nace ninguna): pasa de `Abierto` a
  `Confirmado` en su fila de indice y en su campo `Estado`, y recibe una nota fechada 2026-09-18 con
  la evidencia de `D-083`.
- **`_persistence/lessons.md`:** nacen `L-023` y `L-024`.
- **`_audit/findings.md`:** **edita** `F-030` (no nace ninguno): su fila de indice y su ficha pasan
  de `Abierto`/`—` a `Aceptado — pendiente`/``T-049`, `D-091``.
- **`_audit/S-020.md`:** se anade una nota fechada 2026-09-18 al final del archivo, que declara
  `30276a9` como commit de anclaje de esa sesion, citando `F-030` y `D-091`. No se reescribe la
  linea 261 original.
- **`_persistence/tasks.md`:** nacen `T-049` (`Origen: report_auditor`) y `T-050`
  (`Origen: session-closer`, abierta por el Paso 2d de este mismo cierre).
- **`_persistence/progress.md`:** sobrescribe las secciones 1, 2 y 3, y anade la entrada `S-021` a
  la seccion 5 (Bitacora) con su fila de indice. Seis hunks: cabecera de identidad, las tres
  secciones fijas, la fila del indice de sesiones y el cuerpo de `S-021`.

## 2. Que NO se hizo, y por que

El Paso 4 de `010_prototype` —**construir el prototipo**— quedo decidido (`D-090`) pero **no
ejecutado**: no existe todavia `010_prototype/app/` ni ningun archivo HTML. Es la actividad
principal de la proxima sesion.

Tareas abiertas (`No implementada`), del indice de `tasks.md`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
T-030, T-031, T-032, T-033, T-035, T-037, T-042, T-046, T-047, T-048, T-049, T-050
```

De ellas, `T-049` y `T-050` nacen en esta sesion. Las demas siguen sin cambios: en su mayoria son
ordenes de «Verificacion»/«Contexto» de sesiones anteriores que dejaron de reproducir y esperan
ancla o nota fechada (`T-030`, `T-031`, `T-033`, `T-035`, `T-037`, `T-042`, `T-046`), la correccion
pendiente de `F-028`/`F-029` (`T-047`, `T-048`), y la decision formal sobre el criterio de `T-032`
(Alta, No bloqueante).

## 3. Decisiones tomadas

- **`D-083` — `A-008` se confirma:** el `robots.txt` de baloto.com autoriza la ruta de resultados que
  el producto necesita (`Disallow` solo cubre `/admin-baloto/` y `/api/`), y el unico documento legal
  enlazado desde el sitio declara un alcance ajeno al sitio informativo y no contiene ninguna
  clausula sobre uso automatizado. **Alternativas descartadas:** dejar `A-008` abierto a la espera
  de respuesta del operador; refutarlo por la clausula de propiedad intelectual (no aplica: habla de
  software, no de resultados); replantear la fuente hacia carga manual (ya descartada por `D-061`).
- **`D-084` — Carpetas del prototipo declaradas por adelantado:** `010_prototype/` para entregables,
  `010_prototype/app/` para el codigo descartable. **Alternativas descartadas:** carpeta de primer
  nivel propia para el prototipo; mezclar codigo y artefactos sin subcarpeta; dejar la carpeta sin
  declarar hasta el primer artefacto.
- **`D-085` — Adopcion del reparto de `_workflow/010_prototype.md`:** tal cual, sin alterar ninguna
  fila. Registra tres consecuencias operativas (autonomia de impacto relevante en el Paso 4, la IA
  fuera de las sesiones, el historial de `git` como vigilante de la inmovilidad) y una anomalia
  declarada (el unico Generador es tambien el patrocinador). **Alternativas descartadas:** adoptarlo
  con cambios; no escribir la decision.
- **`D-086` — Camino feliz y tarea sellados, combinacion arbitraria:** un unico camino, tarea sellada
  en `010_prototype/005_happy_path.md`, seis numeros arbitrarios en vez de una combinacion calculada
  a mano (recomendacion de `manager` que el patrocinador no siguio). **Alternativas descartadas:**
  combinacion calculada a mano; que el prototipo calcule de verdad; meter otras necesidades en el
  mismo recorrido; dejar elegir la prioridad Baloto/Revancha.
- **`D-087` — Participantes y sesiones:** 1 participante, 5 sesiones en 5 dias de sorteo distintos,
  umbral 4 de 5. **Alternativas descartadas:** las cinco el mismo dia; reclutar mas participantes;
  fijar menos de cinco sesiones.
- **`D-088` — El espaciado se desvia por decision del patrocinador:** las 5 sesiones se corren una
  por jornada de trabajo, sin esperar a los dias de sorteo, declarado como desviacion en §5 **sin
  tocar la hipotesis sellada**. `manager` objeto parcialmente (el efecto memoria se reduce con el
  cierre de jornada entre sesiones, pero no desaparece). **Alternativas descartadas:** reescribir la
  ventana en la hipotesis; correr las cinco en una sola jornada; mantener el calendario de sorteos;
  recortar el numero de sesiones.
- **`D-089` — Facilitadora externa:** una persona ajena a la construccion del prototipo facilita las
  sesiones; la segunda persona propuesta no participa en ninguna operacion. Guion en
  `010_prototype/012_facilitator_guide.md`. **Alternativas descartadas:** que las dos personas
  participen como usuarios; seguir en autorreporte; guion fuera de la carpeta de la etapa; que la
  facilitadora ayude a construir.
- **`D-090` — Forma del prototipo:** un unico archivo HTML sin dependencias; combinacion y razones
  visibles de golpe al abrir, sin gesto previo ni cronometro en pantalla. **Alternativas
  descartadas:** wireframe estatico; numeros uno a uno en el orden del metodo; razones ocultas tras
  un gesto; pedir la combinacion con un gesto; framework/build/servidor.
- **`D-091` — `F-030` se acepta en sus dos mitades:** nota fechada ya escrita en `_audit/S-020.md`
  (primera mitad, hecha); ampliar el control de huecos a marcadores inline como `T-049` (segunda
  mitad, pendiente). Se anota que es la **cuarta** vez que esta familia de defecto aparece
  (`F-013`, `F-026`, `F-028`, `F-030`), y de ahi nace `L-024`.

## 4. Supuestos vigentes y riesgos

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -oE '\[A-[0-9]+\]' | grep -oE 'A-[0-9]+' | sort -u
A-001
A-002
A-003
A-004
A-005
A-006
A-007
A-008
```

Los ocho estan `Refutado` (`A-001`, `A-002`) o `Confirmado` (`A-003` a `A-008`). **No queda ningun
`A-XXX` en estado `Abierto`** tras esta sesion: `A-008` era el ultimo, y `D-083` lo cierra.

**Riesgo que si queda vigente, sin codigo propio, y hay que decirlo:** `D-085` y `D-087` declaran una
anomalia que no se resuelve —el unico participante es tambien el patrocinador, quien dirige el
proyecto y quien revisara el prototipo—, y que la hipotesis de `005_discovery/020_hypothesis.md` §4
y §5 ya acepto a sabiendas debilitando la dimension de comprension. No es un supuesto (no se va a
confirmar ni refutar: es una condicion estructural aceptada), asi que no le corresponde un `A-XXX`;
si el Gate 1 lo considera insuficiente, la salida es una `D-XXX` nueva, no la reapertura de un
supuesto que nunca existio como tal.

## 5. Siguiente tarea propuesta

**Construir el prototipo — Paso 4 de `010_prototype`** (`_phases/010_prototype.md`), siguiendo la
forma decidida en `D-090`: un unico archivo HTML en `010_prototype/app/`, sin build ni dependencias,
que muestra de golpe los seis numeros y la razon de cada uno, sin cronometro en pantalla. Importancia
Alta, Urgencia Bloqueante (bloquea las cinco sesiones de `010_participants.md`). Al terminar, el
patrocinador revisa el prototipo entero contra la tarea sellada, no por muestreo (`D-085`).

## 6. Que pedimos auditar

- **La confirmacion de `A-008` (`D-083`) se apoya en la lectura de un `robots.txt` y de un unico PDF
  legal enlazado desde el pie del sitio.** No se busco si existe otro documento legal en el sitio que
  el pie de pagina no enlace. Si existiera uno que si cubra el sitio informativo con otras
  condiciones, la confirmacion quedaria construida sobre una base incompleta.
- **La desviacion del espaciado (`D-088`) se declaro despues de `D-087`, dentro de la misma
  jornada, y antes de que se corriera ninguna sesion.** Es el orden correcto segun `_phases/010_prototype.md`
  §2, pero convendria que la auditoria confirme que no hay ningun archivo `010_prototype/015_session_*.md`
  en el commit —la orden de `D-087` lo comprueba y da vacio hoy— y que por tanto la desviacion en
  verdad se declaro antes del primer dato.
- **El Paso 2d de este cierre encontro que cinco ordenes `grep -n` publicadas hoy en `D-086` y
  `D-087` ya no localizan la linea que citan, aunque el contenido de esa linea es identico.** Se
  abrio `T-050` para que se ancle o se feche, pero el hecho de que aparezca en la **misma sesion**
  en que se escribio (no en una posterior) sugiere que el archivo se siguio editando despues de
  correr la verificacion y antes de sellarlo. Conviene que la auditoria revise si `010_prototype/005_happy_path.md`
  y `010_prototype/010_participants.md` de verdad quedaron **sellados** en el estado que sus
  decisiones describen, o si el sello se aplico sobre una version que ya no es la commiteada.
- **El estado de `T-032`** (Alta, No bloqueante) sigue sin resolverse pese a que `A-006`/`A-007` ya
  estan `Confirmado` desde `S-020`. Nadie ha decidido formalmente si su criterio literal («tras
  reiniciar Claude Code») se sustituye.

## 7. Evidencia del Paso 2d

Orden (sobre el area de staging, antes del commit):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Recuento de lineas devueltas: **22**

```
 1	+$ git rev-parse --short 30276a9
 2	+$ git rev-parse --short 30276a9^
 3	+$ git log --format='%h %ad %s' --date=short -- _audit/S-020.md
 4	+  $ WebFetch https://www.baloto.com/robots.txt
 5	+  $ grep -a -o -i -E ".{80}Alcance.{60}" flat.txt | head -1
 6	+  $ grep -a -o -i -E "automatiz[a-z]*|robot[a-z]*|scrap[a-z]*|spider|crawl[a-z]*|ingenieriainversa" flat.txt | sort | uniq -c
 7	+  $ grep -a -o -i -E ".{150}prohibid[a-z]*.{250}" flat.txt | head -1
 8	+  $ grep -n "<" 010_prototype/005_happy_path.md
 9	+  $ grep -n "Guia de llenado" 010_prototype/005_happy_path.md
10	+  $ grep -niE "clic|boton|menu|pestaña|luego|despues|primero|selecciona" 010_prototype/005_happy_path.md
11	+  $ grep -n "<" 010_prototype/010_participants.md
12	+  $ grep -n "Guia de llenado" 010_prototype/010_participants.md
13	+  $ ls 010_prototype/015_session_*.md
14	+  $ git log -p -- 010_prototype/010_participants.md | grep "^-"
15	+  $ grep -nE '<se completa|<pendiente|<en blanco|<rellenar' _audit/S-020.md
16	+  $ git log --format='%h %ad %s' --date=short -- _audit/S-020.md
17	+  $ grep -c 'rev-parse' _audit/S-020.md
18	+  $ grep -n "<" 010_prototype/005_happy_path.md
19	+  $ grep -n "Guia de llenado" 010_prototype/005_happy_path.md
20	+  $ grep -niE "clic|boton|menu|pestaña|luego|despues|primero|selecciona" 010_prototype/005_happy_path.md
21	+  $ grep -n "<" 010_prototype/010_participants.md
22	+  $ grep -n "Guia de llenado" 010_prototype/010_participants.md
```

`sort | uniq -d` sobre esa misma lista (pares repetidos):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+  $ grep -n "<" 010_prototype/005_happy_path.md
+  $ grep -n "<" 010_prototype/010_participants.md
+  $ grep -n "Guia de llenado" 010_prototype/005_happy_path.md
+  $ grep -n "Guia de llenado" 010_prototype/010_participants.md
+  $ grep -niE "clic|boton|menu|pestaña|luego|despues|primero|selecciona" 010_prototype/005_happy_path.md
+  $ git log --format='%h %ad %s' --date=short -- _audit/S-020.md
```

(Las lineas 8-12 se repiten en 18-22 porque `T-050`, escrito por este cierre, cita las mismas
ordenes que `D-086`/`D-087` para documentar la discrepancia; la 3 y la 16 son la misma orden citada
dos veces dentro de `D-091`.)

**Reejecucion de cada linea, numerada por su posicion en la lista de arriba:**

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git rev-parse --short 30276a9` | Si | `30276a9` |
| 2 | `git rev-parse --short 30276a9^` | Si | `7b0c48f` |
| 3 | `git log --format='%h %ad %s' --date=short -- _audit/S-020.md` | Si | dos lineas, coinciden con lo publicado |
| 4 | `WebFetch https://www.baloto.com/robots.txt` | No anclable | pregunta por el estado de un sitio externo en el instante en que se corre, no por el commit; no se reejecuta en este cierre |
| 5 | `grep ... flat.txt \| head -1` | No anclable | `flat.txt` es un archivo temporal fuera del repositorio, producido al momento por `D-083`; no existe para reejecutar |
| 6 | `grep ... flat.txt \| sort \| uniq -c` | No anclable | mismo motivo que la 5 |
| 7 | `grep ... flat.txt \| head -1` | No anclable | mismo motivo que la 5 |
| 8 | `grep -n "<" 010_prototype/005_happy_path.md` | **No** | publica `137, 142`; hoy da `144, 149`. Ver `T-050` |
| 9 | `grep -n "Guia de llenado" 010_prototype/005_happy_path.md` | **No** | publica `138, 143`; hoy da `145, 150`. Ver `T-050` |
| 10 | `grep -niE "clic\|boton\|..." 010_prototype/005_happy_path.md` | **No** | las cinco lineas de contenido (11, 22, 73, 75, 76) coinciden; las dos ultimas publican `134, 144` y hoy dan `141, 151`. Ver `T-050` |
| 11 | `grep -n "<" 010_prototype/010_participants.md` | **No** | publica `162, 170, 174`; hoy da `174, 182, 186`. Ver `T-050` |
| 12 | `grep -n "Guia de llenado" 010_prototype/010_participants.md` | **No** | publica `162, 175`; hoy da `174, 187`. Ver `T-050` |
| 13 | `ls 010_prototype/015_session_*.md` | Si | falla igual: «No such file or directory», como documenta `D-087` |
| 14 | `git log -p -- 010_prototype/010_participants.md \| grep "^-"` | Si | vacio, como documenta `D-087` (archivo aun no commiteado cuando se escribio) |
| 15 | `grep -nE '<se completa\|<pendiente\|<en blanco\|<rellenar' _audit/S-020.md` | Si | mismas dos lineas (123 y 261) que publica `D-091` |
| 16 | (identica a la 3) | Si | idem |
| 17 | `grep -c 'rev-parse' _audit/S-020.md` | Si | `2` |
| 18-22 | (identicas a 8-12) | **No**, pero **coinciden con lo que `T-050` documenta como «Hoy da»** | mismo hallazgo que 8-12, citado en `tasks.md` |

**Conclusion de esta seccion:** las ordenes 4-7 son inherentemente no anclables (dependen de un
`WebFetch` y de un archivo temporal fuera del repositorio) y quedan asi, igual que un `git status`.
Las ordenes 8-12 (y su repeticion 18-22) **no reproducen contra los numeros que `D-086`/`D-087`
publicaron el dia que se escribieron**, aunque si reproducen contra los valores que `T-050` acaba de
documentar como el estado real de hoy. Esta discrepancia se registra en `T-050`
(`Origen: session-closer`), porque vive en `_persistence/decisions.md`, que este protocolo no puede
editar. El resto reproduce.

---

**NOTA DE CIERRE — commit de sesion `22747a9`, commit de anclaje `<se completa tras commitear el
anclaje>`.**

Esta sesion **no anclo ningun bloque «Criterio de cierre» de `decisions.md` ni de `tasks.md`**: las
nueve decisiones nacidas hoy (`D-083`-`D-091`) usan secciones «Verificacion» ya resueltas con su
orden y su salida pegadas en el momento de escribirlas (sin `<hash>` pendiente), y las dos tareas
nacidas hoy (`T-049`, `T-050`) tienen su «Criterio de cierre» en prosa, sin ninguna orden `$ `
dentro. El **CONTROL** de abajo lo confirma en cero. Por eso el commit de anclaje de esta sesion
**solo toca este informe** — no anade `_persistence/decisions.md`.

**BARRIDO DE ANCLAJE — salida:**

```
$ for f in $(git ls-tree -r --name-only 22747a9 _persistence _audit | grep -v '_audit/S-021.md'); do
  n=$(git show 22747a9:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/R-021.md: 2
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_audit/S-018.md: 2
_persistence/decisions.md: 19
_persistence/tasks.md: 1

$ for f in $(git diff --name-only 22747a9^ 22747a9 -- _persistence _audit ":(exclude)_audit/S-021.md"); do
  n=$(git diff -U0 22747a9^ 22747a9 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
(sin salida)
```

El CENSO (universal, heredado incluido) devuelve lineas con `<hash>` en 26 archivos — todas
preexistentes a esta sesion, marcadores dentro de bloques que la regla de no reescribir congela, o
citas del propio patron dentro de la prosa que lo explica (como el parrafo que abre `D-091`, que
cita literalmente `<hash>`). El CONTROL (acotado a lo que este commit anadio, con la ranura vacia
`git show <hash>:`) devuelve **vacio**: ninguna orden nacida hoy quedo con el hueco de anclaje sin
resolver. **No queda ninguna orden de la lista sin forma anclada** porque no se creo ninguna con esa
forma esta sesion.

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | outside > antes.txt
  outside < "$f" > despues.txt
  echo "== $f =="
  diff antes.txt despues.txt | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea de prosa desaparecio (no hay que correr este control con edicion real, porque el
Paso 7c-bis no toco ninguno de los dos archivos esta sesion; se publica igual, limpio).

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /\*\*Ordenes:\*\*/{m="O"} /\*\*Salidas:\*\*/{m="S"} /^[[:space:]]*```/{f=!f; if(!f) m=""; next} c&&f&&m!="O"&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | salidas > sal_pub.txt
  salidas < "$f" > sal_ancl.txt
  echo "== $f =="
  diff sal_pub.txt sal_ancl.txt | grep -E '^[<>]'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna salida publicada se sustituyo (mismo motivo: no hubo edicion del Paso 7c-bis esta sesion).

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d 22747a9^ 22747a9); do
  n=$(git show 22747a9:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
(sin salida)

$ git diff --name-only --diff-filter=d 22747a9^ 22747a9 | wc -l
14

$ for f in $(git diff --name-only --diff-filter=d 22747a9^ 22747a9); do git show 22747a9:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

Cero caracteres de control en los 14 archivos que el commit de sesion lleva de verdad (2 mas que
los 12 que el Paso 2e del cierre pudo medir en el area de staging: `_audit/S-021.md` y
`_audit/index.md`, que se escribieron despues). El recuento de archivos coincide con la tabla de la
seccion 8, y los dos archivos que quedaron fuera del Paso 2e original quedan cubiertos aqui, limpios.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-021.md
_audit/S-021.md:48: `_audit/S-021.md` (este informe) y la fila de `_audit/index.md`, que no estan todavia en el momento
_audit/S-021.md:95: De ellas, `T-049` y `T-050` nacen en esta sesion. Las demas siguen sin cambios: en su mayoria son
_audit/S-021.md:160: Los ocho estan `Refutado` (`A-001`, `A-002`) o `Confirmado` (`A-003` a `A-008`). **No queda ningun
_audit/S-021.md:161: `A-XXX` en estado `Abierto`** tras esta sesion: `A-008` era el ultimo, y `D-083` lo cierra.
_audit/S-021.md:209: Recuento de lineas devueltas: **22**
_audit/S-021.md:248: (Las lineas 8-12 se repiten en 18-22 porque `T-050`, escrito por este cierre, cita las mismas
_audit/S-021.md:249: ordenes que `D-086`/`D-087` para documentar la discrepancia; la 3 y la 16 son la misma orden citada
_audit/S-021.md:303: La tabla tiene 12 filas (una por archivo staged) y las 12 estan vacias (0 caracteres de control cada
_audit/S-021.md:304: una); el contraste 1 confirma que son 12 archivos, y el contraste 2 confirma que la suma es 0. Los
```

**Lectura linea por linea, contra el bloque que precede a cada una:**

- **48:** cita nombres de archivo (`S-021.md`, `index.md`), no una cifra de un bloque de salida. No
  aplica.
- **95:** «`T-049` y `T-050` nacen en esta sesion» — verificable contra el bloque de arriba (lista de
  `No implementada`, que los incluye) y contra el diff de `tasks.md` (seccion 1, ambos nacen). OK.
- **160-161:** «Los ocho» — el bloque previo lista `A-001` a `A-008`, 8 lineas. «Ningun `A-XXX`
  Abierto» — verificado con `grep -v Confirmado Refutado` sobre el mismo bloque, cero filas. OK.
- **209:** «22» — es el recuento que abre el bloque siguiente; contado a mano contra las 22 lineas
  numeradas debajo (1 a 22). OK.
- **248-249:** «Las lineas 8-12 se repiten en 18-22... la 3 y la 16» — seis pares, y el bloque previo
  (`uniq -d`) tiene exactamente 6 lineas. OK.
- **303-304:** «12 filas... 12 estan vacias... contraste 1... contraste 2» — el bloque previo publica
  `12` y `0` en ese orden; la prosa los repite sin alterarlos. OK.

Ninguna de las nueve lineas obliga a corregir nada: todas las cifras adyacentes a una salida cruda
salen de esa salida.

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 22747a9^ 22747a9 -- _persistence _audit ":(exclude)_audit/S-021.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
22
```

Mismo recuento que el publicado en la seccion 7 sobre el area de staging: entre el commit de sesion
y el momento en que se corrio esa version no se anadio ni se quito ninguna orden de las que el Paso
2d vigila.

---

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia). Ningun archivo del commit contiene caracteres de control en el rango buscado.

**Los dos contrastes de la tabla:**

```
$ git diff --cached --name-only --diff-filter=d | wc -l
12
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

La tabla tiene 12 filas (una por archivo staged) y las 12 estan vacias (0 caracteres de control cada
una); el contraste 1 confirma que son 12 archivos, y el contraste 2 confirma que la suma es 0. Los
dos cuadran.

⚠️ Esta seccion mide el area de staging previa al commit: `progress.md` esta incluido aqui porque ya
se escribio antes de correr este control, pero `_audit/S-021.md` (este informe) y la fila de
`_audit/index.md` se escriben despues y quedan fuera por construccion. Los cubre la SEGUNDA PASADA
anclada, en la NOTA DE CIERRE de la seccion 7.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
```

Salida: (vacia). El esqueleto de arranque esta al dia con este repositorio en las seis areas
agnosticas: no hay promociones pendientes.

---

🕐 **NOTA 2026-09-18 (`F-031`, `D-092`) — el commit de anclaje de esta sesion es `6a7d7f7`.**

La NOTA DE CIERRE de la linea 287 publico el commit de sesion y dejo el de anclaje con el texto
literal de la plantilla, sin instanciar. **Esa linea no se reescribe**: lo que dijo el dia que se
escribio se queda como se escribio, y esta nota lo completa. El dato que faltaba, derivado con una
orden y no tecleado:

```
$ git rev-parse --short 6a7d7f7
6a7d7f7
$ git rev-parse --short 6a7d7f7^
22747a9
$ git log --format='%h %ad %s' --date=short -- _audit/S-021.md
6a7d7f7 2026-09-18 S-021: ancla el informe al hash 22747a9
22747a9 2026-09-18 S-021: A-008 confirmado (D-083/C-006), Pasos 1-4 de 010_prototype sellados y decididos (D-084-D-090), F-030 evaluado (D-091)
```

**Commit de sesion `22747a9`; commit de anclaje `6a7d7f7`.** Es la segunda nota de esta familia en
dos sesiones consecutivas, y el motivo es el mismo que ya publica `D-091`: el control del cierre
ancla en `^<` y este hueco va inline, asi que nadie lo vio. La correccion del **control** sigue
siendo `T-049`, que a la fecha de esta nota continua `No implementada`; lo que esta nota corrige es
el **archivo**, no el mecanismo.
