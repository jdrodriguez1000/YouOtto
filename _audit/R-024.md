# Auditoria R-024 — sobre S-022

| Campo | Valor |
|---|---|
| Auditoria | R-024 |
| Informe auditado | `_audit/S-022.md` |
| Commit auditado | e45185d |
| Fecha | 2026-09-18 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-022.md
07ca614
$ git show 07ca614:_audit/S-022.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `e45185d` |
$ git show --stat --name-only --format= 07ca614 | grep -c .
1
$ git show --stat --name-only --format= e45185d | grep -c .
10
```

El literal de la cabecera es `e45185d` y difiere del derivado: el cierre uso su Paso 7c. `07ca614`
es el commit de anclaje (un solo archivo) y `e45185d` el commit de sesion (10 archivos, que es la
lista que la seccion 1 del informe enumera). **Se audita `e45185d`.**

### 1.2 Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show e45185d:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show e45185d:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete sin diferencias.

### 1.3 Carpetas declaradas — una diferencia, con motivo escrito

```
$ diff <(git ls-tree -d --name-only e45185d | sed 's|$|/|' | sort) \
       <(git show e45185d:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
10a11
> temporal/
```

`temporal/` esta declarada en `project.md` y excluida en `.gitignore`, asi que nunca aparecera en el
arbol versionado. La razon esta escrita: no es hallazgo. `010_prototype/` ya no aparece como
diferencia, porque esta sesion escribio su primer artefacto versionado — la declaracion anticipada de
`D-084` se resolvio sola, como el propio `project.md` anticipaba.

### 1.4 Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" e45185d -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

Las seis areas, el mismo ambito que barre el cierre.

### 1.5 Fuga por codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' e45185d -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(sin salida)
```

### 1.6 El Paso 2d del informe, reejecutado sobre el commit

```
$ git diff -U0 e45185d^ e45185d -- _persistence _audit ":(exclude)_audit/S-022.md" \
  | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
17
```

Coincide con el recuento que publica la seccion 7 del informe. El control de ranura vacia de anclaje
tambien reproduce en vacio:

```
$ for f in $(git diff --name-only e45185d^ e45185d -- _persistence _audit ":(exclude)_audit/S-022.md"); do
  n=$(git diff -U0 e45185d^ e45185d -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
(sin salida)
```

### 1.7 Caracteres de control en los 10 archivos del commit — cero

```
$ for f in $(git diff --name-only --diff-filter=d e45185d^ e45185d); do git show e45185d:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

### 1.8 El prototipo, que es lo que la seccion 6 pidio mirar

Los cuatro controles de ausencia de `D-093` reproducen sobre el archivo tal como quedo en el commit:

```
$ git show e45185d:010_prototype/app/index.html > /tmp/idx.html
$ grep -ncE "http://|https://|src=|<link|cdn|import |require\(" /tmp/idx.html
0
$ grep -ncE "localStorage|sessionStorage|indexedDB|fetch\(|XMLHttpRequest|cookie" /tmp/idx.html
0
$ grep -ncE "setInterval|setTimeout|Date\(|performance\.now|cronom" /tmp/idx.html
0
$ grep -ncE "<input|<form|<select|required|validat" /tmp/idx.html
0
$ git ls-tree -r --name-only e45185d 010_prototype/app | wc -l
1
```

**Lectura estructural del archivo** (no es un render en navegador — esta auditoria no ejecuta un
navegador, y eso se declara en la seccion 4):

- `<!doctype html>` presente, `<html lang="es">`, `<head>` con `charset` y `viewport`, `<body>` y
  `<main>` cerrados; las dos `<section>` abren y cierran.
- Los tres `id` que el script usa existen en el marcado: `darla-por-buena`, `pantalla-combinacion`,
  `pantalla-confirmacion`. El `addEventListener` corre despues del marcado (script al final del
  `body`), asi que `getElementById` no devuelve `null`.
- `pantalla-confirmacion` nace con el atributo `hidden` y ninguna regla CSS de autor le fija
  `display` sobre `.confirmacion`, asi que el `display:none` del UA no queda anulado: la segunda
  pantalla no se ve al abrir y se ve al pulsar. El unico comportamiento es el intercambio de
  pantallas mas un `scrollTo`.
- La combinacion respeta lo que `D-093` afirma: `07 19 24 31 38` sin tres consecutivos, superbalota
  `11` dentro de 1-16 y sin repetir ninguna balota.

Con eso, lo que `D-093` declara sobre el archivo se sostiene. Lo que sigue sin comprobarse por nadie
es el render real — ver seccion 4.

### 1.9 Informe contra el diff — afirmacion por afirmacion

| Afirmacion del informe | Contraste | Resultado |
|---|---|---|
| 10 archivos en el commit, 8 en staging mas `S-022.md` e `index.md` | `git show --stat` sobre `e45185d` | OK |
| `010_prototype/app/index.html` nace | `git log --diff-filter=A -- 010_prototype/app/` → `e45185d` | OK |
| `_audit/S-021.md` recibe nota fechada con `6a7d7f7`, sin reescribir la linea 287 | el diff solo anade 25 lineas al final; la 287 sigue literal | OK |
| `F-031` pasa a `Aceptado — pendiente` en fila **y** ficha | diff de `_audit/findings.md`, dos hunks | OK |
| nacen `D-092`, `D-093`, `D-094` sin editar decisiones anteriores | diff de `decisions.md`: dos hunks, indice y detalle al final | OK |
| nace `A-009` `Abierto` | `assumptions.md`, ficha completa con disparador | OK |
| nace `L-025` | `lessons.md` | OK |
| nace `T-051` `Implementada`, `Origen: report_auditor` | `tasks.md` | OK |
| doce tareas `No implementada` | `git show e45185d:_persistence/tasks.md \| sed -n '/^## Indice/,/^---/p' \| grep -c "No implementada"` → `12` | OK |
| ninguna afirmacion usa `Implementado` en la seccion 0 | la columna dice `Aceptado — corregido en este commit` | OK |
| las 17 ordenes del Paso 2d reproducen | reejecutadas las que dependen del repositorio | **una dejo de reproducir tras el commit → F-033** |

### 1.10 Paso 2f — esqueleto al dia

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
(sin salida en los seis)
```

### 1.11 Coherencia de `findings.md` tras esta auditoria

```
$ diff <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) \
       <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -oE '^#{3} F-[0-9]+'   | grep -oE 'F-[0-9]+' | sort -u)
(sin salida)
```

## 2. Hallazgos

### F-032 — La NOTA DE CIERRE de S-022 repite por tercera sesion consecutiva el hueco de plantilla en el commit de anclaje

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_audit/S-022.md`, linea 258 (version del commit de anclaje `07ca614`) |

- **Que se observo:**

  ```
  $ git show 07ca614:_audit/S-022.md | grep -nE '<se completa|<pendiente|<en blanco|<rellenar'
  207: 5	+  $ grep -nE '<se completa|<pendiente|<en blanco|<rellenar' _audit/S-021.md
  239:| 5 | `grep -nE '<se completa\|<pendiente\|<en blanco\|<rellenar' _audit/S-021.md` | Si | lineas 228, 272 y 287, coinciden con lo publicado en `D-092` |
  258:**NOTA DE CIERRE — commit de sesion `e45185d`, commit de anclaje `<se completa tras commitear el
  ```

  Las lineas 207 y 239 son citas legitimas del patron dentro de la evidencia del Paso 2d. El hueco
  real es la 258, y es el unico. El dato que falta es derivable y no esta escrito en ninguna parte
  del archivo:

  ```
  $ git log --format='%h %ad %s' --date=short -- _audit/S-022.md
  07ca614 2026-09-18 S-022: ancla el informe al hash e45185d
  e45185d 2026-09-18 S-022: F-031 evaluado (D-092/T-051), el prototipo se construye (D-093) y el patrocinador lo aprueba (D-094)
  ```

  El control que debia verlo sigue sin ampliarse, y `.claude/` no se toco en el commit de sesion:

  ```
  $ git diff --name-only e45185d^ e45185d -- .claude | wc -l
  0
  $ git show e45185d:_persistence/tasks.md | grep -E '^\| \[T-049\]'
  | [T-049](#t-049---ampliar-el-control-de-huecos-del-cierre-a-los-marcadores-inline-de-la-nota-de-cierre) | Ampliar el control de huecos del cierre a los marcadores inline de la NOTA DE CIERRE | No implementada | Media | No bloqueante | 010_prototype |
  ```

- **Por que importa:** es la sexta instancia de la familia (`F-013`, `F-026`, `F-028`, `F-030`,
  `F-031`) y la **tercera sesion consecutiva** con el mismo hueco en el mismo rotulo. El registro de
  `S-022` no dice donde quedo su anclaje, igual que no lo decian `S-020` ni `S-021`. `Media` porque
  el registro queda incompleto justo en el punto que la nota existe para cubrir, sin afirmar nada
  falso; `No bloqueante` porque el dato es recuperable con una orden y nada posterior hereda un valor
  erroneo.
  ⚠️ **La seccion 6 del informe anticipa este hallazgo y lo declara «con conocimiento de causa».**
  Anticiparlo no lo corrige: el archivo sigue sin decir su commit de anclaje, y esa es la unica razon
  por la que se abre. No se abre por la reincidencia — se abre por el archivo.
- **Que lo corregiria:** una nota fechada al final de `_audit/S-022.md` que declare `07ca614` como
  commit de anclaje, con la salida de `git rev-parse --short` corrida y pegada, sin reescribir la
  linea 258. El **mecanismo** sigue siendo `T-049`. ⚠️ Es una recomendacion, no una orden.

### F-033 — La verificacion de orden de D-093 publica «(sin salida)» para una orden que a partir del commit devuelve una linea, y nadie la releyo pese a que la propia decision lo exige

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/decisions.md`, `D-093`, bloque «Verificacion del orden que exige el Gate» |

- **Que se observo:** `D-093` publica, y lo marca con un aviso propio:

  ```
  $ git show e45185d:_persistence/decisions.md | sed -n '/Verificacion del orden que exige el Gate/,/releerla/p'
  - **Verificacion del orden que exige el Gate — la tarea se sello ANTES que el codigo:**

    ```
    $ git log --diff-filter=A --format='%h %ad %s' --date=short -- 010_prototype/005_happy_path.md
    22747a9 2026-09-18 S-021: A-008 confirmado (D-083/C-006), Pasos 1-4 de 010_prototype sellados y decididos (D-084-D-090), F-030 evaluado (D-091)
    $ git log --diff-filter=A --format='%h %ad' --date=short -- 010_prototype/app/
    (sin salida)
    ```

    El artefacto sellado nacio en `22747a9`; el codigo del prototipo no existe todavia en el historial
    y nace en el commit de esta sesion, posterior. ⚠️ **Esta salida hay que releerla despues del
  ```

  Reejecutada hoy, esa segunda orden ya no devuelve vacio:

  ```
  $ git log --diff-filter=A --format='%h %ad' --date=short -- 010_prototype/app/
  e45185d 2026-09-18
  ```

  El Paso 2d del informe la reejecuto **antes** del commit y la dio por reproducida (fila 14 de su
  tabla: «vacio, coincide con lo publicado como "sin salida"»), y el commit de anclaje `07ca614` no
  toco `_persistence/decisions.md`:

  ```
  $ git show --stat --name-only --format= 07ca614
  _audit/S-022.md
  ```

  Y la tarea que cubre esta familia de desfases no alcanza a `D-093`:

  ```
  $ git show e45185d:_persistence/tasks.md | grep -E '^### T-050'
  ### T-050 - Anclar o corregir los numeros de linea de las verificaciones de D-086/D-087 que ya no reproducen
  ```

- **Por que importa:** ese bloque es **la evidencia del orden que el Gate 1 exige comprobar** —que el
  artefacto se sellara antes que el codigo—, y es justo la mitad que lo demuestra la que quedo
  publicada como «(sin salida)». Un lector que reejecute la orden obtiene `e45185d` y no lo publicado;
  peor, «(sin salida)» es ambiguo: se puede leer como «la carpeta no existe» en vez de «el codigo aun
  no estaba commiteado». `Baja` porque la conclusion que el bloque sostiene es **cierta y hoy es mas
  facil de probar que entonces** (la salida nueva la confirma mejor que la vieja); `No bloqueante`
  porque nada construido encima hereda un valor erroneo.
- **Que lo corregiria:** una nota fechada en `D-093` que publique la salida anclada posterior al
  commit —`e45185d 2026-09-18` para `010_prototype/app/`— sin reescribir el bloque original, o el
  mismo tratamiento que `manager` decida para `T-050`, ampliando su alcance a `D-093`. ⚠️ Es una
  recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | `Aceptado — pendiente` (sin cambio) | `T-030` sigue `No implementada`; `_persistence/decisions.md` no anade nota a `D-041` en este commit |
| F-017 | `Aceptado — pendiente` (sin cambio) | `T-031` sigue `No implementada`; `_audit/S-013.md` no se toca en este commit |
| F-028 | `Aceptado — pendiente` (sin cambio) | `T-047` sigue `No implementada`; `git show e45185d:_audit/S-019.md \| grep -n '70fe40c'` devuelve las mismas lineas 278 y 284 que abrieron el hallazgo |
| F-029 | `Aceptado — pendiente` (sin cambio) | `T-048` sigue `No implementada`; el `D-XXX` de la plantilla sigue en `_audit/005_discovery/005_phase_exit_record_001.md` |
| F-030 | `Aceptado — pendiente` (sin cambio) | `T-049` sigue `No implementada`; `git show e45185d:_audit/S-020.md \| grep -nE '<se completa'` sigue devolviendo la linea 261 con el hueco |
| F-031 | **`Implementado`** — `e45185d` | La correccion esta en el diff: `_audit/S-021.md` recibe una nota fechada 2026-09-18 que declara `6a7d7f7` como commit de anclaje, con `git rev-parse --short 6a7d7f7` y `git rev-parse --short 6a7d7f7^` corridas y pegadas; la linea 287 sigue literal (25 lineas anadidas al final, cero borradas). El hash declarado es el correcto: `git log --format='%h' -- _audit/S-021.md` devuelve `6a7d7f7` y `22747a9` |

⚠️ **`F-031` se cierra por el archivo, no por el mecanismo.** `T-049` —el control del cierre que deja
pasar el hueco inline— sigue abierta bajo `F-030`, y `F-032` demuestra que el defecto volvio a ocurrir
en esta misma sesion.

## 4. Lo que NO se pudo comprobar

- **El render real de `010_prototype/app/index.html` en un navegador.** La seccion 6 del informe lo
  pidio expresamente. Esta auditoria lee archivos y `git`; no ejecuta un navegador ni toma capturas.
  Lo que si se hizo esta en §1.8: lectura estructural del marcado, los tres `id` que el script usa
  existen, el orden script-despues-de-marcado es correcto y el mecanismo de `hidden` no queda anulado
  por CSS de autor. **Eso no es un render, y no se presenta como tal.** La unica persona que ha visto
  la pagina renderizada es el patrocinador (`D-094`), y esa limitacion ya esta escrita en `D-093`,
  `D-094` y la seccion 4 del informe. No se abre hallazgo: la limitacion esta declarada, no ocultada.
- **`A-009`.** Es un supuesto sobre conducta humana en vivo. No hay nada en el repositorio contra lo
  que contrastarlo hasta que se corra la primera sesion. Su ficha declara disparador y salida, que es
  todo lo que se le puede exigir hoy.

## 5. Recomendaciones sin hallazgo

- **`T-049` esta subestimada en urgencia, y la seccion 6 del informe pregunta justo por eso.** A la
  fecha de esta auditoria el defecto que corrige ha aparecido en `S-020`, `S-021` y `S-022`
  seguidas, y cada repeticion cuesta una tarea de correccion por archivo (`T-049` mas `T-051` mas la
  que pida `F-032`). Sigue siendo `Media`/`No bloqueante`. La pregunta para `manager` no es si el
  hallazgo individual bloquea, sino si un control que falla tres veces seguidas y genera trabajo cada
  vez sigue siendo `No bloqueante`. **No es un hallazgo**: la tarea existe, esta registrada y su
  prioridad es una decision de `manager`, no del auditor.
- **`D-092` decidio no abrir tarea nueva para el control apoyandose en que `T-049` ya existe, y ese
  razonamiento se sostiene.** `T-049` cubre el mecanismo y su criterio de cierre lo dice; duplicarla
  habria dejado dos tareas para un unico cambio en `.claude/skills/protocol-close/SKILL.md`. Lo que
  `D-092` no podia prever es que la tercera repeticion llegaria en la misma sesion que la acepto.
- **`L-025` es buena candidata a leccion global.** Describe un fallo de metodo —escribir un bloque de
  verificacion antes de correr la orden— que no depende de nada de este proyecto, y su formulacion ya
  viaja tal cual. Queda a criterio de `manager` en la cosecha de la etapa.
