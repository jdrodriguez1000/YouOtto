# Auditoria R-002 — sobre S-002

| Campo | Valor |
|---|---|
| Auditoria | R-002 |
| Informe auditado | `_audit/S-002.md` |
| Commit auditado | e222812 |
| Fecha | 2026-09-15 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-002.md
4b27ae4
$ git show 4b27ae4:_audit/S-002.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | e222812 |
$ git show --stat --name-only --format= e222812 | grep -c .
14
$ git show --stat --format= 4b27ae4 | tail -1
 3 files changed, 151 insertions(+), 21 deletions(-)
```

Hash literal de la cabecera (`e222812`) distinto del derivado (`4b27ae4`): el cierre uso su Paso 7c.
Se audita `e222812`, cuyos 14 archivos coinciden uno por uno con la lista de la seccion 1 del
informe. El commit de anclaje lleva **tres** archivos y no uno (`_audit/S-002.md`,
`_persistence/decisions.md`, `_persistence/tasks.md`): es el Paso 7c-bis, que autoriza anclar
criterios de cierre de esa sesion. Lo que el anclaje quito de las dos entradas de `_persistence/`:

```
$ git show 4b27ae4 -- _persistence/ | grep -E '^-  \$' | wc -l
9
```

Las nueve (seis en `decisions.md`, tres en `tasks.md`) son las ordenes `git show <hash>:` /
`git grep ... <hash>` sustituidas por su forma con `e222812` dentro de su bloque — las siete que
cuenta el CONTROL del informe, mas las dos de `D-009` que no empiezan por `git show <hash>:`
(`git grep ... <hash>` y el bucle `for`), igualmente ancladas; lo unico anadido fuera de bloque son las lineas `📌 **Ancladas por el
Paso 7c-bis al commit ...**`, la forma que prescribe `.claude/skills/protocol-close/SKILL.md:1776`.
Del informe, el anclaje solo quito la cabecera `PENDIENTE`, la lista de staging y el marcador de la
NOTA DE CIERRE:

```
$ git diff e222812 4b27ae4 -- _audit/S-002.md | grep -E '^-'
--- a/_audit/S-002.md
-| Commit auditado | PENDIENTE — se rellena en el Paso 7c |
-Salida cruda de `git diff --cached --stat --name-only` (el commit de esta sesion todavia no existia
-al escribir esta seccion; la version anclada al commit la pega el Paso 7c en la NOTA DE CIERRE de
-esta misma seccion):
-
-$ git diff --cached --stat --name-only
-Esta lista es del area de staging previa al commit y **no incluye** los dos archivos que este mismo
-cierre anade despues de generarla: `_audit/S-002.md` (este informe) y su fila en `_audit/index.md`.
-Los dos entran igual en el commit de la sesion.
-> NOTA DE CIERRE — la escribe el Paso 7c-bis despues del commit, con el CENSO y el CONTROL, y el
-> `CONTROL DE PROSA BORRADA`, la `SEGUNDA PASADA anclada del Paso 2e` y el `CONTROL DE CIFRA
-> ADYACENTE` de este mismo Paso 6b.
```

### 1.2 Informe contra el diff

```
$ git diff --name-status e222812^ e222812
M	.claude/agents/gate1_auditor.md
M	.claude/agents/gate2_auditor.md
M	.claude/agents/phase_exit_auditor.md
M	.claude/skills/protocol-close/SKILL.md
M	_audit/S-001.md
A	_audit/S-002.md
M	_audit/findings.md
M	_audit/index.md
M	_persistence/assumptions.md
M	_persistence/decisions.md
M	_persistence/lessons.md
M	_persistence/progress.md
M	_persistence/tasks.md
M	_persistence/techdebt.md
$ git log -1 --format=%ad --date=short e222812; git log -1 --format=%ad --date=short 4b27ae4; date +%F
2026-09-15
2026-09-15
2026-09-15
```

Contrastado con `git show e222812` archivo por archivo: `D-008`, `D-009`, `A-002` (`Refutado`, con
nota), `A-003`, `L-003`, `L-004`, `DT-001`, `T-003`..`T-005`, la nota de `T-001`, la nota de
`_audit/S-001.md`, las tres filas y fichas de `findings.md`, la fila del tablero y las secciones 1,
2, 3 y la bitacora de `progress.md` estan en el diff tal como el informe las describe. Ninguna
entrada de sesiones anteriores se edito salvo por nota fechada (`T-001`, `A-002`, `S-001.md`) o por
la correccion del ancla que pedia `F-003`. La seccion 0 no usa `Implementado`.

**D-009 — la cabecera YAML.** Los tres agentes cambian una sola linea, `NO decision: ` →
`NO decision — `. Antes y despues:

```
$ for a in gate1_auditor gate2_auditor phase_exit_auditor; do printf '%s ' $a; git show 45e33a4:.claude/agents/$a.md | python -c "import sys,re,yaml; s=sys.stdin.read(); fm=re.match(r'^---\r?\n(.*?)\r?\n---',s,re.S).group(1); yaml.safe_load(fm); print('OK')" 2>&1 | grep -E '^(OK|yaml\.)' ; done
gate1_auditor yaml.scanner.ScannerError: mapping values are not allowed here
gate2_auditor yaml.scanner.ScannerError: mapping values are not allowed here
phase_exit_auditor yaml.scanner.ScannerError: mapping values are not allowed here
$ git grep -nE "^description:.*: " e222812 -- .claude/agents | wc -l
0
$ for a in gate1_auditor gate2_auditor phase_exit_auditor report_auditor session-closer session-starter; do printf '%s ' $a; git show e222812:.claude/agents/$a.md | python -c "import sys,re,yaml; s=sys.stdin.read(); fm=re.match(r'^---\r?\n(.*?)\r?\n---',s,re.S).group(1); yaml.safe_load(fm); print('OK')" 2>&1 | grep -E '^(OK|yaml\.)' ; done
gate1_auditor OK
gate2_auditor OK
phase_exit_auditor OK
report_auditor OK
session-closer OK
session-starter OK
$ git grep -nE "^description:.*: " 45e33a4 -- .claude/agents | cut -d: -f1-3
45e33a4:.claude/agents/gate1_auditor.md:3
45e33a4:.claude/agents/gate2_auditor.md:3
45e33a4:.claude/agents/phase_exit_auditor.md:3
```

**D-008 — los once patrones.** El diff de `.claude/skills/protocol-close/SKILL.md` cambia los
patrones en las lineas 320, 366, 389, 410, 427 (tres), 1643, 1654 y 1726 (dos) —once— y reescribe el
parrafo que los explica. Las cifras que `D-008` publica para antes y despues reproducen:

```
$ git show 45e33a4:.claude/skills/protocol-close/SKILL.md | grep -cF '^\+\$ '
9
$ git show 45e33a4:.claude/skills/protocol-close/SKILL.md | grep -cF '^\$ .*<hash>'
1
$ git show 45e33a4:.claude/skills/protocol-close/SKILL.md | grep -cF 'f&&/^\$ /'
2
$ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF '^\+\$ '
0
$ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF '^\$ .*<hash>'
0
$ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF 'f&&/^\$ /'
0
$ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF '[[:space:]]*\$ '
12
```

Verificaciones previas de `D-008`, `T-004` y `T-005` contra `45e33a4`, reejecutadas:

```
$ git show 45e33a4:_persistence/tasks.md | grep -nE '^[[:space:]]*\$ .*<hash>'
132:  $ git show <hash>:project.md | grep -cE '<[A-Za-z]'
$ for f in $(git ls-tree -r --name-only 45e33a4 _persistence _audit | grep -v '_audit/S-001.md'); do n=$(git show 45e33a4:"$f" | grep -cE '^[[:space:]]*> *\$ '); [ "$n" != "0" ] && echo "$f: $n"; done; echo "(fin)"
(fin)
$ git diff -U0 5cae773^ 5cae773 -- _persistence _audit ":(exclude)_audit/S-001.md" | grep -E '^\+\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
0
$ git show 45e33a4:_audit/S-001.md | grep -n -A12 "CONTROL DE CIFRA ADYACENTE — salida" | grep -E "Devolvio|CIFRA"
279:> **CONTROL DE CIFRA ADYACENTE — salida:**
288-> Devolvio 7 lineas (ver el detalle corrido antes de commitear, en la seccion 6 de este cierre). Las
$ git show 45e33a4:_persistence/progress.md | grep -nE '^### S-001'
120:### S-001 - Primera sesion de trabajo: `project.md` completo y el porque del arranque
$ git show 5cae773:project.md | grep -cE '<[A-Za-z]'
0
```

Criterios de cierre anclados de `T-003`, `T-004` y `T-005`:

```
$ git show e222812:_persistence/tasks.md | grep -cE '^[[:space:]]*\$ git show 5cae773:project.md'
1
$ git show e222812:_audit/S-001.md | grep -cE '^> S-001\.md:(36|81|82|130|131|173|210|211): '
8
$ git show e222812:_persistence/progress.md | grep -oE '\]\(#s-001[^)]*\)'
](#s-001---primera-sesion-de-trabajo-projectmd-completo-y-el-porque-del-arranque)
$ git show e222812:_persistence/progress.md | grep -nE '^#+ S-001'
131:### S-001 - Primera sesion de trabajo: `project.md` completo y el porque del arranque
```

### 1.3 La NOTA DE CIERRE del informe (seccion 7), reejecutada

CENSO y CONTROL del Paso 7c:

```
$ for f in $(git ls-tree -r --name-only e222812 _persistence _audit | grep -v '_audit/S-002.md'); do n=$(git show e222812:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-001.md: 3
_persistence/decisions.md: 7
_persistence/tasks.md: 4
$ for f in $(git diff --name-only e222812^ e222812 -- _persistence _audit ":(exclude)_audit/S-002.md"); do n=$(git diff -U0 e222812^ e222812 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 4
_persistence/tasks.md: 3
```

Lo que queda con `<hash>` tras el anclaje, en el commit `4b27ae4`:

```
$ git show 4b27ae4:_persistence/tasks.md | grep -nE '^[[:space:]]*\$ .*<hash>'; git show 4b27ae4:_persistence/decisions.md | grep -nE '^[[:space:]]*\$ .*<hash>'
135:  $ git show <hash>:project.md | grep -cE '<[A-Za-z]'
368:  $ git show 45e33a4:_persistence/tasks.md | grep -nE '^[[:space:]]*\$ .*<hash>'
400:  $ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF '^\$ .*<hash>'
```

Las tres son las que el informe explica: la 135 es la orden original de `T-001`, con su forma anclada
en la nota fechada debajo; la 368 y la 400 citan `<hash>` como texto buscado.

CONTROL DE CIFRA ADYACENTE sobre el informe tal como entro en `e222812` (copia en el scratchpad):

```
$ git show e222812:_audit/S-002.md > S-002.md; awk '<orden literal del Paso 6b>' S-002.md
S-002.md:51: cierre anade despues de generarla: `_audit/S-002.md` (este informe) y su fila en `_audit/index.md`.
S-002.md:94: Una sola tarea abierta: `T-002`, asignar quien firma el dictamen de cada Gate. No estaba prevista
S-002.md:95: para esta sesion — nacio en `S-001` y sigue igual — pero con los tres agentes de Gate y de acta ya
S-002.md:135: De los tres, solo `A-001` sigue abierto (`A-002` quedo `Refutado` y `A-003` `Confirmado` en esta
S-002.md:136: misma sesion, ver seccion 0 y 3 mas arriba).
S-002.md:241: La tabla (vacia) cuadra con los dos: 12 archivos en el area de staging (sin contar `_audit/S-002.md`
S-002.md:242: ni la fila de `_audit/index.md`, que este cierre anade despues), 0 caracteres de control en total.
S-002.md:260: Cuatro lineas — 4 (`wc -l` sobre la salida de arriba). Son promociones pendientes, no un fallo del
S-002.md:261: cierre: `D-008` y `D-009` tocaron `.claude/` sin promoverlo al esqueleto de arranque. Ninguna linea es
```

(La orden `awk` es la literal de `protocol-close`, Paso 6b; se abrevia solo en la linea del prompt.)
Las mismas nueve lineas que publica el informe.

CONTROL DE PROSA BORRADA, literal y con cerca tolerante, comparando `e222812` con `4b27ae4`:

```
$ outside() { awk '/^```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show e222812:"$f" | outside > a; git show 4b27ae4:"$f" | outside > b; echo "== $f =="; diff a b | grep -c '^<'; done
== _persistence/decisions.md ==
6
== _persistence/tasks.md ==
3
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show e222812:"$f" | outside > a; git show 4b27ae4:"$f" | outside > b; echo "== $f =="; diff a b | grep '^<'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

(Los temporales `a` y `b` se escribieron en el scratchpad de la sesion de auditoria; se abrevia su
ruta.) Reproduce lo que el informe publica: nueve falsas «prosa borrada» con la orden literal, cero con la
tolerante. Ver `F-004`.

### 1.4 Controles mecanicos (Paso 4)

**a) Indice ↔ detalle en `_persistence/`:**

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show e222812:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show e222812:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

**b) Carpetas del arbol contra las declaradas:**

```
$ diff <(git ls-tree -d --name-only e222812 | sed 's|$|/|' | sort) <(git show e222812:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con razon escrita: `005_discovery/` declarada por adelantado por `D-002`
(`_persistence/decisions.md`, lineas 161-165); `temporal/` excluida en `.gitignore` con su motivo.
Sin hallazgo.

**c) Fuga de datos propios en las seis areas agnosticas:**

```
$ git grep -nE "YouOtto|C:\\\\Users\\\\USUARIO|C:/Users/USUARIO|Company_TripleS|github\.com" e222812 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas. Incluye el parrafo nuevo de `protocol-close` («Una version anterior del patron
exigia...»), que describe el fallo sin citar codigo.

**d) Codigos instanciados en `_phases` y `_workflow`:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' e222812 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.5 Anclas de los indices y coherencia fila/ficha de `findings.md`

```
$ slug(){ echo "$1" | tr 'A-Z' 'a-z' | sed -E 's/[^a-z0-9 _-]//g; s/ /-/g'; }
$ for f in progress tasks decisions assumptions constraints lessons techdebt; do git show e222812:_persistence/$f.md | awk '/^```/{c=!c; next} !c' | grep -E '^### [A-Z]+-[0-9]+' | while read -r l; do s=$(slug "${l#\#\#\# }"); git show e222812:_persistence/$f.md | grep -qF "](#$s)" && echo "$f OK  #$s" || echo "$f ROTO #$s"; done; done
progress OK  #s-001---primera-sesion-de-trabajo-projectmd-completo-y-el-porque-del-arranque
progress OK  #s-002---atendidos-los-tres-hallazgos-de-r-001-y-corregida-la-cabecera-yaml-de-los-agentes-de-gate-y-acta
tasks OK  #t-001---completar-projectmd-y-montar-el-andamiaje-minimo-de-000_preproject
tasks OK  #t-002---asignar-las-firmas-del-gate-1-y-del-gate-2-antes-de-cerrar-sus-etapas
tasks OK  #t-003---hacer-que-los-barridos-de-anclaje-del-cierre-vean-ordenes-indentadas
tasks OK  #t-004---publicar-la-salida-real-del-control-de-cifra-adyacente-de-s-001
tasks OK  #t-005---corregir-el-ancla-de-la-fila-s-001-en-el-indice-de-progressmd
decisions OK  #d-001---etapas-declaradas-del-proyecto
decisions OK  #d-002---carpetas-de-entregables-declaradas-al-arrancar-cada-etapa
decisions OK  #d-003---adopcion-del-gate-1-y-del-gate-2
decisions OK  #d-004---se-conserva-el-historial-heredado-del-esqueleto
decisions OK  #d-005---consulta-de-arranque-de-las-lecciones-globales
decisions OK  #d-006---se-escribe-el-inventario-de-acciones-irreversibles
decisions OK  #d-007---dueno-y-sitio-de-evaluacion-observabilidad-y-seguridad
decisions OK  #d-008---los-barridos-de-anclaje-de-protocol-close-admiten-ordenes-indentadas
decisions OK  #d-009---se-corrige-la-cabecera-yaml-de-los-agentes-de-gate-y-de-acta
assumptions OK  #a-001---los-juegos-registrados-pueden-ser-datos-de-personas
assumptions OK  #a-002---los-agentes-de-gate-y-de-acta-se-cargan-al-reiniciar-claude-code
assumptions OK  #a-003---con-la-cabecera-yaml-valida-los-agentes-de-gate-y-de-acta-cargan
constraints OK  #c-001---inventario-de-acciones-irreversibles
lessons OK  #l-001---el-historial-heredado-del-esqueleto-se-comprueba-antes-del-primer-push
lessons OK  #l-002---un-registro-que-una-regla-cita-se-comprueba-que-existe-al-arrancar
lessons OK  #l-003---un-control-con-patron-literal-se-prueba-contra-el-formato-real-del-registro
lessons OK  #l-004---un-agente-que-no-aparece-se-diagnostica-validando-su-cabecera-antes-de-suponer
techdebt OK  #dt-001---claude-y-protocol-close-se-alejan-del-esqueleto-de-arranque
$ diff <(git show e222812:_audit/findings.md | awk '/^```/{c=!c; next} !c' | grep -E '^\| \[F-[0-9]+\]' | awk -F'|' '{match($2,/F-[0-9]+/); e=$(NF - 1); gsub(/^ +| +$/,"",e); print substr($2,RSTART,RLENGTH)" "e}' | sort) <(git show e222812:_audit/findings.md | awk '/^```/{c=!c; next} c{next} /^### F-[0-9]+ /{match($0,/F-[0-9]+/); cur=substr($0,RSTART,RLENGTH); got=0} /^\| Estado \|/ && cur!="" && !got{s=$0; sub(/^\| Estado \| */,"",s); sub(/ *\|$/,"",s); print cur" "s; got=1}' | sort); echo "exit=$?"
exit=0
```

Veinticinco anclas `OK`, cero `ROTO`; fila y ficha de `findings.md` coinciden.

### 1.6 Seccion 6 del informe, punto por punto

- **`T-003`..`T-005` a `Implementada` por el cierre.** `protocol-close` lo prescribe
  (`.claude/skills/protocol-close/SKILL.md:826`: «Mueve a `Implementada` solo lo que la evidencia
  respalde») y los tres criterios anclados reproducen (1.2). Frontera bien puesta; sin hallazgo.
- **Orden #9 de `D-009` sobre el arbol de trabajo.** Es la «Prueba del cambio», no el criterio; el
  criterio anclado (#11) hace la misma validacion sobre `e222812` y da seis `OK` (1.2). Sin hallazgo.
- **`DT-001` propuesta.** La confirmacion es del usuario; la entrada existe con su fila. Sin hallazgo.
- **`findings.md` en `Aceptado — pendiente` frente a «corregido en este commit».** Es lo correcto; los
  estados los cambia esta auditoria (seccion 3).
- **`outside()` no admite cercas indentadas.** Se sostiene: `F-004`.

## 2. Hallazgos

### F-004 — El CONTROL DE PROSA BORRADA de `protocol-close` no reconoce cercas indentadas y no puede pasar en literal sobre este registro

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `.claude/skills/protocol-close/SKILL.md:1826` (Paso 7c-bis, `outside()`); mismo patron en las lineas 218, 219, 256, 257 y 1462 |

- **Que se observo:** `outside()` alterna el estado de bloque solo con `/^```/`, en la primera
  columna. En `_persistence/decisions.md` y `_persistence/tasks.md` todas las cercas van indentadas:

  ```
  $ for f in decisions tasks; do printf "$f col0=%s indent=%s\n" $(git show e222812:_persistence/$f.md | grep -cE '^```') $(git show e222812:_persistence/$f.md | grep -cE '^[[:space:]]+```'); done
  decisions col0=0 indent=18
  tasks col0=0 indent=14
  ```

  Con eso `outside()` nunca entra en un bloque y trata el archivo entero como prosa: cada orden que el
  Paso 7c-bis sustituye —justo lo que el paso existe para hacer— sale como «prosa borrada». Sobre el
  anclaje real de esta sesion, 6 + 3 lineas con la orden literal y 0 con la cerca tolerante (salidas
  completas en 1.3). La tabla del protocolo manda, ante cualquier linea `<`, **detenerse y restaurar**
  (`SKILL.md`, linea 1838); el cierre no pudo seguirla en literal y publico el resultado de una
  variante corregida a mano, declarandolo en su seccion 6 y en la NOTA DE CIERRE. Las otras
  apariciones del mismo patron:

  ```
  $ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -nE "/\^\`\`\`/"
  218:  diff <(awk '/^```/{c=!c; next} !c' "_persistence/$f.md" | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
  219:       <(awk '/^```/{c=!c; next} !c' "_persistence/$f.md" | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  256:diff <(awk '/^```/{c=!c; next} !c' _audit/findings.md | grep -E '^\| \[F-[0-9]+\]' | awk -F'|' '{match($2,/F-[0-9]+/); e=$(NF - 1); gsub(/^ +| +$/,"",e); print substr($2,RSTART,RLENGTH)" "e}' | sort) \
  257:     <(awk '/^```/{c=!c; next} c{next} /^### F-[0-9]+ /{match($0,/F-[0-9]+/); cur=substr($0,RSTART,RLENGTH); got=0} /^\| Estado \|/ && cur!="" && !got{s=$0; sub(/^\| Estado \| */,"",s); sub(/ *\|$/,"",s); print cur" "s; got=1}' _audit/findings.md | sort)
  1462:  /^```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  1826:outside() { awk '/^```/{f=!f; next} !f'; }
  ```

  Las de 218-257 hoy no dan error visible (sin cerca reconocida simplemente no filtran nada, y las
  salidas de 1.4a y 1.5 salen limpias); la de 1462 corre sobre el informe, que escribe sus cercas en
  columna cero salvo dentro de la NOTA DE CIERRE (`> ```` `). No se abre hallazgo aparte por ellas:
  se citan para que la correccion, si se acepta, decida su alcance de una vez. `protocol-audit`
  lleva el mismo patron en sus controles 4a y 7d.
- **Por que importa:** es el mismo defecto que `F-001` en otro control del mismo protocolo. Un control
  obligatorio que, con el formato real del registro, **siempre** devuelve lineas tras un anclaje deja
  dos salidas malas: detenerse cada vez, o hacer lo que hizo este cierre —correr una orden distinta de
  la del protocolo—, y entonces el resultado publicado ya no es el del control escrito. Ademas, hoy el
  hallazgo solo vive en la seccion 6 de `_audit/S-002.md`: no tiene `T-XXX`, `D-XXX` ni `DT-XXX`, y
  sin registro se pierde. No es `Alta` porque el defecto da falsos positivos, no falsos negativos: no
  deja pasar prosa borrada.
- **Que lo corregiria:** admitir sangria en la cerca de `outside()` (`/^[[:space:]]*```/`), decidiendo
  a la vez si se extiende a las lineas 218-257 y 1462 —y a `protocol-audit`—, y llevarlo al esqueleto
  por la via de `DT-001`. Alternativa: fijar por convencion cercas en columna cero, que `D-008` ya
  descarto para el caso analogo. Es una recomendacion.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-001` | Aceptado — pendiente (`T-003`, `D-008`) | **Implementado** en `e222812` | Los once patrones admiten sangria y no queda ninguna forma antigua (`0`, `0`, `0`, `12` en 1.2); la orden de `T-001` tiene su forma anclada a `5cae773` en nota fechada y reproduce (`1` y `0` en 1.2) |
| `F-002` | Aceptado — pendiente (`T-004`) | **Implementado** en `e222812` | La nota fechada de `_audit/S-001.md` lleva las ocho lineas reales (`8` en 1.2), con su orden y su lectura, sin tocar el texto original (el diff de ese archivo es solo `+`) |
| `F-003` | Aceptado — pendiente (`T-005`) | **Implementado** en `e222812` | El ancla de la fila `S-001` es `...-projectmd-...` y resuelve (`progress OK #s-001...` en 1.5) |

```
$ git show e222812 -- _audit/S-001.md | grep -cE '^-[^-]'
0
```

## 4. Lo que NO se pudo comprobar

- **`A-003` `Confirmado` y la parte de `A-002` que se apoya en la sesion.** Que Claude Code anunciara
  los tres agentes como lanzables tras `D-009`, y que el usuario hubiera reiniciado antes, no deja
  rastro en el repositorio; la propia nota de `A-003` lo declara. Lo comprobable —que las seis
  cabeceras son YAML valido en `e222812` y que las tres fallaban en `45e33a4`— esta en 1.2.
- **Seccion 9 del informe (Paso 2f) y la salida de `DT-001`.** Comparan el arbol de trabajo contra el
  esqueleto local en el momento del cierre; no se reejecutaron contra el esqueleto porque el arbol de
  trabajo ya no es el del commit auditado por definicion. Lo que si consta es que los cuatro archivos
  que nombran son exactamente los cuatro de `.claude/` que cambia `e222812` (1.2).

## 5. Recomendaciones sin hallazgo

- `_audit/S-002.md`, seccion 1: tras el anclaje, la vineta de `_audit/index.md` sigue diciendo «fuera
  de la lista de arriba», pero la lista anclada ya lo incluye. Es un residuo de la version previa; una
  nota fechada lo aclararia.
- `DT-001` pega la salida de `diff -rq` con las rutas del esqueleto abreviadas a `.../SDAI_TripleS/...`;
  la salida literal completa esta en `_audit/S-002.md`, seccion 9. En el registro que perdura convendria
  la cruda, o citar donde esta.
- `_audit/S-002.md`, seccion 9: «Cuatro lineas — 4 (`wc -l` sobre la salida de arriba)» cita un `wc -l`
  que no esta pegado. El recuento se ve contando la salida, pero la orden nombrada deberia ir con ella.
