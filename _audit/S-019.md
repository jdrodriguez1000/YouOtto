# Informe de auditoria — S-019

| Campo | Valor |
|---|---|
| Sesion | S-019 |
| Fecha | 2026-09-18 |
| Etapa | `005_discovery` (cerrada esta sesion) → `010_prototype` |
| Rama | main |
| Commit auditado | `d80a965` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`e9846f6`), sobre el estado que `ed64348` dejo (el commit que `R-020`
audito). Ademas de los tres hallazgos de `R-020`, la tabla incluye `F-016` y `F-017` (`R-015`), que
siguen sin cerrar y no se atendieron esta sesion.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-016 — `D-041` afirma el resultado de una prueba sobre commits historicos sin publicar la orden | Aceptado — pendiente | `T-030`, `No implementada` |
| F-017 — la NOTA DE CIERRE de `S-013` dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete | Aceptado — pendiente | `T-031`, `No implementada` |
| F-025 — `T-042` publica «seis» ordenes donde su propia ficha enumera ocho casos | Aceptado — pendiente | `T-043`, `Implementada`; `D-075` |
| F-026 — la NOTA DE CIERRE de `S-018` declara `ed64348` como commit de anclaje, y el anclaje esta en `a22ef55` | Aceptado — pendiente | `T-044`, `Implementada`; `D-076` |
| F-027 — el CONTROL DE SALIDA REPRODUCIDA es ciego al formato «Ordenes:»/«Salidas:» | Aceptado — pendiente | `T-045`, `Implementada`; `D-077` |

📌 **`F-025`/`F-026`/`F-027` llevan veredicto `Aceptado — pendiente` aunque su tarea ya este
`Implementada`.** El estado `Implementado` de un hallazgo lo escribe la auditoria posterior, nunca
este cierre — `CLAUDE.md` y `_audit/findings.md` lo dicen literal. La correccion esta en el diff de
esta sesion; que quede `Implementado` es cosa de la proxima pasada de `report_auditor`.

## 1. Que se hizo

Salida cruda de `git diff --cached --stat --name-only` (el commit todavia no existe al escribir esta
seccion; la version anclada la pega el Paso 7c):

```
$ git diff --cached --stat --name-only
.claude/skills/protocol-close/SKILL.md
_audit/005_discovery/005_phase_exit_record_001.md
_audit/S-018.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

📌 **Esta lista no incluye `_audit/S-019.md` (este mismo informe).** Al escribir esta seccion el
informe todavia no esta en `git add`; entra en el commit de todas formas y el Paso 7b lo comprueba
por separado.

**NOTA DE CIERRE — version anclada al commit `d80a965` (Paso 7c):**

```
$ git show --stat --name-only --format= d80a965
.claude/skills/protocol-close/SKILL.md
_audit/005_discovery/005_phase_exit_record_001.md
_audit/S-018.md
_audit/S-019.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

📌 **Ahora si incluye `_audit/S-019.md`** (este informe), que en el momento de escribir la lista de
arriba todavia no estaba en `git add`. Once archivos, el mismo diez de la version en staging mas
este informe.

Por archivo, lo que muestra el diff (2 hunks o mas → hay entradas editadas ademas de las que nacen;
derivado con `git diff d80a965^ d80a965 -U0 -- <archivo> | grep -cE '^@@'`):

- **`.claude/skills/protocol-close/SKILL.md`** — 4 hunks. Edita en su sitio la plantilla de la NOTA
  DE CIERRE (Paso 6b) para exigir derivar los dos hashes de la NOTA DE CIERRE con
  `git rev-parse --short HEAD`/`HEAD^` (`D-076`), y corrige la funcion `salidas()` del CONTROL DE
  SALIDA REPRODUCIDA (Paso 7c-bis) para reconocer el formato `**Ordenes:**`/`**Salidas:**` (`D-077`).
  No nace ninguna entrada nueva: es codigo del protocolo, no un registro con `### D-XXX`.
- **`_audit/005_discovery/005_phase_exit_record_001.md`** — 1 hunk. Se editan los tres huecos
  `<en blanco>` de la seccion 5.2: `Quien`, `Fecha` y `Decision`. Es la firma del patrocinador
  (`D-074`); la etapa `005_discovery` queda cerrada.
- **`_audit/S-018.md`** — 1 hunk. Se anade una nota fechada bajo la NOTA DE CIERRE existente,
  declarando que el commit de anclaje es `a22ef55` y no `ed64348` (`F-026`, `D-076`). No se reescribe
  la linea original.
- **`_audit/findings.md`** — 4 hunks. La fila de `F-025`/`F-026`/`F-027` pasa de `Abierto` a
  `Aceptado — pendiente` con su `T-XXX`/`D-XXX`, y sus tres fichas se actualizan igual (`Estado` y
  `Registrado en`), fila y ficha juntas.
- **`_audit/index.md`** — 1 hunk (este cierre). Fila nueva de `S-019` con columnas `Pendiente`.
- **`_persistence/assumptions.md`** — 3 hunks. `A-004` (nace) y `A-005` (nace) no: ambas ya existian
  y **se editan** — pasan de `Abierto` a `Confirmado` en el indice, y cada una recibe una nota
  fechada con su evidencia (`D-079`). Ninguna entrada nace en este archivo.
- **`_persistence/decisions.md`** — 2 hunks. Nacen `D-073` a `D-079` (siete decisiones): el orden de
  la jornada, el cierre de la etapa, la evaluacion de `F-025`/`F-026`/`F-027`, la sexta pasada de la
  cosecha, y la confirmacion de `A-004`/`A-005`. Ninguna entrada existente se edita.
- **`_persistence/lessons.md`** — 2 hunks. Nace `L-021` (indice y ficha): «una salida se pega de la
  ejecucion, nunca se predice, por trivial que parezca la orden».
- **`_persistence/progress.md`** — 7 hunks (`git diff <hash>^ <hash> -- <archivo> | grep -cE '^@@'`;
  no 3, como decia una version anterior de esta linea — corregido al derivarlo). Nace la fila de
  `S-019` en el indice de sesiones (1 hunk); las secciones 1, 2 y 3 se sobrescriben en cinco hunks
  (etapa, avance, bloqueos, ultimo realizado, siguiente paso); y nace la entrada de detalle `S-019`
  en la bitacora (1 hunk).
- **`_persistence/tasks.md`** — 3 hunks. Nacen `T-043`, `T-044`, `T-045` (cierran `F-025`/`F-026`/
  `F-027`) y `T-046` (el Paso 2d de este mismo cierre, `Origen: session-closer`). Ninguna entrada
  existente se edita.

## 2. Que NO se hizo, y por que

Tareas `No implementada` del indice de `tasks.md` (derivado, no de memoria):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
| [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita) | Publicar por nota la orden sin filtro que D-041 cita | No implementada | Baja | No bloqueante | 005_discovery |
| [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013) | Corregir por nota la cifra de la NOTA DE CIERRE de S-013 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-032](#t-032---limitar-las-skills-que-puede-invocar-el-agente-de-cierre) | Limitar las skills que puede invocar el agente de cierre | No implementada | Alta | No bloqueante | 005_discovery |
| [T-033](#t-033---publicar-por-nota-las-ordenes-del-paso-1b-y-del-barrido-que-d-045-resume) | Publicar por nota las ordenes del Paso 1b y del barrido que D-045 resume | No implementada | Baja | No bloqueante | 005_discovery |
| [T-035](#t-035---anclar-por-nota-la-cifra-del-barrido-del-paso-1-de-protocol-harvest-en-d-062) | Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-037](#t-037---anclar-o-fechar-dos-ordenes-del-contexto-de-d-064-que-ya-no-reproducen) | Anclar o fechar dos ordenes del Contexto de D-064 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-042](#t-042---anclar-o-fechar-seis-ordenes-de-contexto-en-d-068d-069d-070d-071d-072-que-ya-no-reproducen) | Anclar o fechar seis ordenes de Contexto en D-068/D-069/D-070/D-071/D-072 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-046](#t-046---anclar-o-fechar-la-orden-de-verificacion-de-d-073-sobre-f-025f-026f-027-que-ya-no-reproduce) | Anclar o fechar la orden de Verificacion de D-073 sobre F-025/F-026/F-027 que ya no reproduce | No implementada | Baja | No bloqueante | 005_discovery |
```

Las ocho quedaron sin tocar esta sesion. `T-032` es la de mayor importancia (`Alta`): decidir si
`D-050`/`A-007` sustituye el criterio literal de «tras reiniciar Claude Code» en `_workflow/`. Las
demas son correcciones de nota o de anclaje sobre ordenes de contexto que dejaron de reproducir por
el paso del tiempo, no por error de contenido.

No se toco codigo de producto: la etapa `010_prototype` acaba de abrirse con la firma de `D-074` y
todavia no tiene ninguna casilla verificada.

## 3. Decisiones tomadas

- **`D-073`** — el acta de cierre de `005_discovery` va antes que la evaluacion de los tres
  hallazgos `Abierto` de `R-020`, por decision del patrocinador (`usuario`). Alternativa descartada:
  evaluar los hallazgos primero, como pide el orden por defecto de `CLAUDE.md`; se descarto porque
  los tres son `No bloqueante` y el coste de invertir el orden es nulo.
- **`D-074`** — el patrocinador (JD Rodriguez, Main Sponsor) firma la seccion 5.2 del acta de
  `005_discovery` con `ETAPA CERRADA`. Alternativas descartadas: esperar a que los cinco hallazgos
  abiertos se cierren (nunca terminaria, ninguno es casilla de salida); dar la etapa por cerrada solo
  con el dictamen del auditor, sin la firma del patrocinador (exactamente lo que `010_prototype` §3.3
  impide).
- **`D-075`** — se acepta `F-025`: la cifra correcta de `T-042` es **ocho**, no seis; se corrige por
  nota fechada, sin reescribir el titulo que es ancla del indice. Alternativa descartada: reescribir
  el titulo (rompe la navegacion y borra el error en vez de corregirlo).
- **`D-076`** — se acepta `F-026` en sus dos mitades: nota fechada con los dos hashes derivados, y
  regla nueva en `protocol-close` que exige derivarlos con `git rev-parse --short HEAD`/`HEAD^`.
  Alternativas descartadas: solo la nota (deja el defecto listo para repetirse) o solo la regla (el
  informe de `S-018` seguiria afirmando algo falso).
- **`D-077`** — se acepta `F-027` y **se corrige ahora**, no se aplaza a `DT-XXX`: el defecto crecia
  con cada decision nueva (10 → 16 ordenes coladas). Alternativa descartada: registrarlo como deuda
  tecnica y aplazarlo — se descarta porque el coste sube con cada decision nueva y el arreglo cabe en
  una condicion del `awk`.
- **`D-078`** — sexta pasada de la cosecha sobre `005_discovery`: `L-021` no sube, ya cubierta por
  `LG-32`. Se hace **despues** de la firma del patrocinador, con la tension de orden declarada
  explicitamente: la casilla que exige cero lecciones `Sin evaluar` ya se marco cuando era cierta, y
  esta pasada no la vuelve a marcar, solo devuelve el arbol a ese estado.
- **`D-079`** — `A-004` y `A-005` se confirman: sus disparadores (el acta de cierre y `T-029`) ya
  corrieron. Alternativa descartada: dejarlos `Abierto` hasta la etapa siguiente — sus disparadores
  ya no volveran a correr, asi que seguirian abiertos sin que nadie tuviera la obligacion de volver a
  ellos.

## 4. Supuestos vigentes y riesgos

```
$ grep -E '^\| \[A-' _persistence/assumptions.md | grep -c Abierto
1
```

Un solo supuesto `Abierto`: **`A-008`** — «las condiciones de uso de Baloto permiten leer su
historico de forma automatica» — en el camino critico del producto. Su disparador es la primera
linea de codigo del prototipo, en `010_prototype`, que acaba de abrirse y todavia no ha empezado. Si
resulta falso, el prototipo no puede automatizar la lectura del historico y hay que replantear la
fuente de datos.

## 5. Siguiente tarea propuesta

`T-046` (`Baja`/`No bloqueante`, `Origen: session-closer`): corregir por nota la orden de
«Verificacion» de `D-073` sobre `F-025`/`F-026`/`F-027`, que publico `3` y hoy devuelve `0` porque
esos tres hallazgos cambiaron de estado mas tarde en la misma sesion que la escribio. Y de fondo,
leer `_phases/010_prototype.md` y `_workflow/010_prototype.md` para fijar el primer paso del
procedimiento de la etapa que se acaba de abrir.

## 6. Que pedimos auditar

- **La correccion de `F-027` en el mismo commit que evalua el hallazgo.** `D-077` corrige la funcion
  `salidas()` de `protocol-close` a la vez que acepta el hallazgo, en vez de abrir una tarea aparte.
  Es la excepcion declarada a «aceptar primero, corregir despues» de las otras dos (`F-025`, `F-026`)
  de la misma tanda, y conviene que se revise si el criterio de «se corrige ahora si el coste sube
  con cada decision nueva» es el correcto o abre una puerta a corregir siempre en caliente.
- **La sexta pasada de la cosecha corrida despues de firmar el acta de cierre**, invirtiendo el orden
  que `protocol-harvest` fija (cosecha antes de la firma). `D-078` declara la tension y explica por
  que la razon de esa regla no aplica aqui, pero es la primera vez que este proyecto invierte ese
  orden y conviene que quede contrastado.
- **El nuevo hallazgo `T-046`** es en si mismo la evidencia de lo fragil que es una orden de
  «Verificacion» (fuera de un bloque «Criterio de cierre») cuando el estado que describe cambia mas
  tarde en la misma sesion. No es la primera vez que este patron aparece (`T-030`, `T-031`, `T-037`,
  `T-042` son la misma familia de defecto), y ya son cinco tareas abiertas del mismo tipo sin que
  exista un mecanismo que lo evite en el momento de escribir, solo despues.

## 7. Evidencia del Paso 2d

Orden y salida completa (20 lineas, sin deduplicar):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+$ git log --oneline --format='%h %s' -2 ed64348..a22ef55
+  $ for d in 051 052 053 054 055 056 057 058 061; do sed -n "/^### D-$d /,/^- \*\*Contexto/p" _persistence/decisions.md | grep -c '^| Origen | usuario |'; done | sort -u
+  $ grep -cE '^\| \[T-029\].*\| Implementada \|' _persistence/tasks.md
+  $ grep -cE '^\| \[F-02[567]\].*\| No bloqueante \| Abierto \|' _audit/findings.md
+  $ sed -n '/^### 5.2 Aprobacion del patrocinador/,/^| Donde queda/p' _audit/005_discovery/005_phase_exit_record_001.md | grep -cE '^\| (Quien \| JD Rodriguez - Main Sponsor|Fecha \| `2026-09-18`|Decision \| `ETAPA CERRADA`) \|'
+  $ grep -c '<en blanco>' _audit/005_discovery/005_phase_exit_record_001.md
+  $ sed -n '/^### T-042 /,/^### T-038 /p' _persistence/tasks.md | grep -cE '^  - \*\*D-0'
+  $ grep -n 'T-042' _persistence/tasks.md | grep -oE 'seis' | wc -l
+  $ git rev-parse --short a22ef55
+  $ git rev-parse --short a22ef55^
+  $ eval "$(grep -m1 '^salidas() {' .claude/skills/protocol-close/SKILL.md)"
+  $ salidas < _persistence/decisions.md | grep -c 'git show'
+  $ salidas < _persistence/decisions.md | grep 'git show'
+  $ grep -E '^\| \[L-' _persistence/lessons.md | grep '005_discovery' | grep -c 'Sin evaluar'
+  $ grep -E '^\| \[L-' _persistence/lessons.md | grep -c 'Global candidata'
+  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" rev-parse --short HEAD
+  $ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" status --short
+  $ for d in 051 052 053 054 055 056 057 058 061; do sed -n "/^### D-$d /,/^- \*\*Contexto/p" _persistence/decisions.md | grep -c '^| Origen | usuario |'; done | sort -u
+  $ grep -cE '^\| \[T-029\].*\| Implementada \|' _persistence/tasks.md
+  $ sed -n '/^### T-042 /,/^### T-038 /p' _persistence/tasks.md | grep -cE '^  - \*\*D-0'
```

Recuento (20, LINEAS devueltas):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
20
```

Reejecucion, numerada:

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git log --oneline --format='%h %s' -2 ed64348..a22ef55` | Si | `a22ef55 S-018: ancla el informe y los criterios de cierre al hash ed64348` |
| 2 | `for d in 051...061; do sed -n ... \| grep -c '^\| Origen \| usuario \|'; done \| sort -u` | Si | `1` |
| 3 | `grep -cE '^\| \[T-029\].*\| Implementada \|' _persistence/tasks.md` | Si | `1` |
| 4 | `grep -cE '^\| \[F-02[567]\].*\| No bloqueante \| Abierto \|' _audit/findings.md` | **No** | publica `3` (en `D-073`, «Verificacion»); hoy da `0` — `F-025`/`F-026`/`F-027` ya no son `Abierto`. Abierta como `T-046` |
| 5 | `sed -n '/^### 5.2.../p' ... \| grep -cE ...` | Si | `3` |
| 6 | `grep -c '<en blanco>' _audit/005_discovery/005_phase_exit_record_001.md` | Si | `0` |
| 7 | `sed -n '/^### T-042 /,/^### T-038 /p' _persistence/tasks.md \| grep -cE '^  - \*\*D-0'` | Si | `8` |
| 8 | `grep -n 'T-042' _persistence/tasks.md \| grep -oE 'seis' \| wc -l` | Si | `3` |
| 9 | `git rev-parse --short a22ef55` | Si | `a22ef55` |
| 10 | `git rev-parse --short a22ef55^` | Si | `ed64348` |
| 11 | `eval "$(grep -m1 '^salidas() {' ...)"` | Si | define la funcion, sin salida |
| 12 | `salidas < _persistence/decisions.md \| grep -c 'git show'` | Si | `1` |
| 13 | `salidas < _persistence/decisions.md \| grep 'git show'` | Si | `D-008 \| T-001 \|   $ git show <hash>:project.md \| grep -cE '<[A-Za-z]'` |
| 14 | `grep -E '^\| \[L-' _persistence/lessons.md \| grep '005_discovery' \| grep -c 'Sin evaluar'` | Si | `0` |
| 15 | `grep -E '^\| \[L-' _persistence/lessons.md \| grep -c 'Global candidata'` | Si | `0` |
| 16 | `git -C ".../TripleS_Lessons" rev-parse --short HEAD` | Si | `e915383` |
| 17 | `git -C ".../TripleS_Lessons" status --short` | Si | (vacio) |
| 18 | (repite la 2) | Si | `1` |
| 19 | (repite la 3) | Si | `1` |
| 20 | (repite la 7) | Si | `8` |

Pares repetidos, `uniq -d` completo:

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
+  $ for d in 051 052 053 054 055 056 057 058 061; do sed -n "/^### D-$d /,/^- \*\*Contexto/p" _persistence/decisions.md | grep -c '^| Origen | usuario |'; done | sort -u
+  $ grep -cE '^\| \[T-029\].*\| Implementada \|' _persistence/tasks.md
+  $ sed -n '/^### T-042 /,/^### T-038 /p' _persistence/tasks.md | grep -cE '^  - \*\*D-0'
```

Las tres repetidas son las lineas 2/18, 3/19 y 7/20 de la tabla. Ninguna otra se repite.

**Unica orden que no reprodujo: la #4.** Se abrio la tarea `T-046` (`Origen: session-closer`), con el
archivo, la orden literal y las dos salidas, en `_persistence/tasks.md`.

**NOTA DE CIERRE — commit de sesion `d80a965`, commit de anclaje `<se completa tras commitear el anclaje>`.**

📌 **NOTA 2026-09-18 — el commit de anclaje es `70fe40c` (derivado, no tecleado).** El parrafo de
arriba se escribio antes de que ese commit existiera, con un marcador en vez del hash. Derivado tras
commitear el anclaje, tal como exige la regla de `D-076`/`T-044`:

```
$ git rev-parse --short HEAD
70fe40c
$ git rev-parse --short HEAD^
d80a965
```

El primero es el commit de anclaje (este mismo, el que anadio esta nota); el segundo es su padre, el
commit de sesion — coincide con el que ya declaraba el parrafo original. No se reescribe la linea de
arriba: es la misma correccion que aplica `D-076` a `S-018`, aplicada aqui a si misma en la primera
sesion que usa la regla nueva.

Diez decisiones y tareas nacidas esta sesion (`D-073`-`D-079`, `T-043`-`T-045`) publicaban su
`Criterio de cierre` con `<hash>` literal, con 21 ordenes en total (15 en `decisions.md`, 6 en
`tasks.md`, derivado con `grep -c 'git show d80a965:' _persistence/decisions.md _persistence/tasks.md`).
El Paso 7c-bis las ancla al commit de sesion `d80a965`.

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal, anclado al commit de la sesion; no tiene que salir vacio):

```
$ for f in $(git ls-tree -r --name-only d80a965 _persistence _audit | grep -v '_audit/S-019.md'); do
  n=$(git show d80a965:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_audit/S-018.md: 2
_persistence/decisions.md: 19
_persistence/tasks.md: 1
```

📌 **`decisions.md: 19` y `tasks.md: 1` son ordenes con `<hash>` que NO nacieron esta sesion** —los
mismos que sostienen `T-030`, `T-035`, `T-037`, `T-042` y `T-046` (ya abiertas antes o durante esta
sesion), y el resto de sesiones anteriores— y que la regla de «no reescribir historia» deja intactos. Las
ordenes de `D-073`-`D-079`/`T-043`-`T-045` ya no llevan `<hash>`: se anclaron al commit de sesion.

CONTROL (acotado a lo que este commit anadio y a la ranura vacia `$ git show <hash>:`):

```
$ for f in $(git diff --name-only d80a965^ d80a965 -- _persistence _audit ":(exclude)_audit/S-019.md"); do
  n=$(git diff -U0 d80a965^ d80a965 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
```

Salida: (vacia). **No queda ninguna orden de la forma exacta `$ git show <hash>:` sin anclar en lo
que este commit anadio.** Las 21 ordenes de esta sesion usan el formato en tres puntos
(`**Ordenes:**`/`**Salidas:**`, sin prefijo `$ `), que el CONTROL no cubre por diseno —esta declarado
en el Paso 7c: el CONTROL solo reconoce la cerca suelta con `$ ` al frente—, y todas quedaron
ancladas a `d80a965` a mano, verificadas una por una contra las salidas publicadas (ver el
CONTROL DE SALIDA REPRODUCIDA, mas abajo).

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | outside > /tmp/prosa_antes
  outside < "$f" > /tmp/prosa_despues
  echo "== $f =="
  diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea `< …`: ninguna linea de prosa desaparecio en los dos archivos.

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /\*\*Ordenes:\*\*/{m="O"} /\*\*Salidas:\*\*/{m="S"} /^[[:space:]]*```/{f=!f; if(!f) m=""; next} c&&f&&m!="O"&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | salidas > /tmp/salida_publicada
  salidas < "$f" > /tmp/salida_anclada
  echo "== $f =="
  diff /tmp/salida_publicada /tmp/salida_anclada | grep -E '^[<>]'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea `<` ni `>`: las 21 salidas ancladas son identicas, byte a byte, a las publicadas antes
de anclar. Las diez lineas `📌 **Ancladas por el Paso 7c-bis al commit `d80a965`.**` pueden decir
que reproducen.

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d d80a965^ d80a965); do
  n=$(git show d80a965:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia).

```
$ git diff --name-only --diff-filter=d d80a965^ d80a965 | wc -l
11
$ for f in $(git diff --name-only --diff-filter=d d80a965^ d80a965); do git show d80a965:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El commit real lleva **11** archivos (los 10 de la tabla de la Seccion 8 mas este informe,
`_audit/S-019.md`, que quedo fuera por construccion). El total sigue en `0`: el informe tampoco
introduce caracteres de control.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-019.md
_audit/S-019.md:49: 📌 **Esta lista no incluye `_audit/S-019.md` (este mismo informe).** Al escribir esta seccion el
_audit/S-019.md:50: informe todavia no esta en `git add`; entra en el commit de todas formas y el Paso 7b lo comprueba
_audit/S-019.md:102: Las ocho quedaron sin tocar esta sesion. `T-032` es la de mayor importancia (`Alta`): decidir si
_audit/S-019.md:103: `D-050`/`A-007` sustituye el criterio literal de «tras reiniciar Claude Code» en `_workflow/`. Las
_audit/S-019.md:148: Un solo supuesto `Abierto`: **`A-008`** — «las condiciones de uso de Baloto permiten leer su
_audit/S-019.md:207: Recuento (20, LINEAS devueltas):
_audit/S-019.md:248: Las tres repetidas son las lineas 2/18, 3/19 y 7/20 de la tabla. Ninguna otra se repite.
_audit/S-019.md:294: La tabla tiene 10 filas, todas con `0`. Cuadra con las dos: 10 archivos y total `0`. Este informe
_audit/S-019.md:295: (`_audit/S-019.md`) todavia no esta en `git add` al escribir esta seccion, asi que no aparece en la
_audit/S-019.md:323: 6 lineas (`{ ... } | wc -l` → `6`). Ninguna es `Only in` el arbol del esqueleto: las dos `Only in .claude:` son carpetas que
```

Diez lineas. Cada una se leyo contra el bloque que la precede antes de publicar el informe (Paso
6b): las dos de la linea 49-50 y las de 294-295 explican por que la lista de la Seccion 1 no incluye
este informe (correcto, verificado arriba); la de 102-103 coincide con las 8 filas de la tabla de
tareas abiertas; la de 148 coincide con el `1` del `grep -c Abierto` de la Seccion 4; la de 207
coincide con el `wc -l` de la propia Seccion 7 (`20`, antes de anclar); la de 248 coincide con la
salida de `uniq -d` (3 pares); la de 323 se corrigio de «5 lineas» a «6 lineas» tras derivarla con
`{ ... } | wc -l` en vez de contarla a ojo (el error se detecto y corrigio antes del commit
sustantivo, no es una nota fechada).

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 d80a965^ d80a965 -- _persistence _audit ":(exclude)_audit/S-019.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
21
```

📌 **`21`, no `20`.** El commit real anadio una linea `$ …` mas que el area de staging que se
midio antes de commitear: es la orden de «Verificacion (Paso 2d…)» que la propia `T-046` publica en
su cuerpo (nacida al escribir esa misma tarea, dentro de este commit). Se reejecuto y reproduce
(`grep -cE '^\| \[F-02[567]\].*\| No bloqueante \| Abierto \|' _audit/findings.md` → `0`, igual que
publica `T-046`). Las otras 20 son las ya reejecutadas y numeradas en la Seccion 7 antes del commit;
solo una no reproducia (la `#4`, sobre `D-073`) y quedo registrada como `T-046`.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia — ningun archivo del commit contiene caracteres de control).

Tabla por archivo (misma orden, sin el filtro `[ "$n" -gt 0 ]`):

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  echo "$f: $n"
done
.claude/skills/protocol-close/SKILL.md: 0
_audit/005_discovery/005_phase_exit_record_001.md: 0
_audit/S-018.md: 0
_audit/findings.md: 0
_audit/index.md: 0
_persistence/assumptions.md: 0
_persistence/decisions.md: 0
_persistence/lessons.md: 0
_persistence/progress.md: 0
_persistence/tasks.md: 0
```

Los dos contrastes de la tabla:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
10
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

La tabla tiene 10 filas, todas con `0`. Cuadra con las dos: 10 archivos y total `0`. Este informe
(`_audit/S-019.md`) todavia no esta en `git add` al escribir esta seccion, asi que no aparece en la
tabla — es exactamente la brecha que la SEGUNDA PASADA anclada de la NOTA DE CIERRE existe para
cubrir.

⚠️ Esta seccion mide el area de staging previa al commit. Los archivos que el cierre escribe
despues de este punto —este mismo informe y cualquier ajuste posterior— quedan fuera por
construccion. La SEGUNDA PASADA anclada, en la NOTA DE CIERRE de la Seccion 7, cubre esa brecha
despues del commit de anclaje.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
```

Salida:

```
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/agents/session-closer.md and .claude/agents/session-closer.md differ
Only in .claude: checks
Only in .claude: hooks
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/005_discovery/015_stakeholders.md and _templates/005_discovery/015_stakeholders.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_workflow/005_discovery.md and _workflow/005_discovery.md differ
```

6 lineas (`{ ... } | wc -l` → `6`). Ninguna es `Only in` el arbol del esqueleto: las dos `Only in .claude:` son carpetas que
existen aqui (`checks`, `hooks`) y no en el esqueleto todavia — promociones pendientes, lo normal. El
`protocol-close/SKILL.md` difiere ademas por los cambios de esta misma sesion (`D-076`, `D-077`).
Esto **no es un fallo**: son promociones pendientes de aprobacion del usuario, sin cambios respecto
a lo que ya reportaba `S-018`.
