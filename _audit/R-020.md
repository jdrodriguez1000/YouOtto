# Auditoria R-020 — sobre S-018

| Campo | Valor |
|---|---|
| Auditoria | R-020 |
| Informe auditado | `_audit/S-018.md` |
| Commit auditado | ed64348 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (3) |

## 1. Que se comprobo

### Anclaje del commit

El informe se deriva de `a22ef55`, que es el **commit de anclaje**; el hash literal de su cabecera
—y el que se audita— es `ed64348`.

```
$ git log -1 --format=%h -- _audit/S-018.md
a22ef55
$ git show a22ef55:_audit/S-018.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | ed64348 |
$ git show --stat --name-only --format= a22ef55
_audit/S-018.md
_persistence/decisions.md
$ git show --stat --name-only --format= ed64348 | grep -c .
11
```

Los 11 archivos de `ed64348` coinciden uno a uno con la lista que la seccion 1 del informe enumera.
El commit de anclaje toca dos archivos —el informe y los criterios de cierre de `decisions.md`—, que
es lo que el Paso 7c/7c-bis autoriza.

### Indice ↔ detalle en `_persistence/` — coherente

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show ed64348:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show ed64348:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

### Carpetas del arbol contra las declaradas — una diferencia, con motivo escrito

```
$ diff <(git ls-tree -d --name-only ed64348 | sed 's|$|/|' | sort) \
       <(git show ed64348:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
9a10
> temporal/
```

`temporal/` esta declarada en `project.md` como excluida en `.gitignore`: la razon esta escrita, no
es hallazgo.

### Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" ed64348 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(vacio)
```

### Fuga por codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' ed64348 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(vacio)
```

### Fila ↔ ficha en `_audit/findings.md` (control del Paso 2b, con la columna nueva) — coherente

Estado:

```
$ diff <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -E '^\| \[F-[0-9]+\]' | awk -F'|' '{match($2,/F-[0-9]+/); e=$(NF - 2); gsub(/^ +| +$/,"",e); print substr($2,RSTART,RLENGTH)" "e}' | sort) \
       <(awk '/^[[:space:]]*```/{c=!c; next} c{next} /^### F-[0-9]+ /{match($0,/F-[0-9]+/); cur=substr($0,RSTART,RLENGTH); got=0} /^\| Estado \|/ && cur!="" && !got{s=$0; sub(/^\| Estado \| */,"",s); sub(/ *\|$/,"",s); print cur" "s; got=1}' _audit/findings.md | sort)
(vacio)
```

`Registrado en` (la columna que nacio en este commit), fila contra ficha:

```
$ diff <(… awk -F'|' '{r=$(NF-1) …}' …) <(… campo `Registrado en` de cada ficha …)
(vacio)
```

Orden completa reejecutada:

```
$ diff <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -E '^\| \[F-[0-9]+\]' | awk -F'|' '{match($2,/F-[0-9]+/); r=$(NF-1); gsub(/^ +| +$/,"",r); gsub(/`/,"",r); print substr($2,RSTART,RLENGTH)" "r}' | sort) \
       <(awk '/^[[:space:]]*```/{c=!c; next} c{next} /^### F-[0-9]+ /{match($0,/F-[0-9]+/); cur=substr($0,RSTART,RLENGTH); got=0} /^\| Registrado en \|/ && cur!="" && !got{s=$0; sub(/^\| Registrado en \| */,"",s); sub(/ *\|$/,"",s); gsub(/`/,"",s); print cur" "s; got=1}' _audit/findings.md | sort)
(vacio)
```

Las 22 filas del indice y su cabecera nueva:

```
$ git show ed64348:_audit/findings.md | grep -cE '^\| \[?F-[0-9]+'
22
$ git show ed64348:_audit/findings.md | grep -n '^| Codigo | Hallazgo'
23:| Codigo | Hallazgo | Auditoria | Gravedad | Urgencia | Estado | Registrado en |
```

### Los cinco criterios de cierre anclados, reejecutados contra `ed64348`

Cada orden anclada por el Paso 7c-bis, corrida de nuevo. A la derecha, lo que su bloque «Salidas:»
publica.

```
$ git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -c '^## Paso 2b-bis'            → 1  (D-068 publica 1)
$ git show ed64348:_persistence/progress.md | grep -cE '\(#s-0(07|08|10|15|16|17)---'            → 6  (D-068 publica 6)
$ git show ed64348:_audit/findings.md | grep -c '^| Codigo | Hallazgo | … | Registrado en |'     → 1  (D-069 publica 1)
$ git show ed64348:_audit/findings.md | grep -c '^| Registrado en | la `T-XXX` o la `D-XXX`'     → 1  (D-069 publica 1)
$ git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -cF 'e=$(NF - 2)'               → 1  (D-069 publica 1)
$ git show ed64348:_persistence/techdebt.md | grep -cE '\| Sin confirmar \|'                     → 0  (D-070 publica 0)
$ git show ed64348:_persistence/techdebt.md | grep -cF 'Propuesta (pendiente del usuario)'       → 3  (D-070 publica 3)
$ git show ed64348:_persistence/decisions.md | grep -cE 'NOTA 2026-09-17 — (la remision a|esa remision no resuelve)' → 2  (D-071 publica 2)
$ git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -cF 'no remite a «Sin resolver»' → 1  (D-071 publica 1)
$ git show ed64348:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar' → 0  (D-072 publica 0)
```

Las diez reproducen exactamente lo publicado. El desglose que el informe declara (`D-068` 2,
`D-069` 3, `D-070` 2, `D-071` 2, `D-072` 1 = 10) cuadra con lo que el diff del commit de anclaje
sustituyo.

### La orden del Paso 2d, reejecutada anclada

```
$ git diff -U0 ed64348^ ed64348 -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
12
$ … | sort | uniq -d
(vacio)
```

Las doce lineas son, una por una, las mismas doce que el informe numera en su seccion 7. La tabla de
reejecucion del informe tambien se comprobo entera: las siete que declara `No` devuelven hoy
exactamente lo que el informe dice que devuelven, y las dos que declara `Si` reproducen.

```
$ grep -n '(#s-017' _persistence/progress.md | grep -oE 'l-018-+d-063'      → l-018--d-063   (informe: l-018--d-063)
$ grep -A7 '^### F-003' _audit/findings.md | grep -E '^\| (Estado|Cerrado en) \|' → 1 linea  (informe: 1)
$ grep -E '^\| \[F-020\]' _audit/findings.md | awk -F'|' '{print $(NF-1)}'  →  `T-036` y `D-064`
$ grep -cE '^\|.*\| Sin confirmar \|' _persistence/techdebt.md              → 0
$ grep -c "Sin resolver" _audit/S-017.md                                    → 0
$ grep -c "Sin resolver" .claude/skills/protocol-close/SKILL.md             → 17
$ grep -E '^\| \[L-' _persistence/lessons.md | grep '005_discovery' | grep -c 'Sin evaluar' → 0
$ node --test .claude/checks/anchors.test.js 2>&1 | grep -E 'tests|pass|fail'
ℹ tests 10
ℹ pass 10
ℹ fail 0
```

### El control nuevo `.claude/checks/anchors.js`, probado en rojo

No basta con que su test pase: un control que no puede fallar es documentacion. Se le dio un archivo
con un ancla rota a proposito y otro con un ancla derivada de un titulo con flecha y punto.

```
$ node .claude/checks/anchors.js <archivo con ancla correcta, titulo con → y .>
total anclas rotas: 0
exit=0
$ node .claude/checks/anchors.js <archivo con ancla rota>
ROTA …/t2.md:1 -> #y-001---titulo-inexistente
total anclas rotas: 1
exit=1
```

Y sobre el arbol del propio commit auditado, extraido con `git archive ed64348`:

```
$ node .claude/checks/anchors.js _persistence/*.md _audit/*.md      # sobre el arbol de ed64348
total anclas rotas: 0
exit=0
```

### Las seis anclas de `progress.md`: se corrigio el ancla, no el titulo

```
$ git diff -U0 ed64348^ ed64348 -- _persistence/progress.md | grep -E '^[-+]### '
+### S-018 - `F-021`/`F-022`/`F-023`/`F-024` de `R-019` evaluados (`D-068`-`D-071`), …
```

Ningun titulo anterior se toco. Normalizando el enlace, las seis filas antes y despues son
byte-identicas en su texto visible: solo cambio el ancla, que es lo que el Paso 2b-bis exige.

### Paso 2f (desfase con el esqueleto), reejecutado — 6 lineas, identicas

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files …/SDAI_TripleS/.claude/agents/session-closer.md and .claude/agents/session-closer.md differ
Only in .claude: checks
Only in .claude: hooks
Files …/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files …/SDAI_TripleS/_templates/005_discovery/015_stakeholders.md and _templates/005_discovery/015_stakeholders.md differ
Files …/SDAI_TripleS/_workflow/005_discovery.md and _workflow/005_discovery.md differ
```

### Fecha del informe contra la del commit

```
$ git log -1 --format='%h %ad' --date=short ed64348
ed64348 2026-09-17
$ git log -1 --format='%h %ad' --date=short a22ef55
a22ef55 2026-09-17
```

El informe declara `2026-09-17`, y la fila de `S-018` en `progress.md` tambien.

### Secciones 2 y 4 del informe, reejecutadas

Las siete tareas `No implementada` y los tres supuestos `Abierto` que el informe lista son
exactamente los que devuelven sus ordenes sobre `ed64348`.

### La seccion 0 no cierra ningun hallazgo

```
$ git show ed64348:_audit/findings.md | grep -E '^\| \[F-' | awk -F'|' '{print $(NF-2)}' | sort | uniq -c
      6  Aceptado — pendiente
     16  Implementado
```

Los cuatro hallazgos de `R-019` quedaron en `Aceptado — pendiente`, no en `Implementado`: `manager`
no se cerro sus propios hallazgos. Los 16 `Implementado` son los que cerraron auditorias anteriores.

## 2. Hallazgos

### F-025 — `T-042` publica «seis» ordenes donde su propia ficha enumera ocho casos, y el informe repite la cifra

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_persistence/tasks.md` (`T-042`, titulo y fila del indice) y `_audit/S-018.md` seccion 5 |

- **Que se observo:** el titulo de la tarea, su fila del indice y la seccion 5 del informe dicen
  «seis ordenes»; la ficha enumera ocho vinetas y nueve ordenes, y su propia prosa dice «los ocho
  casos de arriba».

  ```
  $ git show ed64348:_persistence/tasks.md | sed -n '/^### T-042 /,/^### T-038 /p' | grep -cE '^  - \*\*D-0'
  8
  $ git show ed64348:_persistence/tasks.md | sed -n '/^### T-042 /,/^### T-038 /p' | grep -E '^  - \*\*D-0'
    - **D-068:** `grep -n '(#s-017' _persistence/progress.md | grep -oE 'l-018-+d-063'`. Publica
    - **D-068:** `node .claude/checks/anchors.js _persistence/*.md _audit/*.md`. Publica la lista de seis
    - **D-068:** `node slug.mjs '...'` y `node validate2.mjs ...`. Los dos scripts eran deliberadamente
    - **D-069:** `grep -E '^\| \[F-020\]' _audit/findings.md | awk -F'|' '{print $(NF-1)}'`. Publica
    - **D-069:** `grep -A7 '^### F-003' _audit/findings.md | grep -E '^\| (Estado|Cerrado en) \|'`.
    - **D-070:** `grep -cE '^\|.*\| Sin confirmar \|' _persistence/techdebt.md`. Publica `1`; reejecutada
    - **D-071:** `grep -c "Sin resolver" .claude/skills/protocol-close/SKILL.md`. Publica `16`;
    - **D-072:** `grep -E '^\| \[L-' _persistence/lessons.md | grep '005_discovery' | grep -c 'Sin evaluar'`.
  $ git show ed64348:_persistence/tasks.md | grep -n 'T-042' | grep -coE 'seis'
  3
  ```

  Las tres lecturas posibles dan 7, 8 o 9, y ninguna da 6: **siete** ordenes que ya no reproducen
  (las siete `No` de la tabla de la seccion 7 del informe), **ocho** vinetas, **nueve** ordenes
  contando los dos scripts temporales de la tercera vineta. La propia prosa de la ficha se
  contradice con su titulo: dice «los ocho casos de arriba» debajo de un titulo que dice seis.

  La tabla del informe, contada sobre su propia salida:

  ```
  $ git diff -U0 ed64348^ ed64348 -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
  12
  ```

  De esas doce: 7 `No`, 2 `Si`, 3 `No aplica` — segun la tabla del propio informe, verificada orden
  por orden en la seccion 1 de esta auditoria.

- **Por que importa:** es una cifra tecleada que su propia enumeracion contradice, y es la tercera
  vez que el mismo patron aparece en este registro (`F-017`, `F-019`). La tarea es el mecanismo que
  hace avanzar el pendiente: quien la retome la proxima vez comparara «seis» con lo que encuentre y
  tendra que decidir cual de las dos cifras es la buena, sin nada que lo zanje. `Media` porque el
  registro afirma un numero falso en tres sitios y ademas se contradice consigo mismo en la misma
  ficha; no `Alta` porque el contenido esta completo —las nueve ordenes estan enumeradas con su
  archivo y su causa—, asi que no se pierde informacion. `No bloqueante` porque ningun control lee
  esa cifra.
- **Que lo corregiria:** derivar la cifra de la enumeracion y publicarla con la orden que la cuenta,
  en el titulo, en la fila del indice y en la prosa, declarando cual de las tres se cuenta (ordenes
  que no reproducen / vinetas / ordenes totales). Y, como la tarea ya esta commiteada, hacerlo por
  nota fechada en vez de reescribir. ⚠️ Es una recomendacion, no una orden.

### F-026 — La NOTA DE CIERRE de `S-018` declara `ed64348` como commit de anclaje, y el anclaje esta en `a22ef55`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_audit/S-018.md`, linea de la NOTA DE CIERRE |

- **Que se observo:** la nota nombra el mismo hash para las dos cosas.

  ```
  $ git show a22ef55:_audit/S-018.md | grep -n 'NOTA DE CIERRE'
  249:**NOTA DE CIERRE — commit de anclaje `ed64348`, informe sobre el commit de sesion `ed64348`.**
  ```

  Pero el anclaje —la sustitucion de `<hash>` por `ed64348` en el informe y en los cinco criterios de
  cierre— ocurrio en un commit posterior:

  ```
  $ git show --stat --name-only --format= a22ef55
  _audit/S-018.md
  _persistence/decisions.md
  $ git log -1 --format='%h %s' a22ef55
  a22ef55 S-018: ancla el informe y los criterios de cierre al hash ed64348
  $ git diff a22ef55^ a22ef55 -- _audit/S-018.md | grep -c '^+.*ed64348'
  25
  $ git show ed64348:_audit/S-018.md | grep -n 'NOTA DE CIERRE' | sed -n '2p'
  247:**NOTA DE CIERRE:** <se completa en el Paso 7c/7c-bis, despues del commit>
  ```

  Es decir: en `ed64348` —el commit que la nota llama «de anclaje»— la NOTA DE CIERRE todavia estaba
  en su marcador de pendiente, y el diff de `a22ef55` introduce 25 lineas con el hash. El anclaje
  entero, incluida la propia nota, nace en `a22ef55`.

- **Por que importa:** el protocolo de auditoria distingue commit de sesion y commit de anclaje
  precisamente porque los dos hashes mandan a estados distintos, y la nota es el sitio donde esa
  distincion queda escrita para quien la lea despues. Publicar `ed64348` como commit de anclaje manda
  a buscar el anclaje a un commit que no lo contiene — el mismo defecto que `F-013` abrio en su
  version anterior (un anclaje completado en un commit que la nota no declaraba). `Media` porque el
  registro afirma algo comprobablemente falso sobre su propia trazabilidad; no `Alta` porque el
  contenido anclado si reproduce y el hash del estado auditado es correcto. `No bloqueante` porque
  nada posterior hereda el error.
- **Que lo corregiria:** una nota fechada bajo la NOTA DE CIERRE que diga que el commit de anclaje es
  `a22ef55` y el de sesion `ed64348`, sin reescribir la linea original; y, hacia adelante, que
  `protocol-close` derive los dos hashes con una orden en vez de teclearlos. ⚠️ Es una recomendacion,
  no una orden.

### F-027 — El CONTROL DE SALIDA REPRODUCIDA es ciego al formato «Ordenes:»/«Salidas:» y su limite no quedo registrado

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `.claude/skills/protocol-close/SKILL.md`, Paso 7c-bis (funcion `salidas()`) |

- **Que se observo:** la funcion excluye del computo las lineas que empiezan por `$ `, y las
  decisiones de este proyecto ya no escriben sus ordenes con ese prefijo: usan dos cercas separadas,
  «Ordenes:» y «Salidas:». Resultado, el control clasifica **lineas de orden** como si fueran
  salidas.

  ```
  $ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /^[[:space:]]*```/{f=!f; next} c&&f&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }; salidas < _persistence/decisions.md | grep -E '^D-(068|069|070|071|072) ' | grep -c 'git show'
  10
  $ salidas < _persistence/decisions.md | grep -E '^D-(068|069|070|071|072) ' | grep 'git show' | head -4
  D-068 | git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -c '^## Paso 2b-bis'
  D-068 | git show ed64348:_persistence/progress.md | grep -cE '\(#s-0(07|08|10|15|16|17)---'
  D-069 | git show ed64348:_audit/findings.md | grep -c '^| Codigo | Hallazgo | Auditoria | Gravedad | Urgencia | Estado | Registrado en |'
  D-069 | git show ed64348:_audit/findings.md | grep -c '^| Registrado en | la `T-XXX` o la `D-XXX`'
  ```

  El informe lo declara con honestidad y lo suple con una verificacion aparte —extrae solo los
  bloques «Salidas:» y los compara antes y despues, byte a byte—, de modo que **esta sesion no
  sustituyo ninguna evidencia**: eso se verifico y es cierto. Lo que no ocurrio es el registro: el
  defecto no esta en `techdebt.md` ni en una `T-XXX`.

  ```
  $ git show ed64348:_persistence/techdebt.md | sed -n '/^## Indice/,/^---/p' | grep -c 'salidas()\|formato'
  0
  ```

  `DT-002` cubre otra limitacion del mismo control («no reejecuta las ordenes»), no esta.

- **Por que importa:** el 7c-bis es obligatorio y su salida ruidosa se repetira en **cada** cierre,
  porque todas las decisiones nuevas usan el formato que el control no entiende. Un control que
  siempre avisa deja de leerse —eso ya esta escrito en la convencion de `findings.md`—, y el cierre
  siguiente puede dar por declarado lo que en realidad no reviso. `Media` porque el control no miente
  sobre el registro, produce ruido; no `Alta` porque este cierre publico la verificacion sustitutiva
  entera. `No bloqueante` porque nada de lo commiteado hereda un dato falso.
- **Que lo corregiria:** o adaptar `salidas()` al formato «Ordenes:»/«Salidas:» (reconocer la segunda
  cerca como la de salidas), o —si se decide no tocarlo ahora— registrarlo como `DT-XXX` con su coste,
  porque hoy es un limite conocido sin dueno. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | `Aceptado — pendiente` (sin cambio) | `T-030` sigue `No implementada` en `ed64348`; la correccion no esta en el diff |
| F-017 | `Aceptado — pendiente` (sin cambio) | `T-031` sigue `No implementada` en `ed64348`; la correccion no esta en el diff |
| F-021 | **`Implementado`** en `ed64348` | las seis anclas estan corregidas (`grep -cE '\(#s-0(07\|08\|10\|15\|16\|17)---'` → `6`), ningun titulo se toco, el Paso 2b-bis existe con su control, y el control probado en rojo detecta un ancla rota de verdad. Sobre el arbol de `ed64348`: `total anclas rotas: 0` |
| F-022 | **`Implementado`** en `ed64348` | la columna `Registrado en` existe en el indice (22 filas), su convencion la declara, los valores de `Estado` usados son todos de la lista (`Aceptado — pendiente` y `Implementado`), el Paso 2b lee `$(NF - 2)` y los dos controles fila↔ficha salen vacios |
| F-023 | **`Implementado`** en `ed64348` | `DT-005` lleva `Propuesta (pendiente del usuario)` en la fila y en la ficha, y `grep -cE '\| Sin confirmar \|'` sobre `ed64348` devuelve `0` |
| F-024 | **`Implementado`** en `ed64348` | las dos notas fechadas estan en `D-062` y `D-064` (`grep -cE 'NOTA 2026-09-17 — …'` → `2`) sin reescribir las lineas originales, y el 7c-bis prohibe el puntero hacia adelante (`grep -cF 'no remite a «Sin resolver»'` → `1`). La parte rechazada —la atribucion a la skill— esta registrada en `D-071` con su evidencia, que se comprobo: las menciones de «Sin resolver» en la skill son instrucciones del reporte en pantalla |

## 4. Lo que NO se pudo comprobar

- **La validacion del algoritmo de anclas contra la implementacion de referencia sobre 1532 titulos.**
  Los dos scripts (`slug.mjs`, `validate2.mjs`) fueron deliberadamente temporales y no quedaron en el
  arbol, asi que esa cifra no es reproducible. El informe lo declara en su seccion 6 y `anchors.js`
  lleva el limite escrito en su cabecera. Se suplio con lo que si se puede: su test (10/10) y una
  prueba en rojo propia, que confirman que el control puede fallar y que acierta sobre titulos con
  flecha y con punto. No es hallazgo: el limite esta declarado, no ocultado.
- **Las cuatro primeras ordenes del criterio de cierre de `D-072`**, que hablan del repositorio de
  lecciones globales. Van con el hash `e915383` delante y la propia decision declara que no se pueden
  reproducir desde aqui. La quinta, que si es de este repositorio, reproduce (`0`).
- **El reporte en pantalla del cierre.** No persiste en el commit; nada de lo que dependa solo de el
  es auditable, y por eso `F-024`/`D-071` acertaron al prohibir que el registro lo cite.

## 5. Recomendaciones sin hallazgo

- **El commit de anclaje empieza a llevar carga propia, y conviene que el tablero lo diga.** La fila
  de `_audit/index.md` publica un solo hash —el de sesion, que es el correcto— pero el anclaje ya vive
  en otro commit con cinco criterios de cierre dentro. Una columna o una nota con el hash de anclaje
  ahorraria al auditor siguiente el paso de derivarlo, que es justo donde `F-026` se produjo.
- **El acta de cierre de `005_discovery` sigue sin correr con las siete casillas cumplidas desde
  `S-017`.** El informe lo declara como el trabajo de fondo mas atrasado, y esta auditoria lo
  confirma: `protocol-phase-exit` no ha dejado nada en `_audit/`. No es un defecto de este cierre
  —no es su alcance—, pero llevar dos sesiones mas encima solo hace mas caro reconstruir la evidencia
  que el acta tiene que verificar.
- **Tres tareas de la misma forma acumuladas (`T-035`, `T-037`, `T-042`) apuntan a una convencion que
  falta, no a tres descuidos.** La propia seccion 6 del informe lo pregunta. Anclar la evidencia de
  «Contexto» al commit **anterior** en el momento de escribirla la dejaria reproducible desde el
  primer dia y vaciaria esta cola; hoy cada sesion que diagnostica y corrige en la misma entrada
  genera una tarea nueva que nadie cierra.
