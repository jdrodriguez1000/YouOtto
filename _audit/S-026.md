# Informe de auditoria — S-026

| Campo | Valor |
|---|---|
| Sesion | S-026 |
| Fecha | 2026-09-19 |
| Etapa | 010_prototype |
| Rama | main |
| Commit auditado | `c424ae8` |

## 0. Respuesta a la auditoria anterior

`R-027` (sobre `S-025`, commit `0c07cc8`) entrego veredicto **Con hallazgos (1)**: `F-036`.

Verificado contra `HEAD` al empezar esta sesion (`b374d01`, la auditoria `R-027` sobre `S-025`):

```
$ git log -1 --format='%h %s' HEAD
b374d01 auditoria R-027 sobre S-025 (0c07cc8)
```

Todos los hallazgos entregados y no cerrados en `_audit/findings.md` a este commit, uno por fila:

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-016 — `D-041` afirma el resultado de una prueba sobre commits historicos sin publicar la orden | Aceptado — pendiente | `T-030`, `No implementada`. No se toco en esta sesion |
| F-017 — la NOTA DE CIERRE de `S-013` dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete | Aceptado — pendiente | `T-031`, `No implementada`. No se toco en esta sesion |
| F-028 — la nota de anclaje de `S-019` dice que la escribio `70fe40c`, y esta en `e610906` | Aceptado — pendiente | `T-047`, `No implementada`. No se toco en esta sesion |
| F-029 — el acta de cierre de `005_discovery` conserva el `D-XXX` de la plantilla donde debe citar `D-074` | Aceptado — pendiente | `T-048`, `No implementada`. No se toco en esta sesion |
| F-030 — la NOTA DE CIERRE de `S-020` deja el commit de anclaje como hueco de plantilla, sin instanciar y sin derivar | Aceptado — pendiente | `T-049`, `No implementada`, pendiente de decision del usuario. No se toco en esta sesion |
| F-034 — el registro de `S-023` afirma una ronda de cuatro sesiones donde la sellada es de cinco | Aceptado — pendiente | `T-056`, `Implementada` desde `S-024`. El hallazgo sigue `Aceptado — pendiente` porque el estado de un hallazgo solo lo cambia una auditoria posterior, no `manager` |
| F-036 — la seccion 0 de `S-025` enumera como `Aceptado — pendiente` cuatro hallazgos donde el registro tiene seis | Aceptado — pendiente | `D-103`. Se corrigio en este commit: nota fechada al final de `_audit/S-025.md` que completa la lista con `F-016` y `F-017`, sin reescribir la seccion 0 (`T-059`, `Implementada`). La segunda recomendacion (que el cierre derive esa lista con una orden publicada) se acepta y se aplaza a `T-060` |

📌 **`F-036` se marca `Aceptado — pendiente` y no `Aceptado — corregido en este commit`**, porque el
hallazgo trajo dos recomendaciones y solo una se implemento en esta sesion (`T-059`); la segunda
(`T-060`) queda abierta. El estado de la fila en `findings.md` sigue siendo el que decide `manager`,
no el auditor, y ya esta puesto asi en el commit.

## 1. Que se hizo

Salida cruda de la lista de archivos (area de staging, antes del commit; el Paso 7c la traduce a su
forma anclada en la nota de cierre de esta seccion). Incluye los archivos que el propio cierre anade
—`progress.md`, este informe y la fila de `_audit/index.md`—:

```
$ git diff --cached --stat --name-only
010_prototype/010_participants.md
010_prototype/015_session_004.md
_audit/S-025.md
_audit/S-026.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

📌 **Forma anclada, tras el commit `c424ae8` (Paso 7c):**

```
$ git show --stat --name-only --format= c424ae8
010_prototype/010_participants.md
010_prototype/015_session_004.md
_audit/S-025.md
_audit/S-026.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

Identica a la de staging: no hubo cambios entre el area de staging y el commit.

Y lo que muestra el diff:

- `010_prototype/015_session_004.md` (nace): registro de la cuarta sesion del Paso 5 de
  `010_prototype`, con JD Rodriguez como Actor Generador y Juana Ramirez como facilitadora,
  transcrito al dictado por `manager` (`D-104`). Estado `Exito autonomo`, 09:44 a 09:45 (50.45 s). El
  comentario espontaneo de §6 llega en estilo indirecto y se declara **transmitido, no literal**; las
  respuestas de §7 son literales.
- `010_prototype/010_participants.md`: la fila `004` pasa de `pendiente`/`pendiente` a
  `2026-09-19`/`si`.
- `_persistence/decisions.md`: nacen `D-103` (`F-036` se acepta: la lista de pendientes de `S-025` se
  completa por nota, y derivarla se aplaza a `T-060`) y `D-104` (el registro de la sesion 004 se
  transcribe al dictado, y su comentario espontaneo se declara no literal).
- `_persistence/lessons.md`: nace `L-027` (un comentario que llega en estilo indirecto ya no se puede
  volver literal: las palabras exactas se anotan en la sesion).
- `_persistence/tasks.md`: nacen `T-059` (`Implementada`, completar por nota la lista de la seccion 0
  de `S-025`) y `T-060` (`No implementada`, derivar del registro con orden publicada las listas y
  cifras que hoy se escriben a mano).
- `_audit/S-025.md`: se anade una nota fechada (`F-036`, `D-103`) al final del archivo, que completa
  la lista de hallazgos `Aceptado — pendiente` de su seccion 0 con `F-016` y `F-017`, sin tocar la
  linea de la seccion 0 ya auditada.
- `_audit/findings.md`: fila y ficha de `F-036` pasan de `Abierto` a `Aceptado — pendiente`, citando
  `D-103` / `T-059`, `T-060` en las dos.

## 2. Que NO se hizo, y por que

- La quinta y ultima sesion del Paso 5 de `010_prototype` (`010_prototype/015_session_005.md`) no se
  corrio: es el siguiente trabajo de producto.
- `T-060` queda `No implementada`: la segunda recomendacion de `F-036` (derivar con orden publicada
  las listas de hallazgos que el cierre escribe) se acepto y se aplazo por decision del usuario, que
  pidio esta jornada para el Paso 5 de `010_prototype`.
- Tareas abiertas del indice de `tasks.md` (filtrando `No implementada`), sin cambios en esta sesion
  salvo lo ya dicho:

```
| [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita) | Publicar por nota la orden sin filtro que D-041 cita | No implementada | Baja | No bloqueante | 005_discovery |
| [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013) | Corregir por nota la cifra de la NOTA DE CIERRE de S-013 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-032](#t-032---limitar-las-skills-que-puede-invocar-el-agente-de-cierre) | Limitar las skills que puede invocar el agente de cierre | No implementada | Alta | No bloqueante | 005_discovery |
| [T-033](#t-033---publicar-por-nota-las-ordenes-del-paso-1b-y-del-barrido-que-d-045-resume) | Publicar por nota las ordenes del Paso 1b y del barrido que D-045 resume | No implementada | Baja | No bloqueante | 005_discovery |
| [T-035](#t-035---anclar-por-nota-la-cifra-del-barrido-del-paso-1-de-protocol-harvest-en-d-062) | Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-037](#t-037---anclar-o-fechar-dos-ordenes-del-contexto-de-d-064-que-ya-no-reproducen) | Anclar o fechar dos ordenes del Contexto de D-064 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-042](#t-042---anclar-o-fechar-seis-ordenes-de-contexto-en-d-068d-069d-070d-071d-072-que-ya-no-reproducen) | Anclar o fechar seis ordenes de Contexto en D-068/D-069/D-070/D-071/D-072 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-046](#t-046---anclar-o-fechar-la-orden-de-verificacion-de-d-073-sobre-f-025f-026f-027-que-ya-no-reproduce) | Anclar o fechar la orden de Verificacion de D-073 sobre F-025/F-026/F-027 que ya no reproduce | No implementada | Baja | No bloqueante | 005_discovery |
| [T-047](#t-047---corregir-por-nota-el-commit-que-contiene-la-nota-de-anclaje-de-s-019-y-prohibir-el-autorreferente) | Corregir por nota el commit que contiene la nota de anclaje de S-019 y prohibir el autorreferente | No implementada | Media | No bloqueante | 010_prototype |
| [T-048](#t-048---instanciar-a-d-074-la-fila-de-firma-del-acta-de-005_discovery-y-ampliar-el-control-de-huecos) | Instanciar a D-074 la fila de firma del acta de 005_discovery y ampliar el control de huecos | No implementada | Baja | No bloqueante | 010_prototype |
| [T-049](#t-049---ampliar-el-control-de-huecos-del-cierre-a-los-marcadores-inline-de-la-nota-de-cierre) | Ampliar el control de huecos del cierre a los marcadores inline de la NOTA DE CIERRE | No implementada | Media | No bloqueante | 010_prototype |
| [T-050](#t-050---anclar-o-corregir-los-numeros-de-linea-de-las-verificaciones-de-d-086d-087-que-ya-no-reproducen) | Anclar o corregir los numeros de linea de las verificaciones de D-086/D-087 que ya no reproducen | No implementada | Baja | No bloqueante | 010_prototype |
| [T-054](#t-054---anclar-o-fechar-la-orden-de-historial-de-s-022md-que-el-commit-de-esta-sesion-deja-de-reproducir) | Anclar o fechar la orden de historial de S-022.md que el commit de esta sesion deja de reproducir | No implementada | Baja | No bloqueante | 010_prototype |
| [T-055](#t-055---corregir-por-nota-el-grep--tail--1-de-d-096-que-ya-apunta-a-su-propia-prosa) | Corregir por nota el `grep \| tail -1` de D-096 que ya apunta a su propia prosa | No implementada | Baja | No bloqueante | 010_prototype |
| [T-058](#t-058---anclar-o-fechar-la-cuarta-orden-de-evidencia-de-d-102-que-el-commit-de-esta-sesion-deja-de-reproducir) | Anclar o fechar la cuarta orden de evidencia de D-102 que el commit de esta sesion deja de reproducir | No implementada | Baja | No bloqueante | 010_prototype |
| [T-060](#t-060---derivar-del-registro-con-orden-publicada-las-listas-y-cifras-de-hallazgos-y-sesiones-que-escribe-el-cierre) | Derivar del registro, con orden publicada, las listas y cifras de hallazgos y sesiones que escribe el cierre | No implementada | Baja | No bloqueante | 010_prototype |
```

(salida del comando: `sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"`)

## 3. Decisiones tomadas

- **`D-103` — `F-036` se acepta: la lista de pendientes de `S-025` se completa por nota, y derivarla
  se aplaza a `T-060`.** El hallazgo trajo dos recomendaciones. Se acepta la nota fechada (hecha ya,
  `T-059`) y se aplaza la derivacion automatica (`T-060`), porque toca `protocol-close` (andamiaje
  agnostico que se promueve al esqueleto) y la jornada estaba dedicada al Paso 5 de `010_prototype`.
  **Alternativas descartadas:** reescribir la seccion 0 de `S-025` (convertiria «dijo cuatro» en
  «siempre dijo seis», igual que ya se descarto en `D-100`/`D-101`); rechazar el hallazgo porque el
  dato ya estaba en `findings.md` y en la seccion 2 del mismo informe (la seccion 0 es la que se lee
  para saber que sigue vivo, y el informe se contradecia a si mismo); implementar `T-060` ya (habria
  ampliado la jornada fuera de lo pedido por el usuario).
- **`D-104` — el registro de la sesion 004 se transcribe al dictado, y su comentario espontaneo se
  declara no literal.** Se sigue el mismo patron de `D-097`/`D-099`/`D-102`. El unico comentario
  espontaneo llego en estilo indirecto («menciono que...»), y se registra declarado **transmitido**
  en vez de entrecomillado. **Alternativas descartadas:** entrecomillarlo (pondria en boca del
  participante palabras que no se sabe si dijo); omitirlo (la plantilla pide registrar todo
  comentario espontaneo, y su contenido coincide con la peticion literal de §7).

## 4. Supuestos vigentes y riesgos

`A-009` sigue `Confirmado` desde `S-023` (`D-098`). Esta sesion no encontro comentarios que anotar
segun su limite: el comentario de §6 trata de la organizacion de la pagina, no de la discrepancia que
`A-009` vigila.

Supuestos `Abierto` en `_persistence/assumptions.md` a este commit:

```
$ grep -B6 "^| Estado | Abierto |" _persistence/assumptions.md | grep -E "^### A-"
### A-XXX - Titulo
### A-004 - El patrocinador revisa cada salida de la IA antes de que entre al registro
### A-005 - Hay acceso a personas que conocen el proceso real, ademas del patrocinador
```

La primera linea (`### A-XXX - Titulo`) es un falso positivo: sale de la plantilla comentada dentro
del propio archivo, no de una entrada real. Los dos supuestos vigentes son `A-004` y `A-005`;
ninguno de los dos se toco en esta sesion, que fue de producto sobre `010_prototype`.

## 5. Siguiente tarea propuesta

**Correr la quinta y ultima sesion del Paso 5 de `010_prototype`**
(`010_prototype/015_session_005.md`), con la misma mecanica que las cuatro anteriores (`D-097`,
`D-099`, `D-102`, `D-104`). Importancia `Alta`, Urgencia `Bloqueante` (es la ultima sesion que
falta para poder comprobar la condicion de salida del Paso 5).

## 6. Que pedimos auditar

- **La linea entre "correccion" y "aplazamiento" en `D-103`.** `F-036` traia dos recomendaciones y
  solo una se ejecuto en esta sesion. Nos gustaria que se revisara si marcar el hallazgo entero como
  `Aceptado — pendiente` (en vez de dividirlo en dos filas, una por recomendacion) es la lectura
  correcta de la convencion de `findings.md`, o si un hallazgo con recomendaciones parcialmente
  implementadas necesita otra forma de registrarlo.
- **La seccion 4 de este informe** usa un `grep -B6` que no imprime el titulo completo de las
  entradas de `assumptions.md` porque el patron de esa seccion no sigue el formato `### A-XXX -
  Titulo` de los demas archivos del registro; se dejo como salida literal en vez de reconstruirla a
  mano. Conviene revisar si el patron generico de "supuestos abiertos" necesita ajustarse al formato
  real de `assumptions.md`.
- **`T-058`** (anclar o fechar la cuarta orden de evidencia de `D-102`) sigue sin tocarse por tercera
  sesion consecutiva; no es descuido, es que ninguna de las tres sesiones desde que nacio ha sido una
  sesion de mantenimiento del registro. Si el patron se repite, podria valer la pena revisar si
  `session-closer` deberia priorizar estas tareas de anclaje cuando se acumulan.

## 7. Evidencia del Paso 2d

Orden anclada al commit de esta sesion (forma prescrita, con el propio informe excluido):

```
$ git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-026.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

**Lista completa devuelta ANTES del commit** (sobre el area de staging, con
`git diff --cached -U0 -- _persistence _audit`), numerada, con recuento y reejecucion de cada linea:

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cat -n
     1	+  $ git rev-parse --short HEAD
     2	+  $ grep -n "<" 010_prototype/015_session_004.md
     3	+  $ grep -n "Guia de llenado" 010_prototype/015_session_004.md
     4	+  $ git log --diff-filter=A --format=%ad -- 010_prototype/015_session_004.md
     5	+  $ git log --oneline --name-only b374d01 -- 010_prototype/
     6	+  $ grep -c "NOTA 2026-09-19 (\`F-036\`, \`D-103\`)" _audit/S-025.md
     7	+  $ sed -n "15p" _audit/S-025.md | cut -c1-60
```

Recuento de LINEAS devueltas (la unica cifra de este paso):

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
7
```

Repetidas (`uniq -d`), salida entera:

```
$ git diff --cached -U0 -- _persistence _audit \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
(vacio: ninguna orden se repite)
```

Procedencia por archivo (derivada del diff):

```
$ git diff -U0 -- _persistence _audit | awk '/^\+\+\+ /{f=$2} /^\+[[:space:]]*\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
b/_persistence/decisions.md :: +  $ git rev-parse --short HEAD
b/_persistence/decisions.md :: +  $ grep -n "<" 010_prototype/015_session_004.md
b/_persistence/decisions.md :: +  $ grep -n "Guia de llenado" 010_prototype/015_session_004.md
b/_persistence/decisions.md :: +  $ git log --diff-filter=A --format=%ad -- 010_prototype/015_session_004.md
b/_persistence/decisions.md :: +  $ git log --oneline --name-only b374d01 -- 010_prototype/
b/_persistence/tasks.md :: +  $ grep -c "NOTA 2026-09-19 (\`F-036\`, \`D-103\`)" _audit/S-025.md
b/_persistence/tasks.md :: +  $ sed -n "15p" _audit/S-025.md | cut -c1-60
```

Reejecucion de cada linea, numerada segun `cat -n` de arriba:

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git rev-parse --short HEAD` | Si | `b374d01`; pregunta por el arbol de trabajo antes de este commit, no anclable a un commit que aun no existia; el valor coincide con el publicado en `D-104` |
| 2 | `grep -n "<" 010_prototype/015_session_004.md` | Si | mismas dos lineas (la casilla y la orden que la describe) |
| 3 | `grep -n "Guia de llenado" 010_prototype/015_session_004.md` | Si | misma linea |
| 4 | `git log --diff-filter=A --format=%ad -- 010_prototype/015_session_004.md` | Si | vacio, porque el archivo aun no estaba commiteado cuando se corrio |
| 5 | `git log --oneline --name-only b374d01 -- 010_prototype/` | Si | falso positivo conocido del patron (el hash va detras de flags, no lo reconoce como ancla): la orden **si** esta anclada a `b374d01`, y reproduce exactamente lo publicado |
| 6 | `grep -c "NOTA 2026-09-19 (\`F-036\`, \`D-103\`)" _audit/S-025.md` | Si | `1` |
| 7 | `sed -n "15p" _audit/S-025.md \| cut -c1-60` | Si | misma linea publicada en `D-104` |

Las 7 lineas reproducen. Ninguna vive dentro de un bloque «Criterio de cierre» de `decisions.md` ni
de `tasks.md` salvo las lineas 6 y 7 (dentro del criterio de cierre de `T-059`): esas dos las ancla el
Paso 7c-bis. Las lineas 1 a 5 estan en la seccion «Evidencia» de `D-104`, que no lleva el rotulo
«Criterio de cierre», por lo que quedan fuera del alcance mecanico del 7c-bis; como las 5 reproducen
hoy sin fallo, no se abre tarea nueva (tabla del Paso 2d, fila 2: «sigue, pero anclala» es una
recomendacion, no una condicion de bloqueo).

**NOTA DE CIERRE**, escrita por el Paso 7c/7c-bis despues del commit `c424ae8`:

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal y anclado a `c424ae8`, cuenta lineas `$ ... <hash>` heredadas incluidas; no tiene
que salir vacio):

```
$ for f in $(git ls-tree -r --name-only c424ae8 _persistence _audit | grep -v '_audit/S-026.md'); do
  n=$(git show c424ae8:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/R-021.md: 2
_audit/R-027.md: 1
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_audit/S-018.md: 2
_audit/S-025.md: 1
_persistence/decisions.md: 19
_persistence/tasks.md: 1
```

CONTROL (acotado a lo que este commit anadio, y a la ranura vacia literal `git show <hash>:` al
principio de la orden; tiene que ser vacio para poder anclar):

```
$ for f in $(git diff --name-only c424ae8^ c424ae8 -- _persistence _audit ":(exclude)_audit/S-026.md"); do
  n=$(git diff -U0 c424ae8^ c424ae8 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
(vacio)
```

El CONTROL salio vacio: no queda ninguna orden nacida en esta sesion con la ranura de ancla sin
rellenar. Las dos ordenes del criterio de cierre de `T-059` ya se ancladaron a `c424ae8` antes de
este commit de anclaje, y por eso el CONTROL no las lista (no llevan la forma literal `git show
<hash>:` con el marcador sin sustituir).

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | outside > /tmp/prosa_antes
  outside < "$f" > /tmp/prosa_despues
  echo "== $f =="
  diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea de prosa desaparecio: solo las dos lineas `== … ==`.

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /\*\*Ordenes:\*\*/{m="O"} /\*\*Salidas:\*\*/{m="S"} /^[[:space:]]*```/{f=!f; if(!f) m=""; next} c&&f&&m!="O"&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | salidas > /tmp/salida_publicada
  salidas < "$f" > /tmp/salida_anclada
  echo "== $f =="
  diff /tmp/salida_publicada /tmp/salida_anclada | grep -E '^[<>]'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Todas las salidas ancladas son las publicadas: solo las dos lineas `== … ==`. La linea `📌` bajo el
criterio de cierre de `T-059` dice que reproducen, y esta comprobacion lo sostiene.

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d c424ae8^ c424ae8); do
  n=$(git show c424ae8:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
(vacio)
$ git diff --name-only --diff-filter=d c424ae8^ c424ae8 | wc -l
10
$ for f in $(git diff --name-only --diff-filter=d c424ae8^ c424ae8); do git show c424ae8:"$f"; done \
  | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El barrido anclado sale vacio, igual que el previo al commit. El contraste 1 anclado da `10`,
identico al numero de filas de la tabla de la seccion 8 (medida sobre staging): **ningun archivo
quedo fuera de la tabla**, porque nada se anadio entre el staging previo y el commit final. El
contraste 2 anclado da `0`, igual que el de staging.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-026.md
_audit/S-026.md:147: del propio archivo, no de una entrada real. Los dos supuestos vigentes son `A-004` y `A-005`;
_audit/S-026.md:185: `git diff --cached -U0 -- _persistence _audit`), numerada, con recuento y reejecucion de cada linea:
_audit/S-026.md:263: Contraste 1 (filas que deberia tener la tabla):
_audit/S-026.md:270: Contraste 2 (total, sin pasar por la tabla):
_audit/S-026.md:278: Los dos contrastes cuadran: 10 archivos, 0 caracteres de control en total. La tabla esta vacia porque
```

Lectura linea por linea: `147` cita codigos (`A-004`/`A-005`), no cifras de una salida — correcto.
`185` es texto de la orden misma (`-U0`), no una cifra derivada — correcto. `263` y `270` son
etiquetas de seccion («Contraste 1», «Contraste 2»), no afirman un resultado — correcto. `278` cita
`10` y `0`: el `0` sale del bloque inmediatamente anterior (Contraste 2), y el `10` del bloque
Contraste 1, tres lineas antes; los dos coinciden con las salidas reales publicadas. Ninguna linea
exige correccion. (Esta salida es la del archivo ya con el commit auditado rellenado; la primera
ejecucion, antes de conocer el hash, dio el mismo resultado salvo el numero de linea de la cabecera.)

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 c424ae8^ c424ae8 -- _persistence _audit ":(exclude)_audit/S-026.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
7
```

Identico al recuento previo al commit (7 lineas, en `_persistence/decisions.md` y
`_persistence/tasks.md`). De esas 7, solo 2 vivian dentro de un bloque «Criterio de cierre» de
`tasks.md` (`T-059`) y ya quedaron ancladas a `c424ae8` arriba en esta misma pasada. Las 5 restantes
viven en la seccion «Evidencia» de `D-104` (sin rotulo «Criterio de cierre») y quedan fuera del
alcance mecanico del 7c-bis; las 7 reproducen hoy, verificado en la seccion 7 mas arriba.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
(vacio: ningun archivo staged contiene caracteres de control 0x01-0x08, 0x0b, 0x0c, 0x0e-0x1f)
```

Contraste 1 (filas que deberia tener la tabla):

```
$ git diff --cached --name-only --diff-filter=d | wc -l
10
```

Contraste 2 (total, sin pasar por la tabla):

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done \
  | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

Los dos contrastes cuadran: 10 archivos, 0 caracteres de control en total. La tabla esta vacia porque
no hay ninguna fila que publicar.

⚠️ Esta seccion mide el area de staging previa al commit (antes de que se anadieran `progress.md`,
este informe y la fila de `_audit/index.md`). La SEGUNDA PASADA anclada, que cubre esa brecha, va en
la NOTA DE CIERRE de la seccion 7.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do
  diff -rq --strip-trailing-cr "$ESQ/$d" "$d"
done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
(vacio)
```

Sin salida: el esqueleto de arranque esta al dia con este repositorio en las seis areas agnosticas;
no hay promociones pendientes.
