# Auditoria R-022 — sobre S-020

| Campo | Valor |
|---|---|
| Auditoria | R-022 |
| Informe auditado | `_audit/S-020.md` |
| Commit auditado | 7b0c48f |
| Fecha | 2026-09-18 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

**Estado del arbol al arrancar.** El arbol traia un `_audit/R-022.md` sin commitear, de una pasada de
auditoria anterior que no llego al Paso 8 (no existe commit suyo, y `findings.md`/`index.md` no
estaban tocados). No se uso como evidencia: esta auditoria reejecuto todos los controles desde cero y
sobrescribio el archivo con su propia salida.

```
$ git status --porcelain
?? _audit/R-022.md
$ git log --oneline -3
30276a9 S-020: ancla el informe y los criterios de cierre al hash 7b0c48f
7b0c48f S-020: F-028/F-029 de R-021 evaluados (D-080/D-081), promocion al esqueleto (D-082), y leccion L-022
00f09e1 auditoria R-021 sobre S-019 (d80a965)
```

**Que commit se audita.** El derivado no es el de la sesion: el cierre uso el Paso 7c y el informe
lleva el literal en su cabecera.

```
$ git log -1 --format=%h -- _audit/S-020.md
30276a9
$ git show 30276a9:_audit/S-020.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `7b0c48f` |
$ git show --stat --name-only --format= 7b0c48f | grep -c .
7
$ git show --stat --name-only --format= 7b0c48f
_audit/S-020.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
$ git show --stat --name-only --format= 30276a9
_audit/S-020.md
_persistence/decisions.md
```

Se audita `7b0c48f` (siete archivos, el trabajo de la sesion, y cuadra con la lista que declara la
seccion 1 del informe); `30276a9` es el commit de anclaje (dos archivos, sin trabajo) y su contenido
se lee como parte del informe anclado.

**(a) Indice contra detalle en `_persistence/` (sobre `7b0c48f`), los siete archivos:**

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show 7b0c48f:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show 7b0c48f:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Sin diferencias en ninguno de los siete.

**(b) Carpetas del arbol contra las declaradas:**

```
$ diff <(git ls-tree -d --name-only 7b0c48f | sed 's|$|/|' | sort) \
       <(git show 7b0c48f:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
9a10
> temporal/
-- exit 1 --
```

Unica diferencia: `temporal/`, que `project.md` declara excluida en `.gitignore`. Diferencia con
motivo escrito — no es hallazgo.

**(c) Fuga de datos propios en las seis areas agnosticas:**

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" 7b0c48f -- .claude CLAUDE.md _phases _methodology _templates _workflow
-- exit 1 --
```

Cero lineas.

**(d) Fuga por codigos instanciados en `_phases` y `_workflow`:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 7b0c48f -- _phases _workflow | grep -vE ':PI-[0-9]+$'
-- exit 1 --
```

Cero lineas.

**El informe contra el diff.** Hunks por archivo, reejecutados sobre el commit:

```
$ for f in _audit/findings.md _audit/index.md _persistence/decisions.md _persistence/lessons.md _persistence/progress.md _persistence/tasks.md; do printf '%s: ' "$f"; git diff -U0 7b0c48f^ 7b0c48f -- "$f" | grep -cE '^@@'; done
_audit/findings.md: 3
_audit/index.md: 1
_persistence/decisions.md: 2
_persistence/lessons.md: 2
_persistence/progress.md: 6
_persistence/tasks.md: 2
```

Coinciden uno a uno con lo que publica la seccion 1 (3/1/2/2/6/2). Las entradas que nacen:

```
$ git diff -U0 7b0c48f^ 7b0c48f -- _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md | grep -E '^\+### '
+### D-080 - F-028 se acepta: la nota de anclaje declara donde vive, y el autorreferente queda prohibido
+### D-081 - F-029 se acepta: la fila de firma se instancia a D-074 y el control de huecos ve los codigos genericos
+### D-082 - Promocion al esqueleto de arranque de los seis candidatos del desfase
+### L-022 - El estado de una tarea no dice si su mecanismo funciona: eso lo dicen los supuestos que la respaldan
+### T-047 - Corregir por nota el commit que contiene la nota de anclaje de S-019 y prohibir el autorreferente
+### T-048 - Instanciar a D-074 la fila de firma del acta de 005_discovery y ampliar el control de huecos
```

Son exactamente las seis que el informe declara. En `findings.md`, `F-028` y `F-029` pasan de
`Abierto` a `Aceptado — pendiente` en **fila y ficha** (`Estado` y `Registrado en`), sin tocar
ninguna otra entrada; en `_audit/index.md` entra una sola fila, la de `S-020`, con columnas
`Pendiente`. Ninguna entrada preexistente se reescribe.

Ademas, la seccion 0 del informe **no** usa `Implementado` en su columna `Veredicto`: los cuatro
hallazgos que trata llevan `Aceptado — pendiente`, que es lo que `manager` puede escribir.

**Cifras que el informe publica, reejecutadas:**

```
$ git show 7b0c48f:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "No implementada"
10
$ git show 7b0c48f:_persistence/assumptions.md | grep -E '^\| \[A-' | grep -c Abierto
1
$ git diff -U0 7b0c48f^ 7b0c48f -- _persistence _audit ":(exclude)_audit/S-020.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
14
```

Las tres coinciden con el informe: diez tareas `No implementada`, un supuesto `Abierto` (`A-008`),
catorce ordenes en la ORDEN DEL PASO 2d ANCLADA.

**Criterios de cierre anclados por el Paso 7c-bis, leidos en el commit de anclaje:**

```
$ git diff 30276a9^ 30276a9 -- _persistence/decisions.md
-     git show <hash>:_persistence/decisions.md | grep -c 'Commit del esqueleto `5b3fb6e`'
-     git show <hash>:_persistence/decisions.md | grep -c 'desde el origen `00f09e1`'
+     git show 7b0c48f:_persistence/decisions.md | grep -c 'Commit del esqueleto `5b3fb6e`'
+     git show 7b0c48f:_persistence/decisions.md | grep -c 'desde el origen `00f09e1`'
       [...]
+     **Salida anclada (Paso 7c-bis, commit `7b0c48f`):**
+     1
+     2
+📌 **Ancladas por el Paso 7c-bis al commit `7b0c48f`. La primera orden reproduce; la segunda no.**
```

El anclaje toco solo las ordenes y anadio la salida anclada debajo, **sin borrar la publicada y sin
escribir «reproduce»** donde no reproduce. Es el comportamiento que `protocol-close` prescribe. Y la
discrepancia es real y tiene la causa que la nota dice:

```
$ git show 7b0c48f:_persistence/decisions.md | grep -c 'Commit del esqueleto `5b3fb6e`'
1
$ git show 7b0c48f:_persistence/decisions.md | grep -c 'desde el origen `00f09e1`'
2
```

**La promocion externa de `D-082`, verificada contra el repositorio del esqueleto** (ruta declarada
en `project.md`; solo lectura):

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ git -C "$ESQ" log --oneline -1
5b3fb6e promocion desde el proyecto YouOtto (origen 00f09e1)
$ git -C "$ESQ" status -sb
## main...origin/main
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
(sin salida)
```

Los dos hashes que `D-082` publica existen, el push del esqueleto ocurrio, y el desfase esta cerrado
en las seis areas.

**Fecha y anclas.**

```
$ git log -1 --format='%h %ad' --date=short 7b0c48f
7b0c48f 2026-09-18
$ date +%F
2026-09-18
$ git show 7b0c48f:_persistence/progress.md | grep -nE '^#{3} S-020'
651:### S-020 - `F-028`/`F-029` de `R-021` evaluados (`D-080`/`D-081`), promocion al esqueleto de los seis candidatos (`D-082`), y leccion `L-022` sobre el error de la puerta
```

El informe, su fila en `_audit/index.md` y la entrada de `progress.md` declaran `2026-09-18`, igual
que el commit. El ancla del indice de `progress.md` para `S-020` resuelve al titulo de su detalle:
la familia de `F-003`/`F-021` no se repite.

**Control de huecos de plantilla sobre el informe:**

```
$ git show 30276a9:_audit/S-020.md | grep -nE '^<'
-- exit 1 --
```

Limpio a principio de linea — y ahi esta el limite que abre el unico hallazgo de esta pasada: el
hueco que quedo esta dentro de una linea.

## 2. Hallazgos

### F-030 — La NOTA DE CIERRE de S-020 deja el commit de anclaje como hueco de plantilla, sin instanciar y sin derivar

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_audit/S-020.md`, seccion 7 (NOTA DE CIERRE), linea 261 |

- **Que se observo:** el rotulo publica el commit de sesion y deja el de anclaje con el texto literal
  de la plantilla:

  ```
  $ git show 30276a9:_audit/S-020.md | grep -nE '<se completa|<pendiente|<en blanco|<rellenar'
  123:  sobre las secciones de firma, porque el control existente busca `<en blanco>` y esta fila nunca
  261:**NOTA DE CIERRE — commit de sesion `7b0c48f`, commit de anclaje `<se completa tras commitear el anclaje>`.**
  ```

  La linea 123 es una cita legitima del marcador dentro de la prosa de `D-081`; el hueco real es la
  261. No hay un commit posterior que lo complete, y no se publico la orden que lo derivaria:

  ```
  $ git log --format='%h %ad %s' --date=short -- _audit/S-020.md
  30276a9 2026-09-18 S-020: ancla el informe y los criterios de cierre al hash 7b0c48f
  7b0c48f 2026-09-18 S-020: F-028/F-029 de R-021 evaluados (D-080/D-081), promocion al esqueleto (D-082), y leccion L-022
  $ grep -c 'rev-parse' _audit/S-020.md
  0
  $ sed -n '261p' _audit/S-020.md
  **NOTA DE CIERRE — commit de sesion `7b0c48f`, commit de anclaje `<se completa tras commitear el anclaje>`.**
  ```

  El hash que la nota deberia nombrar es derivable y vale `30276a9`:

  ```
  $ git rev-parse --short 30276a9
  30276a9
  $ git rev-parse --short 30276a9^
  7b0c48f
  ```

  `protocol-close` lo exige literal:

  ```
  $ sed -n '1316,1322p' .claude/skills/protocol-close/SKILL.md
  <y los DOS hashes que la nota nombra —el commit de sesion y el commit de anclaje— se DERIVAN con una
  orden y se pega su salida, nunca se teclean. Son commits distintos y la nota existe para distinguirlos:
  tecleados, salen iguales, y entonces la nota manda a buscar el anclaje a un commit que no lo contiene.
  El de sesion es `HEAD` cuando corre el Paso 7b; el de anclaje **todavia no existe** mientras se redacta
  la nota, asi que se lee DESPUES de commitear el anclaje y antes de publicarla:
    git rev-parse --short HEAD        # corrido tras el commit de anclaje: ese es el de ANCLAJE
    git rev-parse --short HEAD^       # su padre: ese es el de SESION
  ```

  Y ningun control del cierre lo ve: el de huecos de plantilla ancla en `^<` y este hueco esta
  inline; el `7c-ter` solo comprueba que los seis rotulos de la NOTA DE CIERRE existan, no que su
  contenido este instanciado.

- **Por que importa:** la NOTA DE CIERRE existe precisamente para distinguir el commit de sesion del
  de anclaje. Sin el segundo, el registro no dice donde quedaron escritas las salidas ancladas ni los
  criterios del Paso 7c-bis, y quien lo busque tiene que reconstruirlo con `git log`. Es la misma
  familia que `F-013`, `F-026` y `F-028`, que ya costaron tres correcciones por nota: alli el hash
  estaba mal atribuido o llegaba en un tercer commit no declarado; aqui directamente no esta.
  `Media` porque el registro queda incompleto en el punto que el mecanismo existe para cubrir, aunque
  no afirma nada falso; `No bloqueante` porque la informacion es recuperable con una orden y nada de
  lo que venga despues hereda un dato erroneo.

- **Que lo corregiria:** una nota fechada en `_audit/S-020.md` que declare `30276a9` como commit de
  anclaje, con la salida de `git rev-parse --short HEAD`/`HEAD^` corrida tras el anclaje, **sin
  reescribir la linea 261**; y —la mitad que evita la repeticion— un control en `protocol-close` que,
  antes de commitear el anclaje, busque marcadores entre angulos **en cualquier posicion** de la NOTA
  DE CIERRE, no solo a principio de linea. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | `Aceptado — pendiente` (sin cambio) | `T-030` sigue `No implementada`; la correccion no aparece en el diff de `7b0c48f` |
| F-017 | `Aceptado — pendiente` (sin cambio) | `T-031` sigue `No implementada`; la correccion no aparece en el diff de `7b0c48f` |
| F-028 | `Aceptado — pendiente` (sin cambio) | evaluado y registrado (`T-047`, `D-080`), pero la nota correctiva no esta en el diff |
| F-029 | `Aceptado — pendiente` (sin cambio) | evaluado y registrado (`T-048`, `D-081`), pero la fila del acta sigue sin instanciar |

Salidas crudas de las dos ultimas, sobre el commit auditado:

```
$ git show 7b0c48f:_audit/S-019.md | grep -n 'este mismo, el que anadio'
289:El primero es el commit de anclaje (este mismo, el que anadio esta nota); el segundo es su padre, el
$ git show 7b0c48f:_audit/005_discovery/005_phase_exit_record_001.md | sed -n '/^| Donde queda/p'
| Donde queda registrada | `_persistence/decisions.md`, con su `D-XXX` |
```

Ninguno de los cuatro pasa a `Implementado`: el aplazamiento esta registrado con su tarea y su
decision, que es lo que el protocolo permite, pero un hallazgo solo se cierra con la correccion en el
diff.

## 4. Lo que NO se pudo comprobar

Ninguno. Los cuatro controles mecanicos corrieron y devolvieron resultado, y la evidencia externa de
`D-082` se pudo verificar porque el repositorio del esqueleto esta clonado en la ruta que declara
`project.md`.

## 5. Recomendaciones sin hallazgo

- **La discrepancia de la segunda orden de `D-082` no tiene puntero accionable.** El cierre la dejo
  declarada en la propia entrada, que es lo que `protocol-close` prescribe, y por eso no es un
  defecto. Pero «Sin resolver» muere con la sesion: `grep -n 'D-082' _persistence/tasks.md` no
  devuelve nada, y el arranque lee `tasks.md` y `findings.md`, no `decisions.md`. Las ordenes que
  dejaron de reproducir en sesiones anteriores si abrieron tarea (`T-035`, `T-037`, `T-042`,
  `T-046`); conviene decidir si esa practica se aplica tambien cuando la discrepancia la detecta el
  CONTROL DE SALIDA REPRODUCIDA del 7c-bis.
- **Sobre el punto 1 de la seccion 6 del informe** (si basta con registrar el error de la puerta de
  promocion en `D-082` y `L-022`): la evidencia sostiene que si. `D-082` nombra la premisa falsa, la
  contrasta contra `A-006`/`A-007`, explica que `T-032` sigue abierta por una casilla formal, y deja
  dicho que el mensaje del commit del esqueleto arrastra la afirmacion y no se reescribe; `L-022`
  generaliza el patron. Lo que quedaria mas solido es que la ficha de `T-032` recoja el puntero a
  `D-082`, para que quien la abra no repita la lectura que causo el error.
- **Sobre el punto 3 de la seccion 6** (si la promocion es auditable desde frio): lo es, pero hoy
  depende de que el esqueleto este clonado en esta maquina. Conviene que `D-082` —o la practica de
  `protocol-promote`— publique tambien el remoto del esqueleto junto a su hash, para que un auditor
  sin el clone pueda llegar a `5b3fb6e` sin salir del registro.
- **Una pasada de auditoria anterior dejo un `_audit/R-022.md` escrito y sin commitear.** No afecta
  al commit auditado y no abre hallazgo, pero conviene saber que un `R-XXX.md` sin commit ni fila en
  el tablero es invisible para el arranque de la sesion siguiente: si esta pasada no hubiera corrido,
  `S-020` habria quedado `Pendiente` con una auditoria escrita que nadie veria.
