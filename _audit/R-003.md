# Auditoria R-003 — sobre S-003

| Campo | Valor |
|---|---|
| Auditoria | R-003 |
| Informe auditado | `_audit/S-003.md` |
| Commit auditado | 101db28 |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-003.md
2a72df8
$ git show 2a72df8:_audit/S-003.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `101db28` |
$ git show --stat --format= 2a72df8
 _audit/S-003.md           | 179 ++++++++++++++++++++++++++++++++++++++++++++--
 _persistence/decisions.md |  14 ++--
 _persistence/tasks.md     |  12 ++--
 3 files changed, 190 insertions(+), 15 deletions(-)
$ git show --stat --format='%h %ad %s' --date=iso 101db28
101db28 2026-09-16 09:50:52 -0500 S-003: firmas de los Gates (D-011), hallazgo F-004 de R-002 atendido (D-010, T-006) y cosecha de lecciones de 000_preproject (D-012)
 .claude/skills/protocol-audit/SKILL.md |   8 +-
 .claude/skills/protocol-close/SKILL.md |  12 +-
 .claude/skills/protocol-start/SKILL.md |   4 +-
 _audit/S-003.md                        | 274 +++++++++++++++++++++++++++++++++
 _audit/findings.md                     |  10 +-
 _audit/index.md                        |   1 +
 _persistence/decisions.md              | 167 ++++++++++++++++++++
 _persistence/lessons.md                |  45 +++++-
 _persistence/progress.md               |  62 +++++---
 _persistence/tasks.md                  |  34 +++-
 _persistence/techdebt.md               |  25 +--
 11 files changed, 588 insertions(+), 54 deletions(-)
```

El literal de la cabecera (`101db28`) difiere del derivado (`2a72df8`, commit de anclaje). Se audita
`101db28`: sus 11 archivos cuadran con la lista anclada de la seccion 1 del informe. Fecha del commit
(`2026-09-16`) igual a la del informe.

### 1.2 Informe contra el diff (`git show 101db28`)

Leido el diff completo de los 11 archivos. Cada afirmacion de las secciones 0, 1, 3 y 5 del informe
tiene respaldo:

- Las tres skills: solo cambia el patron de cerca, doce lineas (6 + 4 + 2), nada mas.
- `decisions.md`: solo lineas `+` en indice y `D-010`, `D-011`, `D-012`; ninguna decision previa editada.
- `tasks.md`: `T-002` a `Implementada` con la frase «Cumplido por `D-011`»; nace `T-006`.
- `lessons.md`: `L-001`..`L-004` reclasificadas; nacen `L-005` y `L-006`.
- `findings.md`: `F-004` a `Aceptado — pendiente`, `Registrado en: T-006`, fila y ficha a la vez.
- `index.md`: nace la fila `S-003` pendiente. `techdebt.md`: `DT-001` ampliada a seis archivos.

```
$ git grep -nF '/^```/' 101db28 -- .claude | wc -l
0
$ git grep -cF '/^[[:space:]]*```/' 101db28 -- .claude
101db28:.claude/skills/protocol-audit/SKILL.md:4
101db28:.claude/skills/protocol-close/SKILL.md:6
101db28:.claude/skills/protocol-start/SKILL.md:2
$ git grep -nF '^```' 101db28 -- .claude
(sin salida)
```

### 1.3 Lo que la seccion 6 pidio mirar

**a) `T-002` a `Implementada`.** El criterio de `T-002` pide una `D-XXX` que nombre quien firma cada
Gate. `D-011` lo nombra, y su criterio anclado reproduce:

```
$ git show 101db28:_persistence/decisions.md | awk '/^### D-011/{f=1} /^### D-012/{f=0} f' | grep -v '\$ ' | grep -cE 'gate1_auditor. en el Gate 1, .gate2_auditor. en el Gate 2|es del \*\*usuario\*\*'
2
```

Satisfecho en sustancia, no solo nominalmente. Ver recomendacion 5.1 sobre la forma.

**b) Alcance de `D-012` (`L-005`, `L-006` sin puerta).** La puerta de `protocol-harvest` protege la
escritura fuera del repositorio; `Ya cubierta` y `Solo proyecto` «no sube»:

```
$ git show 101db28:.claude/skills/protocol-harvest/SKILL.md | grep -nE "puerta|aprueb|Solo proyecto|Ya cubierta|Sin evaluar" | head -40
3:description: ... La escritura fuera lleva puerta: nada se toca hasta que el usuario aprueba las candidatas. ...
12:> 🚨 **La regla que gobierna todo el protocolo: escribir fuera tiene una puerta, y la puerta es el
...
128:| `Global candidata` | pasa **los cuatro** filtros | va a la puerta del Paso 5 |
129:| `Ya cubierta por LG-NN` | el archivo global ya lo dice | **no sube**, y se anota cual la cubre |
130:| `Solo proyecto` | no sobrevive al cambio de lenguaje, libreria o dominio | no sube |
```

(salida recortada con `...` en las lineas de prosa larga; ordenes y numeros de linea literales)

Clasificar sin puerta lo que no sube no contradice el protocolo, y `D-012` lo declara. No es hallazgo.
Criterio de `D-012` y la verificacion externa reproducen:

```
$ git show 101db28:_persistence/lessons.md | grep -E '^\| \[L-' | grep '000_preproject' | grep -c 'Sin evaluar'
0
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show 9e883a9:global_lessons.md | grep -nE '^\| \*\*LG-105\*\*|Versión: 4' | cut -c1-80
26:> **Versión: 4 · 2026-09-16** · 105 lecciones · 10 bloques
336:| **LG-105** | **Un paso de arranque que no deja rastro se salta sin que nad
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" ls-remote origin refs/heads/main | cut -c1-7
9e883a9
```

**c) Anclaje de los criterios de `D-010`, `D-011`, `D-012`, `T-006`.** Los cuatro estan anclados en
`2a72df8`. Los de `D-010`, `D-011` y `D-012` reproducen lo publicado. **El de `T-006` no**: ver F-005.

### 1.4 Control 4a — indice ↔ detalle de `_persistence/` a `101db28`

```
$ H=101db28; for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

### 1.5 Control 4b — carpetas

```
$ diff <(git ls-tree -d --name-only 101db28 | sed 's|$|/|' | sort) \
     <(git show 101db28:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
$ git show 101db28:_persistence/decisions.md | grep -nE "005_discovery/|temporal/" | head -5
164:  arranca, no todas de golpe. Hoy se declara solo `005_discovery/`, la siguiente etapa, por
168:  fila pendiente se lee, seis se acaban ignorando. `005_discovery/` se declara ya porque su etapa
169:  esta escrita en `_phases/` y su contenido esta enumerado en `_templates/005_discovery/`, que es la
171:- **Alternativas descartadas:** declarar las seis carpetas desde ya (`005_discovery/` a
175:  `005_discovery`, el Paso 2c senalara la fila `005_discovery/` sin carpeta. Su razon es esta
$ git show 101db28:.gitignore | grep -n temporal
5:temporal/
```

Las dos diferencias tienen razon escrita (declaracion anticipada con su decision; exclusion en
`.gitignore`). No es hallazgo.

### 1.6 Control 4c — fuga de datos propios (seis ambitos)

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" 101db28 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.7 Control 4d — codigos instanciados en `_phases` y `_workflow`

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 101db28 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.8 `F-004`: la correccion funciona sobre dos anclajes reales

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }; for p in "e222812 4b27ae4" "101db28 2a72df8"; do set -- $p; for f in _persistence/decisions.md _persistence/tasks.md; do git show $1:"$f" | outside > $S/a; git show $2:"$f" | outside > $S/b; echo "== $1..$2 $f =="; diff $S/a $S/b | grep '^<'; done; done; echo fin
== e222812..4b27ae4 _persistence/decisions.md ==
== e222812..4b27ae4 _persistence/tasks.md ==
== 101db28..2a72df8 _persistence/decisions.md ==
== 101db28..2a72df8 _persistence/tasks.md ==
fin
```

(`$S` = scratchpad de la sesion de auditoria.) La `outside()` tal como queda en la skill no marca
prosa borrada ni en el anclaje de `S-002` (donde la antigua marcaba 6 + 3) ni en el de `S-003`.

## 2. Hallazgos

### F-005 — El anclaje de `T-006` sustituyo una salida que no reproducia y la nota afirma que reproduce

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md`, `T-006`, «Criterio de cierre» (commit de anclaje `2a72df8`); `_audit/S-003.md`, seccion 7 |

- **Que se observo:** la salida publicada en `101db28` para la segunda orden no es la que devuelve la
  forma anclada. El anclaje reemplazo la salida y anadio una nota que dice que reproduce:

  ```
  $ git show 101db28:_persistence/tasks.md | awk '/^### T-006/,0' | sed -n '/Criterio de cierre/,$p'
  - **Criterio de cierre:** el de `D-010`.

    ```
    $ git grep -nF '/^```/' <hash> -- .claude | wc -l
    0
    $ git grep -cF '/^[[:space:]]*```/' <hash> -- .claude
    .claude/skills/protocol-audit/SKILL.md:4
    .claude/skills/protocol-close/SKILL.md:6
    .claude/skills/protocol-start/SKILL.md:2
    ```
  -----
  $ git show 2a72df8:_persistence/tasks.md | awk '/^### T-006/,0' | sed -n '/Criterio de cierre/,$p'
  - **Criterio de cierre:** el de `D-010`.

    ```
    $ git grep -nF '/^```/' 101db28 -- .claude | wc -l
    0
    $ git grep -cF '/^[[:space:]]*```/' 101db28 -- .claude
    101db28:.claude/skills/protocol-audit/SKILL.md:4
    101db28:.claude/skills/protocol-close/SKILL.md:6
    101db28:.claude/skills/protocol-start/SKILL.md:2
    ```

  📌 **Anclada por el Paso 7c-bis al commit `101db28`.** Las dos reproducen lo publicado arriba.
  ```

  Las tres lineas publicadas no llevan prefijo; las ancladas llevan `101db28:`. La salida publicada
  no pudo salir de la orden con un commit (`git grep` sobre un arbol siempre antepone el hash), asi que
  se obtuvo sobre el arbol de trabajo. `D-010` evita esto con `| cut -d: -f2-`; `T-006` no lo lleva.
  El informe no menciona la discrepancia:

  ```
  $ git show 2a72df8:_audit/S-003.md | grep -nE "no coincid|discrepan|Sin resolver|prefijo"; echo "rc=$?"
  rc=1
  ```

  La regla que se incumple, en la skill vigente a ese commit:

  ```
  $ git show 101db28:.claude/skills/protocol-close/SKILL.md | sed -n '1706,1870p'
  ...
  🚨 **Si la salida anclada NO coincide con la publicada, te detienes.** No la sustituyes y no
  la corriges: **pegas las dos** — la publicada y la anclada — y lo dices en el reporte, en «Sin
  resolver». ...
  ```

- **Por que importa:** los recuentos (4, 6, 2) si se sostienen, y por eso no es `Alta`. Pero el
  registro afirma algo comprobablemente falso («Las dos reproducen lo publicado arriba») y la salida
  publicada original se perdio del bloque sin quedar pegada junto a la anclada. Es justo el caso que la
  regla del 7c-bis existe para frenar: una discrepancia que se «arregla» sin dejar rastro, y que la
  seccion 6 del informe pidio confirmar sin que la NOTA DE CIERRE la detectara.
- **Que lo corregiria:** una nota fechada bajo el bloque de `T-006` que republique las dos salidas
  (publicada y anclada) y diga que la nota `📌` no era exacta, sin reescribir lo ya commiteado. Y,
  opcionalmente, que el 7c-bis compare byte a byte la salida anclada con la publicada antes de
  escribir «reproduce». ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-004` | Aceptado — pendiente (`T-006`, `D-010`) | **Implementado** en `101db28` | Las doce apariciones del patron pasan a `/^[[:space:]]*```/` y no queda la forma antigua (`0`, y `4`/`6`/`2`, en 1.2); la `outside()` corregida no marca prosa en el anclaje de `S-002` donde la antigua marcaba 6 + 3, ni en el de `S-003` (1.8) |

`F-001`, `F-002`, `F-003` ya estaban `Implementado`.

## 4. Lo que NO se pudo comprobar

- **Seccion 9 del informe (Paso 2f) y `DT-001`.** Comparan el arbol de trabajo contra el esqueleto en
  el momento del cierre; no se reejecuto contra el esqueleto. Consta que los seis archivos que nombran
  incluyen los tres de `.claude/` que cambia `101db28`.
- **Que el usuario aprobo en la puerta la clasificacion de `L-001`..`L-004` y eligio las opciones de
  `D-010` y `D-011`.** Ocurrio en la conversacion y no deja rastro en el repositorio. Lo comprobable
  (el commit `9e883a9` y su contenido en el remoto de lecciones) esta en 1.3.

## 5. Recomendaciones sin hallazgo

- **5.1** `T-002` se cerro con la frase «Cumplido por `D-011`» y sin bloque propio de orden y salida.
  La evidencia existe (criterio anclado de `D-011`, 1.3a), pero citar ahi la orden de `D-011` haria la
  Definicion de Terminado de la tarea legible sin saltar de archivo.
- **5.2** `_audit/S-003.md`, seccion 7: «Las cinco ordenes con `<hash>` literal (2, 3, 4, 7, 8, 9 —
  seis en total)» se contradice en la misma frase; son seis.
- **5.3** El CONTROL del Paso 7c solo reconoce `git show <hash>:`; el propio informe lo deja escrito
  como brecha (formas `git grep ... <hash>`). Merece evaluarse si ampliarlo.
