# Auditoria R-009 — sobre S-009

| Campo | Valor |
|---|---|
| Auditoria | R-009 |
| Informe auditado | `_audit/S-009.md` |
| Commit auditado | 8509c18 |
| Fecha | 2026-09-16 |
| Veredicto | Sin hallazgos |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-009.md
f42388a
$ git show f42388a:_audit/S-009.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `8509c18` |
$ git show --stat --name-only --format= 8509c18 | grep -c .
8
$ git show --name-only --format= f42388a
_audit/S-009.md
_persistence/decisions.md
```

Hash literal distinto del derivado: se audita `8509c18`; `f42388a` es el commit de anclaje. Los 8
archivos de `8509c18` coinciden con la lista anclada de la seccion 1 del informe. Fecha de los dos
commits:

```
$ git log -1 --format=%ad --date=short 8509c18
2026-09-16
$ git log -1 --format=%ad --date=short f42388a
2026-09-16
```

Coincide con la fecha del informe y con la de las notas fechadas de `T-014`/`T-015`.

### 1.2 Informe contra el diff

`git show 8509c18` revisado archivo por archivo contra la seccion 1 del informe:

- `.claude/skills/protocol-close/SKILL.md`: un bullet anadido en el Paso 4 («El estado lo decide la
  evidencia del diff, no el anclaje...»). Respaldado.
- `_persistence/decisions.md`: nace `D-027`, fila de indice y ficha, con verificacion contra `HEAD`
  (`f0c1a74`), precedentes, alternativas descartadas y Criterio de cierre. Respaldado.
- `_persistence/tasks.md`: `T-014` y `T-015` pasan a `Implementada` en fila y ficha, con nota
  fechada; nace `T-016` `Implementada`, `Origen: report_auditor`, `Sesion: S-009`. Respaldado.
- `_audit/findings.md`: `F-010` a `Aceptado — pendiente` en fila y ficha, `Registrado en | T-016,
  D-027`, «Que se hizo» rellenado. Respaldado. La seccion 0 del informe no usa `Implementado`.
- `_persistence/lessons.md`: nace `L-011`. Respaldado.
- `_persistence/progress.md`: fila y entrada `S-009`, secciones 1-3 reescritas. Respaldado.
- `_audit/index.md`: fila `S-009` en `Pendiente`. Respaldado.

No hay en el diff nada relevante que el informe omita. Recuento de hunks que el informe declara
(`-U0`):

```
$ for f in .claude/skills/protocol-close/SKILL.md _persistence/decisions.md _persistence/tasks.md _audit/findings.md _persistence/lessons.md; do echo "$f $(git show 8509c18 -U0 --format= -- $f | grep -c '^@@')"; done
.claude/skills/protocol-close/SKILL.md 1
_persistence/decisions.md 2
_persistence/tasks.md 5
_audit/findings.md 3
_persistence/lessons.md 2
```

Coincide con lo declarado (1, 2, 5, 3, 2).

### 1.3 Evidencia de D-027 reejecutada

Verificacion contra `HEAD` de entonces y precedentes:

```
$ git show f0c1a74:_persistence/tasks.md | grep -E '^\| \[T-01[45]\]' | grep -o 'No implementada'
No implementada
No implementada
$ for c in 87d10b8 081385a c07680f; do echo "== $c"; git show $c:_persistence/tasks.md | grep -E '^\| \[T-0(0[7-9]|1[0-3])\]' | awk -F'|' '{print $2,$4}' | sed 's/(#[^)]*)//'; done
== 87d10b8
 [T-007]   Implementada 
 [T-008]   Implementada 
 [T-009]   Implementada 
 [T-010]   Implementada 
 [T-011]   Implementada 
 [T-012]   Implementada 
 [T-013]   Implementada 
== 081385a
 [T-007]   Implementada 
 [T-008]   Implementada 
 [T-009]   Implementada 
 [T-010]   Implementada 
 [T-011]   Implementada 
== c07680f
 [T-007]   Implementada 
$ git show 27c03bb:_persistence/techdebt.md | grep -cE '^(\| \[DT-003\].*\| No implementada \||### DT-003 )'; git show 27c03bb:_audit/findings.md | grep -cE '^\| \[F-009\].*\| Aceptado — pendiente \|$|^\| Estado \| Aceptado — pendiente \|$'; git show 27c03bb:.claude/skills/protocol-close/SKILL.md | grep -cF 'partida en dos lineas'
2
2
1
```

Criterio de cierre de `D-027`, sobre el commit auditado:

```
$ git show 8509c18:.claude/skills/protocol-close/SKILL.md | grep -cF 'El estado lo decide la evidencia del diff'
1
$ git show 8509c18:_persistence/tasks.md | grep -E '^\| \[T-01[456]\]' | grep -c '| Implementada |'
3
$ git show 8509c18:_persistence/tasks.md | grep -A3 -E '^### T-01[456] ' | grep -c '^| Estado | Implementada |$'
3
$ git show 8509c18:_audit/findings.md | grep -cE '^\| \[F-010\].*\| Aceptado — pendiente \|$|^\| Estado \| Aceptado — pendiente \|$'
2
$ git grep -nE "YouOtto|Company_TripleS|github.com" 8509c18 -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
$ git show 8509c18:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
0
```

Las seis reproducen lo publicado en `D-027`.

### 1.4 Commit de anclaje f42388a

El diff de `f42388a` sobre `decisions.md` solo sustituye `<hash>` por `8509c18` en las seis ordenes de
`D-027` y anade la linea `📌`. Sin prosa borrada:

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do echo "== $f"; diff <(git show 8509c18:$f | outside) <(git show f42388a:$f | outside) | grep '^<'; done
== _persistence/decisions.md
== _persistence/tasks.md
```

Reejecucion de la NOTA DE CIERRE:

```
$ for f in $(git ls-tree -r --name-only 8509c18 _persistence _audit | grep -v '_audit/S-009.md'); do n=$(git show 8509c18:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_persistence/decisions.md: 8
_persistence/tasks.md: 1
$ for f in $(git diff --name-only 8509c18^ 8509c18 -- _persistence _audit ":(exclude)_audit/S-009.md"); do n=$(git diff -U0 8509c18^ 8509c18 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
_persistence/decisions.md: 5
$ git diff -U0 8509c18^ 8509c18 -- _persistence _audit ":(exclude)_audit/S-009.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
7
```

Identico a lo publicado. Ordenes con `<hash>` que quedan en `decisions.md`/`tasks.md` tras el anclaje:

```
$ git show f42388a:_persistence/decisions.md | grep -nE '^[[:space:]]*\$ .*<hash>'
391:  $ git show 45e33a4:_persistence/tasks.md | grep -nE '^[[:space:]]*\$ .*<hash>'
423:  $ git show e222812:.claude/skills/protocol-close/SKILL.md | grep -cF '^\$ .*<hash>'
$ git show f42388a:_persistence/tasks.md | grep -nE '^[[:space:]]*\$ .*<hash>'
146:  $ git show <hash>:project.md | grep -cE '<[A-Za-z]'
```

Las dos de `decisions.md` llevan `<hash>` como texto del patron, no como ancla; la de `tasks.md:146`
ya figuraba en el censo antes de esta sesion (`_persistence/tasks.md: 1`) y no la toca `8509c18`.

### 1.5 Paso 2f — desfase con el esqueleto, sobre el arbol del commit

```
$ git archive 8509c18 | tar -x -C <scratchpad>/t8509
$ cd <scratchpad>/t8509 && for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-start/SKILL.md and .claude/skills/protocol-start/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/000_preproject/020_decisions.md and _templates/000_preproject/020_decisions.md differ
```

(`ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"`, esqueleto en `4d20ce2`.) Las mismas
tres lineas que publica la seccion 9, bajo `DT-003`.

### 1.6 Controles mecanicos del Paso 4

a) Indice ↔ detalle en `_persistence/`:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 8509c18:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 8509c18:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas del arbol contra las declaradas:

```
$ diff <(git ls-tree -d --name-only 8509c18 | sed 's|$|/|' | sort) <(git show 8509c18:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con razon escrita, igual que en `R-008`: `temporal/` excluida en `.gitignore`, y
`005_discovery/` declarada por adelantado con `D-002`. No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" 8509c18 -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
$ git grep -nE "YouOtto|Company_TripleS|github.com" f42388a -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
```

d) Codigos instanciados en `_phases` y `_workflow`:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 8509c18 -- _phases _workflow | grep -vE ':PI-[0-9]+$' | wc -l
0
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' f42388a -- _phases _workflow | grep -vE ':PI-[0-9]+$' | wc -l
0
```

### 1.7 Lo que la seccion 6 del informe pide auditar

- **Cambio de estado de `T-014`/`T-015` sin reabrir `S-008`.** El cambio va con nota fechada
  (`🕐 Nota 2026-09-16`), mecanismo ya en uso en `tasks.md` antes de esta sesion, y `S-008.md` no se
  toca en `8509c18` ni en `f42388a` (no aparece en ninguno de los dos `--name-only` de 1.1):

  ```
  $ git show 27c03bb:_persistence/tasks.md | grep -c '🕐 \*\*Nota'
  2
  ```

  Es coherente con corregir sin reescribir: el pasado auditado queda intacto y el estado nuevo dice
  cuando y por que cambio. No es hallazgo.
- **Caso no cubierto por la regla del Paso 4** (trabajo en el diff pero criterio que no reproduce):
  ver seccion 5. No es un defecto del commit: los dos bullets vecinos del mismo Paso 4 («Mueve a
  `Implementada` solo lo que la evidencia respalde» y «Lo que quedo a medias sigue en `No
  implementada`», visibles en el contexto del hunk de `SKILL.md`) ya lo resuelven en la practica.
- **`DT-003` sigue creciendo.** Registrado y declarado en la propia `D-027`, `progress.md` y la
  seccion 9; no es un defecto. Ver seccion 5.

## 2. Hallazgos

Ninguno.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-010 | Aceptado — pendiente | Implementado (8509c18) | La correccion esta en el diff de `8509c18`: `T-014` y `T-015` en `Implementada` en fila y ficha, con nota fechada; la regla escrita en el Paso 4 de `protocol-close/SKILL.md`; la practica contradictoria queda descartada en `D-027`. Criterio de cierre reproducido en 1.3 |

Ningun otro hallazgo abierto: `F-001` a `F-009` ya estaban `Implementado`.

## 4. Lo que NO se pudo comprobar

Nada: todos los controles corrieron. El Paso 2f se contrasto contra el esqueleto en su estado actual
(`4d20ce2`), no contra el que habia al cerrar la sesion; si el esqueleto hubiera cambiado entre medias
se veria como diferencia, y no la hay.

## 5. Recomendaciones sin hallazgo

- El bullet nuevo del Paso 4 solo enuncia el caso positivo. Una media frase explicita para el caso
  «trabajo en el diff, criterio que no reproduce → sigue `No implementada`» cerraria la duda que la
  propia seccion 6 del informe declara, sin depender de leer juntos tres bullets.
- La orden de precedentes de `D-027` solo muestra el estado de las tareas en cada commit, no que su
  criterio estuviera aun en `<hash>` en ese momento; esa segunda mitad la aporta `R-008`. Una orden
  mas sobre esos commits haria la decision autosuficiente.
- `DT-003` lleva tres sesiones seguidas acumulando ediciones al andamiaje sin pagarse; conviene
  priorizar `protocol-promote` antes de que el desfase deje de ser de tres archivos.
- La seccion 3 del informe escribe «`S-004`/`S-006`/`S-007` (`87d10b8`, `081385a`, `c07680f`)», con
  los hashes en orden inverso a las sesiones. No induce a error (el `D-027` los etiqueta bien), pero
  una lectura rapida emparejaria mal.
