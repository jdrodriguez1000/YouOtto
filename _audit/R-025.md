# Auditoria R-025 — sobre S-023

| Campo | Valor |
|---|---|
| Auditoria | R-025 |
| Informe auditado | `_audit/S-023.md` |
| Commit auditado | 66e6413 |
| Fecha | 2026-09-18 |
| Veredicto | Con hallazgos (2) |

## 1. Que se comprobo

### 1.1 Que commit se audita

```
$ git log -1 --format=%h -- _audit/S-023.md
cbafc6f
$ git show cbafc6f:_audit/S-023.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `66e6413` |
$ git show --stat --name-only --format= 66e6413 | grep -c .
11
$ git show --stat --format= cbafc6f
 _audit/S-023.md | 211 +++++++++++++++++++++++++++++++++++++++++++++++++++++---
 1 file changed, 202 insertions(+), 9 deletions(-)
```

`cbafc6f` es el commit de anclaje (un solo archivo). El literal de la cabecera es `66e6413`, y su
`--stat` (11 archivos) coincide con la lista de la seccion 1 del informe. **Se audita `66e6413`**; el
informe se lee en la version de `cbafc6f`, que es la que lleva la cabecera anclada.

### 1.2 Informe contra el diff

```
$ git show --stat --format='%h %ad %s' --date=iso 66e6413
66e6413 2026-09-18 17:14:59 -0500 S-023: F-032/F-033 evaluados (D-095/D-096), primera sesion del Paso 5 corrida (D-097), y A-009 confirmado (D-098)

 010_prototype/010_participants.md |   2 +-
 010_prototype/015_session_001.md  | 213 +++++++++++++++++++++++++
 _audit/S-022.md                   |  24 +++
 _audit/S-023.md                   | 316 ++++++++++++++++++++++++++++++++++++++
 _audit/findings.md                |  12 +-
 _audit/index.md                   |   1 +
 _persistence/assumptions.md       |  14 +-
 _persistence/decisions.md         | 214 ++++++++++++++++++++++++++
 _persistence/progress.md          | 114 +++++++++-----
 _persistence/tasks.md             | 141 +++++++++++++++++
 _persistence/techdebt.md          |  34 ++++
 11 files changed, 1041 insertions(+), 44 deletions(-)
```

Cada archivo de la seccion 1 del informe esta en el diff y ninguno del diff falta en el informe.
Contrastado a mano contra `git show 66e6413`: `D-095`..`D-098` nacen con su fila de indice; la nota
`F-033` se anade a `D-093`; `T-052`..`T-055` nacen; la nota `D-095` se anade a `T-049`; `DT-006`
nace; `A-009` pasa a `Confirmado`; `010_participants.md` §4 cambia solo la fila `001`.

Reproduccion de las verificaciones publicadas en el commit, corridas sobre `66e6413`:

```
$ H=66e6413; echo "T-052:"; git show $H:_audit/S-022.md | tail -24 | grep -c '07ca614'; git show $H:_audit/S-022.md | sed -n '258p'; git show $H:_audit/S-022.md | grep -c 'rev-parse'; echo "T-053:"; git show $H:_persistence/decisions.md | grep -c 'NOTA 2026-09-18 (`F-033`, `D-096`)'; git show $H:_persistence/decisions.md | grep -n '^  (sin salida)' | tail -1; git show $H:_persistence/decisions.md | grep -c '^e45185d 2026-09-18$'; echo "T-055:"; git show $H:_persistence/decisions.md | grep -n "(sin salida)" | tail -1; echo "D-097:"; git show $H:010_prototype/015_session_001.md | grep -n "<"; echo "D-098:"; git show $H:010_prototype/015_session_001.md | grep -n "no emitio palabras diferentes a la lectura\|Total de intervenciones\|57.61"
T-052:
6
**NOTA DE CIERRE — commit de sesion `e45185d`, commit de anclaje `<se completa tras commitear el
10
T-053:
1
5713:  (sin salida)
1
T-055:
5888:  - **Reescribir `(sin salida)` por `e45185d 2026-09-18`:** el bloque diria algo que no era cierto
D-097:
198:- [ ] **No queda ni un solo `<` en el archivo.**
202:grep -n "<" 010_prototype/015_session_001.md                 # debe no devolver nada
D-098:
126:**Total de intervenciones que dieron informacion:** `0`
145:- > Ninguno. Durante toda la tarea leyo en voz alta y «no emitio palabras diferentes a la lectura» (Juana Ramirez).
175:- Cronometro, desde que empezo hasta que dio la combinacion por buena y vio la confirmacion: 57.61 s.
```

Todas coinciden con lo publicado. La orden del Paso 2d anclada, tal como la publica la NOTA DE
CIERRE:

```
$ git diff -U0 66e6413^ 66e6413 -- _persistence _audit ":(exclude)_audit/S-023.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
23
```

Coincide. Tareas abiertas y supuestos (secciones 2 y 4 del informe):

```
$ git show 66e6413:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada" | grep -oE '^\| \[T-[0-9]+' | tr -d '|[ ' | tr '\n' ' '
T-030 T-031 T-032 T-033 T-035 T-037 T-042 T-046 T-047 T-048 T-049 T-050 T-054 T-055
```

Catorce, coincide. Los nueve supuestos: `A-001` y `A-002` `Refutado`, `A-003`..`A-009`
`Confirmado`, coincide con la seccion 4. La seccion 0 lista los siete hallazgos no `Implementado`
de `findings.md` y ninguno lleva `Implementado` en su columna `Veredicto`.

**Discrepancias encontradas:** dos, en la seccion 2 de este informe (`F-034`, `F-035`).

### 1.3 Controles mecanicos (Paso 4), sobre `66e6413`

**a) Indice ↔ detalle en `_persistence/`:**

```
$ H=66e6413; for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

**b) Carpetas del arbol contra las declaradas:**

```
$ diff <(git ls-tree -d --name-only $H | sed 's|$|/|' | sort) \
     <(git show $H:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
10a11
> temporal/
```

`temporal/` esta declarada y su motivo esta escrito en `project.md` («Fuera del repositorio,
excluida en `.gitignore`»). No es hallazgo.

**c) Fuga de datos propios en las seis areas agnosticas:**

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" $H -- .claude CLAUDE.md _phases _methodology _templates _workflow
$ echo "exit=$?"
exit=1
```

Cero lineas.

**d) Codigos instanciados en `_phases` y `_workflow`:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $H -- _phases _workflow | grep -vE ':PI-[0-9]+$'
$ echo "exit=$?"
exit=1
```

Cero lineas.

### 1.4 Donde estan las correcciones de `F-032` y `F-033`

```
$ for h in e45185d 07ca614 cc16375 66e6413; do printf "%s S-022-nota=%s D-093-nota=%s\n" $h "$(git show $h:_audit/S-022.md | grep -c 'NOTA 2026-09-18 (`F-032`')" "$(git show $h:_persistence/decisions.md | grep -c 'NOTA 2026-09-18 (`F-033`')"; done
e45185d S-022-nota=0 D-093-nota=0
07ca614 S-022-nota=0 D-093-nota=0
cc16375 S-022-nota=0 D-093-nota=0
66e6413 S-022-nota=1 D-093-nota=1
```

### 1.5 Los cuatro puntos de la seccion 6 del informe

- **`D-095`: ¿el rotulo pide un dato imposible?** Si. La NOTA DE CIERRE se escribe dentro del commit
  de anclaje, y un commit no puede contener su propio hash. La lectura de `D-095` es correcta. La
  propia `S-023` lo confirma: vuelve a dejar el hueco.

  ```
  $ git show cbafc6f:_audit/S-023.md | grep -nE '<se completa|<pendiente|<en blanco|<rellenar'
  218: 8	+  $ grep -nE '<se completa|<pendiente|<en blanco|<rellenar' _audit/S-022.md
  258:| 8 | `grep -nE '<se completa\|<pendiente\|<en blanco\|<rellenar' _audit/S-022.md` | Si | lineas 207, 239, 258, coinciden |
  285:**NOTA DE CIERRE — commit de sesion `66e6413`, commit de anclaje `<se completa tras commitear el
  $ git log --format='%h %s' -- _audit/S-023.md
  cbafc6f S-023: ancla el informe al hash 66e6413
  66e6413 S-023: F-032/F-033 evaluados (D-095/D-096), primera sesion del Paso 5 corrida (D-097), y A-009 confirmado (D-098)
  ```

  El hueco real es la linea 285; el commit de anclaje de `S-023` es **`cbafc6f`**, derivado arriba.
  **No abro hallazgo por esto**, y lo digo expresamente: `R-024` abrio `F-032` por el mismo hueco,
  pero desde entonces `D-095` diagnostico que el procedimiento no se puede cumplir y dejo el
  mecanismo en `T-049`, pendiente de una decision del usuario. Abrir un `F-NNN` por sesion mientras
  esa decision no se tome solo repetiria `F-030` y obligaria a una nota por cierre. Queda registrado
  en la seccion 3, bajo `F-030`. Si se implementa `T-049` y el hueco persiste, eso si sera hallazgo.
  Faltan alternativas en `D-095`: ver la seccion 5.
- **`T-054`/`T-055`, ¿leccion sobre `tail -1`/`grep` sin anclar?** `T-055` reproduce lo que dice
  (arriba, `5888:`). No es un defecto de esta sesion: la tarea esta abierta con su motivo. Queda
  como recomendacion, seccion 5.
- **`DT-006`, ¿alcance correcto?** Si. La plantilla contiene la casilla y la orden con `<`:

  ```
  $ git show 66e6413:_templates/010_prototype/015_session_NNN.md | grep -n '<' | tail -5
  159:| **Proceso actual** | **¿Como haces esto hoy?** | `<…>` |
  172:- `<…>`
  173:- `<…>`
  194:- [ ] **No queda ni un solo `<` en el archivo.**
  198:grep -n "<" 010_prototype/015_session_NNN.md                 # debe no devolver nada
  ```

  El defecto vive en `_templates/`, que es agnostica, asi que su alcance es el esqueleto de arranque.
- **`D-097`, ¿basta la cabecera?** La cabecera declara `Juana Ramirez (facilitadora), transcrito al
  dictado por manager`, y el archivo nace el dia de la sesion:

  ```
  $ git log --diff-filter=A --format=%ad --date=short -- 010_prototype/015_session_001.md
  2026-09-18
  ```

  La desviacion esta declarada en la cabecera y justificada en `D-097`, y la fecha coincide con la de
  la sesion. Si el Gate 1 la acepta es cosa de `gate1_auditor`, no de esta auditoria. Lo que
  corresponde aqui sale limpio.

## 2. Hallazgos

### F-034 — El registro de `S-023` afirma una ronda de cuatro sesiones donde la sellada es de cinco

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | Bloqueante |
| Donde | `_persistence/progress.md` (lineas 107, 128, 791, 803); `_audit/S-023.md` secciones 2, 4 y 5 |

- **Que se observo:**

  ```
  $ git show 66e6413:_persistence/progress.md | grep -n "cuatro sesiones\|sesiones restantes\|002\`, \`003\`"
  107:`T-053`). Se corre la primera de las cuatro sesiones del Paso 5 de `010_prototype`:
  128:alguna de las tres sesiones restantes el participante vuelve sobre la discrepancia, se anota en su
  791:  primera de las cuatro sesiones del Paso 5: `010_prototype/015_session_001.md`, con JD Rodriguez
  803:- **Que quedo abierto:** faltan las sesiones `002`, `003` y `004` del Paso 5; el prototipo sigue sin
  $ git show 66e6413:_persistence/decisions.md | sed -n '5308,5309p'
  - **Decision:** **1 participante** y **5 sesiones**, en **cinco dias de sorteo distintos**. Umbral
    `4 de 5`, y solo cuenta `Exito autonomo`. Queda escrito en
  $ git show 66e6413:010_prototype/010_participants.md | grep -n '^| `00[1-5]`'
  121:| `001` | 2026-09-18 | Unico | si | `015_session_001.md` | Sin reclutamiento: es el Actor Generador |
  122:| `002` | pendiente | Unico | pendiente | `015_session_002.md` | Sin reclutamiento: es el Actor Generador |
  123:| `003` | pendiente | Unico | pendiente | `015_session_003.md` | Sin reclutamiento: es el Actor Generador |
  124:| `004` | pendiente | Unico | pendiente | `015_session_004.md` | Sin reclutamiento: es el Actor Generador |
  125:| `005` | pendiente | Unico | pendiente | `015_session_005.md` | Sin reclutamiento: es el Actor Generador |
  ```

  El informe repite la cifra: seccion 2 («Faltan las sesiones `002`, `003` y `004`»), seccion 4
  («una sola de las cuatro sesiones previstas») y seccion 5. `A-009` y `D-098`, del mismo commit,
  dicen cinco.
- **Por que importa:** `progress.md` se lee al abrir cada sesion, y la tarea siguiente es correr
  esta misma ronda. Si se lee tal cual, la ronda acaba en `004` y el umbral sellado `4 de 5` no se
  puede evaluar. Ademas, el mismo commit se contradice. `Media` porque la cifra correcta sigue en
  `D-087`, `D-088` y `010_participants.md`; `Bloqueante` porque el registro que guia la sesion
  siguiente afirma algo falso sobre el trabajo que esa sesion va a hacer.
- **Que lo corregiria:** una nota fechada en `progress.md` y en `_audit/S-023.md` que declare que la
  ronda es de cinco sesiones y que faltan de la `002` a la `005`, sin reescribir lo sellado; y que el
  proximo cierre sobrescriba las secciones 2 y 3 de `progress.md` con la cifra correcta.
  ⚠️ Es una recomendacion, no una orden.

### F-035 — La seccion 0 de `S-023` situa en el commit anterior las correcciones de `F-032` y `F-033`, que estan en `66e6413`

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_audit/S-023.md`, seccion 0 (lineas 24, 25 y 28) |

- **Que se observo:**

  ```
  $ git show 66e6413:_audit/S-023.md | grep -n "ya en el commit anterior\|que precede a esta sesion"
  24:| F-032 — La NOTA DE CIERRE de S-022 repite por tercera sesion consecutiva el hueco de plantilla en el commit de anclaje | Aceptado — pendiente | `D-095`, `T-052` (`Implementada`): se completo por nota fechada en `_audit/S-022.md`, ya en el commit anterior a este cierre |
  25:| F-033 — La verificacion de orden de D-093 publica «(sin salida)» para una orden que a partir del commit devuelve una linea | Aceptado — pendiente | `D-096`, `T-053` (`Implementada`): se completo por nota fechada en `D-093`, ya en el commit anterior a este cierre |
  28:commit `e45185d`/`07ca614` que precede a esta sesion (evaluados por `manager` como `D-095`/`D-096`,
  ```

  Las dos notas nacen en `66e6413` y no estan en ningun commit anterior (salida de la seccion 1.4).
  `F-032` y `F-033` los abrio `R-024` en `cc16375`, auditando `e45185d`: la correccion no podia estar
  en ninguno de esos dos. La seccion 1 del mismo informe lo dice bien («ya venia editado por
  manager al llegar a este cierre»), y el `--stat` de `66e6413` lista `_audit/S-022.md` (+24).
  Tambien es falsa la frase «Ninguno de los siete se toca en este cierre»: dos de ellos se tocan en
  este commit.
- **Por que importa:** la auditoria lee la seccion 0 para saber donde verificar una correccion.
  Quien la siga buscara las notas en `e45185d` o en `07ca614` y no las encontrara. `Baja` porque la
  seccion 1 y el diff la contradicen y el dato se recupera; `No bloqueante` porque esta auditoria ya
  cierra los dos hallazgos contra el commit correcto y nada posterior hereda el error.
- **Que lo corregiria:** una nota fechada al final de `_audit/S-023.md` que declare que las dos
  correcciones van en el commit de sesion `66e6413`, sin reescribir la seccion 0.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | Aceptado — pendiente (sin cambios) | `T-030` sigue `No implementada`; el commit no toca lo que la corregiria |
| F-017 | Aceptado — pendiente (sin cambios) | `T-031` sigue `No implementada` |
| F-028 | Aceptado — pendiente (sin cambios) | `T-047` sigue `No implementada` |
| F-029 | Aceptado — pendiente (sin cambios) | `T-048` sigue `No implementada` |
| F-030 | Aceptado — pendiente (sin cambios) | La mitad que corrige el archivo `S-020` ya estaba hecha; la del mecanismo, `T-049`, sigue `No implementada` y ahora espera una decision del usuario (`D-095`, nota en `T-049`). **El hueco se repite en `_audit/S-023.md:285`** (seccion 1.5); el anclaje de `S-023` es `cbafc6f` |
| F-032 | **Implementado** — `66e6413` | `_audit/S-022.md` termina en una nota fechada que declara `07ca614` con `git rev-parse` corrido y pegado; la linea 258 sigue literal. Salidas en la seccion 1.2 (`6`, linea 258 literal, `10`) |
| F-033 | **Implementado** — `66e6413` | `D-093` lleva la nota fechada con `e45185d 2026-09-18` para `010_prototype/app/` y la prueba `merge-base --is-ancestor`; el `(sin salida)` original sigue en la linea 5713. Salidas en la seccion 1.2 |

Ninguno de los `No se implementa` del registro es de esta pasada.

## 4. Lo que NO se pudo comprobar

- **La seccion 9 del informe (Paso 2f, desfase con el esqueleto)** no se reprodujo: compara contra
  un repositorio fuera de este, y el protocolo de auditoria no lo incluye entre sus controles. Queda
  **NO MIRADO**, no limpio.
- **El contenido de la sesion `001` en si** (lo que dijo el participante, los tiempos): solo se puede
  contrastar contra el propio archivo, no contra un hecho externo. Se comprobo la coherencia entre
  `D-098`, `A-009` y el archivo, y la fecha de nacimiento del archivo.

El resto de los controles se corrio completo.

## 5. Recomendaciones sin hallazgo

- **Sobre `T-049`/`D-095`:** hay una tercera salida que `D-095` no considera. Que el rotulo diga
  «commit de anclaje: el que contiene este informe — `git log -1 --format=%h -- _audit/S-XXX.md`» y
  que **el cierre siguiente** publique el hash ya derivado en su propio informe. Asi no hace falta un
  tercer commit y el dato queda escrito en algun sitio. Tambien vale no pedir el hash: el tablero ya
  publica el commit auditado, y el de anclaje se deriva con una orden.
- **Sobre `T-054`/`T-055`:** la familia «una orden sin anclar deja de reproducir» ya suma
  `F-030`..`F-033`, `T-050`, `T-054` y `T-055`. Una leccion en `lessons.md` estaria justificada:
  cuando se va a volver a leer, cada verificacion lleva `git show <hash>:` o un patron anclado a
  linea completa (`^...$`), nunca `grep | tail -1` sobre un archivo que la misma nota edita. La decide
  `manager`.
- **Sobre `D-096`:** su verificacion publica `5709:`, y a `66e6413` esa linea ya es la `5713` (se
  anadieron cuatro filas al indice), aparte del `tail -1` que recoge `T-055`. Conviene que la nota de
  `T-055` cubra las dos causas.
