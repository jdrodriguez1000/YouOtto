# Auditoria R-019 — sobre S-017

| Campo | Valor |
|---|---|
| Auditoria | R-019 |
| Informe auditado | `_audit/S-017.md` |
| Commit auditado | a0949c5 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (4) |

🔑 **El commit auditado es el literal de la cabecera del informe anclado, no el que deriva
`git log -1 -- _audit/S-017.md`.** Ese devuelve `0854fd3`, el commit de anclaje del Paso 7c/7c-bis:

```
$ git log -1 --format=%h -- _audit/S-017.md
0854fd3
$ git show 0854fd3:_audit/S-017.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | a0949c5 |
```

⚠️ El `--stat` del commit de anclaje trae **dos** archivos, no uno (`_audit/S-017.md` y
`_persistence/decisions.md`), porque el Paso 7c-bis de `protocol-close` autoriza anclar tambien los
bloques «Criterio de cierre» de las decisiones nacidas en la sesion. Se comprobo que la lista de la
seccion 1 del informe cuadra con el commit sustantivo, no con el de anclaje:

```
$ git show --stat --name-only --format= 0854fd3 | grep -c .
2
$ git show --stat --name-status --format= a0949c5
M	005_discovery/005_needs.md
M	005_discovery/015_stakeholders.md
A	_audit/S-017.md
M	_audit/findings.md
M	_audit/index.md
M	_persistence/decisions.md
M	_persistence/lessons.md
M	_persistence/progress.md
M	_persistence/tasks.md
M	_persistence/techdebt.md
M	_templates/005_discovery/015_stakeholders.md
M	_workflow/005_discovery.md
```

Doce archivos, uno a uno iguales a los que la seccion 1 del informe enumera.

---

## 1. Que se comprobo

### 1.1 Control a — indice ↔ detalle de `_persistence/` a `a0949c5`

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
    echo "== $f"
    diff <(git show a0949c5:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
         <(git show a0949c5:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
  done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete coherentes: ninguna diferencia entre indice y detalle.

### 1.2 Control b — carpetas del arbol contra las declaradas

```
$ diff <(git ls-tree -d --name-only a0949c5 | sed 's|$|/|' | sort) \
       <(git show a0949c5:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
9a10
> temporal/
```

Una sola diferencia, y **con motivo escrito**: `project.md` declara `temporal/` como area de trabajo
del usuario, excluida en `.gitignore`, y su propia nota explica por que nunca aparecera en el arbol
versionado. No es hallazgo.

### 1.3 Control c — fuga de datos propios en lo agnostico

Mismo ambito y mismos tres datos que el Paso 1b de `protocol-close`
(`.claude/skills/protocol-close/SKILL.md:105`), resueltos desde `project.md`: nombre `YouOtto`,
carpeta raiz de las rutas absolutas `Company_TripleS`, host del remoto `github.com`.

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" a0949c5 -- .claude CLAUDE.md _phases _methodology _templates _workflow
```

Cero lineas.

### 1.4 Control d — codigos instanciados en `_phases` y `_workflow`

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' a0949c5 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
```

Cero lineas, incluida la fila nueva que `D-064` anadio a `_workflow/005_discovery.md`.

### 1.5 Recuento de hunks por archivo, contra lo que la seccion 1 declara

```
$ for f in _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md _persistence/techdebt.md _audit/findings.md 005_discovery/005_needs.md 005_discovery/015_stakeholders.md _templates/005_discovery/015_stakeholders.md _workflow/005_discovery.md _persistence/progress.md; do printf "%-50s %s\n" "$f" "$(git diff a0949c5^ a0949c5 -U0 -- $f | grep -c '^@@')"; done
_persistence/decisions.md                          2
_persistence/lessons.md                            2
_persistence/tasks.md                              2
_persistence/techdebt.md                           2
_audit/findings.md                                 3
005_discovery/005_needs.md                         6
005_discovery/015_stakeholders.md                  1
_templates/005_discovery/015_stakeholders.md       1
_workflow/005_discovery.md                         2
_persistence/progress.md                           6
```

Los diez recuentos coinciden **uno a uno** con los que el informe publica.

### 1.6 Las entradas que el informe dice que nacen

```
$ git diff a0949c5^ a0949c5 -- _persistence/decisions.md | grep -E '^\+### D-'
+### D-063 - L-016 sube como enmienda de LG-32: segunda pasada de la cosecha de 005_discovery
+### D-064 - F-020 se acepta y se corrige hacia adelante: en los copiables y por nota en el artefacto
+### D-065 - L-017 no sube: ya cubierta por LG-85, tercera pasada de la cosecha de 005_discovery
+### D-066 - Se cierra el artefacto de necesidades
+### D-067 - L-018 no sube: ya cubierta por LG-101, cuarta pasada de la cosecha de 005_discovery
$ git diff a0949c5^ a0949c5 -- _persistence/lessons.md | grep -E '^\+### L-'
+### L-017 - Los sitios que cita un hallazgo son una muestra, no el inventario
+### L-018 - Un barrido de estado encuentra tambien el archivo que prohibe ese estado
$ git diff a0949c5^ a0949c5 -- _persistence/tasks.md | grep -E '^\+### T-'
+### T-036 - Corregir la cifra de condiciones de salida de 005_discovery en la plantilla, el reparto y el artefacto
+### T-037 - Anclar o fechar dos ordenes del Contexto de D-064 que ya no reproducen
$ git diff a0949c5^ a0949c5 -- _persistence/techdebt.md | grep -E '^\+### DT-'
+### DT-005 - La cifra de casillas «que llevan juicio» no cuadra en dos archivos de reparto
$ git diff a0949c5^ a0949c5 -- _audit/findings.md | grep -E '^\+### F-'
```

Cinco decisiones, dos lecciones, dos tareas, una deuda y **ningun `F-NNN` nuevo** — exactamente lo
declarado. `findings.md` solo cambio la fila y la ficha de `F-020`:

```
$ git diff a0949c5^ a0949c5 -- _audit/findings.md | grep -E '^[-+]\| \[F-020\]' | sed 's/(#[^)]*)/(...)/'
-| [F-020](...) | `015_stakeholders.md` queda `CERRADO` afirmando «seis condiciones de salida» donde su etapa enumera siete | R-018 | Baja | No bloqueante | Abierto |
+| [F-020](...) | `015_stakeholders.md` queda `CERRADO` afirmando «seis condiciones de salida» donde su etapa enumera siete | R-018 | Baja | No bloqueante | Aceptado — pendiente (`T-036`, `D-064`) |
```

`manager` **no** escribio `Implementado`: la fila y la ficha quedaron en `Aceptado — pendiente`, que
es lo que le corresponde.

### 1.7 La correccion de `F-020` en el diff

```
$ git diff a0949c5^ a0949c5 -- _templates/005_discovery/015_stakeholders.md | grep -E '^[-+].*condiciones de salida'
-- [ ] **Los interesados estan identificados** — es una de las seis condiciones de salida de la etapa.
+- [ ] **Los interesados estan identificados** — es una de las siete condiciones de salida de la etapa.
$ git diff a0949c5^ a0949c5 -- _workflow/005_discovery.md | grep -E '^[-+][^-+]'
-Las seis casillas de `_phases/005_discovery.md` §6, separadas por quien las puede comprobar:
+Las siete casillas de `_phases/005_discovery.md` §6, separadas por quien las puede comprobar:
+| cosecha hecha, ninguna leccion sin evaluar | el recuento de la columna de portabilidad | que leccion sube, cual ya esta cubierta y cual es solo de este proyecto |
```

Y el artefacto `CERRADO` se corrige por nota fechada, sin reescribir la linea:

```
$ git diff a0949c5^ a0949c5 -- 005_discovery/015_stakeholders.md | grep -E '^\+[^+]' | head -3
+  📌 **NOTA 2026-09-17 — la cifra de la linea de arriba es falsa y no se reescribe.** La etapa
+  enumera **siete** condiciones de salida, no seis. La frase llego asi desde la plantilla del
+  esqueleto y este archivo la copio; el defecto no cambia nada de lo comprobado — las siete
```

Es exactamente lo que `R-018` recomendaba en `F-020` (`_audit/R-018.md:278-280`: «una nota fechada en
el artefacto cerrado (que no se reescribe) y la correccion de `six → siete` en
`_templates/005_discovery/015_stakeholders.md`»), **mas** el tercer archivo que el hallazgo no citaba.

### 1.8 Los criterios de cierre anclados, reejecutados uno por uno

Las nueve ordenes que el Paso 7c-bis dejo ancladas a `a0949c5`, corridas de nuevo:

```
$ git show a0949c5:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
0
$ git show a0949c5:_persistence/lessons.md | grep -cF 'Promovida a LG-32'
1
$ git show a0949c5:_phases/005_discovery.md | sed -n '/^## 6. Condicion de salida/,/^## 7/p' | grep -c '^- \[ \]'
7
$ git show a0949c5:_workflow/005_discovery.md | sed -n '/^## 5\./,/^## 6\./p' | grep -c '^| '
8
$ git show a0949c5:_templates/005_discovery/015_stakeholders.md | grep -c 'siete condiciones de salida'
1
$ git show a0949c5:005_discovery/005_needs.md | grep -c 'Guia de llenado'
3
$ git show a0949c5:005_discovery/005_needs.md | grep -c "^### N-00"
4
$ git show a0949c5:_persistence/lessons.md | grep -cF 'Ya cubierta por LG-85'
1
$ git show a0949c5:_persistence/lessons.md | grep -cE '^\| \[L-018\].*Ya cubierta por LG-101'
1
```

Las nueve reproducen literalmente lo publicado. El `3` de «Guia de llenado» esta declarado en
`D-066` como las tres lineas de §4 del propio artefacto, y se comprobo que ninguna de las tres es la
seccion borrada:

```
$ git show a0949c5:005_discovery/005_needs.md | grep -nE 'Guia de llenado'
143:- [x] La seccion «Guia de llenado» de abajo **esta borrada**.
150:grep -n "Guia de llenado" 005_discovery/005_needs.md   # debe no devolver nada
158:unicas lineas que quedan con `<` y con «Guia de llenado» son **las de esta misma seccion**, que los
```

### 1.9 El commit de anclaje `0854fd3`: que toco de verdad

```
$ git diff 0854fd3^ 0854fd3 -- _persistence/decisions.md | grep -cE '^-'
16
$ git diff 0854fd3^ 0854fd3 -- _persistence/decisions.md | grep -E '^-' | grep -vcE '^---|<hash>'
0
```

Dieciseis lineas empiezan por `-`, y **ninguna** de ellas queda fuera del filtro «cabecera del diff
o linea con `<hash>`»: todas las borradas son ordenes con `<hash>`, sustituidas por su forma anclada; no
desaparecio ni una linea de prosa. Lo anadido son las formas ancladas mas cinco notas `📌` de
anclaje y el bloque de la cuarta orden de `D-064` que no reproduce, publicada **debajo** de la
original en vez de en su lugar — que es lo que el protocolo pide.

### 1.10 La septima casilla de la etapa, que el informe da por cumplida

```
$ git ls-tree --name-only a0949c5 005_discovery/
005_discovery/005_needs.md
005_discovery/010_actors.md
005_discovery/015_stakeholders.md
005_discovery/020_hypothesis.md
$ for f in $(git ls-tree --name-only a0949c5 005_discovery/); do echo "-- $f: $(git show a0949c5:$f | grep -m1 '^| Estado |')"; done
-- 005_discovery/005_needs.md: | Estado | `CERRADO` |
-- 005_discovery/010_actors.md: | Estado | `CERRADO` |
-- 005_discovery/015_stakeholders.md: | Estado | `CERRADO` |
-- 005_discovery/020_hypothesis.md: | Estado | `SELLADA` |
$ git show a0949c5:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
0
```

Ninguna leccion de la etapa queda `Sin evaluar` y ninguna quedo `Global candidata` sin subir. La
afirmacion «las siete casillas cumplidas» se sostiene.

### 1.11 La cosecha de `D-063`, contra el protocolo y contra el archivo global

El filtro 4 de `protocol-harvest` declara la salida de enmienda en
`.claude/skills/protocol-harvest/SKILL.md:137-140`: «si la leccion es una **cara nueva** de una que
ya existe, el archivo global pide **enmendar el enunciado de la existente**». `D-063` clasifica
`L-016` como cara nueva de `LG-32` por el **soporte** (donde ya no se puede escribir), no por el
nucleo. **La lectura es correcta**, y la enmienda existe:

```
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" log --oneline -3
b1915d3 Cosecha de 005_discovery desde YouOtto, segunda pasada: enmienda de LG-32 (version 6)
a9ed27b Cosecha de 005_discovery desde YouOtto: LG-106 a LG-109 y enmienda de LG-22 (version 5)
9e883a9 Cosecha de 000_preproject desde YouOtto: LG-105 (version 4)
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" status -sb | head -1
## main...origin/main
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show b1915d3:global_lessons.md | grep -cF "donde no se puede corregir no hay segunda pasada"
1
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show b1915d3:global_lessons.md | grep -cF "YouOtto` · L-016"
1
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show b1915d3:global_lessons.md | grep -nE "^> \*\*Versión: 6"
26:> **Versión: 6 · 2026-09-17** · 109 lecciones · 10 bloques
```

`LG-32` no consumio codigo nuevo, el recuento sigue en 109 y el `status` sin `[ahead N]` confirma el
push. La puerta del usuario esta declarada en `D-063` («el usuario aprobo el texto en la puerta del
Paso 5, con el antes y el despues a la vista»).

### 1.12 La brecha 9 ↔ 12 del Paso 2e, que el informe declara

La seccion 8 publica `9` sobre el area de staging y el commit trae 12 archivos. **No es un defecto:**
la NOTA DE CIERRE del commit de anclaje publica la segunda pasada anclada con `12` y explica que los
3 de la brecha son `progress.md`, `S-017.md` e `index.md`, que el cierre anade despues de esa tabla.
Comprobado que el commit no borra ningun archivo (`--name-status` de la cabecera: ninguna `D`), asi
que `--diff-filter=d` sobre el commit tiene que dar 12, y da 12.

### 1.13 Coherencia de `findings.md` despues de escribir esta auditoria

```
$ diff <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -oE '^\| \[?F-[0-9]+' | grep -oE 'F-[0-9]+' | sort -u) \
       <(awk '/^[[:space:]]*```/{c=!c; next} !c' _audit/findings.md | grep -oE '^#{3} F-[0-9]+'   | grep -oE 'F-[0-9]+' | sort -u)
```

Sin salida: indice y detalle coinciden.

---

## 2. Hallazgos

### F-021 — Ancla rota en el indice de `progress.md` para `S-017`: la flecha del titulo

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/progress.md:53` (indice) contra `:526` (entrada), a `a0949c5` |

- **Que se observo:** el titulo de `S-017` lleva un `→` entre espacios, y el ancla del indice lo
  omite junto con uno de sus dos espacios.

  ```
  $ git grep -n '^### .*→' a0949c5 -- _persistence _audit
  a0949c5:_persistence/progress.md:526:### S-017 - `F-020` de `R-018` aceptado y corregido (`D-064`), tres pasadas mas de la cosecha (`L-016`/`L-017`/`L-018` → `D-063`/`D-065`/`D-067`), y se cierra `005_needs.md` (`D-066`)
  $ git show a0949c5:_persistence/progress.md | sed -n '53p' | grep -oE '\(#[a-z0-9_-]+\)'
  (#s-017---f-020-de-r-018-aceptado-y-corregido-d-064-tres-pasadas-mas-de-la-cosecha-l-016l-017l-018-d-063d-065d-067-y-se-cierra-005_needsmd-d-066)
  ```

  El ancla escrita dice `...l-016l-017l-018-d-063...`, con **un** guion. Las dos unicas lecturas
  posibles del slug de GitHub dan **dos caracteres** ahi, porque el `→` esta rodeado de espacios y
  los espacios siempre se vuelven guiones:

  ```
  $ node -e "const s='S-017 - \`F-020\` de \`R-018\` aceptado y corregido (\`D-064\`), tres pasadas mas de la cosecha (\`L-016\`/\`L-017\`/\`L-018\` → \`D-063\`/\`D-065\`/\`D-067\`), y se cierra \`005_needs.md\` (\`D-066\`)';
  const t=s.replace(/[\u2000-\u206F\u2E00-\u2E7F\\'!\"#\$%&()*+,.\/:;<=>?@\[\]^\`{|}~]/g,'');
  console.log('flecha conservada:', t.toLowerCase().replace(/ /g,'-'));
  console.log('flecha eliminada :', t.replace(/[\u2190-\u21FF]/g,'').toLowerCase().replace(/ /g,'-'));"
  flecha conservada: s-017---f-020-de-r-018-aceptado-y-corregido-d-064-tres-pasadas-mas-de-la-cosecha-l-016l-017l-018-→-d-063d-065d-067-y-se-cierra-005_needsmd-d-066
  flecha eliminada : s-017---f-020-de-r-018-aceptado-y-corregido-d-064-tres-pasadas-mas-de-la-cosecha-l-016l-017l-018--d-063d-065d-067-y-se-cierra-005_needsmd-d-066
  ```

  Que el resto del ancla sea correcto lo confirma el precedente del propio registro: un guion
  literal entre espacios produce tres guiones (`#a-001---los-juegos...` en
  `_persistence/assumptions.md:24`), asi que el slug no colapsa guiones consecutivos.

  Es el **primer** titulo del registro con un `→`:

  ```
  $ git grep -nc '^### .*→' a0949c5 -- _persistence _audit
  a0949c5:_persistence/progress.md:1
  ```
- **Por que importa:** el indice de `progress.md` es el primer sitio por donde se entra a la
  bitacora, y `session-starter` lo lee en cada arranque. Un ancla rota manda a la cabecera del
  archivo en vez de a la sesion. `Baja` porque la entrada existe y se encuentra buscando el codigo;
  `No bloqueante` porque nada de lo que venga despues hereda el defecto.
- **Que lo corregiria:** o reponer el tramo que falta en el ancla, o —mas robusto y coherente con la
  regla de los nombres en ASCII— no usar `→` en titulos que necesitan ancla. ⚠️ Es una
  recomendacion, no una orden.

### F-022 — La fila de `F-020` publica un valor de `Estado` que la convencion de `findings.md` no declara, y el Paso 2b del cierre lo reporto como discrepancia

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_audit/findings.md:42` (fila) contra su ficha (`### F-020`), a `a0949c5` |

- **Que se observo:** la fila del indice escribe el estado con la cita pegada dentro de la celda; la
  ficha escribe el valor limpio y pone la cita en su campo propio.

  ```
  $ git show a0949c5:_audit/findings.md | sed -n '42p' | awk -F'|' '{print $(NF-1)}'
   Aceptado — pendiente (`T-036`, `D-064`)
  $ git show a0949c5:_audit/findings.md | sed -n '/^### F-020/,/^- \*\*Que se observo/p' | grep -E '^\| (Estado|Registrado en) \|'
  | Estado | Aceptado — pendiente |
  | Registrado en | `T-036` y `D-064` |
  ```

  La convencion del propio archivo enumera los valores posibles de `Estado` y no incluye ninguno con
  cita anexa:

  ```
  $ git show a0949c5:_audit/findings.md | grep -E '^\| Estado \| `Abierto`'
  | Estado | `Abierto` / `Aceptado — pendiente` / `Implementado` / `No se implementa` |
  ```

  Y las otras dos filas vivas del mismo archivo escriben el valor limpio, asi que el mismo estado
  aparece en dos formas dentro del mismo indice:

  ```
  $ git show a0949c5:_audit/findings.md | grep -E '^\| \[F-01[67]\]' | awk -F'|' '{print $2, "->", $(NF-1)}' | sed 's/(#[^)]*)//'
   [F-016]  ->  Aceptado — pendiente
   [F-017]  ->  Aceptado — pendiente
  ```
- **Por que importa:** el Paso 2b de `protocol-close` compara fila y ficha en cada cierre, y por esto
  reporto una discrepancia que el informe tuvo que escalar a mano (seccion 6). El origen es un choque
  real de reglas: `CLAUDE.md` pide actualizar la fila «citando la `T-XXX` o la `D-XXX` donde quedo»
  —y la fila no tiene columna para eso—, mientras `findings.md` cierra la lista de valores de
  `Estado`. Mientras no se zanje cual manda, **cada hallazgo aceptado volvera a disparar el mismo
  aviso**, y un control que siempre avisa deja de leerse. `Baja` porque nada de lo escrito es falso:
  la cita es correcta y la ficha lleva sus dos campos al dia. `No bloqueante` porque el registro no
  afirma un estado equivocado.
- **Que lo corregiria:** decidir una de las dos formas y escribirla en la convencion de
  `findings.md` —columna `Registrado en` propia en el indice, o valor limpio en la fila y la cita solo
  en la ficha—, con su `D-XXX`. ⚠️ Es una recomendacion, no una orden.

### F-023 — `DT-005` lleva `Confirmacion: Sin confirmar`, un valor que la convencion de `techdebt.md` no declara y que ademas evita nombrar a quien confirma

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/techdebt.md`, fila e indice de `DT-005`, a `a0949c5` |

- **Que se observo:** el valor no esta en la lista, y es el unico caso del archivo.

  ```
  $ git show a0949c5:_persistence/techdebt.md | grep -nE '^\| Confirmacion \|'
  41:| Confirmacion | `Confirmada` / `Propuesta (pendiente de <quien>)` |
  80:| Confirmacion | |
  96:| Confirmacion | Confirmada |
  158:| Confirmacion | Confirmada |
  177:| Confirmacion | Confirmada |
  217:| Confirmacion | Confirmada |
  247:| Confirmacion | Sin confirmar |
  $ git show a0949c5:_persistence/techdebt.md | grep -cE '^\|.*\| Sin confirmar \|'
  1
  ```

  La convencion del archivo no solo lista los valores: prohibe explicitamente la salida sin dueno.

  ```
  $ git show a0949c5:_persistence/techdebt.md | sed -n '50,53p'
  🚨 **`Propuesta` lleva dueno dentro del valor, siempre.** No existe `Propuesta` a secas: quien
  confirma va escrito (`Propuesta (pendiente del usuario)`), porque una propuesta sin dueno no espera
  —se queda propuesta para siempre—. Si no sabes quien confirma, entonces lo que falta no es la
  confirmacion: es saber de quien es la decision, y eso es una `T-XXX`.
  ```
- **Por que importa:** `DT-005` es deuda declarada y sin pagar sobre una cifra que afecta a dos
  etapas; sin dueno de la confirmacion, no hay nada que la haga avanzar y se queda propuesta para
  siempre — que es justo lo que esa convencion existe para impedir. `Baja` porque la deuda esta
  escrita con su evidencia y `progress.md` la cita en dos sitios; `No bloqueante` porque nada de lo
  que se haga despues hereda el defecto.
- **Que lo corregiria:** poner `Propuesta (pendiente del usuario)` —o el dueno que corresponda— en la
  fila y en la ficha, en la misma pasada; o, si no se sabe de quien es la decision, abrir la `T-XXX`
  que la convencion pide. ⚠️ Es una recomendacion, no una orden.

### F-024 — La nota de anclaje de `D-064` remite a una seccion «Sin resolver» que no existe en ningun archivo del repositorio

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `_persistence/decisions.md`, `D-064`, en el commit de anclaje `0854fd3` |

- **Que se observo:** la nota que el Paso 7c-bis anadio cierra con un puntero que no resuelve.

  ```
  $ git show 0854fd3:_persistence/decisions.md | grep -n "Sin resolver"
  1217:  - **Que la tabla mande publicar la salida en «Sin resolver» y seguir:** trata el sintoma; con el
  2336:  suya. Lo que es del cierre lo corrige en el anclaje; lo que es de `manager` va a **Sin resolver**
  3544:  hasta que `L-016` se evalue. Ver `T-035` en `tasks.md` y «Sin resolver» del informe de `S-016`.
  3746:  No se sustituye la salida publicada ni se corrige: queda declarado aqui. Ver «Sin resolver» del
  $ git show 0854fd3:_persistence/decisions.md | sed -n '3746,3747p'
    No se sustituye la salida publicada ni se corrige: queda declarado aqui. Ver «Sin resolver» del
    informe de esta sesion.
  ```

  ⚠️ **No es nuevo de esta sesion y hay que decirlo:** la linea `3544` es `D-062`, de `S-016`, y hace
  la misma remision al informe de aquella sesion. `R-018` no lo abrio. Las lineas `1217` y `2336` son
  decisiones que **describen** el protocolo y ahi la mencion es legitima.

  Pero `_audit/S-017.md` no tiene esa seccion:

  ```
  $ git show 0854fd3:_audit/S-017.md | grep -c "Sin resolver"
  0
  $ git show 0854fd3:_audit/S-017.md | grep -n '^## '
  11:## 0. Respuesta a la auditoria anterior
  30:## 1. Que se hizo
  82:## 2. Que NO se hizo, y por que
  101:## 3. Decisiones tomadas
  129:## 4. Supuestos vigentes y riesgos
  146:## 5. Siguiente tarea propuesta
  155:## 6. Que pedimos auditar
  174:## 7. Evidencia del Paso 2d
  413:## 8. Evidencia del Paso 2e
  434:## 9. Evidencia del Paso 2f
  ```

  «Sin resolver» es una seccion del **reporte en pantalla** del Paso 8 de `protocol-close`
  (`.claude/skills/protocol-close/SKILL.md:1943`), no del informe que queda en el commit — es decir,
  el registro permanente apunta a algo que no persiste.
- **Por que importa:** `decisions.md` es el registro que se lee dentro de meses, y `D-064` es
  precisamente la entrada cuya cuarta orden **no reproduce**; el puntero que deberia llevar al detalle
  del fallo no lleva a ninguna parte. El contenido no se perdio —`D-064` publica las dos salidas, la
  original y la anclada, una debajo de la otra—, asi que el defecto es solo el puntero: `Baja`. `No
  bloqueante` porque nada lo hereda.
- **Que lo corregiria:** una nota fechada en `D-064` —y en `D-062`, que tiene el mismo puntero— que
  sustituya la remision por lo que si persiste: «ver las dos salidas publicadas arriba en esta misma
  entrada», sin reescribir la linea original. Y, de fondo, que `protocol-close` no mande citar desde
  el registro una seccion que solo existe en pantalla. ⚠️ Es una recomendacion, no una orden.

---

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-016` | Aceptado — pendiente | **Aceptado — pendiente** | su `T-030` sigue `No implementada` y `D-041` no se toco en este commit |
| `F-017` | Aceptado — pendiente | **Aceptado — pendiente** | su `T-031` sigue `No implementada` y la NOTA DE CIERRE de `S-013` no se toco |
| `F-020` | Aceptado — pendiente | **Implementado** (`a0949c5`) | la correccion esta en el diff, es la que `R-018` recomendaba y ademas alcanza un tercer archivo — ver §1.7 |

Evidencia del estado de `T-030` y `T-031`, y de que `S-013` no se toco:

```
$ git show a0949c5:_persistence/tasks.md | grep -E '^\| \[T-03[01]\]' | awk -F'|' '{print $2, "->", $4}'
 [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita)  ->  No implementada
 [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013)  ->  No implementada
$ git diff --name-only a0949c5^ a0949c5 | grep -E 'S-013|S-011'
$ git diff a0949c5^ a0949c5 -- _persistence/decisions.md | grep -c 'D-041'
0
```

Sin salida en las dos ultimas: ni `_audit/S-013.md` ni `D-041` entran en este commit.

---

## 4. Lo que NO se pudo comprobar

Ninguno. Los cuatro controles mecanicos del protocolo (§1.1 a §1.4) corrieron y devolvieron
resultado, y las nueve ordenes ancladas de los criterios de cierre se reejecutaron una por una.

El Paso 2f del informe (diff contra el esqueleto de arranque) no se re-ejecuto por estar fuera del
alcance de este protocolo. Su contenido —cuatro diferencias, todas `Only in` el arbol de este
proyecto— es consistente con el «Siguiente paso» de `progress.md`, que las enumera las cuatro como
promocion pendiente de aprobacion del usuario.

---

## 5. Recomendaciones sin hallazgo

- **`D-063` deja una cifra rancia del archivo global «reportada para decidirse aparte» sin ningun
  codigo que la sostenga.** Su guia de lectura dice «104 lecciones» donde la cabecera declara 109, y
  la unica mencion en todo el registro es la propia linea de alternativas descartadas de `D-063`:

  ```
  $ git grep -n "104 lecciones\|cifra rancia" a0949c5 -- _persistence _audit
  a0949c5:_persistence/decisions.md:3608:  - **Arreglar de paso una cifra rancia del archivo global:** su guia de lectura sigue diciendo «104
  ```

  No hay `T-XXX` ni `DT-XXX`, y el informe de la sesion no la menciona. No se abre hallazgo porque el
  objeto vive fuera de este repositorio y la deuda **si** esta escrita; pero un pendiente sin codigo
  no aparece en ningun arranque, y el archivo global es la vara con la que se audita.
- **El Paso 1 de este propio protocolo afirma que «un cierre con dos commits deja el `--stat` del
  segundo con un solo archivo».** Desde que `CLAUDE.md` admite el Paso 7c-bis, el commit de anclaje
  trae dos (`_audit/S-XXX.md` y `_persistence/decisions.md`), como aqui. El control sigue sirviendo
  —lo que importa es contrastar contra la lista de la seccion 1—, pero la cifra literal ya no se
  cumple y puede hacer dudar de si se esta mirando el commit correcto.
- **La columna `Portabilidad` de `lessons.md` sigue sin distinguir «promovida como entrada nueva» de
  «promovida como enmienda».** `R-018` ya lo recomendo; `D-063` vuelve a usar `Promovida a LG-32` para
  una enmienda, asi que el caso ya se repitio dos veces (`L-015` y `L-016`).
- **La tabla de hitos de `progress.md` sigue vacia** (`| — | — | — | — |`) despues de diecisiete
  sesiones y con una etapa a punto de cerrarse. No es defecto de esta sesion, pero el cierre de
  `005_discovery` es el momento natural para poner el primero.
