# Auditoria R-008 — sobre S-008

| Campo | Valor |
|---|---|
| Auditoria | R-008 |
| Informe auditado | `_audit/S-008.md` |
| Commit auditado | 27c03bb |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-008.md
9e169f3
$ git show 9e169f3:_audit/S-008.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | 27c03bb |
$ git show --stat --name-only --format= 27c03bb | grep -c .
9
$ git show --stat --format= 9e169f3
 _audit/S-008.md           | 134 +++++++++++++++++++++++++++++++++++++++++-----
 _persistence/decisions.md |  14 +++--
 2 files changed, 129 insertions(+), 19 deletions(-)
```

Hash literal distinto del derivado: se audita `27c03bb`; `9e169f3` es el commit de anclaje. Los 9
archivos de `27c03bb` coinciden con la lista de la seccion 1 del informe. El diff de `9e169f3` sobre
`decisions.md` solo sustituye `<hash>` por `27c03bb` en las cinco ordenes de `D-025`/`D-026` y anade
las dos lineas `📌`; no toca prosa.

### 1.2 Informe contra el diff

`git show 27c03bb` revisado archivo por archivo contra la seccion 1 del informe:

- `.claude/skills/protocol-close/SKILL.md`: una frase anadida al limite del 7c-quater
  («⚠️ **Y un punto ciego que no tiene reverso:** ... **partida en dos lineas** ...»). Respaldado.
- `_audit/findings.md`: `F-009` pasa a `Aceptado — pendiente` en fila y ficha, `Registrado en |
  T-014, D-025`, parrafo «Que se hizo». Respaldado. Seccion 0 no usa `Implementado`.
- `_persistence/decisions.md`: nacen `D-025` y `D-026`, indice y ficha. Respaldado.
- `_persistence/lessons.md`: nace `L-010`. Respaldado.
- `_persistence/tasks.md`: nacen `T-014` y `T-015`, `No implementada`. Respaldado (ver F-010).
- `_persistence/techdebt.md`: nace `DT-003`, `No implementada`, `Confirmada`, `Media`. Respaldado.
- `_persistence/progress.md`: fila y entrada `S-008`, secciones 1-3 reescritas. Respaldado.

No hay en el diff nada relevante que el informe omita.

### 1.3 Criterios de cierre de D-025 y D-026, reejecutados

```
$ git show 27c03bb:.claude/skills/protocol-close/SKILL.md | grep -cF 'partida en dos lineas'
1
$ git grep -nE "YouOtto|Company_TripleS|github.com" 27c03bb -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
$ git show 27c03bb:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
0
$ git show 27c03bb:_persistence/techdebt.md | grep -cE '^(\| \[DT-003\].*\| No implementada \||### DT-003 )'
2
$ git show 27c03bb:_audit/findings.md | grep -cE '^\| \[F-009\].*\| Aceptado — pendiente \|$|^\| Estado \| Aceptado — pendiente \|$'
2
```

Las cinco reproducen lo publicado.

### 1.4 Prueba del punto ciego de D-026, reejecutada en el scratchpad del auditor

```
$ S="<scratchpad>"; printf '## 1. Archivos\nla cifra sale de `git diff --cached --name-only\n| wc -l`: 3\n## 2. Otra\n' > "$S/split.md"; sed -n '/^## 1\./,/^## 2\./p' "$S/split.md" | grep -E '^[[:space:]]*(> )?\$ git diff --cached|`git diff --cached[^`]*\|' | grep -vF -- '--stat --name-only' | wc -l; printf '## 1. Archivos\nla cifra sale de `git diff --cached --name-only | wc -l`: 3\n## 2. Otra\n' > "$S/oneline.md"; sed -n '/^## 1\./,/^## 2\./p' "$S/oneline.md" | grep -E '^[[:space:]]*(> )?\$ git diff --cached|`git diff --cached[^`]*\|' | grep -vF -- '--stat --name-only' | wc -l
0
1
```

Reproduce (`0` partida, `1` en una linea).

### 1.5 Controles mecanicos (Paso 4)

a) Indice contra detalle en `_persistence/`:

```
$ H=27c03bb; for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show $H:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas:

```
$ diff <(git ls-tree -d --name-only $H | sed 's|$|/|' | sort) <(git show $H:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con razon escrita: `temporal/` excluida en `.gitignore` (linea `temporal/`), y
`005_discovery/` declarada por adelantado con `D-002`. No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" $H -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

Cero lineas.

d) Codigos instanciados:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' $H -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

Cero lineas.

### 1.6 Paso 2f (DT-003) contra el esqueleto local

El andamiaje no cambia entre `27c03bb` y `HEAD` (`git diff --stat 27c03bb HEAD -- .claude CLAUDE.md
_phases _methodology _templates _workflow` sin salida), asi que el arbol de trabajo vale para esto:

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; git -C "$ESQ" log --oneline -1; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md; echo "exit=$?"
4d20ce2 Promocion del andamiaje desde YouOtto (origen 4fad4dd)
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-start/SKILL.md and .claude/skills/protocol-start/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/000_preproject/020_decisions.md and _templates/000_preproject/020_decisions.md differ
exit=0
```

Coincide con la seccion 9 del informe y con `DT-003`. Punto 1 de la seccion 6: comprobable, no `SIN
COMPROBAR`.

### 1.7 Punto 3 de la seccion 6 (T-014/T-015 en `No implementada`)

Ver F-010.

## 2. Hallazgos

### F-010 — T-014 y T-015 quedan `No implementada` con el trabajo en el diff, y el informe dice que es la practica de sesiones anteriores
| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_persistence/tasks.md` (`T-014`, `T-015`); `_audit/S-008.md` secciones 2 y 6; `_persistence/progress.md` entrada `S-008` |

- **Que se observo:** el trabajo de las dos tareas esta en `27c03bb` (1.2 y 1.3: `DT-003` abierta,
  frase del punto ciego en la skill, criterios reproducen), pero ambas siguen `No implementada`,
  tambien despues del anclaje:

  ```
  $ git show 9e169f3:_persistence/tasks.md | grep -E '^\| \[T-01[45]\]' | grep -o 'No implementada'
  No implementada
  No implementada
  ```

  La seccion 6 del informe justifica que «El criterio elegido (no cerrar hasta que el 7c-bis ancle la
  evidencia) es consistente con sesiones anteriores». El historial dice lo contrario: `S-007` marco
  `T-012` `Implementada` en el propio commit de sesion, con su criterio aun en `<hash>`:

  ```
  $ git show 87d10b8:_persistence/tasks.md | grep -nA12 '^### T-012' | grep -E 'Estado|Criterio'
  424-| Estado | Implementada |
  $ git show 87d10b8:_persistence/decisions.md | grep -nA60 '^### D-023' | grep -E '\$ git .*<hash>' | head -3
  1199-  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'Linea de orden significa una de dos formas'
  1201-  $ git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF "grep -F 'diff --cached'"
  1203-  $ git grep -nE "YouOtto|Company_TripleS|github.com" <hash> -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
  ```

  Lo mismo en `S-006` (`git show 081385a:_persistence/tasks.md`: `T-008`, `T-009`, `T-010`, `T-011`
  en `Implementada`) y en `S-004` (`git show c07680f:_persistence/tasks.md`: `T-007` `Implementada`).
  Ademas, la seccion 2 del informe y `progress.md` (linea 287 a `27c03bb`) dan a entender que el Paso
  7c-bis resolvera el estado, y el 7c-bis no puede cambiarlo:

  ```
  $ git show 27c03bb:.claude/skills/protocol-close/SKILL.md | sed -n 895,900p
  ⚠️ **Hay exactamente una excepcion, y esta acotada al Paso 7c-bis:** despues del
  commit, sustituyes el ancla de las ordenes del bloque «Criterio de cierre» de las decisiones nacidas
  en esta sesion y pegas su salida. **Eso no es escribir el porque** — es correr una orden ya escrita
  y publicar lo que devolvio, y no pide ni un dato de la jornada. **Ni una palabra de prosa se toca**,
  ```

  Y `9e169f3` en efecto no toca `tasks.md` (1.1).
- **Por que importa:** el registro afirma pendientes dos tareas cuyo trabajo esta hecho y verificado,
  sin ningun mecanismo previsto que las mueva; `session-starter` las presentara como trabajo por hacer.
  Y el informe justifica la divergencia con un precedente que el historial contradice, de modo que
  dos sesiones seguidas aplican criterios opuestos a la misma situacion sin decision que lo registre.
  `Media`: no se pierde trabajo, pero el estado de `tasks.md` y la justificacion del informe son
  falsos contra `git`.
- **Que lo corregiria:** decidir cual de los dos criterios se sostiene (marcar `Implementada` en el
  commit de sesion, como hasta `S-007`, o esperar al anclaje) y dejarlo escrito con su `D-XXX`; si es
  el primero, mover `T-014` y `T-015` segun la evidencia de 1.3; si es el segundo, que exista un paso
  que efectivamente las mueva. `S-008.md` no se reescribe. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-009 | Implementado (27c03bb) | La correccion decidida en `D-025` esta en el diff: `DT-003` abierta en indice y ficha para el desfase con el esqueleto (1.3), con la medicion que coincide con el esqueleto local (1.6); `F-009` registrado con `T-014`/`D-025`. No reescribir `S-007.md` esta decidido en `D-025`. El estado de `T-014` no cierra ni impide cerrar el hallazgo; su problema va aparte en F-010 |

No hay otros hallazgos abiertos: F-001 a F-008 ya estaban en `Implementado`.

## 4. Lo que NO se pudo comprobar

- Que `D-025` y `D-026` las eligiera el usuario: ocurrio fuera de `git`.
- La medicion de 1.6 es contra el esqueleto local en su estado actual, no en el momento del cierre.
- Que la primera prueba mal construida que narra `L-010` existiera: ocurrio en el scratchpad y no
  quedo en `git`.

## 5. Recomendaciones sin hallazgo

- El commit de anclaje `9e169f3` no lleva linea `Co-Authored-By`, a diferencia de `27c03bb`. No afecta
  al registro; si la atribucion se quiere uniforme, el Paso 7c podria exigirla.
- `DT-003` fija su medicion «antes de D-026»; como `D-026` no cambia la lista de archivos que
  difieren (1.6), no hay cifra falsa, pero una nota de que la lista sigue igual tras `D-026` ahorraria
  la reejecucion a quien la lea.
