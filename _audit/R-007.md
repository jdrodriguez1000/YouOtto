# Auditoria R-007 — sobre S-007

| Campo | Valor |
|---|---|
| Auditoria | R-007 |
| Informe auditado | `_audit/S-007.md` |
| Commit auditado | 87d10b8 |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-007.md
ee59542
$ git show ee59542:_audit/S-007.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | 87d10b8 |
$ git show --stat --name-only --format= ee59542
_audit/S-007.md
_persistence/decisions.md
$ git show --stat --name-only --format= 87d10b8
.claude/skills/protocol-close/SKILL.md
.claude/skills/protocol-start/SKILL.md
_audit/S-007.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_templates/000_preproject/020_decisions.md
```

Hash literal distinto del derivado: se audita `87d10b8`; `ee59542` es el commit de anclaje (informe y
los siete criterios de cierre de `D-023`/`D-024`, sustituyendo solo `<hash>` por `87d10b8` y anadiendo
las dos lineas `📌`). Los 10 archivos de `87d10b8` coinciden con la lista de la seccion 1 del informe.

### 1.2 Informe contra el diff

`git show 87d10b8`, archivo por archivo, contra la seccion 1:

- `protocol-close/SKILL.md`: segunda orden del 7c-quater con el patron de linea de orden y parrafo
  `🚨` de regla, origen y limite. Respaldado.
- `protocol-start/SKILL.md`: nota `⚠️` sobre `Revocada en parte por D-XXX`. Respaldado.
- `_templates/000_preproject/020_decisions.md` y convenciones de `decisions.md`: estado nuevo y su
  regla. Respaldado.
- `decisions.md`: nacen D-023 y D-024; D-015 y D-022 pasan a `Revocada en parte por ...` en indice y
  ficha. Respaldado.
- `tasks.md`: nacen T-012 y T-013; nota fechada en T-008 sin tocar su «Que». Respaldado.
- `lessons.md` (L-009), `progress.md` (secciones 1-3, S-007 e indice), `findings.md` (F-008 a
  `Aceptado — pendiente` en fila y ficha, con `Registrado en`), `index.md` (fila S-007). Respaldado;
  ningun hallazgo escrito como `Implementado` por el auditado.
- Nada relevante en el diff que el informe no mencione.

### 1.3 Criterios de cierre de D-023 y D-024, reejecutados sobre `87d10b8`

```
$ git show 87d10b8:.claude/skills/protocol-close/SKILL.md | grep -cF 'Linea de orden significa una de dos formas'
1
$ git show 87d10b8:.claude/skills/protocol-close/SKILL.md | grep -cF "grep -F 'diff --cached'"
0
$ git grep -nE "YouOtto|Company_TripleS|github.com" 87d10b8 -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
$ git show 87d10b8:.claude/skills/protocol-close/SKILL.md | grep -noE '\b[A-Z]{1,2}-[0-9]+\b' | grep -vE ':PI-[0-9]+$' | wc -l
0
$ git show 87d10b8:_templates/000_preproject/020_decisions.md | grep -cF 'Revocada en parte por D-XXX'
2
$ git show 87d10b8:.claude/skills/protocol-start/SKILL.md | grep -cF 'Revocada en parte por D-XXX` no es cerrada'
1
$ git show 87d10b8:_persistence/decisions.md | grep -E '^\| .*\| Revocada (en parte )?por D-[0-9]+ \|$'
| [D-015](#d-015---etapa-000_preproject-cerrada) | Etapa 000_preproject cerrada | 2026-09-16 | Revocada en parte por D-020 |
| [D-022](#d-022---como-se-implementan-t-008-t-009-y-t-011-en-protocol-close) | Como se implementan T-008, T-009 y T-011 en protocol-close | 2026-09-16 | Revocada en parte por D-023 |
| Estado | Revocada en parte por D-020 |
| Estado | Revocada en parte por D-023 |
```

Las siete salidas son las publicadas en `decisions.md` de `ee59542`.

### 1.4 Punto 1 de la seccion 6 — el patron nuevo, ni falso positivo ni ciego

Sobre las secciones 1 de los cuatro informes, patron nuevo:

```
$ for c in 74749f7:_audit/S-004.md 2846f62:_audit/S-005.md efbc8d5:_audit/S-006.md ee59542:_audit/S-007.md; do echo "== $c"; git show $c | sed -n '/^## 1\./,/^## 2\./p' | grep -E '^[[:space:]]*(> )?\$ git diff --cached|`git diff --cached[^`]*\|' | grep -vF -- '--stat --name-only' | cut -c1-140; done
== 74749f7:_audit/S-004.md
- **`.claude/skills/protocol-close/SKILL.md`** — 7 hunks (`git diff --cached -U0 -- .claude/skills/protocol-close/SKILL.md | grep -c '^@@'
$ git diff --cached -U0 -- _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md | grep -E '^\+### '
== 2846f62:_audit/S-005.md
== efbc8d5:_audit/S-006.md
== ee59542:_audit/S-007.md
```

Y la cadena suelta sobre las mismas secciones, para ver lo que el patron deja fuera:

```
$ for c in 74749f7:_audit/S-004.md 2846f62:_audit/S-005.md efbc8d5:_audit/S-006.md ee59542:_audit/S-007.md; do echo "== $c"; git show $c | sed -n '/^## 1\./,/^## 2\./p' | grep -F 'diff --cached' | cut -c1-140; done
== 74749f7:_audit/S-004.md
- **`.claude/skills/protocol-close/SKILL.md`** — 7 hunks (`git diff --cached -U0 -- .claude/skills/protocol-close/SKILL.md | grep -c '^@@'
$ git diff --cached -U0 -- _persistence/decisions.md _persistence/lessons.md _persistence/tasks.md | grep -E '^\+### '
== 2846f62:_audit/S-005.md
`git diff --cached --stat --name-only` y traia cuatro archivos: `_persistence/decisions.md`,
Por archivo, lo que nace y lo que se edita (derivado con `git diff --cached -U0`, no de memoria):
  `git diff --cached` de la seccion 1 del informe, tercera recomendacion de `R-004`). Ninguna decision
  `git diff --cached` de la seccion 1, `Origen: report_auditor`, cita `D-019`). Ninguna tarea existente
== efbc8d5:_audit/S-006.md
  por el Paso 1c); el Paso 7c traduce **en su sitio** las ordenes `git diff --cached` de la prosa de
== ee59542:_audit/S-007.md
$ git diff --cached --stat --name-only
  del Paso 7c-quater pasa a exigir una **linea de orden** (un bloque `$ git diff --cached...` o una
  orden entre comillas invertidas con tuberia), no la cadena `diff --cached` en cualquier sitio; se
```

Lo que el patron descarta son menciones sin recuento al lado (la unica orden sin tuberia, en S-005,
es «derivado con `git diff --cached -U0`», sin cifra que reproducir). Detecta las dos ordenes reales
de S-004 y no salta con la prosa de S-006 ni con la del propio S-007. El 7c-quater completo sobre el
informe anclado sale vacio:

```
$ git show ee59542:_audit/S-007.md > /tmp/S7.md; grep -qE '^(> )?\$ git show --stat --name-only --format= 87d10b8$' /tmp/S7.md || echo FALTA; sed -n '/^## 1\./,/^## 2\./p' /tmp/S7.md | grep -E '^[[:space:]]*(> )?\$ git diff --cached|`git diff --cached[^`]*\|' | grep -vF -- '--stat --name-only' && echo "SIN ANCLAR"
(sin salida)
```

### 1.5 Punto 2 de la seccion 6 — las lineas 3 a 9 y su anclaje

`git show ee59542 -- _persistence/decisions.md` muestra exactamente siete lineas `$ ` cambiadas, las
cuatro de D-023 y las tres de D-024, solo en `<hash>` → `87d10b8`, mas una linea `📌` por decision; las
salidas quedan intactas y reproducen (1.3). Orden del Paso 2d anclada:

```
$ git diff -U0 87d10b8^ 87d10b8 -- _persistence _audit ":(exclude)_audit/S-007.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
9
```

CENSO de la NOTA DE CIERRE, reejecutado:

```
$ for f in $(git ls-tree -r --name-only 87d10b8 _persistence _audit | grep -v '_audit/S-007.md'); do n=$(git show 87d10b8:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_persistence/decisions.md: 9
_persistence/tasks.md: 1
```

Identico al publicado. Linea #1 de la tabla de reejecucion:

```
$ git show efbc8d5:_audit/S-006.md | sed -n '/^## 1\./,/^## 2\./p' | grep -F 'diff --cached' | grep -vF -- '--stat --name-only'
  por el Paso 1c); el Paso 7c traduce **en su sitio** las ordenes `git diff --cached` de la prosa de
```

### 1.6 Punto 3 de la seccion 6 — ninguna tercera revocacion

```
$ git show 87d10b8:_persistence/decisions.md | grep -oE "^\| Estado \| .*" | sort | uniq -c
      1 | Estado | Revocada en parte por D-020 |
      1 | Estado | Revocada en parte por D-023 |
     23 | Estado | Vigente |
      1 | Estado | `Vigente` / `Revocada en parte por D-XXX` / `Revocada por D-XXX` |
```

Solo D-015 y D-022 tienen estado distinto de `Vigente`, y en `efbc8d5` el unico revocado era D-015
(salida publicada en D-024 y en R-006, seccion 5). No hay una tercera revocacion sin tocar. Si alguna
de las 23 `Vigente` estuviera revocada en parte sin decirlo, este barrido no lo ve: exigiria leer cada
decision contra las posteriores, y no se hizo.

### 1.7 Secciones 2 y 4 del informe

```
$ git show 87d10b8:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -c "No implementada"
0
$ git show 87d10b8:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep -cE '^\| \[T-'
13
$ git show 87d10b8:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -E 'A-[0-9]+'
| [A-001](#a-001---los-juegos-registrados-pueden-ser-datos-de-personas) | Los juegos registrados pueden ser datos de personas | 2026-09-15 | Abierto |
| [A-002](#a-002---los-agentes-de-gate-y-de-acta-se-cargan-al-reiniciar-claude-code) | Los agentes de Gate y de acta se cargan al reiniciar Claude Code | 2026-09-15 | Refutado |
| [A-003](#a-003---con-la-cabecera-yaml-valida-los-agentes-de-gate-y-de-acta-cargan) | Con la cabecera YAML valida, los agentes de Gate y de acta cargan | 2026-09-15 | Confirmado |
```

Respaldado.

### 1.8 Seccion 9 (Paso 2f), contra `87d10b8`

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"; for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md; do git show 87d10b8:$f | diff -q --strip-trailing-cr "$ESQ/$f" - >/dev/null && echo "igual $f" || echo "difiere $f"; done
difiere .claude/skills/protocol-close/SKILL.md
difiere .claude/skills/protocol-start/SKILL.md
difiere _templates/000_preproject/020_decisions.md
```

Los tres archivos difieren, como publica el informe. La atribucion a `DT-001` no se sostiene: F-009.

### 1.9 Fecha

```
$ git log -2 --format='%h %ad' --date=short
ee59542 2026-09-16
87d10b8 2026-09-16
$ date +%F
2026-09-16
```

Coincide con la fecha del informe.

### 1.10 Controles mecanicos (Paso 4)

a) Indice ↔ detalle en `_persistence/`, sobre `87d10b8`:

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show 87d10b8:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show 87d10b8:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas:

```
$ diff <(git ls-tree -d --name-only 87d10b8 | sed 's|$|/|' | sort) <(git show 87d10b8:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Dos diferencias con razon escrita en `project.md` («Carpetas propias»): `temporal/` excluida en
`.gitignore` (`git show 87d10b8:.gitignore` → linea 5 `temporal/`) y `005_discovery/` declarada por
adelantado con su decision (`decisions.md`, lineas 181-192). No es hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" 87d10b8 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "exit=$?"
exit=1
```

Cero lineas.

d) Codigos instanciados:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 87d10b8 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "exit=$?"
exit=1
```

Cero lineas.

## 2. Hallazgos

### F-009 — La seccion 9 de S-007 atribuye el desfase con el esqueleto a DT-001, que esta Implementada

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Donde | `_audit/S-007.md`, seccion 9 (linea 239 en `87d10b8`); `_persistence/techdebt.md` (DT-001) |

- **Que se observo:** el informe cierra la evidencia del Paso 2f diciendo que la promocion se hara
  «como ya viene arrastrando `DT-001`», y en el mismo commit `DT-001` esta pagada:

  ```
  $ git show 87d10b8:_audit/S-007.md | grep -n 'DT-001'
  239:punto por punto, como ya viene arrastrando `DT-001` desde `S-004`/`S-005`/`S-006`.
  $ git show 87d10b8:_persistence/techdebt.md | grep -nE '^\| \[DT-'
  24:| [DT-001](#dt-001---claude-se-aleja-del-esqueleto-de-arranque) | `.claude/` se aleja del esqueleto de arranque | Implementada | Confirmada | Media | No bloqueante |
  25:| [DT-002](#dt-002---el-control-de-salida-reproducida-no-reejecuta-las-ordenes) | El CONTROL DE SALIDA REPRODUCIDA no reejecuta las ordenes | No implementada | Confirmada | Baja | No bloqueante |
  ```

  La ficha de DT-001 en ese commit termina con la nota «pagada: `D-016` promovio los seis archivos» y
  `Estado de la deuda: **Implementada**`. El desfase actual nacio despues de ese pago (consecuencias de
  D-022 en S-006 y de D-023/D-024 en S-007) y no hay ninguna `DT-XXX` abierta que lo siga.
- **Por que importa:** el informe afirma abierta una deuda que el registro da por cerrada; quien busque
  en DT-001 el seguimiento del desfase encuentra una entrada pagada. `Baja`: el desfase si esta escrito
  (consecuencias de D-022/D-024, `progress.md` secciones 1 y 3), asi que no se pierde.
- **Que lo corregiria:** una nota fechada en la seccion 9 de `S-007.md` o, si se prefiere no tocar un
  informe auditado, dejarlo registrado en la evaluacion; y decidir si el desfase nuevo merece su propia
  `DT-XXX` o se paga con `protocol-promote`. Es una recomendacion; decide `manager`.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-008 | Implementado (87d10b8) | La correccion esta en el diff: la segunda orden del 7c-quater busca lineas de orden (1.3). Reejecutado sobre los informes, no salta con la prosa de S-006 ni con la de S-007 y sigue detectando las dos ordenes reales de S-004 (1.4); el 7c-quater completo sobre S-007 anclado sale vacio. T-012 y D-023 registrados; no reescribir S-006 esta decidido en D-023 |

No hay otros hallazgos abiertos: F-001 a F-007 ya estaban en `Implementado`.

## 4. Lo que NO se pudo comprobar

- Que el 7c-quater se corriera de verdad antes del commit de anclaje `ee59542`: su tabla no manda
  publicar la salida vacia. Se comprobo que hoy, sobre el informe anclado, sale vacia (1.4).
- Que D-024 la eligiera el usuario y que T-012 se aceptara «a peticion del usuario»: ocurrio fuera de
  `git`.
- La salida del Paso 2f describe arboles de trabajo; se contrasto contra `87d10b8` y el esqueleto
  local en su estado actual (1.8), no contra el esqueleto en el momento del cierre.

## 5. Recomendaciones sin hallazgo

- **Limite del patron nuevo (punto 1 de la seccion 6).** Una orden entre comillas invertidas que se
  parta en dos lineas, con la tuberia en la segunda, no la detecta `[^`]*\|` (grep es por linea). No
  aparece en ninguno de los cuatro informes (1.4); si llegara a pasar, el parrafo de limite del
  7c-quater podria mencionarlo.
- **El desfase con el esqueleto no tiene deuda abierta.** Desde el pago de DT-001 el desfase vuelve a
  crecer (un archivo tras S-006, tres tras S-007) y solo consta en consecuencias de decisiones y en
  `progress.md`. Valorar una `DT-XXX` o la promocion.
- En la seccion 9 de S-007, «las tres son `Only in` el sentido de «difieren»» es confuso: la salida
  no tiene ninguna linea `Only in`, y la frase siguiente lo dice bien.
