# Informe de auditoria — S-018

| Campo | Valor |
|---|---|
| Sesion | S-018 |
| Fecha | 2026-09-17 |
| Etapa | 005_discovery |
| Rama | main |
| Commit auditado | ed64348 |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`31ece08`, el commit de la auditoria — `git log -1 --format='%h %s' HEAD` da
`31ece08 auditoria R-019 sobre S-017 (a0949c5)`), sobre el estado que `a0949c5` dejo.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-021 — Ancla rota en el indice de `progress.md` para `S-017`: la flecha del titulo | Aceptado — corregido en este commit | `D-068`, `T-038`. El barrido propio (ambito mas ancho que el hallazgo) encontro **seis** anclas rotas, no una; las seis quedan corregidas y se anadio el Paso 2b-bis con su control `.claude/checks/anchors.js` |
| F-022 — La fila de `F-020` publica un valor de `Estado` que la convencion de `findings.md` no declara | Aceptado — corregido en este commit | `D-069`, `T-039`. El sintoma ya no reproducia contra `HEAD` (la propia auditoria cerro esa fila); se corrigio la causa: columna `Registrado en` nueva en el indice (22 filas), y el Paso 2b leyendo la columna correcta |
| F-023 — `DT-005` lleva `Confirmacion: Sin confirmar`, un valor que la convencion de `techdebt.md` no declara | Aceptado — corregido en este commit | `D-070`, `T-040`. `DT-005` pasa a `Propuesta (pendiente del usuario)` |
| F-024 — La nota de anclaje de `D-064` remite a una seccion «Sin resolver» que no existe | Aceptado en su parte comprobable — corregido en este commit; rechazada la atribucion a la skill | `D-071`, `T-041`. Nota fechada en `D-062` y `D-064`; nueva prohibicion en el 7c-bis de `protocol-close`. Se rechaza que la skill mandara ese puntero: sus menciones de «Sin resolver» son todas instrucciones para el reporte en pantalla, verificado con `grep -c` |

## 1. Que se hizo

```
$ git show --stat --name-only --format= ed64348
```
```
.claude/checks/anchors.js
.claude/checks/anchors.test.js
.claude/skills/protocol-close/SKILL.md
_audit/S-018.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
_persistence/techdebt.md
```

Detalle, con codigos:

- `.claude/checks/anchors.js` (nace) — control mecanico que replica el algoritmo de anclas de GitHub
  (github-slugger) sobre los titulos reales del repositorio; sale `1` si encuentra alguna ancla rota
  en los archivos que se le pasen, `0` si todas resuelven.
- `.claude/checks/anchors.test.js` (nace) — su test, 10 casos, 10/10 en verde
  (`node --test .claude/checks/anchors.test.js`).
- `.claude/skills/protocol-close/SKILL.md` — 4 hunks (`git diff ed64348^ ed64348 -- .claude/skills/protocol-close/SKILL.md | grep -c '^@@'`):
  se corrige el control fila↔ficha de `findings.md` para leer `$(NF - 2)` en vez de `$(NF - 1)` (la
  columna nueva desplazo la posicion); se anade el **Paso 2b-bis** completo (ancla interna, control,
  tabla de resultados, limite declarado del algoritmo); se anade al Paso 7c-bis la prohibicion de
  remitir desde una entrada del registro a la seccion «Sin resolver» del reporte en pantalla; y se
  anade la fila del Paso 2b-bis a la plantilla del reporte del Paso 8.
- `_audit/S-018.md` (nace) — este informe, el que la auditoria va a juzgar.
- `_audit/index.md` — una fila nueva: `S-018.md` con `Pendiente`/`Pendiente`/`Pendiente` en las tres
  columnas que solo puede rellenar la auditoria.
- `_audit/findings.md` — 7 hunks: el indice gana la columna `Registrado en` (22 filas, derivadas del
  campo homonimo de cada ficha, no tecleadas); su convencion declara la columna nueva y explica por
  que existe; y las cuatro fichas de `F-021`-`F-024` (entradas ya existentes, abiertas por `R-019`, no
  nacidas hoy) pasan su `Estado` de `Abierto` a `Aceptado — pendiente` y su `Registrado en` de vacio a
  la `T-XXX`/`D-XXX` correspondiente.
- `_persistence/decisions.md` — `D-068` (nace), `D-069` (nace), `D-070` (nace), `D-071` (nace), `D-072`
  (nace): las cinco decisiones de esta sesion, con su Contexto verificado contra `HEAD`, su Decision,
  su Por que, sus Alternativas descartadas y su Criterio de cierre.
- `_persistence/lessons.md` — `L-019` (nace), `L-020` (nace): las dos lecciones escritas al tratar los
  hallazgos.
- `_persistence/progress.md` — 8 hunks: `S-018` (nace, indice + secciones 1/2/3 + bitacora); y las
  seis anclas del indice de sesiones corregidas por `D-068` (`S-007`, `S-008`, `S-010`, `S-015`,
  `S-016`, `S-017`) — entradas ya existentes, no nacidas hoy, cuyo titulo no cambia: solo el enlace de
  ancla.
- `_persistence/tasks.md` — `T-038` (nace), `T-039` (nace), `T-040` (nace), `T-041` (nace), `T-042`
  (nace): las primeras cuatro las cerro `manager` como `Implementada` en la misma pasada en que
  trato los hallazgos; `T-042` nace `No implementada`, abierta por este mismo cierre (`Origen:
  session-closer`).
- `_persistence/techdebt.md` — 2 hunks: `DT-005` (entrada ya existente, no nacida hoy) cambia su
  `Confirmacion` de `Sin confirmar` a `Propuesta (pendiente del usuario)`, en la fila del indice y en
  la ficha.

## 2. Que NO se hizo, y por que

Tareas abiertas (`No implementada`, del indice de `tasks.md`):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
```
```
| [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita) | Publicar por nota la orden sin filtro que D-041 cita | No implementada | Baja | No bloqueante | 005_discovery |
| [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013) | Corregir por nota la cifra de la NOTA DE CIERRE de S-013 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-032](#t-032---limitar-las-skills-que-puede-invocar-el-agente-de-cierre) | Limitar las skills que puede invocar el agente de cierre | No implementada | Alta | No bloqueante | 005_discovery |
| [T-033](#t-033---publicar-por-nota-las-ordenes-del-paso-1b-y-del-barrido-que-d-045-resume) | Publicar por nota las ordenes del Paso 1b y del barrido que D-045 resume | No implementada | Baja | No bloqueante | 005_discovery |
| [T-035](#t-035---anclar-por-nota-la-cifra-del-barrido-del-paso-1-de-protocol-harvest-en-d-062) | Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-037](#t-037---anclar-o-fechar-dos-ordenes-del-contexto-de-d-064-que-ya-no-reproducen) | Anclar o fechar dos ordenes del Contexto de D-064 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-042](#t-042---anclar-o-fechar-seis-ordenes-de-contexto-en-d-068d-069d-070d-071d-072-que-ya-no-reproducen) | Anclar o fechar seis ordenes de Contexto en D-068/D-069/D-070/D-071/D-072 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
```

`T-032` (`Alta`, `No bloqueante`) queda pendiente de decidir si `A-007`/`D-050` sustituye su criterio
literal («tras reiniciar Claude Code»). `T-037` y `T-042` son la misma clase de pendiente sobre
distintas decisiones: ordenes de bloques «Contexto» —nunca «Criterio de cierre»— que documentan el
estado **anterior** a la correccion que la propia decision aplico en el mismo commit, y por eso hoy ya
no reproducen; `session-closer` no puede editar `decisions.md` fuera del anclaje mecanico del Paso
7c-bis, que solo toca «Criterio de cierre». `DT-002` (`No implementada`, `Confirmada`) sigue sin
pagar. `DT-005` sigue `No implementada`, ahora con dueno declarado (`Propuesta (pendiente del
usuario)`). No se corrio el acta de cierre de etapa de `005_discovery` (`protocol-phase-exit`): sus
siete casillas de condicion de salida siguen cumplidas desde `S-017`, pero este cierre no la ejecuta.

## 3. Decisiones tomadas

- **D-068** — Se acepta `F-021` y se corrigen **seis** anclas rotas del indice de `progress.md`, no
  una: el barrido con `.claude/checks/anchors.js` (algoritmo de GitHub replicado) encontro cinco mas
  alla de la citada por el hallazgo, todas por la misma causa (titulo retocado despues de escribir su
  ancla). Se anade el Paso 2b-bis a `protocol-close`. Alternativas descartadas: corregir solo la ancla
  citada (ya se hizo una vez con `F-003` y el defecto reaparecio); prohibir simbolos no-ASCII en
  titulos (no cubre la causa mayoritaria); reescribir el titulo para que encaje con el ancla (cambia
  el registro para acomodar la navegacion); usar `github-slugger` como dependencia (anadiria
  `package.json`/`node_modules` a un repositorio sin codigo de producto; se replico el algoritmo,
  validado contra la referencia sobre 1532 titulos reales); dejar el control para mas adelante (habria
  repetido el error de `F-003` a sabiendas).
- **D-069** — Se acepta `F-022`. El sintoma ya no reproducia contra `HEAD`, pero la causa —`CLAUDE.md`
  pide citar en la fila, `findings.md` cerraba los valores de `Estado`— seguia entera. Se anade la
  columna `Registrado en`. Alternativa descartada: dejar la fila limpia y la cita solo en la ficha (mas
  simple, pero obliga a enmendar `CLAUDE.md` y pierde la cita en el sitio por donde se entra); se
  presento al usuario junto con la adoptada, y eligio esta.
- **D-070** — Se acepta `F-023`. `DT-005` pasa a `Propuesta (pendiente del usuario)`. Alternativas
  descartadas: abrir una `T-XXX` para preguntar de quien es la confirmacion (aqui ya se sabe: el
  usuario es el patrocinador y todas las confirmaciones de deuda del registro han sido suyas);
  confirmarla el propio `manager` como `Confirmada` (no le corresponde esa lectura).
- **D-071** — Se acepta la parte comprobable de `F-024` (nota fechada en `D-062` y `D-064`, sin
  reescribir, mas prohibicion nueva en el 7c-bis) y se **rechaza** la parte que atribuye el defecto a
  la skill: la skill nunca mando ese puntero (evidencia: `grep -c` sobre sus menciones de «Sin
  resolver», todas instrucciones para el reporte en pantalla). Alternativas descartadas: reescribir las
  dos lineas para que citen lo correcto (convierte «falta un puntero» en «hay un puntero que nadie
  escribio»); tratar solo `D-064` (la que el hallazgo titula, dejando fuera `D-062` con el mismo
  defecto); aceptar tambien la atribucion a la skill (habria dejado el registro afirmando que se retiro
  una instruccion que nunca existio).
- **D-072** (usuario) — Quinta pasada de `protocol-harvest`: `L-019` sube como enmienda de `LG-100`;
  `L-020` no sube (`Ya cubierta por LG-06`). Version 7 del archivo global, commit `e915383`, subido.
  Alternativas descartadas: `L-019` como entrada nueva (el filtro de la cosecha lo prohibe cuando el
  nucleo ya existe); `L-020` como enmienda de `LG-06` (el matiz es real pero es una sola aparicion,
  atrapada antes de costar nada — si repite, enmienda); dejar las dos `Sin evaluar` y cerrar la etapa
  igual (la casilla existe para impedir exactamente eso).

## 4. Supuestos vigentes y riesgos

```
$ grep -E '^\| \[A-' _persistence/assumptions.md | grep -i abierto
```
```
| [A-004](#a-004---el-patrocinador-revisa-cada-salida-de-la-ia-antes-de-que-entre-al-registro) | El patrocinador revisa cada salida de la IA antes de que entre al registro | 2026-09-17 | Abierto |
| [A-005](#a-005---hay-acceso-a-personas-que-conocen-el-proceso-real-ademas-del-patrocinador) | Hay acceso a personas que conocen el proceso real, ademas del patrocinador | 2026-09-17 | Abierto |
| [A-008](#a-008---las-condiciones-de-uso-de-baloto-permiten-leer-su-historico-de-forma-automatica) | Las condiciones de uso de Baloto permiten leer su historico de forma automatica | 2026-09-17 | Abierto |
```

Ninguno de los tres cambio en esta sesion. `A-008` sigue en el camino critico del producto (lectura
del historico de Baloto); si resulta falso, el Integrador (`baloto.com`) no es viable como fuente
automatica y el diseño de `010_actors.md` tendria que revisarse. `A-004` y `A-005` condicionan que el
proceso de registro descrito en `005_discovery` sea representativo del real.

## 5. Siguiente tarea propuesta

`T-042` (`Baja`, `No bloqueante`, `Origen: session-closer`): anclar al commit `31ece08` (anterior a
esta sesion) o fechar como snapshot las seis ordenes de Contexto en `D-068`/`D-069`/`D-070`/`D-071`/
`D-072` que hoy ya no reproducen. Es el mismo pendiente mecanico que `T-035` y `T-037`, y conviene
tratarlas juntas la proxima vez que se abra `decisions.md`.

## 6. Que pedimos auditar

- **El Paso 2b-bis es nuevo y su control replica un algoritmo externo (github-slugger) en 14 lineas,
  sin la dependencia.** Se valido contra la implementacion de referencia sobre 1532 titulos reales de
  este repositorio, pero el propio archivo declara el limite: un titulo con caracteres que ninguno de
  esos 1532 usa podria derivar distinto. No hay garantia formal, solo empirica.
- **La tarea T-042 es una acumulacion, no una correccion.** Van tres tareas de esta misma forma
  (`T-035`, `T-037`, `T-042`) sin que ninguna se haya cerrado todavia. El patron de "diagnosticar antes
  del arreglo, en la misma entrada" que produce este pendiente es consistente con como este proyecto
  escribe sus decisiones (evidencia del estado roto, seguida de la correccion), pero cada vez que se
  repite dentro de una sesion que tambien la cierra, el Paso 2d del cierre la marca como no
  reproducible. Vale la pena que `manager` decida si ese patron de redaccion necesita una convencion
  explicita (por ejemplo, anclar la evidencia de Contexto al commit anterior en el momento de
  escribirla, en vez de dejarlo para el cierre).
- **No se corrio el acta de cierre de `005_discovery`.** La etapa tiene sus siete casillas cumplidas
  desde `S-017` y sigue sin el acta de `protocol-phase-exit`; este cierre no lo ejecuta porque no es su
  alcance, pero es el trabajo de fondo mas atrasado del proyecto.

## 7. Evidencia del Paso 2d

Orden (en su forma que reproducira anclada al commit, con el informe excluido):
```
git diff -U0 <hash>^ <hash> -- _persistence _audit ":(exclude)_audit/S-018.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Corrida hoy sobre el area de staging (antes de que el commit exista), con el informe excluido:
```
git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-018.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Recuento de lineas devueltas (la unica cifra de este paso):
```
$ git diff --cached -U0 -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
12
```

Lista completa, numerada:
```
 1	  $ grep -n '(#s-017' _persistence/progress.md | grep -oE 'l-018-+d-063'
 2	  $ node slug.mjs 'S-017 - `F-020` de `R-018` aceptado y corregido (`D-064`), tres pasadas mas de la cosecha (`L-016`/`L-017`/`L-018` → `D-063`/`D-065`/`D-067`), y se cierra `005_needs.md` (`D-066`)'
 3	  $ node .claude/checks/anchors.js _persistence/*.md _audit/*.md
 4	  $ grep -A7 '^### F-003' _audit/findings.md | grep -E '^\| (Estado|Cerrado en) \|'
 5	    $ node validate2.mjs _persistence/*.md _audit/*.md _phases/*.md _workflow/*.md _methodology/*.md _templates/*/*.md project.md CLAUDE.md
 6	  $ node --test .claude/checks/anchors.test.js 2>&1 | grep -E 'tests|pass|fail'
 7	  $ grep -E '^\| \[F-020\]' _audit/findings.md | awk -F'|' '{print $(NF-1)}'
 8	  $ diff <(… awk -F'|' '{… e=$(NF - 1) …}' …) <(… la ficha …) | head -1
 9	  $ grep -cE '^\|.*\| Sin confirmar \|' _persistence/techdebt.md
10	  $ grep -c "Sin resolver" _audit/S-017.md
11	  $ grep -c "Sin resolver" .claude/skills/protocol-close/SKILL.md
12	  $ grep -E '^\| \[L-' _persistence/lessons.md | grep '005_discovery' | grep -c 'Sin evaluar'
```

Las 12 lineas de la lista son las mismas 12 que cuenta la orden de arriba.

Reejecucion de cada una, hoy, y comparacion con lo publicado:

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `grep -n '(#s-017' ... \| grep -oE 'l-018-+d-063'` | No | Publicado `l-018-d-063`; hoy `l-018--d-063` — `D-068` ya corrigio el ancla en este mismo commit. Ver `T-042` |
| 2 | `node slug.mjs '...'` | No aplica | Script temporal, no versionado; no reproducible por construccion |
| 3 | `node .claude/checks/anchors.js ...` | No | Publicado `total anclas rotas: 6` (mas la lista); hoy `total anclas rotas: 0` — `D-068` ya corrigio las seis. Ver `T-042` |
| 4 | `grep -A7 '^### F-003' ... \| grep -E '^\| (Estado\|Cerrado en) \|'` | No | Publicadas 2 lineas; hoy 1 — `D-069` inserto la fila `Registrado en` en la ficha de `F-003`, desplazando `Cerrado en` fuera de la ventana de 7 lineas. Ver `T-042` |
| 5 | `node validate2.mjs ...` | No aplica | Script temporal, no versionado; no reproducible por construccion |
| 6 | `node --test .claude/checks/anchors.test.js ...` | Si | `ℹ tests 10 / ℹ pass 10 / ℹ fail 0`, identico |
| 7 | `grep -E '^\| \[F-020\]' ... \| awk -F'\|' '{print $(NF-1)}'` | No | Publicado ` Implementado`; hoy ` \`T-036\` y \`D-064\` ` — `D-069` desplazo la penultima columna. Ver `T-042` |
| 8 | `diff <(… awk ...) <(… la ficha …) \| head -1` | No aplica | Pseudocodigo ilustrativo con elipsis, no ejecutable literalmente |
| 9 | `grep -cE '^\|.*\| Sin confirmar \|' _persistence/techdebt.md` | No | Publicado `1`; hoy `0` — `D-070` ya corrigio `DT-005`. Ver `T-042` |
| 10 | `grep -c "Sin resolver" _audit/S-017.md` | Si | `0`, identico |
| 11 | `grep -c "Sin resolver" .claude/skills/protocol-close/SKILL.md` | No | Publicado `16`; hoy `17` — la propia `D-071` anadio la prohibicion nueva al 7c-bis en este mismo commit, que menciona la cadena una vez mas. Ver `T-042` |
| 12 | `grep -E '^\| \[L-' ... \| grep '005_discovery' \| grep -c 'Sin evaluar'` | No | Publicado `2`; hoy `0` — `D-072` ya clasifico `L-019`/`L-020` en este mismo commit. Ver `T-042` |

Salida de `uniq -d` sobre la lista completa (repetidas):
```
$ git diff -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
(vacio)
```

Ninguna orden de las doce esta repetida.

**NOTA DE CIERRE — commit de anclaje `ed64348`, informe sobre el commit de sesion `ed64348`.**

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal, anclado al commit de la sesion; no tiene que salir vacio):
```
$ for f in $(git ls-tree -r --name-only ed64348 _persistence _audit | grep -v '_audit/S-018.md'); do n=$(git show ed64348:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>'); [ "$n" != "0" ] && echo "$f: $n"; done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_persistence/decisions.md: 18
_persistence/tasks.md: 1
```

CONTROL (acotado a lo que este commit anadio, forma literal `$ git show <hash>:`; tiene que estar
vacio o solo nombrar archivos que este paso tenga autorizado a anclar):
```
$ for f in $(git diff --name-only ed64348^ ed64348 -- _persistence _audit ":(exclude)_audit/S-018.md"); do n=$(git diff -U0 ed64348^ ed64348 -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:'); [ "$n" != "0" ] && echo "$f: $n"; done
(vacio)
```

El CONTROL sale vacio: ningun archivo tocado en el commit de la sesion anadio una linea con la forma
`$ git show ` seguida del marcador sin resolver. Los marcadores que hacia falta anclar en esta sesion
viven en `decisions.md`, en el formato «Ordenes:»/«Salidas:» (sin prefijo `$ `), que el CONTROL no
reconoce por diseño — su patron
esta acotado a la forma antigua. Se anclaron igual, por fuera de ese patron, en los cinco criterios de
cierre de `D-068`-`D-072`: diez ordenes con `git show <hash>:` sustituidas por `git show ed64348:`
(desglose: `D-068` 2, `D-069` 3, `D-070` 2, `D-071` 2, `D-072` 1 — derivado con
`sed -n '/^### D-068 /,/^### D-069 /p' _persistence/decisions.md | grep -c 'git show ed64348:'` y su
misma forma para cada rango, suma 10). Las cinco reproducen: reejecutadas hoy contra `ed64348`, cada
una devuelve exactamente el numero que su «Salidas:» publica (verificado orden por orden en el propio
`decisions.md`).

No queda ninguna orden de `D-068`-`D-072` sin anclar. El CENSO sigue mostrando `<hash>` en entradas de
sesiones anteriores (`R-001`..`S-017`, y una linea en `_persistence/tasks.md` — la de `T-001`,
historica) y en `_persistence/decisions.md` (`18`, que incluye las menciones descriptivas de `<hash>`
como marcador generico, no solo ordenes sin resolver): esas quedan fuera del alcance de este cierre,
tal como declara la regla de no reescribir el pasado.

**CONTROL DE PROSA BORRADA — salida:**
```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | outside > /tmp/pa; outside < "$f" > /tmp/pd; echo "== $f =="; diff /tmp/pa /tmp/pd | grep '^<'; done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Sin lineas `<` en ninguno de los dos archivos: el anclaje no borro prosa.

**CONTROL DE SALIDA REPRODUCIDA — salida:**
```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /^[[:space:]]*```/{f=!f; next} c&&f&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }; for f in _persistence/decisions.md _persistence/tasks.md; do git show HEAD:"$f" | salidas > /tmp/sp; salidas < "$f" > /tmp/sa; echo "== $f =="; diff /tmp/sp /tmp/sa | grep -E '^[<>]'; done
== _persistence/decisions.md ==
  < D-068 | git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -c '^## Paso 2b-bis'
  < D-068 | git show <hash>:_persistence/progress.md | grep -cE '\(#s-0(07|08|10|15|16|17)---'
  > D-068 | git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -c '^## Paso 2b-bis'
  > D-068 | git show ed64348:_persistence/progress.md | grep -cE '\(#s-0(07|08|10|15|16|17)---'
  < D-069 | git show <hash>:_audit/findings.md | grep -c '^| Codigo | Hallazgo | Auditoria | Gravedad | Urgencia | Estado | Registrado en |'
  < D-069 | git show <hash>:_audit/findings.md | grep -c '^| Registrado en | la `T-XXX` o la `D-XXX`'
  < D-069 | git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'e=$(NF - 2)'
  > D-069 | git show ed64348:_audit/findings.md | grep -c '^| Codigo | Hallazgo | Auditoria | Gravedad | Urgencia | Estado | Registrado en |'
  > D-069 | git show ed64348:_audit/findings.md | grep -c '^| Registrado en | la `T-XXX` o la `D-XXX`'
  > D-069 | git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -cF 'e=$(NF - 2)'
  < D-070 | git show <hash>:_persistence/techdebt.md | grep -cE '\| Sin confirmar \|'
  < D-070 | git show <hash>:_persistence/techdebt.md | grep -cF 'Propuesta (pendiente del usuario)'
  > D-070 | git show ed64348:_persistence/techdebt.md | grep -cE '\| Sin confirmar \|'
  > D-070 | git show ed64348:_persistence/techdebt.md | grep -cF 'Propuesta (pendiente del usuario)'
  < D-071 | git show <hash>:_persistence/decisions.md | grep -cE 'NOTA 2026-09-17 — (la remision a|esa remision no resuelve)'
  < D-071 | git show <hash>:.claude/skills/protocol-close/SKILL.md | grep -cF 'no remite a «Sin resolver»'
  > D-071 | git show ed64348:_persistence/decisions.md | grep -cE 'NOTA 2026-09-17 — (la remision a|esa remision no resuelve)'
  > D-071 | git show ed64348:.claude/skills/protocol-close/SKILL.md | grep -cF 'no remite a «Sin resolver»'
  < D-072 | git show <hash>:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
  > D-072 | git show ed64348:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
== _persistence/tasks.md ==
```

🚨 **Este control no sale limpio, y se declara la razon en vez de forzarlo a limpio.** Su funcion
`salidas()` esta escrita para el formato antiguo `$ orden` seguido de su salida en la misma cerca, y
excluye del computo justo las lineas que empiezan por `$ `. Las cinco entradas de hoy usan el formato
mas reciente «Ordenes:»/«Salidas:» en cercas separadas, sin prefijo `$ ` en ninguna de las dos: el
filtro no tiene nada que excluir, asi que el control trata la propia linea de **orden** como si fuera
la salida, y esa linea cambio a proposito (es el propio anclaje: `<hash>` → `ed64348`). No es una
sustitucion de evidencia.

Verificado aparte, extrayendo solo el contenido de los bloques «Salidas:» (no «Ordenes:») de cada
entrada, antes y despues de este anclaje:
```
$ for d in D-068 D-069 D-070 D-071 D-072; do echo "=== $d antes ==="; git show HEAD:_persistence/decisions.md | awk -v d="$d" '$0 ~ "^### "d" "{p=1} p&&/Salidas:/{s=1;next} s&&/^[[:space:]]*```/{c=!c; if(!c){exit}; next} s&&c{print}'; echo "=== $d despues ==="; awk -v d="$d" '$0 ~ "^### "d" "{p=1} p&&/Salidas:/{s=1;next} s&&/^[[:space:]]*```/{c=!c; if(!c){exit}; next} s&&c{print}' _persistence/decisions.md; done
=== D-068 antes ===
     1
     6
=== D-068 despues ===
     1
     6
=== D-069 antes ===
     1
     1
     1
=== D-069 despues ===
     1
     1
     1
=== D-070 antes ===
     0
     3
=== D-070 despues ===
     0
     3
=== D-071 antes ===
     2
     1
=== D-071 despues ===
     2
     1
=== D-072 antes ===
     1
     1
     26:> **Versión: 7 · 2026-09-17** · 109 lecciones · 10 bloques
     ## main...origin/main
     0
=== D-072 despues ===
     1
     1
     26:> **Versión: 7 · 2026-09-17** · 109 lecciones · 10 bloques
     ## main...origin/main
     0
```

Las cinco «Salidas:» son byte-identicas antes y despues. Las lineas `📌 Ancladas por el Paso 7c-bis`
que se anadieron a `D-068`-`D-072` dicen «reproducen», y esa afirmacion se apoya en la reejecucion
real de cada orden anclada contra `ed64348` (seccion de arriba, BARRIDO DE ANCLAJE), no en este
control de texto — que para este formato no puede pronunciarse.

**SEGUNDA PASADA anclada del Paso 2e — salida:**
```
$ for f in $(git diff --name-only --diff-filter=d ed64348^ ed64348); do n=$(git show ed64348:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(vacio)
$ git diff --name-only --diff-filter=d ed64348^ ed64348 | wc -l
11
$ for f in $(git diff --name-only --diff-filter=d ed64348^ ed64348); do git show ed64348:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El recuento anclado (`11` archivos) coincide con la tabla de la seccion 8 mas los dos archivos que el
propio cierre anadio despues de esa tabla (`_audit/S-018.md`, `_audit/index.md`): `9 + 2 = 11`. Ningun
archivo del commit queda fuera de la cobertura de control de caracteres.

**CONTROL DE CIFRA ADYACENTE — salida:**
```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-018.md
_audit/S-018.md:97: `T-032` (`Alta`, `No bloqueante`) queda pendiente de decidir si `A-007`/`D-050` sustituye su criterio
_audit/S-018.md:98: literal («tras reiniciar Claude Code»). `T-037` y `T-042` son la misma clase de pendiente sobre
_audit/S-018.md:154: Ninguno de los tres cambio en esta sesion. `A-008` sigue en el camino critico del producto (lectura
_audit/S-018.md:222: Las 12 lineas de la lista son las mismas 12 que cuenta la orden de arriba.
_audit/S-018.md:258: Ninguna linea con caracteres de control en los 9 archivos que este commit lleva.
_audit/S-018.md:268: La tabla (implicita, vacia) cuadra con los dos: 9 archivos, 0 caracteres de control en total.
_audit/S-018.md:288: 6 lineas (`git diff -q` no imprime nada cuando coinciden, asi que cuenta como parte de las anteriores;
_audit/S-018.md:288: recuento derivado contando la salida de arriba: 6).
```

Lectura linea por linea: 97-98 citan codigos (`T-032`, `A-007`, `D-050`, `T-037`, `T-042`), no cifras
derivadas de un bloque de salida — no aplica. 154 cita `A-008`, mismo caso. 222 (`12`) coincide con el
recuento de la orden de la seccion 7 de este mismo informe. 258 y 268 (`9`) coinciden con el
`git diff --cached --name-only --diff-filter=d | wc -l` de la seccion 8, corrido antes del commit
sobre el area de staging. 288 (`6`) coincide con la salida de la seccion 9, contada arriba en la misma
seccion. Ninguna linea exige correccion.

**ORDEN DEL PASO 2d ANCLADA — salida:**
```
$ git diff -U0 ed64348^ ed64348 -- _persistence _audit ":(exclude)_audit/S-018.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
12
```

Coincide con el recuento publicado en la seccion 7 (`12`).

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'); [ "$n" -gt 0 ] && echo "$f: $n"; done
(vacio)
```

Ninguna linea con caracteres de control en los 9 archivos que este commit lleva.

Los dos contrastes de la tabla:
```
$ git diff --cached --name-only --diff-filter=d | wc -l
9
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

La tabla (implicita, vacia) cuadra con los dos: 9 archivos, 0 caracteres de control en total.

## 9. Evidencia del Paso 2f

```
ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
for d in .claude _phases _methodology _templates _workflow; do
  diff -rq --strip-trailing-cr "$ESQ/$d" "$d"
done
diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
```
```
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/agents/session-closer.md and .claude/agents/session-closer.md differ
Only in .claude: checks
Only in .claude: hooks
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/.claude/skills/protocol-close/SKILL.md and .claude/skills/protocol-close/SKILL.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_templates/005_discovery/015_stakeholders.md and _templates/005_discovery/015_stakeholders.md differ
Files C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS/_workflow/005_discovery.md and _workflow/005_discovery.md differ
```

6 lineas (`git diff -q` no imprime nada cuando coinciden, asi que cuenta como parte de las anteriores;
recuento derivado contando la salida de arriba: 6).

Esto **informa, no frena**: son promociones pendientes, no un fallo del cierre. `Only in .claude:
checks` es la carpeta nueva de esta sesion (`.claude/checks/anchors.js` y su test); ninguna linea es
`Only in` el arbol del esqueleto, asi que no hay hallazgo de sentido inverso.
