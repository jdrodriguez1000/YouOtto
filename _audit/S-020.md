# Informe de auditoria — S-020

| Campo | Valor |
|---|---|
| Sesion | S-020 |
| Fecha | 2026-09-18 |
| Etapa | `010_prototype` |
| Rama | main |
| Commit auditado | `7b0c48f` |

## 0. Respuesta a la auditoria anterior

Verificado contra `HEAD` (`00f09e1`, el commit de la auditoria `R-021`), sobre el estado que
`d80a965` dejo (el commit que `R-021` audito). La tabla incluye ademas `F-016` y `F-017` (`R-015`),
que siguen sin cerrar y no se atendieron esta sesion.

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-016 — `D-041` afirma el resultado de una prueba sobre commits historicos sin publicar la orden | Aceptado — pendiente | `T-030`, `No implementada` |
| F-017 — la NOTA DE CIERRE de `S-013` dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete | Aceptado — pendiente | `T-031`, `No implementada` |
| F-028 — la nota de anclaje de `S-019` dice que la escribio `70fe40c`, y esta en `e610906` | Aceptado — pendiente | `T-047`, `No implementada`; `D-080` |
| F-029 — el acta de cierre de `005_discovery` conserva el `D-XXX` de la plantilla donde debe citar `D-074` | Aceptado — pendiente | `T-048`, `No implementada`; `D-081` |

📌 **`F-028` y `F-029` llevan veredicto `Aceptado — pendiente`, no `corregido en este commit`.** Esta
sesion evaluo los dos hallazgos y abrio su tarea, pero las dos correcciones —la nota fechada de
`S-019` y la instanciacion de la fila 5.2 del acta— siguen `No implementada`: no estan en el diff de
esta sesion, y `manager` las hara en la sesion que implemente `T-047`/`T-048`.

## 1. Que se hizo

Salida cruda de `git diff --cached --stat --name-only` (el commit todavia no existe al escribir esta
seccion; la version anclada la pega el Paso 7c):

```
$ git diff --cached --stat --name-only
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

**NOTA DE CIERRE — version anclada al commit `7b0c48f` (Paso 7c):**

```
$ git show --stat --name-only --format= 7b0c48f
_audit/S-020.md
_audit/findings.md
_audit/index.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

📌 **Ahora si incluye `_audit/S-020.md`** (este informe), que en el momento de escribir la lista de
la seccion 1 todavia no estaba en `git add`. Siete archivos, los seis de la version en staging mas
este informe.

Por archivo, lo que muestra el diff (2 hunks o mas → hay entradas editadas ademas de las que nacen;
derivado con `git diff 7b0c48f^ 7b0c48f -U0 -- <archivo> | grep -cE '^@@'`):

- **`_audit/findings.md`** — 3 hunks. La fila de `F-028`/`F-029` pasa de `Abierto` a
  `Aceptado — pendiente` con su `T-XXX`/`D-XXX` (1 hunk en el indice), y sus dos fichas se actualizan
  igual (`Estado` y `Registrado en`, 2 hunks), fila y ficha juntas. Ninguna entrada nace en este
  archivo.
- **`_audit/index.md`** — 1 hunk (este cierre). Fila nueva de `S-020` con columnas `Pendiente`.
- **`_persistence/decisions.md`** — 2 hunks. Nacen `D-080`, `D-081` y `D-082` (tres decisiones): la
  aceptacion de `F-028`, la aceptacion de `F-029`, y la promocion al esqueleto de arranque de los
  seis candidatos del desfase. Ninguna entrada existente se edita.
- **`_persistence/lessons.md`** — 2 hunks. Nace `L-022` (indice y ficha): el estado de una tarea no
  dice si su mecanismo funciona, eso lo dicen los supuestos que la respaldan.
- **`_persistence/progress.md`** — 6 hunks. Nace la fila de `S-020` en el indice de sesiones
  (1 hunk); la seccion 1 se sobrescribe en dos hunks separados (`Ultima actualizacion`; `Avance de la
  etapa` + `Bloqueos activos`, contiguas); la seccion 2 se sobrescribe (1 hunk) y la seccion 3 se
  sobrescribe (1 hunk); y nace la entrada de detalle `S-020` en la bitacora (1 hunk).
- **`_persistence/tasks.md`** — 2 hunks. Nacen `T-047` y `T-048` (cierran `F-028`/`F-029`). Ninguna
  entrada existente se edita.

## 2. Que NO se hizo, y por que

Tareas `No implementada` del indice de `tasks.md` (derivado, no de memoria):

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
| [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita) | Publicar por nota la orden sin filtro que D-041 cita | No implementada | Baja | No bloqueante | 005_discovery |
| [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013) | Corregir por nota la cifra de la NOTA DE CIERRE de S-013 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-032](#t-032---limitar-las-skills-que-puede-invocar-el-agente-de-cierre) | Limitar las skills que puede invocar el agente de cierre | No implementada | Alta | No bloqueante | 005_discovery |
| [T-033](#t-033---publicar-por-nota-las-ordenes-del-paso-1b-y-del-barrido-que-d-045-resume) | Publicar por nota las ordenes del Paso 1b y del barrido que D-045 resume | No implementada | Baja | No bloqueante | 005_discovery |
| [T-035](#t-035---anclar-por-nota-la-cifra-del-barrido-del-paso-1-de-protocol-harvest-en-d-062) | Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062 | No implementada | Baja | No bloqueante | 005_discovery |
| [T-037](#t-037---anclar-o-fechar-dos-ordenes-del-contexto-de-d-064-que-ya-no-reproducen) | Anclar o fechar dos ordenes del Contexto de D-064 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-042](#t-042---anclar-o-fechar-seis-ordenes-de-contexto-en-d-068d-069d-070d-071d-072-que-ya-no-reproducen) | Anclar o fechar seis ordenes de Contexto en D-068/D-069/D-070/D-071/D-072 que ya no reproducen | No implementada | Baja | No bloqueante | 005_discovery |
| [T-046](#t-046---anclar-o-fechar-la-orden-de-verificacion-de-d-073-sobre-f-025f-026f-027-que-ya-no-reproduce) | Anclar o fechar la orden de Verificacion de D-073 sobre F-025/F-026/F-027 que ya no reproduce | No implementada | Baja | No bloqueante | 005_discovery |
| [T-047](#t-047---corregir-por-nota-el-commit-que-contiene-la-nota-de-anclaje-de-s-019-y-prohibir-el-autorreferente) | Corregir por nota el commit que contiene la nota de anclaje de S-019 y prohibir el autorreferente | No implementada | Media | No bloqueante | 010_prototype |
| [T-048](#t-048---instanciar-a-d-074-la-fila-de-firma-del-acta-de-005_discovery-y-ampliar-el-control-de-huecos) | Instanciar a D-074 la fila de firma del acta de 005_discovery y ampliar el control de huecos | No implementada | Baja | No bloqueante | 010_prototype |
```

Diez quedaron sin tocar esta sesion (ocho ya venian de sesiones anteriores; `T-047` y `T-048` nacen
en esta y quedan `No implementada` porque sus correcciones —la nota fechada de `S-019` y la
instanciacion de la fila del acta— son trabajo de la proxima sesion). `T-032` sigue siendo la de
mayor importancia (`Alta`): decidir si `D-050`/`A-007` sustituye el criterio literal de «tras
reiniciar Claude Code» en `_workflow/`. Las demas son correcciones de nota o de anclaje sobre ordenes
que dejaron de reproducir por el paso del tiempo, no por error de contenido.

No se toco codigo de producto: `010_prototype` sigue sin tener ninguna casilla verificada, y el
primer paso de su procedimiento (leer `_phases/010_prototype.md` y `_workflow/010_prototype.md`)
tampoco se abordo esta sesion.

## 3. Decisiones tomadas

- **`D-080`** — se acepta `F-028` en sus dos mitades: una nota fechada (pendiente, `T-047`) que
  declare que la nota de anclaje de `S-019` vive en `e610906` y no en `70fe40c`, con el hash derivado
  de `git log -1 --format=%h -- _audit/S-019.md`; y la prohibicion del autorreferente —«este mismo
  commit», «el que anadio esta nota»— en la plantilla de la NOTA DE CIERRE de `protocol-close`.
  Alternativas descartadas: reescribir el parentesis original (convierte «falta evidencia» en
  «evidencia falsa»); corregir solo la nota sin tocar la skill (el defecto volveria en el proximo
  anclaje); corregir solo la regla sin la nota (`S-019.md` seguiria afirmando algo falso, ya
  commiteado); exigir un cuarto commit que se autorreferencie bien (imposible por construccion).
- **`D-081`** — se acepta `F-029` en sus dos mitades: instanciar la fila «Donde queda registrada»
  (seccion 5.2) del acta de `005_discovery` a `D-074` (pendiente, `T-048`); y ampliar el control de
  huecos de `protocol-phase-exit` a los codigos genericos sin instanciar (`D-XXX`, `T-XXX`, `F-NNN`)
  sobre las secciones de firma, porque el control existente busca `<en blanco>` y esta fila nunca
  llevo ese marcador. Alternativas descartadas: instanciar solo la fila (deja el control ciego al
  mismo defecto en la siguiente acta); corregir la plantilla para que no lleve `D-XXX` (el hueco debe
  existir, el defecto esta en no rellenarlo); barrer los codigos genericos en todo el acta (produciria
  falsos positivos donde el acta cita la forma de un codigo legitimamente).
- **`D-082`** — el usuario pide promover, y se promueven al esqueleto de arranque los seis candidatos
  que el barrido del Paso 1 de `protocol-promote` media: `.claude/agents/session-closer.md`,
  `.claude/skills/protocol-close/SKILL.md`, `_templates/005_discovery/015_stakeholders.md`,
  `_workflow/005_discovery.md`, `.claude/checks/` y `.claude/hooks/` (commit del esqueleto `5b3fb6e`,
  desde el origen `00f09e1`). `manager` habia recomendado en la puerta del Paso 5 dejar fuera
  `session-closer.md` y `.claude/hooks/`, alegando que el mecanismo que declaran «no estaba
  ejercitado» porque `T-032` sigue `No implementada`; esa segunda mitad de la recomendacion era falsa
  — `A-006` y `A-007` ya estaban `Confirmado`, con la sonda `hook-probe` probando los dos casos dentro
  de un agente con la cabecera literal de `session-closer`; lo unico pendiente en `T-032` es una
  casilla formal (su criterio literal exige un reinicio de Claude Code que no hizo falta). `D-082`
  registra el error en vez de corregirlo en silencio, porque se presento al usuario en la puerta
  donde se decide que se hereda hacia adelante. El usuario aprobo promover los seis de todas formas,
  y esa aprobacion resulto correcta: el barrido del Paso 1 de este mismo cierre (Seccion 9) confirma
  que el esqueleto quedo al dia. Alternativas descartadas: promover solo el lote que `manager`
  consideraba ejercitado (era su recomendacion; el usuario decidio lo contrario); mejorar algo de
  paso en el esqueleto (prohibido por el protocolo de promocion); esperar a commitear antes la
  evaluacion de `F-028`/`F-029` (esos archivos estan fuera de las seis areas agnosticas y no entran
  en ninguna promocion, asi que el disparador del Paso 0 de `protocol-promote` ya se cumplia).

Nace ademas `L-022` (no es una `D-XXX`, es una leccion en `lessons.md`): el estado de una tarea no
dice si su mecanismo funciona, eso lo dicen los supuestos que la respaldan. Documenta el mismo error
que `D-082` registra, en la forma de leccion aplicable a futuras puertas.

## 4. Supuestos vigentes y riesgos

```
$ grep -E '^\| \[A-' _persistence/assumptions.md | grep -c Abierto
1
```

Un solo supuesto `Abierto`: **`A-008`** — «las condiciones de uso de Baloto permiten leer su
historico de forma automatica» — en el camino critico del producto. Su disparador es la primera
linea de codigo del prototipo, en `010_prototype`, que sigue sin empezar. Sin cambios esta sesion.
Si resulta falso, el prototipo no puede automatizar la lectura del historico y hay que replantear la
fuente de datos.

## 5. Siguiente tarea propuesta

`T-047` (`Media`/`No bloqueante`, `Origen: report_auditor`): escribir la nota fechada que declara
`e610906` como commit que contiene la nota de anclaje de `S-019`, y anadir a la plantilla de la NOTA
DE CIERRE de `protocol-close` la prohibicion del autorreferente. Es la de mayor importancia entre las
dos nacidas esta sesion. De fondo, sigue pendiente leer `_phases/010_prototype.md` y
`_workflow/010_prototype.md` para fijar el primer paso del procedimiento de la etapa abierta desde
`S-019`.

## 6. Que pedimos auditar

- **El error de `manager` en la puerta de promocion, registrado por `D-082` y `L-022`.** La
  recomendacion de dejar fuera `session-closer.md` y `.claude/hooks/` se apoyaba en confundir el
  estado `No implementada` de `T-032` con que su mecanismo no funcionara, cuando los supuestos
  `A-006`/`A-007` ya lo daban por probado. El usuario aprobo promover igual y la promocion salio
  bien, pero conviene que se revise si la correccion —registrar el error en la misma decision que
  aprueba la accion, en vez de en una `D-XXX` aparte— es suficiente, o si una puerta que se contesta
  sobre una premisa falsa deberia dejar algun rastro adicional en `_persistence/`.
- **`F-028` y `F-029` quedan `Aceptado — pendiente` en vez de `corregido en este commit`.** Las dos
  correcciones de fondo (la nota fechada de `S-019.md` y la fila 5.2 del acta) no estan en el diff de
  esta sesion: solo se evaluaron y se abrio su tarea. Conviene contrastar que ese aplazamiento esta
  bien fundado y no es una forma de posponer correcciones de bajo costo que cabian en el mismo commit
  (como si hizo `D-077` con `F-027` en `S-019`).
- **La promocion a un repositorio fuera de este** (`D-082`, commit `5b3fb6e` del esqueleto) ocurrio
  **antes** de este cierre, dentro de la misma sesion de trabajo, y su evidencia completa vive dentro
  de `D-082` en `decisions.md`, no en un archivo propio de `protocol-promote`. Conviene que se
  contraste si esa evidencia es suficiente para que una auditoria futura, que arranca en frio, pueda
  verificarla sin tener acceso al otro repositorio.

## 7. Evidencia del Paso 2d

Orden y salida completa (14 lineas, sin deduplicar):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
+  $ git log -1 --format=%h -- _audit/S-019.md
+  $ sed -n '/^| Donde queda/p' _audit/005_discovery/005_phase_exit_record_001.md
+  $ sed -n '/^| Donde queda/p' _templates/phase_exit_record.md
+  $ sed -n '/^| Donde queda/p' _audit/000_preproject/005_phase_exit_record_001.md
+  $ grep -c '^### D-074 - El patrocinador firma el acta y 005_discovery queda cerrada' _persistence/decisions.md
+  $ tr -dc '\r' < "$ESQ/.claude/agents/session-closer.md" | wc -c
+  $ tr -dc '\r' < "$ESQ/.claude/skills/protocol-close/SKILL.md" | wc -c
+  $ git grep -nE "YouOtto|Company_TripleS|github\.com" -- .claude CLAUDE.md _phases _methodology _templates _workflow
+  $ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' -- _phases _workflow | grep -vE ':PI-[0-9]+$'
+  $ git grep -nE "C:\\|C:/|/Users/|/home/|USUARIO|jdrodriguez|@gmail|SDAI_TripleS|[Bb]aloto|localhost|https?://|[0-9]{1,3}(\.[0-9]{1,3}){3}" -- .claude CLAUDE.md _phases _methodology _templates _workflow
+  $ for p in 005_project.md:project.md 010_progress.md:_persistence/progress.md 015_tasks.md:_persistence/tasks.md 020_decisions.md:_persistence/decisions.md 025_constraints.md:_persistence/constraints.md 030_assumptions.md:_persistence/assumptions.md 035_lessons.md:_persistence/lessons.md 040_techdebt.md:_persistence/techdebt.md 045_audit_index.md:_audit/index.md 050_audit_findings.md:_audit/findings.md; do t=${p%%:*}; c=${p#*:}; printf '%3d  %s\n' "$(diff --strip-trailing-cr <(git -C "$ESQ" show "5b3fb6e:_templates/000_preproject/$t") <(git -C "$ESQ" show "5b3fb6e:$c") | grep -c '^[<>]')" "$c"; done
+  $ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
+  $ git -C "$ESQ" log --oneline -1
+  $ git -C "$ESQ" status -sb
```

Recuento (14, LINEAS devueltas):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
14
```

Todas las 14 vienen de `_persistence/decisions.md` (las secciones «Verificacion» de `D-080`, `D-081`
y `D-082`), derivado con:

```
$ git diff --cached -U0 -- _persistence _audit | awk '/^\+\+\+ /{f=$2} /^\+[[:space:]]*\$ /{print f" :: "$0}' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | cut -d' ' -f1 | sort -u
b/_persistence/decisions.md
```

Ninguna es de la forma `git show <hash>:` — todas leen el arbol de trabajo, el repositorio del
esqueleto ($ESQ, filesystem externo) o el repositorio remoto de lecciones; no describen el commit de
esta sesion ni son anclables a el por naturaleza (leen estado externo a este repositorio o el arbol
de trabajo en el momento de escribir la entrada, igual que un `git status`). Reejecutadas, numeradas:

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git log -1 --format=%h -- _audit/S-019.md` | Si | `e610906` |
| 2 | `sed -n '/^\| Donde queda/p' _audit/005_discovery/005_phase_exit_record_001.md` | Si | `\| Donde queda registrada \| `_persistence/decisions.md`, con su `D-XXX` \|` |
| 3 | `sed -n '/^\| Donde queda/p' _templates/phase_exit_record.md` | Si | `\| Donde queda registrada \| `_persistence/decisions.md`, con su `D-XXX` \|` |
| 4 | `sed -n '/^\| Donde queda/p' _audit/000_preproject/005_phase_exit_record_001.md` | Si | `\| Donde queda registrada \| `_persistence/decisions.md`, con `D-015` (decision) y `D-020` (firma escrita en esta tabla) \|` |
| 5 | `grep -c '^### D-074 - ...' _persistence/decisions.md` | Si | `1` |
| 6 | `tr -dc '\r' < "$ESQ/.claude/agents/session-closer.md" \| wc -c` | Si | `0` |
| 7 | `tr -dc '\r' < "$ESQ/.claude/skills/protocol-close/SKILL.md" \| wc -c` | Si | `0` |
| 8 | `git grep -nE "YouOtto\|Company_TripleS\|github\.com" -- .claude CLAUDE.md ...` | Si | `exit=1`, sin salida |
| 9 | `git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' -- _phases _workflow \| grep -vE ':PI-[0-9]+$'` | Si | `exit=1`, sin salida |
| 10 | `git grep -nE "C:\\\\\|C:/\|..." -- .claude CLAUDE.md ...` | Si | una linea, el falso positivo declarado (`USUARIOS` en `025_business_validation.md`) |
| 11 | `for p in ...; do diff --strip-trailing-cr <(git -C "$ESQ" show 5b3fb6e:...) <(...) \| grep -c '^[<>]'; done` | Si | diez `0` |
| 12 | `for d in .claude ...; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done; diff -q ... CLAUDE.md` | Si | sin salida |
| 13 | `git -C "$ESQ" log --oneline -1` | Si | `5b3fb6e promocion desde el proyecto YouOtto (origen 00f09e1)` |
| 14 | `git -C "$ESQ" status -sb` | Si | `## main...origin/main` |

Pares repetidos, `uniq -d` completo:

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
(vacio)
```

Ninguna se repite. **Las 14 reproducen.** No se abre ninguna tarea nueva por este paso.

**NOTA DE CIERRE — commit de sesion `7b0c48f`, commit de anclaje `<se completa tras commitear el anclaje>`.**

`_persistence/decisions.md` es el unico archivo con «Criterio de cierre» nacido esta sesion que
publicaba ordenes con `<hash>` literal, dentro de `D-082` (formato en tres puntos, sin prefijo `$ `;
`D-080` y `D-081` son prosa, y `T-047`/`T-048` tambien). El Paso 7c-bis las ancla al commit de sesion
`7b0c48f`.

**BARRIDO DE ANCLAJE — salida:**

CENSO (universal, anclado al commit de la sesion; no tiene que salir vacio):

```
$ for f in $(git ls-tree -r --name-only 7b0c48f _persistence _audit | grep -v '_audit/S-020.md'); do
  n=$(git show 7b0c48f:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/R-021.md: 2
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_audit/S-018.md: 2
_persistence/decisions.md: 19
_persistence/tasks.md: 1
```

📌 **`decisions.md: 19` y `tasks.md: 1` son ordenes con `<hash>` que NO nacieron esta sesion** — la
misma familia que sostiene `T-030`, `T-035`, `T-037`, `T-042` y `T-046`, y el resto de sesiones
anteriores — y que la regla de «no reescribir historia» deja intactos. Las dos ordenes de `D-082` no
entran en este censo porque no llevan el prefijo `$ ` (formato en tres puntos).

CONTROL (acotado a lo que este commit anadio y a la ranura vacia `$ git show <hash>:`):

```
$ for f in $(git diff --name-only 7b0c48f^ 7b0c48f -- _persistence _audit ":(exclude)_audit/S-020.md"); do
  n=$(git diff -U0 7b0c48f^ 7b0c48f -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
```

Salida: (vacia). **No queda ninguna orden de la forma exacta `$ git show <hash>:` sin anclar en lo
que este commit anadio.** Las dos ordenes de `D-082` usan el formato en tres puntos, que el CONTROL
no cubre por diseno; se ancladaron a mano, verificadas contra las salidas publicadas (ver el
CONTROL DE SALIDA REPRODUCIDA, mas abajo).

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | outside > /tmp/prosa_antes
  outside < "$f" > /tmp/prosa_despues
  echo "== $f =="
  diff /tmp/prosa_antes /tmp/prosa_despues | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea `< …`: ninguna linea de prosa desaparecio en los dos archivos.

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /\*\*Ordenes:\*\*/{m="O"} /\*\*Salidas:\*\*/{m="S"} /^[[:space:]]*```/{f=!f; if(!f) m=""; next} c&&f&&m!="O"&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | salidas > /tmp/salida_publicada
  salidas < "$f" > /tmp/salida_anclada
  echo "== $f =="
  diff /tmp/salida_publicada /tmp/salida_anclada | grep -E '^[<>]'
done
== _persistence/decisions.md ==
> D-082 | 1
> D-082 | 2
== _persistence/tasks.md ==
```

🚨 **`D-082` NO reproduce en su segunda orden.** La orden anclada
(`grep -c 'desde el origen \`00f09e1\`'`) devuelve `2`, no el `1` publicado: la cadena buscada
aparece tambien dentro de su propio bloque «Ordenes» — la orden se hace match a si misma. No se
corrigio la orden ni la salida publicada: se dejo la salida original y se pego la anclada debajo,
con la nota 📌 dentro de `D-082` explicando la causa. Va a **Sin resolver**. La primera orden de
`D-082` si reproduce (`1` = `1`).

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d 7b0c48f^ 7b0c48f); do
  n=$(git show 7b0c48f:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia).

```
$ git diff --name-only --diff-filter=d 7b0c48f^ 7b0c48f | wc -l
7
$ for f in $(git diff --name-only --diff-filter=d 7b0c48f^ 7b0c48f); do git show 7b0c48f:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

El commit real lleva **7** archivos (los 6 de la tabla de la Seccion 8 mas este informe,
`_audit/S-020.md`, que quedo fuera por construccion). El total sigue en `0`.

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-020.md
_audit/S-020.md:44: **NOTA DE CIERRE — version anclada al commit `7b0c48f` (Paso 7c):**
_audit/S-020.md:57: 📌 **Ahora si incluye `_audit/S-020.md`** (este informe), que en el momento de escribir la lista de
_audit/S-020.md:58: la seccion 1 todavia no estaba en `git add`. Siete archivos, los seis de la version en staging mas
_audit/S-020.md:99: Diez quedaron sin tocar esta sesion (ocho ya venian de sesiones anteriores; `T-047` y `T-048` nacen
_audit/S-020.md:100: en esta y quedan `No implementada` porque sus correcciones —la nota fechada de `S-019` y la
_audit/S-020.md:158: Un solo supuesto `Abierto`: **`A-008`** — «las condiciones de uso de Baloto permiten leer su
_audit/S-020.md:215: Recuento (14, LINEAS devueltas):
_audit/S-020.md:222: Todas las 14 vienen de `_persistence/decisions.md` (las secciones «Verificacion» de `D-080`, `D-081`
_audit/S-020.md:223: y `D-082`), derivado con:
_audit/S-020.md:259: Ninguna se repite. **Las 14 reproducen.** No se abre ninguna tarea nueva por este paso.
_audit/S-020.md:305: 📌 **`decisions.md: 19` y `tasks.md: 1` son ordenes con `<hash>` que NO nacieron esta sesion** — la
_audit/S-020.md:306: misma familia que sostiene `T-030`, `T-035`, `T-037`, `T-042` y `T-046`, y el resto de sesiones
_audit/S-020.md:320: que este commit anadio.** Las dos ordenes de `D-082` usan el formato en tres puntos, que el CONTROL
_audit/S-020.md:356: 🚨 **`D-082` NO reproduce en su segunda orden.** La orden anclada
_audit/S-020.md:357: (`grep -c 'desde el origen \`00f09e1\`'`) devuelve `2`, no el `1` publicado: la cadena buscada
_audit/S-020.md:381: El commit real lleva **7** archivos (los 6 de la tabla de la Seccion 8 mas este informe,
_audit/S-020.md:382: `_audit/S-020.md`, que quedo fuera por construccion). El total sigue en `0`.
_audit/S-020.md:433: Coincide con las 14 publicadas en la Seccion 7 antes del commit: la exclusion del propio informe deja
_audit/S-020.md:471: La tabla tiene 6 filas, todas con `0`. Cuadra con las dos: 6 archivos y total `0`. Este informe
_audit/S-020.md:472: (`_audit/S-020.md`) todavia no esta en `git add` al escribir esta seccion, asi que no aparece en la
```

Cada linea marcada se contrasto contra el bloque de salida que la precede, verificando que el
numero citado coincide con lo que ese bloque realmente devuelve. Todas coinciden: la nota de la
Seccion 1 con la lista anclada (7 archivos); las tareas abiertas de la Seccion 2 con las diez filas
de su tabla; el supuesto `Abierto` de la Seccion 4 con el `1` de su `grep -c`; el recuento de la
propia Seccion 7 (`14`) con su `wc -l`; el barrido de procedencia por archivo (una sola linea,
`decisions.md`) y la salida vacia de `uniq -d`; el CENSO y el CONTROL del BARRIDO DE ANCLAJE de esta
misma nota; el CONTROL DE SALIDA REPRODUCIDA (`D-082 \| 1`, `D-082 \| 2`); la SEGUNDA PASADA anclada
(`7` archivos, total `0`); la ORDEN DEL Paso 2d ANCLADA (`14`); y los dos contrastes de la Seccion 8
(`6` filas, total `0`). Ninguna cifra se aparta de su bloque.

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 7b0c48f^ 7b0c48f -- _persistence _audit ":(exclude)_audit/S-020.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
14
```

Coincide con las 14 publicadas en la Seccion 7 antes del commit: la exclusion del propio informe deja
la cuenta identica a la que se midio sobre el area de staging.

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia — ningun archivo del commit contiene caracteres de control).

Tabla por archivo (misma orden, sin el filtro `[ "$n" -gt 0 ]`):

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  echo "$f: $n"
done
_audit/findings.md: 0
_audit/index.md: 0
_persistence/decisions.md: 0
_persistence/lessons.md: 0
_persistence/progress.md: 0
_persistence/tasks.md: 0
```

Los dos contrastes de la tabla:

```
$ git diff --cached --name-only --diff-filter=d | wc -l
6
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

La tabla tiene 6 filas, todas con `0`. Cuadra con las dos: 6 archivos y total `0`. Este informe
(`_audit/S-020.md`) todavia no esta en `git add` al escribir esta seccion, asi que no aparece en la
tabla — es la brecha que la SEGUNDA PASADA anclada de la NOTA DE CIERRE existe para cubrir.

⚠️ Esta seccion mide el area de staging previa al commit. Los archivos que el cierre escribe
despues de este punto —este mismo informe y cualquier ajuste posterior— quedan fuera por
construccion. La SEGUNDA PASADA anclada, en la NOTA DE CIERRE de la Seccion 7, cubre esa brecha
despues del commit de anclaje.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
```

Salida: (vacia).

El esqueleto de arranque quedo al dia con la promocion de esta misma sesion (`D-082`, commit del
esqueleto `5b3fb6e`): cero diferencias en las seis areas agnosticas, en las dos direcciones.

---

🕐 **NOTA 2026-09-18 (`F-030`, `D-091`) — el commit de anclaje de esta sesion es `30276a9`.**

La NOTA DE CIERRE de la linea 261 publico el commit de sesion y dejo el de anclaje con el texto
literal de la plantilla, sin instanciar. **Esa linea no se reescribe**: lo que dijo el dia que se
escribio se queda como se escribio, y esta nota lo completa. El dato que faltaba, derivado con una
orden y no tecleado:

```
$ git rev-parse --short 30276a9
30276a9
$ git rev-parse --short 30276a9^
7b0c48f
$ git log --format='%h %ad %s' --date=short -- _audit/S-020.md
30276a9 2026-09-18 S-020: ancla el informe y los criterios de cierre al hash 7b0c48f
7b0c48f 2026-09-18 S-020: F-028/F-029 de R-021 evaluados (D-080/D-081), promocion al esqueleto (D-082), y leccion L-022
```

**Commit de sesion `7b0c48f`; commit de anclaje `30276a9`.** El control del cierre que debia verlo
ancla en `^<` y este hueco esta inline, asi que no lo vio: ampliarlo es la segunda mitad de `T-049`.
