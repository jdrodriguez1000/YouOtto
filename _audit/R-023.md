# Auditoria R-023 — sobre S-021

| Campo | Valor |
|---|---|
| Auditoria | R-023 |
| Informe auditado | `_audit/S-021.md` |
| Commit auditado | 22747a9 |
| Fecha | 2026-09-18 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### a) Que commit se audita

```
$ git log -1 --format=%h -- _audit/S-021.md
6a7d7f7
$ git show 6a7d7f7:_audit/S-021.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `22747a9` |
$ git show --stat --name-only --format= 6a7d7f7 | grep -c .
1
$ git show --stat --name-only --format= 22747a9 | grep -c .
14
```

El cierre uso su Paso 7c: `6a7d7f7` es el **commit de anclaje** (un solo archivo) y `22747a9` el
**commit de sesion** (14 archivos). Se audita `22747a9`, que es el hash literal de la cabecera, y la
version del informe que se lee es la de `6a7d7f7` (la anclada).

### b) Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show 22747a9:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show 22747a9:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Los siete archivos, sin una sola diferencia.

### c) Carpetas del arbol contra las declaradas — una diferencia, con motivo escrito

```
$ diff <(git ls-tree -d --name-only 22747a9 | sed 's|$|/|' | sort) \
       <(git show 22747a9:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
10a11
> temporal/
```

`temporal/` es la unica diferencia y su razon esta escrita en `project.md` («Area de trabajo del
usuario. **Fuera del repositorio**, excluida en `.gitignore`»). La fila que esta sesion anadio
—`010_prototype/`— ya no aparece como diferencia porque la carpeta nacio en este mismo commit con
sus tres artefactos. No hay nada que reportar.

### d) Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" 22747a9 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

Las seis areas, limpias.

### e) Codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 22747a9 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(sin salida)
```

### f) El informe contra el diff

```
$ git show --stat 22747a9
 010_prototype/005_happy_path.md        | 159 ++++++++++
 010_prototype/010_participants.md      | 195 ++++++++++++
 010_prototype/012_facilitator_guide.md | 137 +++++++++
 _audit/S-020.md                        |  22 ++
 _audit/S-021.md                        | 321 ++++++++++++++++++++
 _audit/findings.md                     |   6 +-
 _audit/index.md                        |   1 +
 _persistence/assumptions.md            |  16 +-
 _persistence/constraints.md            |  19 ++
 _persistence/decisions.md              | 529 +++++++++++++++++++++++++++++++++
 _persistence/lessons.md                |  53 ++++
 _persistence/progress.md               | 104 +++++--
 _persistence/tasks.md                  |  76 +++++
 project.md                             |   1 +
 14 files changed, 1606 insertions(+), 33 deletions(-)
```

Los 14 archivos del commit estan enumerados uno a uno en la seccion 1 del informe, y ninguno queda
sin mencionar. Contraste de lo que cada uno afirma:

```
$ git diff 22747a9^ 22747a9 -- _persistence/decisions.md | grep -E '^\+### D-' | grep -oE 'D-[0-9]+' | tr '\n' ' '
D-083 D-084 D-085 D-086 D-087 D-088 D-089 D-090 D-091
$ git diff 22747a9^ 22747a9 -- _persistence/constraints.md | grep -E '^\+### '
+### C-006 - La lectura del sitio oficial de Baloto se limita a lo que su robots.txt autoriza
$ git diff 22747a9^ 22747a9 -- _persistence/lessons.md | grep -E '^\+### '
+### L-023 - Un documento legal enlazado desde un sitio no siempre regula ese sitio: se lee su alcance antes de citarlo
+### L-024 - Un defecto que reaparece por cuarta vez no es un descuido: es un control que no mira donde hace falta
$ git diff 22747a9^ 22747a9 -- _persistence/tasks.md | grep -E '^\+### '
+### T-049 - Ampliar el control de huecos del cierre a los marcadores inline de la NOTA DE CIERRE
+### T-050 - Anclar o corregir los numeros de linea de las verificaciones de D-086/D-087 que ya no reproducen
```

Nueve decisiones, una restriccion, dos lecciones y dos tareas — exactamente lo que la seccion 1
declara. `_persistence/assumptions.md` edita `A-008` (`Abierto` → `Confirmado`, en su fila de indice
y en su campo `Estado`) y le anade la nota fechada 2026-09-18 que cita `D-083`; no nace ningun
`A-XXX`. `_audit/findings.md` edita solo `F-030` (`Abierto`/`—` → `Aceptado — pendiente`/`T-049`,
`D-091`), en la fila **y** en la ficha, en la misma pasada. `project.md` anade una sola linea, la
fila de `010_prototype/` citando `D-084`. `_audit/S-020.md` recibe 22 lineas al final —la nota
fechada que declara `30276a9`— sin tocar la linea 261. Todo verificado sobre el diff, sin excepcion.

La seccion 0 no marca `Aceptado — corregido en este commit` en ningun hallazgo y **no usa
`Implementado` en ninguna fila**, que es el estado que no le corresponde a `manager`. Los cinco
hallazgos no cerrados que lista coinciden exactamente con los del archivo:

```
$ git show 22747a9:_audit/findings.md | sed -n '/^## Indice/,/^---/p' | grep -E '^\| \[F-' | awk -F'|' '{print $2, $7}' | grep -v "Implementado"
 [F-016](...)   Aceptado — pendiente
 [F-017](...)   Aceptado — pendiente
 [F-028](...)   Aceptado — pendiente
 [F-029](...)   Aceptado — pendiente
 [F-030](...)   Aceptado — pendiente
```

### g) Los tres puntos de la seccion 6 del informe

**Que no exista ningun `015_session_*.md` en el commit** —para que la desviacion de `D-088` quede
declarada antes del primer dato—:

```
$ git ls-tree -r --name-only 22747a9 010_prototype/
010_prototype/005_happy_path.md
010_prototype/010_participants.md
010_prototype/012_facilitator_guide.md
```

Confirmado: ninguna sesion corrida, ningun dato producido. La desviacion se declaro antes. Y
tampoco existe `010_prototype/app/`, como dice la seccion 2 del informe.

**Que los dos artefactos quedaran sellados en el estado que sus decisiones describen**, reejecutando
contra el commit y no contra el arbol:

```
$ git show 22747a9:010_prototype/005_happy_path.md | grep -n "<"
144:- [x] **No queda ni un solo `<` en el archivo.**
149:grep -n "<" 010_prototype/005_happy_path.md                 # debe no devolver nada
$ git show 22747a9:010_prototype/005_happy_path.md | grep -n "Guia de llenado"
145:- [x] La seccion «Guia de llenado» **esta borrada**.
150:grep -n "Guia de llenado" 010_prototype/005_happy_path.md   # debe no devolver nada
$ git show 22747a9:010_prototype/010_participants.md | grep -n "<"
174:- [x] `<` y «Guia de llenado» fuera de §1–§3.
182:- [ ] **No queda ni un solo `<` en el archivo.**
186:grep -n "<" 010_prototype/010_participants.md               # debe no devolver nada
$ git show 22747a9:010_prototype/010_participants.md | grep -n "Guia de llenado"
174:- [x] `<` y «Guia de llenado» fuera de §1–§3.
187:grep -n "Guia de llenado" 010_prototype/010_participants.md # debe no devolver nada
```

Los numeros que salen contra el commit son **exactamente los que `T-050` documenta como «Hoy da»**
(144/149, 145/150, 174/182/186, 174/187). No hay un tercer estado: el cierre reejecuto contra el
arbol que efectivamente se commiteo, y el sello se aplico sobre la version commiteada. Lo unico
desfasado son los numeros que `D-086`/`D-087` publicaron mientras el archivo seguia creciendo, que
es justo lo que `T-050` registra. **El punto que la seccion 6 pedia revisar se resuelve a favor del
informe.** Ademas, `D-086` no esconde que sus tres ordenes no devuelven vacio: publica la salida
cruda entera y explica linea por linea que lo que sale es de la plantilla y no del cuerpo del
artefacto.

**`T-032` sigue sin resolverse** — cierto, y sigue en el indice como `No implementada`; esta
registrada y no es un hallazgo nuevo.

### h) Tareas abiertas y esqueleto de arranque

```
$ git show 22747a9:_persistence/tasks.md | sed -n '/^## Indice/,/^---/p' | grep "No implementada" | grep -oE 'T-[0-9]+' | sort -u | tr '\n' ' '
T-030 T-031 T-032 T-033 T-035 T-037 T-042 T-046 T-047 T-048 T-049 T-050
```

Doce, las mismas doce que publica la seccion 2 del informe.

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
(sin salida)
```

La seccion 9 del informe reproduce: el esqueleto de arranque esta al dia, sin promociones
pendientes.

## 2. Hallazgos

### F-031 — La NOTA DE CIERRE de `S-021` repite el hueco de plantilla en el commit de anclaje, dentro de la misma sesion que acepto `F-030`

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_audit/S-021.md`, linea 287 (version del commit de anclaje `6a7d7f7`) |

- **Que se observo:** el commit de anclaje `6a7d7f7` escribio la NOTA DE CIERRE entera y dejo el
  commit de anclaje con el texto literal de la plantilla, sin instanciar y sin derivar.

  ```
  $ git show 6a7d7f7:_audit/S-021.md | grep -nE '<se completa|<pendiente|<en blanco|<rellenar'
  228:15	+  $ grep -nE '<se completa|<pendiente|<en blanco|<rellenar' _audit/S-020.md
  272:| 15 | `grep -nE '<se completa\|<pendiente\|<en blanco\|<rellenar' _audit/S-020.md` | Si | mismas dos lineas (123 y 261) que publica `D-091` |
  287:**NOTA DE CIERRE — commit de sesion `22747a9`, commit de anclaje `<se completa tras commitear el
  ```

  Las lineas 228 y 272 son citas legitimas del patron dentro de la evidencia del Paso 2d. El hueco
  real es la 287, y es el unico. No hay commit posterior que lo complete:

  ```
  $ git log --format='%h %ad %s' --date=short -- _audit/S-021.md
  6a7d7f7 2026-09-18 S-021: ancla el informe al hash 22747a9
  22747a9 2026-09-18 S-021: A-008 confirmado (D-083/C-006), Pasos 1-4 de 010_prototype sellados y decididos (D-084-D-090), F-030 evaluado (D-091)
  ```

  El control que debia verlo sigue sin ampliarse, porque la segunda mitad de `F-030` (`T-049`) esta
  `No implementada` y `.claude/` no se toco en este commit:

  ```
  $ git diff --name-only 22747a9^ 22747a9 -- .claude | wc -l
  0
  $ grep -n "^grep -nE '\^<' _audit/S-XXX.md" .claude/skills/protocol-close/SKILL.md
  1498:grep -nE '^<' _audit/S-XXX.md
  ```

- **Por que importa:** es la **quinta** instancia de la misma familia (`F-013`, `F-026`, `F-028`,
  `F-030`), y la primera que ocurre **despues** de que el mecanismo fallido quedara diagnosticado por
  escrito en `D-091` y `L-024`, en la misma jornada. El registro de `S-021` no dice donde quedo su
  anclaje, igual que no lo decia el de `S-020`. `Media` porque el registro queda incompleto justo en
  el punto que la nota existe para cubrir, sin afirmar nada falso; `No bloqueante` porque el dato es
  recuperable con una orden y nada posterior hereda un valor erroneo.
- **Que lo corregiria:** `T-049` arregla el **control** hacia adelante, pero **no corrige este
  archivo**: su criterio de cierre habla del control, no del rotulo de `S-021.md`. Hace falta ademas
  una nota fechada en `_audit/S-021.md` que declare `6a7d7f7` como commit de anclaje, con la salida
  de `git rev-parse --short` corrida, sin reescribir la linea 287. ⚠️ Es una recomendacion, no una
  orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | `Aceptado — pendiente` (sin cambio) | `T-030` sigue `No implementada`; `_persistence/decisions.md` no edita `D-041` en este commit |
| F-017 | `Aceptado — pendiente` (sin cambio) | `T-031` sigue `No implementada`; `_audit/S-013.md` no aparece en el `--stat` de `22747a9` |
| F-028 | `Aceptado — pendiente` (sin cambio) | `T-047` sigue `No implementada`; `_audit/S-019.md` no aparece en el `--stat` |
| F-029 | `Aceptado — pendiente` (sin cambio) | `T-048` sigue `No implementada`; `git diff --name-only 22747a9^ 22747a9 -- _audit/005_discovery` devuelve `0` lineas |
| F-030 | `Aceptado — pendiente` (sin cambio) | **corregido a medias.** La primera mitad si esta en el diff (nota fechada de 22 lineas al final de `_audit/S-020.md`, que declara `30276a9` con `git rev-parse` corrido). La segunda no: `protocol-close` sigue anclando en `^<` (linea 1498) y `.claude/` no se toco. Un hallazgo corregido a medias sigue abierto, y `F-031` es la prueba de que la mitad que falta seguia produciendo el defecto ese mismo dia |

## 4. Lo que NO se pudo comprobar

Ninguno de los controles quedo `SIN COMPROBAR`. Los cinco —indice ↔ detalle, carpetas declaradas,
fuga de datos propios, codigos instanciados y esqueleto de arranque— corrieron enteros y sobre el
commit auditado. Las ordenes 4 a 7 de la seccion 7 del informe (un `WebFetch` y tres `grep` sobre un
`flat.txt` temporal) **no son reejecutables por construccion**, y el propio informe las declara «No
anclable» con su motivo en vez de fingir que reproducen: eso es un tratamiento correcto, no un
control que falto.

## 5. Recomendaciones sin hallazgo

- **La plantilla `_templates/010_prototype/005_happy_path.md` trae una casilla incumplible por
  construccion.** El enunciado «No queda ni un solo `<` en el archivo» vive en una linea que contiene
  un `<`, y la orden que lo comprueba tambien, asi que la casilla nunca puede quedar satisfecha:

  ```
  $ git show 22747a9:_templates/010_prototype/005_happy_path.md | grep -n "No queda ni un solo\|Guia de llenado" | head -3
  124:- [ ] **No queda ni un solo `<` en el archivo.**
  125:- [ ] La seccion «Guia de llenado» **esta borrada**.
  144:## Guia de llenado — ⚠️ BORRAR esta seccion antes del sello
  ```

  En el artefacto sellado las dos quedaron `[x]` aunque su orden devuelve dos lineas cada una. **No
  lo abro como hallazgo** porque `D-086` publica la salida cruda completa y explica que lo que sale
  es de la plantilla y no del cuerpo —el registro no afirma nada falso sin decirlo—, y porque el
  defecto es de la plantilla, no de esta sesion. La mejora seria acotar el enunciado y la orden a las
  secciones del cuerpo, como ya hace `010_participants.md` en su casilla «`<` y «Guia de llenado»
  fuera de §1–§3».
- **`T-049` y `F-031` conviene trabajarlos en la misma pasada.** Uno arregla el control y el otro el
  archivo que el control no vio; separarlos arriesga cerrar el control y dejar el rotulo de `S-021`
  como quedo.
