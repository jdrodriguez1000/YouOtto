# Informe de auditoria — S-022

| Campo | Valor |
|---|---|
| Sesion | S-022 |
| Fecha | 2026-09-18 |
| Etapa | 010_prototype |
| Rama | main |
| Commit auditado | `e45185d` |

## 0. Respuesta a la auditoria anterior

**Verificado contra `HEAD` (`386159b`, la auditoria `R-023` sobre el commit `22747a9`), sobre el
estado que `22747a9` dejo.** La tabla lista **todos** los hallazgos de `_audit/findings.md` que no
estan `Implementado`, no solo los de `R-023`:

| Hallazgo | Veredicto | Evidencia / Razon |
|---|---|---|
| F-016 — D-041 afirma el resultado de una prueba sobre commits historicos sin publicar la orden | Aceptado — pendiente | `T-030`, `No implementada` (sin cambios esta sesion) |
| F-017 — La NOTA DE CIERRE de S-013 dice «dieciseis lineas» debajo de un CONTROL DE CIFRA ADYACENTE que publica diecisiete | Aceptado — pendiente | `T-031`, `No implementada` (sin cambios esta sesion) |
| F-028 — La nota de anclaje de S-019 dice que la escribio 70fe40c, y esta en e610906 | Aceptado — pendiente | `T-047`, `No implementada` (sin cambios esta sesion) |
| F-029 — El acta de cierre de 005_discovery conserva el D-XXX de la plantilla donde debe citar D-074 | Aceptado — pendiente | `T-048`, `No implementada` (sin cambios esta sesion) |
| F-030 — La NOTA DE CIERRE de S-020 deja el commit de anclaje como hueco de plantilla, sin instanciar y sin derivar | Aceptado — pendiente | `T-049`, `No implementada` (sin cambios esta sesion) |
| F-031 — La NOTA DE CIERRE de S-021 repite el hueco de plantilla en el commit de anclaje, dentro de la misma sesion que acepto F-030 | Aceptado — corregido en este commit | `D-092`, `T-051` (`Implementada`): `_audit/S-021.md` recibe una nota fechada que declara `6a7d7f7` como commit de anclaje, sin reescribir la linea 287 sellada |

`F-031` es el unico que se toca esta sesion, y se marca `Aceptado — corregido en este commit` porque
la nota fechada que lo cierra esta en el diff de `_audit/S-021.md` que este mismo commit lleva. Los
demas siguen exactamente como los dejo `R-023`.

## 1. Que se hizo

```
$ git diff --cached --stat --name-only
010_prototype/app/index.html
_audit/S-021.md
_audit/findings.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

Lista sobre el area de staging (el commit todavia no existia en el momento de escribir esta
seccion). Version anclada al commit de sesion `e45185d`:

```
$ git show --stat --name-only --format= e45185d
010_prototype/app/index.html
_audit/S-021.md
_audit/S-022.md
_audit/findings.md
_audit/index.md
_persistence/assumptions.md
_persistence/decisions.md
_persistence/lessons.md
_persistence/progress.md
_persistence/tasks.md
```

10 archivos en total, dos mas que los 8 que el area de staging mostraba: `_audit/S-022.md` (este
informe) y `_audit/index.md` (su fila en el tablero), que el propio cierre anade al final.

**Por archivo, que nacio y que se edito:**

- **`010_prototype/app/index.html` (nace):** el prototipo del Paso 4 de `010_prototype` — un unico
  archivo HTML sin build, sin servidor y sin dependencias externas, con la fila de seis numeros y la
  superbalota arriba y las razones encadenadas debajo. `D-093`.
- **`_audit/S-021.md` (edita):** se anade al final una nota fechada 2026-09-18 que declara `6a7d7f7`
  como commit de anclaje de `S-021`, citando `F-031` y `D-092`. No se reescribe la linea 287
  original.
- **`_audit/findings.md` (edita):** `F-031` (no nace ninguno): su fila de indice y su ficha pasan de
  `Abierto`/`—` a `Aceptado — pendiente`/`D-092 / T-051`.
- **`_persistence/assumptions.md`:** nace `A-009` (abierto): que los seis numeros arbitrarios no
  desvien la atencion del participante durante la sesion.
- **`_persistence/decisions.md`:** nacen `D-092`, `D-093` y `D-094`. No edita ninguna decision
  anterior; los dos hunks del diff son el indice y el bloque de detalle que las tres anaden al final
  del archivo.
- **`_persistence/lessons.md`:** nace `L-025`.
- **`_persistence/progress.md`:** sobrescribe las secciones 1, 2 y 3, y anade la entrada `S-022` a la
  seccion 5 (Bitacora) con su fila de indice.
- **`_persistence/tasks.md`:** nace `T-051` (`Origen: report_auditor`, ya `Implementada` en esta
  misma sesion: la correccion que exigia esta escrita en `_audit/S-021.md`, tambien en este commit).

## 2. Que NO se hizo, y por que

El Paso 5 de `010_prototype` —**correr la primera sesion con el participante**— no se ejecuto en
esta sesion: es la actividad principal de la proxima. El prototipo esta construido y aprobado por el
patrocinador (`D-093`/`D-094`), y a partir de ahora `_phases/010_prototype.md` §2 prohibe tocarlo
entre sesiones.

Tareas abiertas (`No implementada`), del indice de `tasks.md`:

```
$ sed -n '/^## Indice/,/^---/p' _persistence/tasks.md | grep "No implementada"
T-030, T-031, T-032, T-033, T-035, T-037, T-042, T-046, T-047, T-048, T-049, T-050
```

Ninguna nace en esta sesion (`T-051` nace `Implementada`, no `No implementada`). Son las mismas doce
que dejo `S-021`: ordenes de «Verificacion»/«Contexto» de sesiones anteriores que dejaron de
reproducir y esperan ancla o nota fechada (`T-030`, `T-031`, `T-033`, `T-035`, `T-037`, `T-042`,
`T-046`, `T-050`), la correccion pendiente de `F-028`/`F-029`/`F-030` (`T-047`, `T-048`, `T-049`), y
la decision formal sobre el criterio de `T-032` (Alta, No bloqueante).

## 3. Decisiones tomadas

- **`D-092` — `F-031` se acepta: el anclaje de `S-021` se completa por nota, y el control sigue
  siendo `T-049`:** se anade una nota fechada a `_audit/S-021.md` que declara `6a7d7f7` como commit
  de anclaje, sin reescribir la linea 287 sellada; no nace tarea nueva para el mecanismo, porque ya
  existe (`T-049`, abierta por `D-091`). **Alternativas descartadas:** reescribir la linea 287 con el
  hash correcto (convertiria «falta evidencia» en «hay evidencia falsa»); rechazarlo como duplicado
  de `F-030` (comparten mecanismo pero no archivo); rechazarlo por menor y esperar a `T-049`
  (dejaria `S-021` sin decir donde quedo su anclaje para siempre).
- **`D-093` — Lo que quedo elegido al construir el prototipo:** `010_prototype/app/index.html`, un
  solo archivo; los seis numeros en fila ascendente arriba (lo que se copia al tiquete) y la lista de
  razones encadenada debajo (lo que se comprueba, en el orden del metodo); numeros que respetan a
  simple vista las restricciones visibles del metodo (sin tres consecutivos, superbalota en rango,
  sin repetir); la confirmacion sustituye a la pantalla anterior y no guarda nada. **Alternativas
  descartadas:** los cinco numeros en orden del metodo tambien arriba (haria coincidir las dos
  lecturas, pero deja la fila del tiquete en un orden que el jugador no usa); las razones en orden
  ascendente (rompe el hilo del encadenamiento); anadir la fecha del sorteo (obligaria a mantenerla
  viva o a mostrar una falsa).
- **`D-094` — El patrocinador da el prototipo por bueno y el Paso 4 queda cerrado:** el patrocinador
  revisa el prototipo entero contra la tarea sellada (`D-085`) y responde «ok lo doy por bueno». Es
  la firma de negocio de la doble validacion, no la revision tecnica independiente (esa es del
  Gate 1). Queda congelado: ningun cambio en `010_prototype/app/index.html` entre sesiones.
  **Alternativas descartadas:** pedir una segunda pasada de revision antes de cerrar el paso (el
  patrocinador ya dio su respuesta); dar el paso por cerrado sin registrar la firma (una firma que no
  se escribe no existe para la auditoria).

## 4. Supuestos vigentes y riesgos

```
$ sed -n '/^## Indice/,/^---/p' _persistence/assumptions.md | grep -oE '\[A-[0-9]+\]' | grep -oE 'A-[0-9]+' | sort -u
A-001
A-002
A-003
A-004
A-005
A-006
A-007
A-008
A-009
```

Ocho estan `Refutado` (`A-001`, `A-002`) o `Confirmado` (`A-003` a `A-008`). **`A-009` nace `Abierto`
en esta sesion**: que los seis numeros arbitrarios del prototipo no desvien la atencion del
participante durante la sesion. Se apoya en el todo el prototipo y la ronda de cinco sesiones — si
falla, lo que se mide (tiempo hasta tener la combinacion y trazabilidad de cada numero) queda
contaminado en las cinco a la vez, porque la combinacion es la misma. Se refuta o se confirma al
cerrar la primera sesion, antes de correr la segunda.

**Riesgo que sigue vigente, sin codigo propio (ya lo dejo dicho `S-021`):** la anomalia de que el
unico participante es tambien el patrocinador, quien dirige el proyecto y revisara el prototipo, no
se resuelve. Sigue aceptada a sabiendas desde `005_discovery/020_hypothesis.md` §4 y §5.

**Riesgo nuevo de esta sesion:** `D-094` deja escrito que `manager` no ha visto el prototipo
renderizado en un navegador — la aprobacion visual es del patrocinador, y lo que `D-093` publica son
controles de ausencia sobre el archivo (`grep -c`), no una comprobacion de que la pagina se vea o se
comporte como se espera. No se le abre `A-XXX` porque no es un supuesto sobre el que se vaya a
construir mas (el Paso 5 ya no toca el archivo); es una limitacion de esta aprobacion, y queda dicha
aqui y en la seccion 6.

## 5. Siguiente tarea propuesta

**Correr la primera sesion — Paso 5 de `010_prototype`** (`_phases/010_prototype.md`), con la
facilitadora externa guiando desde `010_prototype/012_facilitator_guide.md` (`D-089`) y el
patrocinador observando sin responder preguntas del participante. Importancia Alta, Urgencia
Bloqueante (bloquea el resto de la ronda de cinco sesiones). Al cerrarla, revisar si `A-009` se
sostiene o queda refutado.

## 6. Que pedimos auditar

- **El patrocinador aprobo el prototipo sin que `manager` lo viera renderizado en un navegador**
  (`D-094`). Lo que `D-093` verifica son ausencias en el codigo fuente (sin `fetch`, sin
  `localStorage`, sin formularios), no que la pagina abra y se vea como el camino feliz sellado
  describe. Convendria que la auditoria abra `010_prototype/app/index.html` y confirme que renderiza
  sin errores.
- **`A-009` es un supuesto sobre el comportamiento humano que solo se puede refutar en vivo**, en la
  primera sesion real. No hay forma de adelantar esa verificacion desde el repositorio; si el
  supuesto falla, la salida que `A-009` ya declara es registrar la limitacion y llevarla al Gate 1,
  no tocar el prototipo.
- **La NOTA DE CIERRE de esta misma sesion volvera a dejar el commit de anclaje sin instanciar**,
  por la misma razon estructural que `F-030` y `F-031` ya documentaron (huevo-y-gallina: el commit de
  anclaje no existe todavia cuando se escribe la nota que lo nombra). `T-049` sigue siendo la
  correccion pendiente del mecanismo; esta sesion no lo cambia, solo lo repite una vez mas, con
  conocimiento de causa.
- **`D-092` no abre tarea nueva para el control**, apoyandose en que `T-049` ya existe. Convendria
  que la auditoria confirme que ese razonamiento no deja a `T-049` subestimada: a la fecha de esta
  sesion, el defecto que corrige ya ha aparecido en `S-020` y `S-021` seguidas.

## 7. Evidencia del Paso 2d

Orden (sobre el area de staging, antes del commit):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}'
```

Recuento de lineas devueltas: **17**

```
 1	+$ git rev-parse --short 6a7d7f7
 2	+$ git rev-parse --short 6a7d7f7^
 3	+$ git log --format='%h %ad %s' --date=short -- _audit/S-021.md
 4	+  $ git rev-parse --short HEAD
 5	+  $ grep -nE '<se completa|<pendiente|<en blanco|<rellenar' _audit/S-021.md
 6	+  $ git log --format='%h %ad %s' --date=short -- _audit/S-021.md
 7	+  $ grep -n "grep -nE '\^<' _audit/S-XXX.md" .claude/skills/protocol-close/SKILL.md
 8	+  $ grep -ncE "http://|https://|src=|<link|cdn|import |require\(" 010_prototype/app/index.html
 9	+  $ grep -ncE "localStorage|sessionStorage|indexedDB|fetch\(|XMLHttpRequest|cookie" 010_prototype/app/index.html
10	+  $ grep -ncE "setInterval|setTimeout|Date\(|performance\.now|cronom" 010_prototype/app/index.html
11	+  $ grep -ncE "<input|<form|<select|required|validat" 010_prototype/app/index.html
12	+  $ find 010_prototype/app -type f | wc -l
13	+  $ git log --diff-filter=A --format='%h %ad %s' --date=short -- 010_prototype/005_happy_path.md
14	+  $ git log --diff-filter=A --format='%h %ad' --date=short -- 010_prototype/app/
15	+  $ tail -22 _audit/S-021.md | grep -c '6a7d7f7'
16	+  $ sed -n '287p' _audit/S-021.md
17	+  $ grep -c 'rev-parse' _audit/S-021.md
```

`sort | uniq -d` sobre esa misma lista (pares repetidos):

```
$ git diff --cached -U0 -- _persistence _audit | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | sort | uniq -d
(sin salida)
```

Ninguna orden se repite en esta sesion.

**Reejecucion de cada linea, numerada por su posicion en la lista de arriba:**

| # | Orden | Reproduce | Nota |
|---|---|---|---|
| 1 | `git rev-parse --short 6a7d7f7` | Si | `6a7d7f7` |
| 2 | `git rev-parse --short 6a7d7f7^` | Si | `22747a9` |
| 3 | `git log --format='%h %ad %s' --date=short -- _audit/S-021.md` | Si | dos lineas, coinciden con lo publicado |
| 4 | `git rev-parse --short HEAD` | Si | `386159b`, coincide con lo publicado en `D-092` |
| 5 | `grep -nE '<se completa\|<pendiente\|<en blanco\|<rellenar' _audit/S-021.md` | Si | lineas 228, 272 y 287, coinciden con lo publicado en `D-092` |
| 6 | (identica a la 3) | Si | idem |
| 7 | `grep -n "grep -nE '^<' _audit/S-XXX.md" .claude/skills/protocol-close/SKILL.md` | Si | linea 1498, coincide |
| 8 | `grep -ncE "http\|https\|src=\|<link\|cdn\|import\|require\(" 010_prototype/app/index.html` | Si | `0` |
| 9 | `grep -ncE "localStorage\|sessionStorage\|indexedDB\|fetch(\|XMLHttpRequest\|cookie" 010_prototype/app/index.html` | Si | `0` |
| 10 | `grep -ncE "setInterval\|setTimeout\|Date(\|performance.now\|cronom" 010_prototype/app/index.html` | Si | `0` |
| 11 | `grep -ncE "<input\|<form\|<select\|required\|validat" 010_prototype/app/index.html` | Si | `0` |
| 12 | `find 010_prototype/app -type f \| wc -l` | Si | `1` |
| 13 | `git log --diff-filter=A --format='%h %ad %s' --date=short -- 010_prototype/005_happy_path.md` | Si | `22747a9 2026-09-18 ...`, coincide |
| 14 | `git log --diff-filter=A --format='%h %ad' --date=short -- 010_prototype/app/` | Si | vacio, coincide con lo publicado como «sin salida» (el archivo aun no esta commiteado cuando se escribio) |
| 15 | `tail -22 _audit/S-021.md \| grep -c '6a7d7f7'` | Si | `6` |
| 16 | `sed -n '287p' _audit/S-021.md` | Si | linea literal, coincide |
| 17 | `grep -c 'rev-parse' _audit/S-021.md` | Si | `8` |

**Conclusion de esta seccion:** las 17 ordenes reproducen contra lo publicado. Ninguna requiere
tarea nueva de `session-closer`.

---

**NOTA DE CIERRE — commit de sesion `e45185d`, commit de anclaje `<se completa tras commitear el
anclaje>`.**

Esta sesion **no anclo ningun bloque «Criterio de cierre» de `decisions.md` ni de `tasks.md`**: las
tres decisiones nacidas hoy (`D-092`-`D-094`) usan bloques «Verificacion» ya resueltos con su orden y
su salida pegadas en el momento de escribirlas (sin `<hash>` pendiente), y la unica tarea nacida hoy
(`T-051`) tiene su «Criterio de cierre» resuelto igual, con salida pegada. El **CONTROL** de abajo lo
confirma en cero. Por eso el commit de anclaje de esta sesion **solo toca este informe** — no anade
`_persistence/decisions.md` ni `_persistence/tasks.md`.

**BARRIDO DE ANCLAJE — salida:**

```
$ for f in $(git ls-tree -r --name-only e45185d _persistence _audit | grep -v '_audit/S-022.md'); do
  n=$(git show e45185d:"$f" | grep -cE '^[[:space:]]*\$ .*<hash>')
  [ "$n" != "0" ] && echo "$f: $n"
done
_audit/R-001.md: 3
_audit/R-002.md: 6
_audit/R-003.md: 2
_audit/R-007.md: 1
_audit/R-008.md: 1
_audit/R-009.md: 4
_audit/R-010.md: 1
_audit/R-012.md: 1
_audit/R-013.md: 2
_audit/R-015.md: 1
_audit/R-019.md: 1
_audit/R-021.md: 2
_audit/S-003.md: 5
_audit/S-004.md: 2
_audit/S-005.md: 2
_audit/S-006.md: 2
_audit/S-007.md: 2
_audit/S-008.md: 2
_audit/S-010.md: 1
_audit/S-014.md: 2
_audit/S-015.md: 2
_audit/S-016.md: 1
_audit/S-017.md: 3
_audit/S-018.md: 2
_persistence/decisions.md: 19
_persistence/tasks.md: 1

$ for f in $(git diff --name-only e45185d^ e45185d -- _persistence _audit ":(exclude)_audit/S-022.md"); do
  n=$(git diff -U0 e45185d^ e45185d -- "$f" | grep -cE '^\+[[:space:]]*\$ git show <hash>:')
  [ "$n" != "0" ] && echo "$f: $n"
done
(sin salida)
```

El CENSO (universal, heredado incluido) devuelve lineas con `<hash>` en 26 archivos — los mismos 26
que ya devolvia en `S-021`: ninguno nacio ni se edito en esta sesion en ese aspecto. El CONTROL
(acotado a lo que este commit anadio, con la ranura vacia `git show <hash>:`) devuelve **vacio**:
ninguna orden nacida hoy quedo con el hueco de anclaje sin resolver. **No queda ninguna orden de la
lista sin forma anclada** porque no se creo ninguna con esa forma esta sesion.

**CONTROL DE PROSA BORRADA — salida:**

```
$ outside() { awk '/^[[:space:]]*```/{f=!f; next} !f'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | outside > antes.txt
  outside < "$f" > despues.txt
  echo "== $f =="
  diff antes.txt despues.txt | grep '^<'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna linea de prosa desaparecio (no hay que correr este control con edicion real, porque el
Paso 7c-bis no toco ninguno de los dos archivos esta sesion; se publica igual, limpio).

**CONTROL DE SALIDA REPRODUCIDA — salida:**

```
$ salidas() { awk '/^### /{c=0} /^### [DT]-/{d=$2} /Criterio de cierre/{c=1} /^---$/{c=0} /\*\*Ordenes:\*\*/{m="O"} /\*\*Salidas:\*\*/{m="S"} /^[[:space:]]*```/{f=!f; if(!f) m=""; next} c&&f&&m!="O"&&!/^[[:space:]]*\$ /{sub(/^[[:space:]]+/,""); print d" | "$0}'; }
$ for f in _persistence/decisions.md _persistence/tasks.md; do
  git show HEAD:"$f" | salidas > sal_pub.txt
  salidas < "$f" > sal_ancl.txt
  echo "== $f =="
  diff sal_pub.txt sal_ancl.txt | grep -E '^[<>]'
done
== _persistence/decisions.md ==
== _persistence/tasks.md ==
```

Ninguna salida publicada se sustituyo (mismo motivo: no hubo edicion del Paso 7c-bis esta sesion).

**SEGUNDA PASADA anclada del Paso 2e — salida:**

```
$ for f in $(git diff --name-only --diff-filter=d e45185d^ e45185d); do
  n=$(git show e45185d:"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
(sin salida)

$ git diff --name-only --diff-filter=d e45185d^ e45185d | wc -l
10

$ for f in $(git diff --name-only --diff-filter=d e45185d^ e45185d); do git show e45185d:"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

Cero caracteres de control en los 10 archivos que el commit de sesion lleva de verdad. El recuento
coincide con la tabla de la seccion 8 (8 archivos en el area de staging, mas `_audit/S-022.md` y
`_audit/index.md`, que se escribieron despues y quedan cubiertos aqui, limpios).

**CONTROL DE CIFRA ADYACENTE — salida:**

```
$ awk '
  /^[[:space:]]*```/ { infence = !infence; if (!infence) { since=0; armed=1 } ; next }
  infence { next }
  armed { since++; if (since<=3 && $0 ~ /[0-9]/ && $0 !~ /^[[:space:]]*$/) print FILENAME":"NR": "$0; if (since>3) armed=0 }
' _audit/S-022.md
_audit/S-022.md:45: seccion). Version anclada al commit de sesion `e45185d`:
_audit/S-022.md:61: 10 archivos en total, dos mas que los 8 que el area de staging mostraba: `_audit/S-022.md` (este
_audit/S-022.md:99: Ninguna nace en esta sesion (`T-051` nace `Implementada`, no `No implementada`). Son las mismas doce
_audit/S-022.md:100: que dejo `S-021`: ordenes de «Verificacion»/«Contexto» de sesiones anteriores que dejaron de
_audit/S-022.md:146: Ocho estan `Refutado` (`A-001`, `A-002`) o `Confirmado` (`A-003` a `A-008`). **`A-009` nace `Abierto`
_audit/S-022.md:200: Recuento de lineas devueltas: **17**
_audit/S-022.md:309: El CENSO (universal, heredado incluido) devuelve lineas con `<hash>` en 26 archivos — los mismos 26
_audit/S-022.md:310: que ya devolvia en `S-021`: ninguno nacio ni se edito en esta sesion en ese aspecto. El CONTROL
_audit/S-022.md:330: Paso 7c-bis no toco ninguno de los dos archivos esta sesion; se publica igual, limpio).
_audit/S-022.md:346: Ninguna salida publicada se sustituyo (mismo motivo: no hubo edicion del Paso 7c-bis esta sesion).
_audit/S-022.md:364: Cero caracteres de control en los 10 archivos que el commit de sesion lleva de verdad. El recuento
_audit/S-022.md:365: coincide con la tabla de la seccion 8 (8 archivos en el area de staging, mas `_audit/S-022.md` y
_audit/S-022.md:411: Mismo recuento que el publicado en la seccion 7 sobre el area de staging: entre el commit de sesion
_audit/S-022.md:438: La tabla tiene 8 filas (una por archivo staged) y las 8 estan vacias (0 caracteres de control cada
_audit/S-022.md:439: una); el contraste 1 confirma que son 8 archivos, y el contraste 2 confirma que la suma es 0. Los dos
```

**Lectura linea por linea, contra el bloque que precede a cada una:**

- **45:** el digito viene de `e45185d` (el hash), no de una cifra de un bloque de salida. No aplica.
- **61:** «10 archivos en total, dos mas que los 8» — el bloque previo pega la salida anclada de
  `git show --stat --name-only --format= e45185d` (10 lineas) y la lista del area de staging previa
  tenia 8. OK.
- **99-100:** «doce» — el bloque previo pega la salida real de `sed -n ... | grep "No implementada"`,
  que tiene 12 filas (`T-030` a `T-050`). OK.
- **146:** «Ocho» — el bloque previo lista `A-001` a `A-009` (9 lineas); de ellas, 2 `Refutado` + 6
  `Confirmado` = 8, y `A-009` queda `Abierto` aparte. OK.
- **200:** «17» — es el recuento que abre el bloque siguiente; contado a mano contra las 17 lineas
  numeradas debajo (1 a 17). OK.
- **309-310:** «26 archivos — los mismos 26 que ya devolvia en S-021» — el bloque previo (BARRIDO,
  primera orden) lista exactamente 26 archivos con su recuento cada uno; el CENSO de `S-021` tambien
  publico 26. OK.
- **330:** el digito viene de «Paso 7c-bis», una referencia de paso, no una cifra de un bloque de
  salida. No aplica.
- **346:** mismo caso que 330: «Paso 7c-bis». No aplica.
- **364-365:** «10 archivos... 8 archivos en el area de staging» — el bloque previo (SEGUNDA PASADA,
  segunda orden) publica `10`; la seccion 8 de este mismo informe publica `8` filas. OK.
- **411:** el digito viene de «seccion 7», una referencia de seccion, no una cifra de un bloque de
  salida. No aplica.
- **438-439:** «8 filas... 8 estan vacias... contraste 1... contraste 2» — el bloque previo publica
  `8` y `0` en ese orden; la prosa los repite sin alterarlos. OK.

Ninguna de las catorce lineas obliga a corregir nada: las que citan una cifra la toman del bloque de
salida que las precede: las demas citan un numero de paso o de seccion, no una cifra de un bloque.

**ORDEN DEL PASO 2d ANCLADA — salida:**

```
$ git diff -U0 e45185d^ e45185d -- _persistence _audit ":(exclude)_audit/S-022.md" \
  | grep -E '^\+[[:space:]]*\$ ' \
  | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
17
```

Mismo recuento que el publicado en la seccion 7 sobre el area de staging: entre el commit de sesion
y el momento en que se corrio esa version no se anadio ni se quito ninguna orden de las que el Paso
2d vigila.

---

## 8. Evidencia del Paso 2e

```
$ for f in $(git diff --cached --name-only --diff-filter=d); do
  n=$(git show :"$f" | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]')
  [ "$n" -gt 0 ] && echo "$f: $n"
done
```

Salida: (vacia). Ningun archivo del area de staging contiene caracteres de control en el rango
buscado.

**Los dos contrastes de la tabla:**

```
$ git diff --cached --name-only --diff-filter=d | wc -l
8
$ for f in $(git diff --cached --name-only --diff-filter=d); do git show :"$f"; done | grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'
0
```

La tabla tiene 8 filas (una por archivo staged) y las 8 estan vacias (0 caracteres de control cada
una); el contraste 1 confirma que son 8 archivos, y el contraste 2 confirma que la suma es 0. Los dos
cuadran.

⚠️ Esta seccion mide el area de staging previa al commit: `_audit/S-022.md` (este informe) y la fila
de `_audit/index.md` se escriben despues y quedan fuera por construccion. Los cubre la SEGUNDA PASADA
anclada, en la NOTA DE CIERRE de la seccion 7.

## 9. Evidencia del Paso 2f

```
$ ESQ="C:\Users\USUARIO\Documents\Company_TripleS\SDAI_TripleS"
$ for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$ESQ/$d" "$d"; done
$ diff -q --strip-trailing-cr "$ESQ/CLAUDE.md" CLAUDE.md
```

Salida: (vacia). El esqueleto de arranque esta al dia con este repositorio en las seis areas
agnosticas: no hay promociones pendientes.
