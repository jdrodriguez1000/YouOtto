# Auditoria R-012 — sobre S-011

| Campo | Valor |
|---|---|
| Auditoria | R-012 |
| Informe auditado | `_audit/S-011.md` |
| Commit auditado | 079b0a4 |
| Fecha | 2026-09-16 |
| Veredicto | Con hallazgos (1) |

**Sobre el id.** El siguiente id libre en el arbol seria `R-011`, pero `R-011` y `F-012` ya se usaron en
el historial subido: `709050c` los creo y `9eb9d49` los revirtio. `project.md` («Ningun codigo se
reutiliza, en ningun archivo»; «Un id retirado queda retirado») impide reutilizarlos, asi que esta
auditoria es `R-012` y su hallazgo `F-013`. Del commit revertido solo se leyo el diff de
`findings.md`/`index.md` para saber que ids habia consumido; **no se uso como evidencia**: cada
afirmacion de abajo va con una orden corrida en esta pasada.

```
$ git log --all --format=%h -- _audit/R-011.md _audit/R-012.md
9eb9d49
709050c
$ git log --all -S'F-012' --format=%h -- _audit/findings.md
9eb9d49
709050c
$ git log --all -S'F-013' --format=%h -- _audit
(sin salida)
```

## 1. Que se comprobo

### 1.1 Fila del tablero y commit auditado

```
$ sed -n '/^## Tablero/,/^---/p' _audit/index.md | grep S-011
| `S-011.md` | S-011 | 2026-09-16 | Pendiente | Pendiente | Pendiente | - |
$ git log -1 --format=%h -- _audit/S-011.md
1358c3c
$ git show 1358c3c:_audit/S-011.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | 079b0a4 |
$ for h in 079b0a4 fdfca7b 1358c3c; do echo "== $h"; git show --stat --name-only --format= $h | grep -c .; done
== 079b0a4
8
== fdfca7b
2
== 1358c3c
1
```

Hash literal de cabecera `079b0a4`, distinto del derivado: se audita `079b0a4`. Sus 8 archivos
coinciden con la lista de la seccion 1 del informe.

### 1.2 Controles mecanicos sobre `079b0a4`

a) Indice ↔ detalle en `_persistence/` (orden del Paso 4a con `<hash>`=`079b0a4`):

```
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes.

b) Carpetas:

```
$ diff <(git ls-tree -d --name-only 079b0a4 | sed 's|$|/|' | sort) <(git show 079b0a4:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
1a2
> 005_discovery/
8a10
> temporal/
```

Las dos con motivo escrito: `temporal/` excluida en `.gitignore` (razon en `project.md` y en el propio
`.gitignore`); `005_discovery/` declarada por adelantado, con `D-002` («Carpetas de entregables
declaradas al arrancar cada etapa»). Sin hallazgo.

c) Fuga de datos propios:

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" 079b0a4 -- .claude CLAUDE.md _phases _methodology _templates _workflow | wc -l
0
```

d) Codigos instanciados:

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 079b0a4 -- _phases _workflow | grep -vE ':PI-[0-9]+$' | wc -l
0
```

### 1.3 Informe contra el diff: reejecucion de las evidencias publicadas

```
$ git diff a61e453 25b8e41 -- _persistence | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^\+(📌 \*\*Anclada|$)' | wc -l
0
$ git diff --stat a61e453 1add289 -- _persistence | tail -1
 1 file changed, 10 insertions(+)
$ for c in 9e169f3 f42388a 25b8e41; do echo "== $c $(git show --format= --name-only $c | tr '\n' ' ')"; done
== 9e169f3 _audit/S-008.md _persistence/decisions.md
== f42388a _audit/S-009.md _persistence/decisions.md
== 25b8e41 _audit/S-010.md _persistence/decisions.md
$ git show 079b0a4:_persistence/decisions.md | grep -cE '^(\| \[D-033\]|### D-033 )'
2
$ git show 079b0a4:_persistence/tasks.md | sed -n '/^### T-019 /,/^### T-020 /p' | grep -cE '🕐 \*\*Nota 2026-09-16 \(`F-011`, `D-034`\)|^\| Estado \| Implementada \|$'
2
$ git show 079b0a4:_audit/findings.md | grep -cE '^\| \[F-011\].*\| Aceptado — pendiente \|$|^\| Registrado en \| T-020, D-034 \|$'
2
$ git show 079b0a4:_persistence/techdebt.md | grep -E '^\| \[DT-004\]' | grep -c '| Implementada | Confirmada |'
1
$ git show 079b0a4:_persistence/techdebt.md | sed -n '/^### DT-004 /,$p' | grep -cE '^\| (Estado \| Implementada|Confirmacion \| Confirmada) \|$'
2
```

Contra el esqueleto de arranque (`ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"`,
solo lectura):

```
$ [ "$(git rev-parse 1add289:.claude/skills/protocol-close/SKILL.md)" = "$(git -C "$ESQ" rev-parse 7f4381e:.claude/skills/protocol-close/SKILL.md)" ] && echo igual || echo DISTINTO
igual
$ for f in .claude/skills/protocol-close/SKILL.md .claude/skills/protocol-start/SKILL.md _templates/000_preproject/020_decisions.md; do [ "$(git rev-parse 95c5cfd:$f)" = "$(git -C "$ESQ" rev-parse 447c2a0:$f)" ] && echo "igual $f" || echo "DISTINTO $f"; done
igual .claude/skills/protocol-close/SKILL.md
igual .claude/skills/protocol-start/SKILL.md
igual _templates/000_preproject/020_decisions.md
$ git -C "$ESQ" log --oneline -3; git -C "$ESQ" ls-remote origin refs/heads/main | cut -c1-7
7f4381e Promocion del andamiaje desde YouOtto (origen 1add289)
447c2a0 Promocion del andamiaje desde YouOtto (origen 95c5cfd)
4d20ce2 Promocion del andamiaje desde YouOtto (origen 4fad4dd)
7f4381e
$ git -C "$ESQ" show --stat --format='%h %s' 7f4381e | tail -2
 .claude/skills/protocol-close/SKILL.md | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)
$ F=.claude/skills/protocol-close/SKILL.md; diff <(git -C "$ESQ" show 447c2a0:$F) <(git show 1add289:$F)
831c831,832
<   nadie.
---
>   nadie. Y al reves: si el trabajo esta en el diff pero su criterio de cierre no reproduce, la tarea
>   **sigue en `No implementada`**, por hecho que parezca el trabajo.
$ git diff 1add289 079b0a4 --stat -- .claude CLAUDE.md _phases _methodology _templates _workflow | tail -1
(sin salida)
```

Todo reproduce lo publicado en `D-033`, `D-034`, `D-035`, la nota de `T-019` y la seccion 7 del
informe. La sesion no toco las seis areas agnosticas.

Sobre los tres puntos de la seccion 6 del informe:

- **`D-033`:** la lectura se sostiene (las tres ordenes de arriba). Que el aviso se repita en cada
  sesion con anclaje no es un defecto de este commit: va a la seccion 5.
- **`T-020` en `Implementada` en el mismo cierre:** el Paso 4 de `protocol-close` (a `079b0a4`, lineas
  ~826-832) pasa a `Implementada` la tarea cuyo trabajo esta en el diff y cuyo criterio reproduce. La
  nota en `T-019` esta en el diff (`git show 079b0a4 -- _persistence/tasks.md`) y el criterio de `D-034`
  al que remite `T-020` da `2`/`2` anclado (arriba). Bien fundado.
- **Encuadre de los dos pares de blobs:** `95c5cfd`↔`447c2a0` responde que promovio `T-019` (los tres
  archivos `igual`); `1add289`↔`7f4381e` responde que promovio `D-035` (`igual`, y la diferencia
  `447c2a0`→`1add289` es exactamente la frase de `D-028`). Las dos preguntas estan bien encuadradas.

Seccion 0 del informe: usa `Aceptado — corregido en este commit` (no `Implementado`) y la correccion
esta en el diff. `findings.md` a `079b0a4` deja `F-011` en `Aceptado — pendiente` en indice y ficha
(orden de arriba, `2`). Correcto.

### 1.4 Anclaje (commits `fdfca7b` y `1358c3c`)

```
$ git show 079b0a4:.claude/skills/protocol-close/SKILL.md | sed -n '1596,1598p'
**Ahora, y solo ahora, existe el dato que el informe no podia tener mientras se escribia: su propio
hash.** Cuatro sitios lo prometian y quedaban a medias hasta este paso. Se rellenan **los cuatro
juntos, en un unico commit de anclaje**:
$ git diff 079b0a4 fdfca7b -- _persistence/decisions.md | grep -c '📌'
3
$ git show fdfca7b:_persistence/decisions.md | sed -n '/^### D-033 /,$p' | grep -c '<hash>'
0
```

Cabecera, seccion 1 y 7c-bis quedaron anclados en `fdfca7b`. La seccion 7, no: ver `F-013`.

## 2. Hallazgos

### F-013 — El anclaje de S-011 se commiteo con la NOTA DE CIERRE vacia y el 7c-ter fallando, y se completo en un tercer commit que la nota no declara

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Donde | `_audit/S-011.md` seccion 7; commits `fdfca7b` y `1358c3c`; Pasos 7c y 7c-ter de `.claude/skills/protocol-close/SKILL.md` |

- **Que se observo:** el commit de anclaje `fdfca7b` dejo la seccion 7 sin anclar, con el marcador
  literal de pendiente, y con el control del Paso 7c-ter devolviendo las seis ausencias:

  ```
  $ git show fdfca7b:_audit/S-011.md | grep -n 'NOTA DE CIERRE'
  241:NOTA DE CIERRE: <se completa en el Paso 7c-bis, despues del commit>
  $ for c in fdfca7b 1358c3c; do echo "== 7c-ter @ $c"; for m in "**BARRIDO DE ANCLAJE — salida:**" "**CONTROL DE PROSA BORRADA — salida:**" "**CONTROL DE SALIDA REPRODUCIDA — salida:**" "**SEGUNDA PASADA anclada del Paso 2e — salida:**" "**CONTROL DE CIFRA ADYACENTE — salida:**" "**ORDEN DEL PASO 2d ANCLADA — salida:**"; do git show $c:_audit/S-011.md | grep -qF "$m" || echo "FALTA en la NOTA DE CIERRE: $m"; done; done
  == 7c-ter @ fdfca7b
  FALTA en la NOTA DE CIERRE: **BARRIDO DE ANCLAJE — salida:**
  FALTA en la NOTA DE CIERRE: **CONTROL DE PROSA BORRADA — salida:**
  FALTA en la NOTA DE CIERRE: **CONTROL DE SALIDA REPRODUCIDA — salida:**
  FALTA en la NOTA DE CIERRE: **SEGUNDA PASADA anclada del Paso 2e — salida:**
  FALTA en la NOTA DE CIERRE: **CONTROL DE CIFRA ADYACENTE — salida:**
  FALTA en la NOTA DE CIERRE: **ORDEN DEL PASO 2d ANCLADA — salida:**
  == 7c-ter @ 1358c3c
  $ git show --stat --name-only --format= 1358c3c
  _audit/S-011.md
  ```

  El Paso 7c pide «los cuatro juntos, en un unico commit de anclaje» y el 7c-ter dice «No commitees el
  anclaje hasta que salga vacio». La nota se escribio despues, en `1358c3c`, y dice:

  ```
  $ git show 1358c3c:_audit/S-011.md | grep -n '^\*\*NOTA DE CIERRE'
  241:**NOTA DE CIERRE.** Commit sustantivo `079b0a4`; commit de anclaje `fdfca7b`.
  $ git show 1358c3c:_audit/S-011.md | grep -c '1358c3c'
  0
  ```

  La nota nombra `fdfca7b` como commit de anclaje, pero ella misma no esta en `fdfca7b`. El informe no
  menciona `1358c3c` ni que el 7c-ter fallo al commitear el anclaje.
- **Por que importa:** el 7c-ter es la puerta que impide commitear un anclaje con la nota coja, y se
  salto. Leido solo, el informe hace creer que la nota y sus seis salidas se anclaron en `fdfca7b`. Quien
  siga el puntero a ese commit encuentra el marcador de pendiente. Ademas, el cierre queda en tres commits
  (`079b0a4` con 8 archivos, `fdfca7b` con 2 y `1358c3c` con 1), y `git log -1 -- _audit/S-011.md`
  devuelve `1358c3c`, un commit que el informe no nombra. Las evidencias de la nota reproducen
  (seccion 1.3), asi que no hay datos falsos, solo una puerta saltada y un puntero equivocado: `Media`.
- **Que lo corregiria:** una nota fechada en la seccion 7 de `S-011.md` que diga que la NOTA DE CIERRE
  se commiteo en `1358c3c` y no en `fdfca7b`, y que el 7c-ter fallo en `fdfca7b`, sin reescribir la
  linea publicada. Y que `manager` valore si `protocol-close` necesita cortar mecanicamente el commit
  de anclaje cuando falla el 7c-ter, en vez de dejarlo como instruccion. ⚠️ Es una recomendacion, no
  una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| F-011 | Aceptado — pendiente | **Implementado** (079b0a4) | La correccion recomendada como opcion (a) en `R-010` esta en el diff: `git show 079b0a4 -- _persistence/tasks.md` anade a `T-019` la nota fechada que saca la frase de `D-028` de su alcance y remite a `D-032`/`DT-004`, sin reescribir el «Que». La comparacion de blobs de la nota reproduce (`igual` x3, seccion 1.3). `DT-004` queda `Confirmada`/`Implementada` en indice y ficha (`1`/`2`), y el esqueleto lleva la frase (`7f4381e`, blob igual a `1add289`, remoto en `7f4381e`) |

No habia mas hallazgos abiertos: los otros diez del indice estan `Implementado`.

## 4. Lo que NO se pudo comprobar

- La afirmacion de `D-033` y `D-035` de que el usuario aprobo («acepta esa lectura», «confirma
  `DT-004` y aprueba promover») no se puede contrastar contra el repositorio: nace en la conversacion.
  Se deja constancia y no es un hallazgo.
- La tabla de la seccion 7 afirma que las 23 ordenes «se reejecutaron» durante el cierre. Se
  reejecutaron aqui las que tienen salida contrastable (seccion 1.3) y todas reproducen. No se
  reejecutaron las filas 9-12 y 19, que preguntan por el estado vivo del arbol en `1add289`: ese
  estado ya no existe.

## 5. Recomendaciones sin hallazgo

- **Desfase 4 de `protocol-start` y commits de anclaje** (lo senala la seccion 6 del informe). `D-033`
  es la tercera constatacion del mismo patron (`S-008`, `S-009`, `S-010`). Un aviso que salta en casi
  cada arranque acaba ignorandose. Conviene decidir si se afina y registrarlo como tarea o deuda.
- **Ids consumidos por la auditoria revertida.** `9eb9d49` revirtio `R-011`/`F-012`, pero ningun
  registro dice que esos ids quedan retirados. Esta auditoria los salto (ver cabecera). Conviene que
  `manager` lo registre con su `D-XXX` para que el hueco en las series `R-` y `F-` no se lea como un
  error. Tambien, que valore si el fallo de fondo —una auditoria hecha por quien no era `report_auditor`—
  merece una leccion.
- **Trailer de coautoria de los commits de sesion.** `079b0a4`, `fdfca7b` y `1358c3c` llevan
  `Co-Authored-By: Claude Sonnet 5`. No es un defecto del registro, solo un dato para quien reconstruya
  que modelo cerro la sesion.
