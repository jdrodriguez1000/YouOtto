# Auditoria R-018 — sobre S-016

| Campo | Valor |
|---|---|
| Auditoria | R-018 |
| Informe auditado | `_audit/S-016.md` |
| Commit auditado | 205b1f2 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit auditado

El informe vive en dos commits; el derivado es el de anclaje y no es el que se audita.

```
$ git log --format='%h %s' -- _audit/S-016.md
47dc449 S-016: ancla el informe y los criterios de cierre al hash 205b1f2
205b1f2 S-016: F-019 aceptado y corregido (D-059), cierre de 010_actors/015_stakeholders (D-060), alcance y objetivo del proyecto (D-061), y cosecha de 005_discovery (D-062)

$ git show 47dc449:_audit/S-016.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | 205b1f2 |

$ git show --stat --name-only --format= 47dc449
_audit/S-016.md
_persistence/decisions.md

$ git show --stat --name-only --format= 205b1f2 | grep -c .
10
```

La cabecera escribe el hash literal `205b1f2`, distinto del derivado. **Se audita `205b1f2`**, y sus
10 archivos coinciden con la lista que la seccion 1 del informe enumera.

### 1.2 Indices ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do
  echo "== $f"
  diff <(git show 205b1f2:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) \
       <(git show 205b1f2:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+'   | grep -oE '[A-Z]+-[0-9]+' | sort -u)
done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Sin diferencias en los siete.

### 1.3 Carpetas del arbol contra las declaradas — una diferencia, con motivo escrito

```
$ diff <(git ls-tree -d --name-only 205b1f2 | sed 's|$|/|' | sort) \
       <(git show 205b1f2:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
9a10
> temporal/
```

`temporal/` esta declarada en `project.md` como **excluida en `.gitignore`**, con su razon escrita en
la misma tabla. No es hallazgo.

### 1.4 Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" 205b1f2 -- .claude CLAUDE.md _phases _methodology _templates _workflow
(sin salida)
```

### 1.5 Codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' 205b1f2 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
(sin salida)
```

### 1.6 Fecha del commit contra la del informe — coinciden

```
$ git log -1 --format='%h %ad %cd' --date=format:'%F' 205b1f2
205b1f2 2026-09-17 2026-09-17
```

El informe, `progress.md`, las cuatro decisiones nuevas, `L-016` y las notas fechadas llevan
`2026-09-17`.

### 1.7 El informe contra el diff — cada afirmacion respaldada

```
$ git diff -U0 205b1f2^ 205b1f2 -- _persistence/decisions.md | grep -E '^\+### D-'
+### D-059 - F-019 se acepta y se corrige por nota en esta sesion
+### D-060 - Se cierran los artefactos de actores e interesados
+### D-061 - Objetivo y alcance del proyecto
+### D-062 - Cosecha de 005_discovery al archivo de lecciones globales

$ git diff -U0 205b1f2^ 205b1f2 -- _persistence/tasks.md | grep -E '^\+### T-'
+### T-034 - Corregir por nota la cifra del criterio de cierre de T-029
+### T-035 - Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062

$ git diff -U0 205b1f2^ 205b1f2 -- _persistence/lessons.md | grep -E '^\+### L-'
+### L-016 - Una cifra escrita donde no se puede corregir se cuenta antes, no despues
```

Estados de las dos tareas nuevas, tal como el informe los declara:

```
$ git diff -U0 205b1f2^ 205b1f2 -- _persistence/tasks.md | grep -E '^\+\| \[T-03[45]\]'
+| [T-034](...) | Corregir por nota la cifra del criterio de cierre de T-029 | Implementada | Baja | No bloqueante | 005_discovery |
+| [T-035](...) | Anclar por nota la cifra del barrido del Paso 1 de protocol-harvest en D-062 | No implementada | Baja | No bloqueante | 005_discovery |
```

Cierre de los dos artefactos (`D-060`), y contenido de §1 a §5 intacto:

```
$ for f in 010_actors 015_stakeholders; do echo "== $f"; git show 205b1f2:005_discovery/$f.md | grep -nE '^\| Estado \||^\| Cerrado \||^## Guia de llenado'; done
== 010_actors
7:| Estado | `CERRADO` |
9:| Cerrado | `2026-09-17` |
== 015_stakeholders
7:| Estado | `CERRADO` |
9:| Cerrado | `2026-09-17` |
```

El diff de los dos archivos solo toca cabecera, casillas de comprobacion y el borrado de la «Guia de
llenado». Ninguna linea de las secciones 1 a 5 cambia.

Nota fechada en `A-008` (`D-061`), verificada en el diff de `_persistence/assumptions.md`: seis
lineas anadidas, ninguna borrada; el estado sigue `Abierto` y el disparador no cambia.

`_audit/index.md` y `_persistence/progress.md` no llevan bullet propio en la seccion 1 del informe.
Su diff es el esperado de un cierre (fila de `S-016` en el tablero con `Pendiente`, bitacora `S-016`,
estado actualizado) y no contiene nada que el informe no declare en otra seccion. No es hallazgo.

### 1.8 Lo que la seccion 6 del informe pidio auditar

**(a) La septima casilla de la condicion de salida de `005_discovery`.** La lectura del cierre es
correcta. `_phases/005_discovery.md` §6 enumera siete casillas y la septima exige que ninguna leccion
de la etapa quede `Sin evaluar`:

```
$ git show 205b1f2:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
1
```

La unica es `L-016`, nacida en este mismo commit. No hay lectura alternativa defendible: la casilla
esta redactada sobre el estado del archivo, no sobre el momento del barrido, y `L-016` esta en
`lessons.md` con etapa `005_discovery` y `Portabilidad: Sin evaluar`. La etapa **no cierra**, y el
informe lo dice.

**(b) `D-061` frente a `C-005`.** No la reabre. `C-005` ya preve el caso literalmente:

```
$ git show 205b1f2:_persistence/constraints.md | sed -n '/^### C-005 /,/^### C-006 /p' | grep -n "sin dueno"
: - **Que implica:** el historial de juegos se guarda sin dueno; no hay cuentas ni registro de usuarios, y
```

`D-061` §2 escribe «la aplicacion guarda las apuestas jugadas —sin dueno, como ya preve `C-005`—», y
su consecuencia 3 lo declara explicitamente. El desacoplamiento del Gate 1 tambien esta escrito
(consecuencia 1: «el Gate 1 sigue midiendo solo `N-001` y `N-002`… el dictamen no puede exigir
`N-003` ni `N-004`»). Sin hallazgo.

**(c) El tratamiento del barrido del Paso 1 de `protocol-harvest` en `D-062`.** La lectura del cierre
—snapshot no anclado, `T-035` abierta, no se reescribe la cifra— es la correcta. El `8` publicado era
exacto en el estado previo:

```
$ git show e18bf5b:_persistence/lessons.md | grep -E '^\| \[L-' | grep '005_discovery' | grep -c 'Sin evaluar'
8
```

No es una cifra falsa: es una cifra cierta en su momento y sin ancla. Tratarla como `F-019` habria
sido incorrecto; `F-019` publicaba una salida que su orden **nunca** devolvio. Sin hallazgo.

### 1.9 La correccion de `F-019` — verificada

La nota fechada en `T-029` publica la orden anclada a `e18bf5b` con su salida. Reproducida:

```
$ git show e18bf5b:005_discovery/005_needs.md | grep -c "^### N-0"
5
$ git show e18bf5b:005_discovery/005_needs.md | grep -n "^### N-0"
56:### N-001 · Combinacion en maximo un minuto
71:### N-002 · Saber por que entra cada numero
86:### N-003 · Saber cuantos numeros acerte
101:### N-004 · Saber con que acumulado se juega
185:### N-001 · Pedir recogida sin llamar
$ git show e18bf5b:005_discovery/005_needs.md | grep -n "Guia de llenado"
145:- [ ] La seccion «Guia de llenado» de abajo **esta borrada**.
152:grep -n "Guia de llenado" 005_discovery/005_needs.md   # debe no devolver nada
162:## Guia de llenado — ⚠️ BORRAR esta seccion al cerrar el artefacto
```

Coincide linea por linea con lo publicado en la nota. La cifra `4` original no se reescribio.

### 1.10 El Paso 7c-bis del commit de anclaje — dentro de lo autorizado

```
$ git diff 205b1f2 47dc449 -- _persistence/decisions.md | grep -E '^-' | grep -vE '^---' | wc -l
8
```

Las ocho lineas borradas son las ocho ordenes `$ git show <hash>:…` sustituidas por su forma anclada.
**Ninguna linea de prosa se borro.** La novena (la de `D-062`, que no reproduce) se anadio debajo de
la publicada, sin sustituirla, y con su diagnostico escrito. El commit de anclaje toca solo
`_audit/S-016.md` y `_persistence/decisions.md`, los dos archivos que ese paso tiene autorizados.

### 1.11 La cosecha en el repositorio de lecciones — reproduce

```
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" log --oneline -1
a9ed27b Cosecha de 005_discovery desde YouOtto: LG-106 a LG-109 y enmienda de LG-22 (version 5)
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show a9ed27b:global_lessons.md | grep -cE '^\| \*\*LG-10[6789]\*\*'
4
$ git -C "C:/Users/USUARIO/Documents/Company_TripleS/TripleS_Lessons" show a9ed27b:global_lessons.md | grep -cF 'y un bloqueo, con la puerta tapiada'
1
```

Las tres salidas coinciden con lo publicado en `D-062`.

### 1.12 Los huecos `R-011`/`F-012` y `R-014`/`F-015` — con motivo escrito

```
$ grep -n -i "R-011\|R-014\|F-012\|F-015" _audit/index.md _audit/findings.md
(sin salida)
```

`D-038` y `D-047` declaran expresamente que los ids retirados **no llevan fila** en `index.md` ni en
`findings.md`, y que la decision explica el hueco. Motivo escrito → no es hallazgo.

### 1.13 La seccion 0 del informe — no invade el estado `Implementado`

Su columna `Veredicto` dice `Aceptado — corregido en este commit`, y la fila de `F-019` en
`findings.md` quedo en `Aceptado — pendiente`, no en `Implementado`. Correcto.

## 2. Hallazgos

### F-020 — `015_stakeholders.md` queda `CERRADO` afirmando «seis condiciones de salida» donde su etapa enumera siete

| Campo | Valor |
|---|---|
| Gravedad | Baja |
| Urgencia | No bloqueante |
| Donde | `005_discovery/015_stakeholders.md` (casilla de comprobacion), `_templates/005_discovery/015_stakeholders.md:101` |

- **Que se observo:** el commit auditado cierra `015_stakeholders.md` y marca `[x]` una casilla cuyo
  texto afirma un numero que el archivo de etapa contradice.

  ```
  $ git show 205b1f2:005_discovery/015_stakeholders.md | grep -n 'condiciones de salida'
  115:- [x] **Los interesados estan identificados** — es una de las seis condiciones de salida de la etapa.

  $ grep -rn "seis condiciones\|siete condiciones\|las siete\|las seis" _phases/005_discovery.md _templates/005_discovery/
  _phases/005_discovery.md:282:La etapa termina cuando **las siete son ciertas**:
  _phases/005_discovery.md:304:⚠️ **Ninguna de las siete exige que exista la etapa siguiente.** Cual sea se declara dentro de esta
  _templates/005_discovery/015_stakeholders.md:101:- [ ] **Los interesados estan identificados** — es una de las seis condiciones de salida de la etapa.
  ```

  `_phases/005_discovery.md` §6 enumera **siete** casillas; la plantilla, y con ella el artefacto ya
  cerrado, dicen **seis**. El defecto es heredado del esqueleto, no nace en esta sesion:

  ```
  $ git log --oneline -S"siete son ciertas" -- _phases/005_discovery.md
  fa7da56 Estado de partida del esqueleto, tal como estaba antes de sincronizar
  ```

  No hay `D-XXX` que declare la diferencia: el barrido de `decisions.md` por «condicion de salida»
  devuelve entradas sobre las casillas concretas (`D-060`, `D-061`, `D-062`) y ninguna sobre el
  recuento.
- **Por que importa:** un artefacto `CERRADO` es evidencia, y esta afirmando un recuento que el
  archivo de etapa desmiente. Ademas el error vive en `_templates/`, que existe para copiarse: cada
  proyecto que arranque con esa plantilla heredara la misma frase falsa. `Baja` porque ningun control
  lee ese numero y las siete casillas se evaluaron una por una de todos modos; `No bloqueante` porque
  nada de lo que se haga despues depende de la cifra — pero tampoco desaparece sola.
- **Que lo corregiria:** una nota fechada en el artefacto cerrado (que no se reescribe) y la
  correccion de `six → siete` en `_templates/005_discovery/015_stakeholders.md`, que al ser andamiaje
  arrastra su promocion al esqueleto. ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-016` | Aceptado — pendiente | **Aceptado — pendiente** | su `T-030` sigue `No implementada` en `205b1f2`; la nota con la orden sin filtro que `D-041` cita no aparece en el diff |
| `F-017` | Aceptado — pendiente | **Aceptado — pendiente** | su `T-031` sigue `No implementada` en `205b1f2`; la NOTA DE CIERRE de `S-013` no se toco |
| `F-019` | Aceptado — pendiente | **Implementado** (`205b1f2`) | la correccion esta en el diff y reproduce — ver §1.9 |

Evidencia del estado de `T-030` y `T-031`:

```
$ git show 205b1f2:_persistence/tasks.md | grep -E '^\| \[T-03[01]\]'
| [T-030](#t-030---publicar-por-nota-la-orden-sin-filtro-que-d-041-cita) | Publicar por nota la orden sin filtro que D-041 cita | No implementada | Baja | No bloqueante | 005_discovery |
| [T-031](#t-031---corregir-por-nota-la-cifra-de-la-nota-de-cierre-de-s-013) | Corregir por nota la cifra de la NOTA DE CIERRE de S-013 | No implementada | Baja | No bloqueante | 005_discovery |
```

## 4. Lo que NO se pudo comprobar

Ninguno. Los cinco controles mecanicos (§1.2 a §1.6) corrieron y devolvieron resultado.

El Paso 2f del informe (diff contra el esqueleto de arranque) no se re-ejecuto por estar fuera del
alcance de este protocolo; su contenido —dos diferencias, declaradas como promociones pendientes— es
consistente con lo reportado en `S-015` y con el «Siguiente paso» de `progress.md`.

## 5. Recomendaciones sin hallazgo

- **La seccion 1 del informe dice «se marcan sus once casillas» de `010_actors.md`, pero una de las
  once no existia en el artefacto y se anadio en este mismo commit.** El diff muestra diez lineas
  `- [ ]` sustituidas por once `- [x]`; la que aparece sin contraparte es `La seccion «Guia de
  llenado» **esta borrada**`, que si trae la plantilla (`_templates/005_discovery/010_actors.md:126`).
  Es decir, el artefacto se copio en `S-015` con una casilla de menos y este commit la restituyo.
  El resultado es correcto, pero «se marcan» describe algo distinto de «se anade y se marca». Merece
  una linea en un informe futuro, no una correccion.
- **`L-015` figura en `lessons.md` como `Promovida a LG-22`, mientras `D-062` y el informe la
  describen como «enmienda» de `LG-22`.** Las dos lecturas son ciertas y el archivo global lo
  resuelve, pero la columna `Portabilidad` no distingue «promovida como entrada nueva» de «promovida
  como enmienda de una existente». Si la distincion importa en cosechas futuras, conviene un valor
  propio.
