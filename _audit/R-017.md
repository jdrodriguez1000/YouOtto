# Auditoria R-017 — sobre S-015

| Campo | Valor |
|---|---|
| Auditoria | R-017 |
| Informe auditado | `_audit/S-015.md` |
| Commit auditado | eda6314 |
| Fecha | 2026-09-17 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Que commit se audita

```
$ git log -1 --format=%h -- _audit/S-015.md
6e8348e
$ git show 6e8348e:_audit/S-015.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | eda6314 |
$ git show --stat --name-only --format= 6e8348e
_audit/S-015.md
_persistence/decisions.md
$ git show --stat --name-only --format= eda6314 | grep -c .
14
```

La orden derivada devuelve `6e8348e`, que es el **commit de anclaje** del Paso 7c/7c-bis (toca solo el
informe y los dos criterios de cierre que anclo en `decisions.md`). La cabecera del informe escribe el
literal `eda6314`, y su `--stat` de 14 archivos coincide con la lista que enumera la seccion 1. **Se
audita `eda6314`.**

### 1.2 Indice ↔ detalle en `_persistence/` — coherentes

```
$ for f in tasks decisions constraints assumptions lessons techdebt progress; do echo "== $f"; diff <(git show eda6314:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^\| \[?[A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u) <(git show eda6314:_persistence/$f.md | awk '/^[[:space:]]*```/{c=!c; next} !c' | grep -oE '^#{3} [A-Z]+-[0-9]+' | grep -oE '[A-Z]+-[0-9]+' | sort -u); done
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Ninguna diferencia en los siete archivos.

### 1.3 Carpetas del arbol contra las declaradas — una diferencia, con motivo escrito

```
$ diff <(git ls-tree -d --name-only eda6314 | sed 's|$|/|' | sort) <(git show eda6314:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
9a10
> temporal/
```

`temporal/` esta declarada en `project.md` como excluida en `.gitignore`, asi que nunca aparece en el
arbol versionado. **Diferencia con razon escrita: no es hallazgo.** `005_discovery/` ya existe en el
arbol de este commit y por eso no sale senalada.

### 1.4 Fuga de datos propios en lo agnostico — cero lineas

```
$ git grep -nE "YouOtto|Company_TripleS|github\.com" eda6314 -- .claude CLAUDE.md _phases _methodology _templates _workflow
$
```

### 1.5 Fuga por codigos instanciados en `_phases` y `_workflow` — cero lineas

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' eda6314 -- _phases _workflow | grep -vE ':PI-[0-9]+$'
$
```

### 1.6 El informe contra el diff — cada afirmacion de la seccion 1, respaldada

```
$ git diff -U0 eda6314^ eda6314 -- _persistence/decisions.md | grep -E '^\+### D-'
+### D-049 - F-018 se acepta y se corrige con nota fechada en D-045
+### D-050 - T-032 se verifica con una sonda de la cabecera exacta, en los dos casos
+### D-051 - Se abre el artefacto de necesidades y se declara N-XXX
+### D-052 - El producto reproduce el metodo del jugador, no una generacion aleatoria
+### D-053 - Ventanas, prioridad y desempates del metodo
+### D-054 - La revision del sorteo anterior entra en lo que se construye
+### D-055 - Actores del producto y veredicto del Actor Generador
+### D-056 - La aplicacion no guarda identidad, y A-001 queda refutado
+### D-057 - Interesados del proyecto y declaracion de I-XXX
+### D-058 - Hipotesis sellada de 005_discovery
$ git diff -U0 eda6314^ eda6314 -- _persistence/constraints.md | grep -E '^\+### C-'
+### C-002 - El producto no llama a ninguna API de modelos de lenguaje
+### C-003 - El producto se despliega en Vercel
+### C-004 - La aplicacion se mantiene lo mas sencilla posible
+### C-005 - La aplicacion no recoge ningun dato personal
$ git diff -U0 eda6314^ eda6314 -- _persistence/assumptions.md | grep -E '^\+### A-|^\+\| \[A-'
+| [A-001](#a-001---los-juegos-registrados-pueden-ser-datos-de-personas) | Los juegos registrados pueden ser datos de personas | 2026-09-15 | Refutado |
+| [A-007](#a-007---la-cabecera-exacta-de-session-closer-deja-pasar-protocol-close-y-bloquea-las-demas-skills) | La cabecera exacta de session-closer deja pasar protocol-close y bloquea las demas skills | 2026-09-17 | Confirmado |
+| [A-008](#a-008---las-condiciones-de-uso-de-baloto-permiten-leer-su-historico-de-forma-automatica) | Las condiciones de uso de Baloto permiten leer su historico de forma automatica | 2026-09-17 | Abierto |
+### A-007 - La cabecera exacta de session-closer deja pasar protocol-close y bloquea las demas skills
+### A-008 - Las condiciones de uso de Baloto permiten leer su historico de forma automatica
$ git diff -U0 eda6314^ eda6314 -- _persistence/lessons.md | grep -E '^\+### L-'
+### L-015 - Un bloqueo se prueba en los dos casos: el prohibido y el permitido
$ git diff -U0 eda6314^ eda6314 -- _persistence/tasks.md | grep -E '^[+-]\| \[T-'
-| [T-029](...) | Pasos 1 y 2 de 005_discovery con el patrocinador | No implementada | Alta | Bloqueante | 005_discovery |
+| [T-029](...) | Pasos 1 y 2 de 005_discovery con el patrocinador | Implementada | Alta | Bloqueante | 005_discovery |
+| [T-033](...) | Publicar por nota las ordenes del Paso 1b y del barrido que D-045 resume | No implementada | Baja | No bloqueante | 005_discovery |
$ git diff eda6314^ eda6314 -- project.md | grep -E '^[+-]\|'
+| `N-XXX` | `005_discovery/005_needs.md` | necesidad del producto |
+| `I-XXX` | `005_discovery/015_stakeholders.md` | interesado del proyecto |
```

Los cuatro artefactos de `005_discovery/` nacen en este commit con los estados que el informe declara:

```
$ for f in 005_needs 010_actors 015_stakeholders 020_hypothesis; do echo "-- $f"; git show eda6314:005_discovery/$f.md | sed -n '1,12p' | grep -E '^\| (Estado|Abierto|Sellada el) \|'; done
-- 005_needs
| Estado | `BORRADOR` |
| Abierto | `2026-09-17` |
-- 010_actors
| Estado | `BORRADOR` |
| Abierto | `2026-09-17` |
-- 015_stakeholders
| Estado | `BORRADOR` |
| Abierto | `2026-09-17` |
-- 020_hypothesis
| Estado | `SELLADA` |
| Sellada el | `2026-09-17` |
```

Las fechas declaradas cuadran con la del commit (`git log -1 --format='%h %ad' --date=short eda6314`
→ `eda6314 2026-09-17`), y la hipotesis sellada reproduce su unico commit:

```
$ git log --oneline -- 005_discovery/020_hypothesis.md | wc -l
1
$ git show eda6314:005_discovery/020_hypothesis.md | grep -c '^| Estado | `SELLADA` |$'
1
```

### 1.7 Los dos criterios de cierre anclados por el Paso 7c-bis — reproducen

```
$ git show eda6314:_persistence/decisions.md | sed -n '/^### D-045 /,/^### D-046 /p' | grep -cE '🕐 \*\*Nota 2026-09-17 \(`F-018`, `D-049`\)|^\s*\$ for p in 005_project|^\s*\$ W=\$\(mktemp -d\)'
3
$ git show eda6314:_audit/findings.md | grep -cE '^\| \[F-018\].*\| Aceptado — pendiente \|$|^\| Registrado en \| T-033, D-049 \|$'
2
```

Las tres coinciden con las salidas publicadas en `D-049` y `D-058`.

### 1.8 Las trece ordenes del Paso 2d, reejecutadas una por una

Recuento y archivos del commit, reproducidos:

```
$ git diff -U0 eda6314^ eda6314 -- _persistence _audit ":(exclude)_audit/S-015.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
13
$ git diff --name-only --diff-filter=d eda6314^ eda6314 | wc -l
14
```

Las dos ordenes largas de la nota de `D-045` (lineas 1 y 2 de la tabla del informe):

```
$ ESQ="C:/Users/USUARIO/Documents/Company_TripleS/SDAI_TripleS"
$ for p in 005_project.md:project.md 010_progress.md:_persistence/progress.md 015_tasks.md:_persistence/tasks.md 020_decisions.md:_persistence/decisions.md 025_constraints.md:_persistence/constraints.md 030_assumptions.md:_persistence/assumptions.md 035_lessons.md:_persistence/lessons.md 040_techdebt.md:_persistence/techdebt.md 045_audit_index.md:_audit/index.md 050_audit_findings.md:_audit/findings.md; do t=${p%%:*}; c=${p#*:}; printf '%3d  %s\n' "$(diff --strip-trailing-cr <(git -C "$ESQ" show 1bec59a:_templates/000_preproject/$t) <(git -C "$ESQ" show 1bec59a:$c) | grep -c '^[<>]')" "$c"; done
  0  project.md
  0  _persistence/progress.md
  0  _persistence/tasks.md
  0  _persistence/decisions.md
  0  _persistence/constraints.md
  0  _persistence/assumptions.md
  0  _persistence/lessons.md
  0  _persistence/techdebt.md
  0  _audit/index.md
  0  _audit/findings.md
$ W=$(mktemp -d); mkdir "$W/o" "$W/e"; git archive 2063f09 .claude _phases _methodology _templates _workflow CLAUDE.md | tar -x -C "$W/o"; git -C "$ESQ" archive 1bec59a .claude _phases _methodology _templates _workflow CLAUDE.md | tar -x -C "$W/e"; for d in .claude _phases _methodology _templates _workflow; do diff -rq --strip-trailing-cr "$W/e/$d" "$W/o/$d"; done; diff -q --strip-trailing-cr "$W/e/CLAUDE.md" "$W/o/CLAUDE.md"; echo "archivos: $(find "$W/o" -type f | wc -l) $(find "$W/e" -type f | wc -l)"; rm -rf "$W"
archivos: 71 71
```

Las cuatro ordenes del arbol de trabajo que el informe da por reproducidas (lineas 7, 8, 11, 13):

```
$ sed -n '1,109p' 005_discovery/020_hypothesis.md | grep -nE '<|Guia de llenado'; echo "salida=$?"
salida=1
$ grep -n '<' 005_discovery/020_hypothesis.md
124:- [x] **No queda ni un solo `<` en el archivo.**
129:grep -n "<" 005_discovery/020_hypothesis.md                 # debe no devolver nada
137:unicas lineas que quedan con `<` y con «Guia de llenado» son **las de esta misma seccion**, que los
$ test -d 005_discovery && echo "existe 005_discovery"
existe 005_discovery
$ grep -n '^| `N-XXX`' project.md
259:| `N-XXX` | `005_discovery/005_needs.md` | necesidad del producto |
```

La linea 12 **no reproduce**: es el hallazgo `F-019` de la seccion 2.

Las dos declaradas no anclables (lineas 5 y 6) se comprobaron como declaradas:

```
$ git log --oneline --all -- .claude/agents/hook-probe.md
$ test -f .claude/agents/hook-probe.md && echo si || echo no
no
```

`hook-probe.md` no existe en ningun commit ni en el arbol: la orden 5 es efectivamente irreproducible,
y declararlo es correcto.

### 1.9 Hallazgos anteriores, comprobados sobre el diff

```
$ git diff eda6314^ eda6314 -- _persistence/decisions.md | grep -c 'D-041'
0
$ git show eda6314:_audit/S-013.md | grep -nE 'dieciseis|diecisiete'
497:Ninguna de las dieciseis lineas afirma una cifra que no salga del bloque inmediatamente anterior o de
```

`F-016` y `F-017` no se tocaron, como el informe declara.

## 2. Hallazgos

### F-019 — El criterio de cierre de T-029 publica `4` donde su orden devuelve `5`, y el informe lo da por reproducido

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | No bloqueante |
| Donde | `_persistence/tasks.md`, `T-029`; `_audit/S-015.md`, seccion 7, fila 12 de la tabla |

- **Que se observo:** el bloque de verificacion de `T-029` publica

  ```
  $ grep -c "^### N-0" 005_discovery/005_needs.md
  4
  ```

  y la orden devuelve `5`, tanto sobre el commit auditado como sobre el arbol de trabajo:

  ```
  $ git show eda6314:005_discovery/005_needs.md | grep -c "^### N-0"
  5
  $ grep -c "^### N-0" 005_discovery/005_needs.md
  5
  $ git show eda6314:005_discovery/005_needs.md | grep -n "^### N-0"
  56:### N-001 · Combinacion en maximo un minuto
  71:### N-002 · Saber por que entra cada numero
  86:### N-003 · Saber cuantos numeros acerte
  101:### N-004 · Saber con que acumulado se juega
  185:### N-001 · Pedir recogida sin llamar
  ```

  La quinta linea es el ejemplo de ficha que trae la seccion «Guia de llenado» de la plantilla, que
  sigue en el archivo porque el artefacto esta `BORRADOR`. Ademas, la seccion 7 del informe lista esa
  misma orden como la numero 12 con `Reproduce: Si` y nota `4`, asi que el control del Paso 2d la dio
  por buena sin haberla corrido con esta salida.

- **Por que importa:** es la evidencia que cierra `T-029`, una tarea `Alta`/`Bloqueante`, y el
  registro publica una salida que su propia orden no devuelve — exactamente la clase de defecto que
  `F-005` ya abrio una vez. `Media` y no `Alta` porque el enunciado del criterio («al menos una `N-XXX`
  enunciada sin nombrar una pantalla») **si se cumple**: hay cuatro necesidades reales, y ningun
  control depende de la cifra. `No bloqueante`: nada de lo que se haga despues hereda el defecto, y la
  cifra volvera a valer `4` sola el dia que se borre la guia al cerrar el artefacto — momento en que el
  defecto dejaria de poder detectarse, que es la razon de registrarlo ahora.

- **Que lo corregiria:** nota fechada en `T-029` que republique la orden con la salida que devuelve
  (`5`) y diga de donde sale la quinta linea, o bien una orden con el alcance acotado al cuerpo del
  artefacto (por ejemplo, cortando antes de la «Guia de llenado») publicada con su salida. ⚠️ Es una
  recomendacion, no una orden. **No se reescribe la salida publicada**: se corrige por nota, como fijan
  las convenciones de `decisions.md` y el precedente de `F-014`.

## 3. Hallazgos anteriores

| Hallazgo | Estado anterior | Estado nuevo | Por que |
|---|---|---|---|
| `F-016` | Aceptado — pendiente | **Aceptado — pendiente** (sin cambio) | `D-041` no se toca en `eda6314` (`git diff eda6314^ eda6314 -- _persistence/decisions.md \| grep -c 'D-041'` → `0`); `T-030` sigue `No implementada` |
| `F-017` | Aceptado — pendiente | **Aceptado — pendiente** (sin cambio) | `_audit/S-013.md` sigue diciendo «dieciseis lineas» en `eda6314`; `T-031` sigue `No implementada` |
| `F-018` | Aceptado — pendiente | **Implementado** en `eda6314` | `D-045` lleva la nota fechada con las dos ordenes y sus salidas, y las dos reproducen hoy (seccion 1.8): `0` en las diez parejas y `archivos: 71 71` |

## 4. Lo que NO se pudo comprobar

Ninguno de los cinco controles quedo `SIN COMPROBAR`: los cuatro mecanicos corrieron sobre `eda6314`,
y las once ordenes anclables del Paso 2d se reejecutaron. Las dos declaradas no anclables (lineas 5 y 6
de la tabla del informe) no se reejecutan por construccion: la 5 apunta a un archivo que nunca se
commiteo y ya no existe —comprobado—, y la 6 esta declarada en `D-051` como «sobre el arbol de trabajo,
antes del commit».

## 5. Recomendaciones sin hallazgo

- **Las tres preguntas de la seccion 6 del informe, respondidas.**
  1. *Cerrar `T-029` por su criterio literal:* correcto. El criterio es lo que cierra una tarea, y esta
     cumplido; lo que quedo corto fue el titulo, y la nota del `session-closer` lo dice sin maquillar el
     alcance. Lo que si falta es la `T-XXX` del trabajo restante (cerrar `010_actors.md` y
     `015_stakeholders.md` y correr la condicion de salida), que hoy solo vive como «siguiente tarea
     propuesta» en la seccion 5 del informe.
  2. *`T-032` en `No implementada` con nota en vez de reescribir su criterio:* el limite esta bien
     trazado. Cambiar el criterio de una tarea es decidir, y el cierre no decide; la nota deja el
     asunto visible sin cerrarlo por su cuenta.
  3. *Las ordenes 5 y 6 declaradas no anclables:* la 5 lo es de verdad. La 6 **si era anclable** despues
     del commit —`diff <(git show eda6314:_templates/005_discovery/005_needs.md) <(git show
     eda6314:005_discovery/005_needs.md)`—, y su salida publicada (`3 insertions, 3 deletions`) ya no
     reproduce (`69 insertions, 23 deletions`). No se abre hallazgo porque `D-051` declara literalmente
     el alcance «sobre el arbol de trabajo, antes del commit», que las convenciones admiten; pero es una
     orden que hubiera ganado con la forma anclada.
- **Ocho decisiones nuevas sin bloque «Criterio de cierre».** `D-050`..`D-057` no lo llevan
  (`git show eda6314:_persistence/decisions.md | awk '/^### D-/{if(d!="")print d" "ph; d=$2; ph=0} /Criterio de cierre/{ph=1} END{print d" "ph}' | awk '$2==0'` → entre ellas `D-050`..`D-057`), mientras
  `D-022`..`D-049` lo llevan todas. `D-050` y `D-051` si publican bloque de verificacion; `D-052`..`D-057`
  son elecciones de producto sin nada mecanico que comprobar, asi que la ausencia es defendible — pero el
  cambio de practica respecto de las veintiocho anteriores no esta dicho en ningun sitio. Una linea en la
  primera de ellas, o una convencion explicita sobre cuando el criterio de cierre no aplica, evitaria que
  la proxima auditoria tenga que volver a decidirlo.
- **Dos `N-001` distintos coexisten en `005_needs.md`** (lineas 56 y 185). El segundo es el ejemplo de la
  plantilla y desaparece al cerrar el artefacto, asi que no incumple «ningun codigo se reutiliza». Vale la
  pena anotar en la casilla de cierre del artefacto que ese borrado es tambien lo que devuelve su recuento
  al valor esperado.
