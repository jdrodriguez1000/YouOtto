# Auditoria R-028 — sobre S-026

| Campo | Valor |
|---|---|
| Auditoria | R-028 |
| Informe auditado | `_audit/S-026.md` |
| Commit auditado | c424ae8 |
| Fecha | 2026-09-19 |
| Veredicto | Con hallazgos (1) |

## 1. Que se comprobo

### 1.1 Anclaje del commit

```
$ git log -1 --format=%h -- _audit/S-026.md
d13dfe9
$ git show d13dfe9:_audit/S-026.md | sed -n '/^| Commit auditado |/p'
| Commit auditado | `c424ae8` |
$ git show --stat --name-only --format= d13dfe9
_audit/S-026.md
_persistence/tasks.md
$ git show --stat --name-only --format= c424ae8 | grep -c .
10
```

`d13dfe9` es el commit de anclaje (Paso 7c + 7c-bis: informe y criterio de cierre de `T-059`). Se
audita el literal de la cabecera, `c424ae8`, cuyos 10 archivos coinciden con la lista de la seccion 1
del informe. El informe se leyo con `git show c424ae8:_audit/S-026.md`.

Fecha: `git log -1 --format='%h %ad' --date=iso c424ae8` → `c424ae8 2026-09-19 10:11:23 -0500`;
cabecera del informe `| Fecha | 2026-09-19 |`. Coinciden.

### 1.2 Controles mecanicos (Paso 4), sobre `c424ae8`

**a) Indice ↔ detalle de `_persistence/`** (orden literal del protocolo con `<hash>` = `c424ae8`):

```
== tasks
== decisions
== constraints
== assumptions
== lessons
== techdebt
== progress
```

Coherentes en los siete. ⚠️ Este control compara **codigos**, no estados: no ve el defecto de
`F-037` (seccion 2).

**b) Carpetas contra las declaradas:**

```
$ diff <(git ls-tree -d --name-only c424ae8 | sed 's|$|/|' | sort) <(git show c424ae8:project.md | sed -n '/^## Carpetas propias/,/^## /p' | grep -oE '^\| `[^`]+/`' | tr -d '|` ' | sort)
10a11
> temporal/
rc=1
```

Unica diferencia `temporal/`, con motivo escrito en `project.md` («Fuera del repositorio, excluida en
`.gitignore`») y en `.gitignore` de `c424ae8` (linea `temporal/`). No es hallazgo.

**c) Fuga de datos propios en las seis areas agnosticas:**

```
$ git grep -nE "YouOtto|Company_TripleS|github.com" c424ae8 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
$ git grep -nE "YouOtto|Proyectos_TripleS|github\.com" c424ae8 -- .claude CLAUDE.md _phases _methodology _templates _workflow; echo "rc=$?"
rc=1
```

Cero lineas.

**d) Codigos instanciados en `_phases` y `_workflow`:**

```
$ git grep -noE '\b[A-Z]{1,2}-[0-9]+\b' c424ae8 -- _phases _workflow | grep -vE ':PI-[0-9]+$'; echo "rc=$?"
rc=1
```

Cero lineas.

### 1.3 Informe contra el diff (Paso 3)

- **Seccion 1, lista de archivos:** identica al `--stat` de `c424ae8` (1.1). Cada punto descrito
  (nace `015_session_004.md`; fila `004` de `010_participants.md`; `D-103`, `D-104`, `L-027`,
  `T-059`, `T-060`; nota en `S-025.md`; fila y ficha de `F-036`) esta en `git show c424ae8`.
- **Seccion 0, lista de hallazgos no cerrados:** contrastada contra el registro del mismo commit,
  filtrando la columna `Estado`:

  ```
  $ git show c424ae8:_audit/findings.md | awk -F'|' '/^\| \[F-/ && $7 !~ /Implementado/ {match($2,/F-[0-9]+/); print substr($2,RSTART,RLENGTH) " |" $7}'
  F-016 | Aceptado — pendiente
  F-017 | Aceptado — pendiente
  F-028 | Aceptado — pendiente
  F-029 | Aceptado — pendiente
  F-030 | Aceptado — pendiente
  F-034 | Aceptado — pendiente
  F-036 | Aceptado — pendiente
  ```

  Siete filas. La seccion 0 enumera los siete. No
  usa `Implementado` en la columna `Veredicto`, y marca `F-036` como `Aceptado — pendiente`, no como
  «corregido en este commit». Correcto.
- **Ordenes de `D-103`**, reejecutadas:

  ```
  $ git show b374d01:_audit/S-025.md | grep -n "F-028"
  15:`_audit/findings.md` (`F-028`, `F-029`, `F-030`, `F-034`) no fueron tocados en esta sesion: sus
  $ git show b374d01:_audit/S-025.md | grep -c "F-036"
  0
  $ git show b374d01:_audit/findings.md | awk -F'|' '$7 ~ /Aceptado — pendiente/' | cut -c1-12
  | [F-016](#f
  | [F-017](#f
  | [F-028](#f
  | [F-029](#f
  | [F-030](#f
  | [F-034](#f
  $ git show b374d01:_audit/findings.md | grep -n "^| \[F-0" | grep "Aceptado — pendiente" | cut -c1-12
  38:| [F-016]
  39:| [F-017]
  50:| [F-028]
  51:| [F-029]
  52:| [F-030]
  56:| [F-034]
  58:| [F-036]
  ```

  Reproducen lo publicado.
- **Nota de `S-025.md` (`T-059`)** y su criterio anclado por el 7c-bis:

  ```
  $ git show 0c07cc8:_audit/findings.md | awk -F'|' '$7 ~ /Aceptado — pendiente/' | cut -c1-12
  | [F-016](#f
  | [F-017](#f
  | [F-028](#f
  | [F-029](#f
  | [F-030](#f
  | [F-034](#f
  $ git show c424ae8:_audit/S-025.md | grep -c "NOTA 2026-09-19 (\`F-036\`, \`D-103\`)"
  1
  $ git show c424ae8:_audit/S-025.md | sed -n "15p" | cut -c1-60
  `_audit/findings.md` (`F-028`, `F-029`, `F-030`, `F-034`) no
  ```

  Reproducen. La nota va al final, fechada, y la linea 15 de la seccion 0 sigue literal.
- **Registro de la sesion 004 y `D-104`:**

  ```
  $ git log --diff-filter=A --format='%h %ad' -- 010_prototype/015_session_004.md
  c424ae8 Sat Sep 19 10:11:23 2026 -0500
  $ git log --oneline -- 010_prototype/app/
  e45185d S-022: F-031 evaluado (D-092/T-051), el prototipo se construye (D-093) y el patrocinador lo aprueba (D-094)
  $ git log --format='%h %ad %s' --date=iso 0c07cc8^..d13dfe9
  d13dfe9 2026-09-19 10:15:02 -0500 S-026: ancla el informe y el criterio de cierre de T-059 al hash c424ae8
  c424ae8 2026-09-19 10:11:23 -0500 S-026: cuarta sesion del Paso 5 corrida (D-104/L-027), y F-036 de R-027 evaluado (D-103/T-059/T-060)
  b374d01 2026-09-19 09:36:15 -0500 auditoria R-027 sobre S-025 (0c07cc8)
  7cfbe0a 2026-09-19 09:32:11 -0500 S-025: ancla el informe al hash 0c07cc8
  0c07cc8 2026-09-19 09:28:22 -0500 S-025: tercera sesion del Paso 5 corrida (D-102)
  ```

  Fecha de cabecera `2026-09-19` = fecha del commit que crea el archivo. El prototipo no se toco desde
  `e45185d`. La sesion 004 (09:44) se corrio despues del cierre y la auditoria de `S-025`, como exige
  `D-088` («una por jornada … se cierra la jornada»). §6 va declarado «Transmitido … **no literal**»,
  como dice `D-104`; §5 total `0` y estado `Exito autonomo`, coherentes.
- **Paso 2d anclado:**

  ```
  $ git diff -U0 c424ae8^ c424ae8 -- _persistence _audit ":(exclude)_audit/S-026.md" | grep -E '^\+[[:space:]]*\$ ' | grep -vE 'git (show|grep|log|diff) [0-9a-f]{7,40}' | wc -l
  7
  ```

  Coincide con el `7` publicado.
- **Commit de anclaje `d13dfe9`:** toca solo `S-026.md` y el bloque de verificacion de `T-059` en
  `tasks.md` (entrada nacida en esta sesion), mas la linea `📌` y la linea en blanco antes del `---`
  que el Paso 7c-bis autoriza. Lineas borradas:

  ```
  $ git show d13dfe9 | grep -E '^-[^-]'
  -| Commit auditado | <se rellena en el Paso 7c> |
  -**NOTA DE CIERRE** (la completa el Paso 7c/7c-bis despues del commit, mas abajo en este mismo
  -archivo, cuando el hash del commit exista).
  -  $ grep -c "NOTA 2026-09-19 (\`F-036\`, \`D-103\`)" _audit/S-025.md
  -  $ sed -n "15p" _audit/S-025.md | cut -c1-60
  ```

  La cabecera y la NOTA DE CIERRE del informe, que el 7c existe para sustituir, y las dos ordenes
  sin anclar de `T-059`, dentro de su bloque. Ninguna prosa de `tasks.md`.
- **Seccion 4 del informe (supuestos vigentes):** **no se sostiene.** Ver `F-037`.

## 2. Hallazgos

### F-037 — `A-004` y `A-005` estan `Confirmado` en el indice y `Abierto` en su ficha, y el informe de `S-026` los da por vigentes

| Campo | Valor |
|---|---|
| Gravedad | Media |
| Urgencia | Bloqueante |
| Donde | `_persistence/assumptions.md` (fichas `A-004` y `A-005`), `_persistence/decisions.md` (`D-079`), `_audit/S-026.md` §4 y §6 |

- **Que se observo:** en `c424ae8`, el indice de `assumptions.md` da los dos supuestos por
  `Confirmado`, y sus fichas siguen diciendo `Abierto`:

  ```
  $ git show c424ae8:_persistence/assumptions.md | sed -n '/^## Indice/,/^---/p' | grep -E 'A-00[45]\]'
  | [A-004](#a-004---el-patrocinador-revisa-cada-salida-de-la-ia-antes-de-que-entre-al-registro) | El patrocinador revisa cada salida de la IA antes de que entre al registro | 2026-09-17 | Confirmado |
  | [A-005](#a-005---hay-acceso-a-personas-que-conocen-el-proceso-real-ademas-del-patrocinador) | Hay acceso a personas que conocen el proceso real, ademas del patrocinador | 2026-09-17 | Confirmado |
  $ git show c424ae8:_persistence/assumptions.md | grep -n "^### A-\|^| Estado |"
  41:| Estado | `Abierto` / `Confirmado` / `Refutado` / `Riesgo abierto` |
  77:### A-XXX - Titulo
  81:| Estado | Abierto |
  91:### A-001 - Los juegos registrados pueden ser datos de personas
  95:| Estado | Refutado |
  115:### A-002 - Los agentes de Gate y de acta se cargan al reiniciar Claude Code
  119:| Estado | Refutado |
  163:### A-003 - Con la cabecera YAML valida, los agentes de Gate y de acta cargan
  167:| Estado | Confirmado |
  188:### A-004 - El patrocinador revisa cada salida de la IA antes de que entre al registro
  192:| Estado | Abierto |
  218:### A-005 - Hay acceso a personas que conocen el proceso real, ademas del patrocinador
  222:| Estado | Abierto |
  247:### A-006 - El hook de la cabecera de session-closer se dispara dentro del agente
  251:| Estado | Confirmado |
  276:### A-007 - La cabecera exacta de session-closer deja pasar protocol-close y bloquea las demas skills
  280:| Estado | Confirmado |
  314:### A-008 - Las condiciones de uso de Baloto permiten leer su historico de forma automatica
  318:| Estado | Confirmado |
  351:### A-009 - Los seis numeros arbitrarios no desviaran la atencion del participante durante la sesion
  355:| Estado | Confirmado |
  ```

  Las dos fichas llevan debajo su nota `🕐 Nota 2026-09-18 (confirmado, D-079)`. `D-079` afirma haber
  cambiado los dos sitios, y su criterio de cierre solo mira el indice:

  ```
  $ git show c424ae8:_persistence/decisions.md | sed -n '/^### D-079 /,/^### D-080/p' | grep -n "en el indice\|grep -cE\|y en la ficha"
  14:- **Decision:** los dos pasan a `Confirmado`, cada uno con su nota fechada y su evidencia, en el indice
  15:  y en la ficha. No se reescribe ningun enunciado ni ningun disparador: se anade la nota debajo.
  36:  $ grep -cE '^\| \[T-029\].*\| Implementada \|' _persistence/tasks.md
  44:  1. **Enunciado:** a ese commit, los dos supuestos estan `Confirmado` en el indice y en la ficha, y
  48:     git show d80a965:_persistence/assumptions.md | grep -cE '^\| \[A-00[45]\].*\| Confirmado \|'
  49:     git show d80a965:_persistence/assumptions.md | grep -cE '^\| \[A-008\].*\| Abierto \|'
  ```

  El desfase existe desde `d80a965` (`S-019`), el commit de `D-079`:

  ```
  $ git show d80a965:_persistence/assumptions.md | grep -n "^| Estado |"
  40:| Estado | `Abierto` / `Confirmado` / `Refutado` / `Riesgo abierto` |
  80:| Estado | Abierto |
  94:| Estado | Refutado |
  118:| Estado | Refutado |
  166:| Estado | Confirmado |
  191:| Estado | Abierto |
  221:| Estado | Abierto |
  250:| Estado | Confirmado |
  279:| Estado | Confirmado |
  317:| Estado | Abierto |
  ```

  (191 y 221 son las fichas `A-004` y `A-005`; la 80 es la plantilla.) Ninguna auditoria lo vio en
  siete pasadas. Y **esta sesion lo propaga**: la
  seccion 4 de `S-026` filtra por la ficha y concluye lo contrario del indice:

  ```
  $ git show c424ae8:_audit/S-026.md | grep -n "supuestos vigentes"
  147:del propio archivo, no de una entrada real. Los dos supuestos vigentes son `A-004` y `A-005`;
  ```

  Y la seccion 6 lo diagnostica mal: atribuye la rareza a que las entradas «no siguen el formato
  `### A-XXX - Titulo`» (`git show c424ae8:_audit/S-026.md | grep -n "no sigue el formato"` →
  linea 165), cuando la salida de la tercera orden de arriba muestra que las diez cabeceras siguen
  ese formato exacto. El `grep -B6` funciona; lo que falla es el dato de la ficha.
- **Por que importa:** el registro afirma dos estados a la vez para el mismo supuesto, y `D-079`
  afirma haber hecho algo que el archivo no muestra. `protocol-start` lee los supuestos `Abierto` con
  su disparador en cada arranque: segun por donde lea, presenta como abiertos dos supuestos cerrados
  hace un dia, cuyo disparador ya corrio. Ya ocurrio: el informe de este cierre los da por vigentes.
  `Media` porque las notas fechadas de las fichas y el indice tienen el dato correcto; `Bloqueante`
  porque es exactamente «el registro afirma un estado falso que otros van a leer», y ya lo leyo uno.
  El control 4a no lo ve porque compara codigos, no estados.
- **Que lo corregiria:** llevar el campo `Estado` de las fichas `A-004` y `A-005` a `Confirmado`
  (la nota fechada que lo justifica ya esta debajo de cada una), con una nota fechada en `D-079` que
  diga que su enunciado no se cumplio en `d80a965` y cuando se corrigio; y, si `manager` lo considera,
  una nota en `S-026.md` que corrija la seccion 4 sin reescribirla. De fondo, que el cierre compare
  indice y ficha **por estado** en `assumptions.md`, como su Paso 2b ya hace con `findings.md`.
  ⚠️ Es una recomendacion, no una orden.

## 3. Hallazgos anteriores

| Hallazgo | Estado nuevo | Por que |
|---|---|---|
| F-016 | Aceptado — pendiente (sin cambios) | `T-030` sigue `No implementada`; `git diff c424ae8^ c424ae8 -- _persistence/decisions.md \| grep -c '^-[^-]'` → `0`: el diff de `decisions.md` no borra ni cambia ninguna linea, luego no toca `D-041` |
| F-017 | Aceptado — pendiente (sin cambios) | `T-031` sigue `No implementada`; `_audit/S-013.md` no esta en el `--stat` de `c424ae8` |
| F-028 | Aceptado — pendiente (sin cambios) | `T-047` sigue `No implementada`; `_audit/S-019.md` no esta en el `--stat` |
| F-029 | Aceptado — pendiente (sin cambios) | `T-048` sigue `No implementada`; el acta de `005_discovery` no esta en el `--stat` |
| F-030 | Aceptado — pendiente (sin cambios) | `T-049` sigue `No implementada`, pendiente de la decision del usuario (`D-095`) |
| F-034 | **Implementado** — cerrado en `c424ae8` | Ver abajo |
| F-036 | Aceptado — pendiente (sigue abierto) | Corregido a medias: la nota fechada (`T-059`) esta y reproduce (seccion 1.3); la segunda recomendacion, que el cierre derive la lista con orden publicada, queda en `T-060` `No implementada`. `git show --stat --name-only --format= c424ae8` no toca `.claude/skills/protocol-close/` |

**Por que `F-034` se cierra.** Las notas fechadas en `S-023.md` y en `progress.md` ya estaban
verificadas por `R-026`, y siguen:

```
$ for f in _audit/S-023.md _persistence/progress.md; do echo "$f: $(git show c424ae8:$f | grep -c 'NOTA 2026-09-18 (`F-034`')"; done
_audit/S-023.md: 1
_persistence/progress.md: 1
```

Lo que `R-026` y `R-027` dejaron abierto era que las secciones 2 y 3 de `progress.md` dijeran la
cifra correcta de la ronda. En `c424ae8` la dicen (falta solo `005`):

```
$ git show c424ae8:_persistence/progress.md | grep -n "restante\|faltan\|falta \`005\`\|quinta"
97:| Bloqueos activos | Paso 5 de `010_prototype` en marcha: sesiones `001`-`004` de cinco corridas, falta `005`; ...
124:🎯 **Correr la quinta y ultima sesion — Paso 5 de `010_prototype` (`_phases/010_prototype.md`).** Es
616:  instrucciones para el reporte en pantalla — `T-041` `Implementada`. Ademas, quinta pasada de
808:- **Que quedo abierto:** faltan las sesiones `002`, `003` y `004` del Paso 5; el prototipo sigue sin
846:- **Que quedo abierto:** faltan las sesiones `003`, `004` y `005` del Paso 5; el prototipo sigue sin
872:- **Que quedo abierto:** faltan las sesiones `004` y `005` del Paso 5; el prototipo sigue sin tocarse
$ git show c424ae8:_persistence/progress.md | sed -n '/^## 2. Ultimo realizado/,/^## 4/p' | grep -nE "cinco|cuatro|restante|ultima"
3:Veintiseis sesiones de trabajo. Segun el diff: se corre la cuarta de las cinco sesiones del Paso 5
13:`_audit/S-025.md` enumeraba cuatro hallazgos `Aceptado — pendiente` donde el registro del mismo
24:🎯 **Correr la quinta y ultima sesion — Paso 5 de `010_prototype` (`_phases/010_prototype.md`).** Es
30:las cinco sesiones corridas, la siguiente sesion tambien debe comprobar la condicion de salida del
```

(las lineas 616, 808, 846 y 872 son bitacora historica; la 13 habla de hallazgos, no de la ronda.)
Las secciones 1, 2 y 3 ya no llevan la frase «restante» desfasada y las tres cifras cuadran con
`010_participants.md` (`004` corrida, `005` pendiente). Las dos partes de la recomendacion de
`F-034` estan en el registro. La causa de fondo —que la cifra se escribe a mano— queda cubierta por
`T-060` (que `D-103` ata expresamente a `F-034`), bajo `F-036`, que sigue abierto.

## 4. Lo que NO se pudo comprobar

- **Seccion 9 del informe (Paso 2f, diferencia con el esqueleto de arranque):** no se reejecuto.
  Compara contra el arbol de trabajo de otro repositorio, no contra un commit, y no es reproducible
  desde `c424ae8`. Queda `SIN COMPROBAR`; no es un hallazgo.
- **Si los datos dictados de la sesion 004 (horas, citas de §7, estado asignado) son los que dio la
  facilitadora:** fuera del alcance del repositorio; solo se comprobo coherencia interna y fecha.

## 5. Recomendaciones sin hallazgo

- **Respuesta a la primera pregunta de la seccion 6 de `S-026`:** marcar `F-036` entero como
  `Aceptado — pendiente` es la lectura correcta. La convencion no tiene estado intermedio y el
  protocolo de auditoria dice que un hallazgo corregido a medias sigue abierto; partirlo en dos filas
  crearia un `F-NNN` que ninguna auditoria abrio.
- **Seccion 6, tercer punto (`T-058` sin tocar tres sesiones):** es priorizacion, no defecto; decide
  `manager`.
- **La seccion 0 de `S-026` vuelve a escribir la lista de pendientes a mano**, aunque esta vez es
  correcta. Hasta que `T-060` exista en `protocol-close`, pegar en la seccion 0 la salida de la orden
  que `D-103` ya usa (`awk -F'|' '$7 ~ /Aceptado — pendiente/'`) costaria una linea y haria la lista
  contrastable.
- **`progress.md` §3 dice «Secundario, si queda tiempo: lanzar `report_auditor`»**, cuando
  `CLAUDE.md` lo hace el ultimo paso obligatorio del cierre. La redaccion es heredada, pero invita a
  leerlo como opcional.
